{
	"version": "2.0.0",
	"tasks": [
		{
      "label": "Build web (typecheck)",
      "type": "shell",
      "command": "npm run build",
      "options": {
        "cwd": "${workspaceFolder}/web"
      },
      "problemMatcher": ["$tsc"],
      "group": "build"
    }
	]
}