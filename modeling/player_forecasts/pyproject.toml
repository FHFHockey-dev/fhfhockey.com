[build-system]
requires = ["setuptools==75.8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fhfh-player-forecasts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["psycopg[binary]==3.2.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
