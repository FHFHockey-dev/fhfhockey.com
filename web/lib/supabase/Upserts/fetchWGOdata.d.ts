// fetchWGOdata.d.ts

// Declare the module (this is optional if the file is co-located)
// If you use a relative import (as recommended), you usually don’t need to declare a module name.
export declare function main(): Promise<void>;
