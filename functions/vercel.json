{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "ignoreCommand": "if test -n \"$VERCEL_GIT_PREVIOUS_SHA\" && git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA^{commit}\" 2>/dev/null; then git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD ./; else exit 1; fi",
  "functions": {
    "api/**/*.py": {
      "maxDuration": 240,
      "memory": 1024
    }
  },
  "routes": [
    { "src": "/sko/pipeline", "dest": "/api/sko/pipeline/index.py" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
