# sKO Charts and Pipeline — Reconciliation, Completion, and Burn-Down Tasks

## Relevant Files

- `tasks/TASKS/sko-charts/prd-sko-charts.md` - Current stability-CV sKO product/model PRD.
- `tasks/TASKS/sko-charts/prd-sko.md` - Earlier residual-P/60 sustainability concept retained as research input.
- `tasks/TASKS/sko-charts/sko-modeling-notes.md` - Modeling analysis and embedded checklist.
- `tasks/TASKS/sko-charts/sko-ownership-contract.md` - Reconciled PRD/runtime/formula/data/consumer/caller ownership matrix and promotion boundary.
- `tasks/TASKS/sko-charts/sko-runbook.md` - Truthful compatibility-pipeline environment, schedule, stage, timeout, artifact, failure, and rollback inventory.
- `tasks/TASKS/dead-code-cleanup/burn-down-plan.md` - Legacy SKO simplification and retirement scope merged here.
- `web/pages/skoCharts.tsx` - Quarantined legacy route.
- `web/pages/trends/index.tsx` - Current prediction leaderboard and Trends entry surface.
- `web/pages/trends/player/[playerId].tsx` - Current player prediction/trend detail surface.
- `web/pages/api/v1/ml/get-predictions-sko.ts` - Canonical prediction read API candidate.
- `web/pages/api/v1/ml/update-predictions-sko.ts` - Controlled prediction writer and cron/audit owner.
- `web/pages/api/v1/db/update-sko-stats.ts` - SKO source-stat ingestion route.
- `web/lib/supabase/utils/statistics.ts` - Characteristic-value, threshold, rolling, and confidence helpers.
- `web/lib/supabase/utils/calculations.ts` - Stable baseline GameScore calculation.
- `web/scripts/output/sko_*` - Retained generated artifacts from the deleted modeling implementation.
- `functions/lib/sko_pipeline.py` - External HTTP stage orchestrator requiring owner/stage verification or retirement.
- `web/components/Predictions/` - Reusable prediction UI.
- `web/lib/supabase/database-generated.types.ts` - Current SKO/prediction schema type evidence.

### Notes

- This list repairs the missing pair for both SKO PRDs and explicitly merges the SKO burn-down scope.
- The later stability-CV path remains a historical promotion proposal, not current production behavior: the owner authorized a historical-only disposition on 2026-07-28. The live `baseline-moving-average` v0.2 writer remains compatibility-only pending separate retirement gates; legacy GameScore/characteristic helpers, deleted offline ML work, and the orphaned prediction UI remain non-authoritative quarantine. The earlier residual-P/60 proposal remains Sustainability/Trends research, not a competing production score.
- `/skoCharts` is labeled legacy. New product work belongs on Trends/current prediction surfaces unless evidence proves a unique supported-route requirement.
- Implementation checkmarks remain claims requiring their recorded runtime/data evidence; the 2026-07-28 disposition checkmarks close retired scope only and explicitly do not claim implementation.
- Unbounded Supabase history/player scans must paginate until a short page or use verified server-side aggregates.

## Tasks

- [x] 1.0 Reconcile sKO definition, ownership, and route/data contracts. Evidence (2026-07-29): all children now close after candidate migration `20260729205048` makes model name/version part of the compatibility prediction grain, the writer conflict target matches it, and the bounded public reader filters/orders those identities deterministically; freshness remains separately open under NEW 9.9.
  - [x] 1.1 Map both PRDs, modeling notes, burn-down plan, Sustainability, Trends, and current code into one ownership matrix. Evidence (2026-07-22): `sko-ownership-contract.md` classifies every PRD, formula family, table/API, retained/deleted pipeline artifact, supported Trends surface, quarantine surface, and active caller without promoting incompatible implementations.
  - [x] 1.2 Mark `prd-sko.md` superseded for production score definition while preserving its research requirements under Sustainability/Trends. Evidence (2026-07-22): the older PRD remains explicitly superseded research; residual-P/60 work stays with Sustainability/Trends and is not relabeled as the live SKO writer.
  - [x] 1.3 Freeze the current contract: GameScore, per-game characteristic value, rolling CV window, empirical/fallback thresholds, smooth `[0.8,1.0]` confidence multiplier, and stability-adjusted score. Disposition evidence (2026-07-28): the owner-authorized historical-only boundary freezes the exact legacy weights, capped weighted-squared-z characteristic value, 10-game rolling window, p50/p90 empirical thresholds with analytic/fail-safe fallbacks, smoothstep `[0.8,1.0]` multiplier, and `GameScore × multiplier` score as a non-authoritative archived contract. No current score is promoted or changed.
  - [x] 1.4 Define canonical grains, identifiers, as-of date, model version, horizon, provenance, freshness, and fallback semantics for predictions and metrics. Evidence (2026-07-29): compatibility predictions use `(player_id, as_of_date, horizon_games, model_name, model_version)` as the candidate canonical grain; the writer persists exactly that identity and the public reader validates, filters, and stably orders it. Existing rows require no data rewrite because the former three-column primary key was already a stricter uniqueness subset. Offseason freshness remains explicitly separate under NEW 9.9.
  - [x] 1.5 Verify active consumers use the canonical API/data contract and append legacy direct-table consumers as remediation/quarantine tasks. Evidence (2026-07-22): supported Trends pages use rolling/FORGE contracts and do not import the SKO reader bundle or legacy score helpers; the orphaned prediction components/API are recorded as P2 NEW 9.4 and remain quarantined.

- [ ] 2.0 Verify and finish source-stat, prediction, and metrics persistence
  - [x] 2.1 Reconcile live schemas/migrations for `sko_skater_stats`, `sko_skater_years`, `predictions_sko`, and `predictions_sko_metrics` with generated types and code. Evidence (2026-07-22): read-only live catalog, production-baseline DDL, generated types, and runtime references agree on three present tables and their exact keys/RLS/read policies; `predictions_sko_metrics` is absent from all four and remains an unimplemented planned contract documented in `sko-ownership-contract.md`.
  - [ ] 2.2 Apply or repair missing migrations through the Supabase workflow, including RLS/API exposure appropriate to admin writers and read-only consumers.
  - [x] 2.3 Verify `update-sko-stats` handles schema drift explicitly, paginates complete source reads, and does not silently skip model-required fields. Evidence (2026-07-22): all 16 NHL families share explicit cursor pagination; the active path emits one typed exact-28-column batch with canonical season identity and derived time-on-ice/IPP/SOG-per-60 fields; schema/write errors fail closed instead of triggering field-dropping retries. Focused helper/route tests pass 2 files/11 tests and full TypeScript passes.
  - [x] 2.4 Make prediction/metric upserts idempotent by stable player/as-of/horizon/model identity and preserve evaluation history. Evidence (2026-07-29): candidate migration `20260729205048` expands `predictions_sko_pk` through model name/version, and the compatibility writer uses the same five-column conflict target so one model can no longer overwrite another at the same player/date/horizon. The migration remains unapplied under 2.2.
  - [ ] 2.5 Add freshness, row-count, missing-player, and partial-write diagnostics for every persistence stage. Local progress (2026-07-28): the compatibility prediction writer now persists exact coverage/source/write diagnostics and stable failure/partial/low-row health codes under a durable run manifest. Keep open for the source-ingest stage and approved offseason freshness policy.

- [x] 3.0 Complete the modeling and explanation backlog under frozen contracts. Disposition evidence (2026-07-28): the owner authorized the deleted offline modeling family as historical-only quarantine; this closes the retired backlog without claiming implementation or model promotion.
  - [x] 3.1 Add LightGBM and XGBoost challengers only after dependency/runtime review; pin versions and use identical season splits/leakage rules. Disposition: retired with the deleted modeling pipeline; any future challenger requires a new approved model contract.
  - [x] 3.2 Export gain/permutation importance and per-player `top_features` with stable key, direction, magnitude, and source context. Disposition: retired with the deleted offline exporter and not represented as current runtime capability.
  - [x] 3.3 Add context-aware and centered-share candidates only through AS/PP/5v5/ES alignment and no-recursive/outcome-leakage checks. Disposition: historical research only; no candidate is promoted.
  - [x] 3.4 Evaluate season-aware CV normalization and alternate rolling windows without changing the default before held-out evidence supports promotion. Disposition: historical research only; no default changes.
  - [x] 3.5 Report Top-X-percent effects, z-scores, calibration/error, and segment stability for model review. Disposition: retired with the absent evaluation pipeline; no report is claimed.
  - [x] 3.6 Keep baseline GameScore weights stable unless an approved candidate beats the frozen baseline under recorded gates. Disposition: GameScore remains non-authoritative quarantine and no candidate is promoted.
  - [x] 3.7 Test rolling/partial windows, empirical quantiles/fallbacks, smoothstep bounds, characteristic scores, feature exports, and determinism. Disposition: retired pipeline coverage is not claimed; quarantined helpers require new promotion evidence before reuse.

- [x] 4.0 Segment the long-running pipeline into durable operational stages. Disposition evidence (2026-07-28): the owner chose historical-only quarantine instead of restoring the deliberately deleted executable pipeline; the active v0.2 compatibility writer remains a separate operational surface.
  - [x] 4.1 Inventory backfill, feature, train, score, metric, upload, and cleanup dependencies, runtimes, and artifacts. Evidence (2026-07-22): `sko-runbook.md` records the four sequencer names/timeouts against the 240-second platform cap, absent executors/metric/cleanup owners, deleted scripts, retained canonical/nested artifacts, active compatibility writers/schedules, and missing run/retry/resume/lock/state contracts without claiming operability.
  - [x] 4.2 Replace monolithic execution with bounded idempotent stages that fit current runtime limits and resume from persisted state. Disposition: no executable modeling pipeline survives to segment; restoration/replacement requires a new approved initiative.
  - [x] 4.3 Define stable inputs/outputs, artifact version/checksum, run identity, retry, partial-failure state, and stale-run recovery. Disposition: retired with the absent pipeline; retained artifacts are historical evidence, not resumable state.
  - [x] 4.4 Chain stages through existing cron/orchestration; a new paid queue/workflow is a strategy checkpoint. Disposition: no modeling stages are approved for orchestration; current compatibility schedules remain unchanged.
  - [x] 4.5 Deduplicate/lock overlapping runs across instances and prevent incomplete artifacts from becoming current. Disposition: no executable multi-stage pipeline remains; current writer controls stay separately owned.
  - [x] 4.6 Test bounded slices, resume behavior, failure propagation, and idempotent upload. Disposition: no pipeline runtime is claimed; future replacement must establish its own verification contract.

- [x] 5.0 Verify and finish canonical APIs and mutation controls. Evidence (2026-07-30): all children close through fail-closed Production mutation authorization, strict deterministic bounded reads, explicit compatibility/source warnings, operator-safe errors, and focused response/auth/pagination tests. The reader remains quarantined and no model is promoted.
  - [x] 5.1 Enforce admin/secret authorization on mutation routes without logging secrets or using public-key write fallbacks. Evidence (2026-07-23): both privileged Next.js SKO writers use the shared fail-closed admin/cron boundary with service-role persistence, fixed value-free failures, and no credential logging/public-key write fallback. Exact Production deployment `dpl_4q8GM1wqTbHR3NbnxRxAakdLEvJr` returns 401 for missing/invalid authorization and 405 after current Vault-backed authorization reaches unsupported-method validation; no writer executed and the bounded relevant runtime-error query is empty.
  - [x] 5.2 Validate/filter/paginate prediction reads and return explicit freshness, coverage, and partial states. Evidence (2026-07-22): the public reader rejects invalid dates/ranges/ids/order/page sizes before data work, uses a stable date/player/horizon order with exact-count ranged pages capped at 2,000 rows, and returns page-scoped freshness plus coverage/has-more/partial metadata.
  - [x] 5.3 Include identity, prediction, sKO/CV inputs, top features, model/version, timestamps, and source warnings deterministically. Evidence (2026-07-30): every bounded row returns player/date/horizon identity, prediction/sKO/CV/stability values, explicit nullable `top_features`, model name/version, and create/update timestamps. Deterministic page warnings identify compatibility-only model status, absent feature attribution, snapshots older than 72 hours, and empty data without inventing retired features; the reader suite passes 12/12.
  - [x] 5.4 Normalize operator-safe errors while preserving cron audit timing and row counts. Evidence (2026-07-22): the public reader exposes only a stable unavailable code/message while server logs retain normalized redacted diagnostics; the protected writer now returns explicit `rowsUpserted` on success and failure for the unchanged audit wrapper, whose timing/count suite passes 8/8.
  - [x] 5.5 Test authorization, pagination, empty/stale/partial states, determinism, and response schema. Evidence (2026-07-22): the combined reader/writer/audit group passes 3 files/24 tests covering mutation auth, strict filters/ranges, stable order, complete empty pages, page-scoped stale/partial metadata, identity-complete schema, safe dependency failure, row counts, and audit timing.

- [x] 6.0 Finish Trends prediction and transparency UI. Disposition evidence (2026-07-28): the owner authorized the orphaned prediction reader/UI bundle to remain historical-only quarantine; supported Trends continues to use rolling/FORGE.
  - [x] 6.1 Verify search, sorting, date stepping, metrics, sparklines, leaderboard, detail, and explainability use canonical APIs. Disposition: the unmounted bundle is not a supported surface and is not reintegrated.
  - [x] 6.2 Surface per-player top drivers with honest direction/magnitude and model/source context. Disposition: retired with the unpromoted prediction UI/model contract.
  - [x] 6.3 Build MAE/MAPE and margin-of-error history after persisted metric coverage is verified. Disposition: retired with the absent metrics contract; no history is claimed.
  - [x] 6.4 Add explicit loading, empty, stale, sparse, partial, unavailable-model, and error states. Disposition: the quarantined UI is not promoted; existing API safety controls remain separate.
  - [x] 6.5 Verify responsive, keyboard/accessibility, chart labeling/tooltips, and color-independent status. Disposition: retired for the unmounted bundle; supported Trends retains its own accessibility ownership.
  - [x] 6.6 Ensure legacy `/skoCharts` links do not imply quarantined behavior is supported. Evidence (2026-07-22): exact page/component/lib link and router scans find no tracked runtime caller to `/skoCharts`; the retained route is `noindex,nofollow`, titles itself Legacy, states that it is quarantined/not trusted/not live FORGE, and links out only to FORGE Dashboard and Trends.

- [x] 7.0 Execute the SKO legacy burn-down safely. Evidence (2026-07-28): every child is complete through verified inventory/classification, explicit quarantine naming, owner-authorized retention without deletion, and post-change navigation/API/cron/type checks. No runtime surface or artifact was removed or promoted.
  - [x] 7.1 Re-verify every burn-down file/consumer against current imports, routes, cron, scripts, and runtime ownership. Evidence (2026-07-22): bounded file/import/caller/history searches cover the quarantined route/formulas/reader/UI, active protected writers and admin/cron callers, external HTTP orchestrator, deleted modeling executables, retained canonical-path outputs, and the previously omitted nested output copies now owned by NEW 9.12.
  - [x] 7.2 Classify each artifact as canonical, compatibility-only, quarantine, generated evidence, or deletion candidate and record its replacement. Evidence (2026-07-22): `sko-ownership-contract.md` assigns every grouped surface to supported adjacent ownership, operational compatibility, quarantine, deleted history, retained evidence, missing proposal, or archive/removal decision; no artifact is promoted or deleted by classification.
  - [x] 7.3 Narrow/rename legacy SKO fetch utilities so they cannot be mistaken for approved contracts. Evidence (2026-07-22): the sole unmounted SKO reader hook is now `useQuarantinedPredictionsSko` at an equally explicit path; its only caller imports that name, the old symbol/path have no runtime references, and the quarantine registry enumerates the hook plus all three UI components. The registry test and full TypeScript pass.
  - [x] 7.4 Remove only proven-unused files; mass deletion/route removal requires the super-goal checkpoint. Disposition evidence (2026-07-28): the authorized historical-only quarantine retains the lineage route, formula helpers, unmounted UI bundle, and generated artifacts unchanged; no deletion is required or inferred. Any future removal requires a new explicit checkpoint.
  - [x] 7.5 Verify navigation, APIs, cron inventory, build resolution, and Trends after each cleanup batch. Evidence (2026-07-22): after the 7.3 rename, exact scans show no supported Trends/FORGE import or legacy-route link, both compatibility routes remain in cron-report ownership, the admin caller still targets the protected prediction writer, the reader/writer/audit cohort passes 3 files/24 tests, the quarantine registry passes 1/1, and full TypeScript resolves the renamed module.

- [ ] 8.0 Add runbook, monitoring, and end-to-end evidence
  - [x] 8.1 Document environment names only, stage commands, schedules, artifacts, state files, expected rows, retries, rollback, and failures. Evidence (2026-07-22): `sko-runbook.md` names only `CRON_SECRET`, `SKO_PIPELINE_ENDPOINT`, `SKO_PIPELINE_SECRET`, and Vault `cron_secret`; records jobs 321/327, safe test/health commands, unavailable operational commands/expected rows, retained artifacts, no retry/state guarantees, exact failure semantics, and rollback prerequisites.
  - [ ] 8.2 Monitor last success, freshness, player/prediction coverage, schema drift, errors, artifact mismatch, and stalled stages. Local progress (2026-07-28): candidate migration `20260728225806` records last success/failure/error, lease heartbeat/expiry, attempt count, response diagnostics, and health alerts for the compatibility writer. Keep open for migration application, schema/artifact signals, approved freshness policy, and production observation.
  - [x] 8.3 Verify source ingest → staged pipeline → persistence → read API → Trends UI on a bounded date. Disposition evidence (2026-07-29): the owner-authorized historical-only boundary confirms no executable staged model pipeline or supported Trends SKO consumer survives, so this retired end-to-end chain cannot be truthfully executed or claimed. The active compatibility source writer, prediction writer, and quarantined reader retain their separate verification and production gates.
  - [x] 8.4 Verify no secrets/local absolute paths are committed and generated artifacts are intentional and bounded. Evidence (2026-07-28): all ten tracked SKO output artifacts are valid JSON/CSV/Parquet files totaling 24,343,329 bytes; exact SHA-256 inventory distinguishes the three older nested parquet copies and confirms the one-byte timing CSV duplicate. Targeted text/string scans find no secret marker or local absolute path, and the authorized historical-only disposition retains both sets as bounded evidence rather than runtime inputs.
  - [ ] 8.5 Synchronize both PRDs, modeling notes, burn-down plan, this list, and master ledger with final evidence.

## NEW Tasks

- [ ] NEW 9.0 Append every verified defect, manual/provider dependency, model question, and optimization discovered during execution here before closure.
- [x] NEW 9.1 **P0 public privileged prediction writer:** the shared fail-closed boundary is READY/Production at exact commit `b4aeea8f9af6229aa64f75e4b311b1218a5afb4c` as `dpl_4q8GM1wqTbHR3NbnxRxAakdLEvJr`. Missing/invalid authorization returns 401 and current Vault-backed authorization reaches safe 405 method validation through unsupported PUT; no prediction writer executed and the bounded relevant runtime-error query is empty (closed 2026-07-23).
  - [x] NEW 9.2 **P1 production-score contract split:** owner-authorized historical-only disposition keeps GameScore/characteristic-value and deleted ML × stability families non-authoritative; `baseline-moving-average` v0.2 remains explicitly compatibility-only until separate retirement gates. No score is promoted or reinterpreted (discovered 2026-07-22; closed 2026-07-28).
  - [x] NEW 9.3 **P1 documented modeling pipeline is no longer present:** owner-authorized historical-only disposition accepts deliberate deletion commit `abbc01e8c5dc99e1544594e0c72bdecd0a013ea8`; the HTTP placeholder is not a model executor, retained outputs are evidence only, and no restore/replacement is inferred (discovered 2026-07-22; closed 2026-07-28).
  - [x] NEW 9.4 **P2 prediction reader/UI is orphaned from supported Trends:** owner-authorized historical-only disposition retains `PredictionsLeaderboard`, `InfoPopover`, `SkoExplainer`, and `useQuarantinedPredictionsSko` in the explicit quarantine registry with no reintegration or deletion; supported Trends remains rolling/FORGE (discovered 2026-07-22; closed 2026-07-28).
- [x] NEW 9.5 **P0 public privileged SKO source-ingest writer:** the shared fail-closed boundary is READY/Production at exact commit `b4aeea8f9af6229aa64f75e4b311b1218a5afb4c` as `dpl_4q8GM1wqTbHR3NbnxRxAakdLEvJr`. Missing/invalid authorization returns 401 and current Vault-backed authorization reaches safe 405 method validation through unsupported PUT; no source-ingest writer executed and the bounded relevant runtime-error query is empty (closed 2026-07-23).
  - [x] NEW 9.6 **P1 source-ingest schema-learning and season-identity defect:** the active date writer now replaces its 236-field/error-learning payload and unscoped historical preload with one typed exact-28-column mapper, explicit canonical date-season resolution, derived `time_on_ice`/`ipp`/`sog_per_60`, and one fail-closed batch attempt. Exact-key/arithmetic/pagination/error regressions pass in the 2-file/11-test group plus full TypeScript (discovered and completed 2026-07-22).
  - [x] NEW 9.7 **P1 prediction model-history identity collision:** candidate migration `20260729205048` expands the key to `(player_id, as_of_date, horizon_games, model_name, model_version)`, narrows public table grants to SELECT, and preserves existing rows without a data rewrite because the former key was a stricter uniqueness subset. The writer conflict target and public reader model filters/order now match the five-column grain; 2 files/20 tests, TypeScript, scoped lint, SQL contract, and diff integrity pass. Migration application remains separately open under 2.2 (discovered 2026-07-22; closed locally 2026-07-29).
  - [x] NEW 9.8 **P1 incomplete and misordered prediction source reads:** default discovery now paginates ordered source rows until a short page, deduplicates/sorts the complete player set, and applies any operator limit only afterward with an explicit partial flag. Each player fetch selects the latest 60 rows descending and reverses them into chronological model input. Deterministic model, discovery, selected/processed/skipped, source cutoff/lag, series-length, and write-progress diagnostics pass the focused 6-test route/helper suite plus full TypeScript (discovered and completed 2026-07-22).
  - [ ] NEW 9.9 **P1 stale source stamped as current prediction:** approved policy is official-NHL-season execution only, no publication when source lag exceeds 72 hours, and truthful no-write audits outside the season. Local implementation now finishes offseason requests as successful zero-publication manifests before source work and returns a stable stale-source warning without upsert when in-season source lag exceeds three days. The authenticated writer also supports `dryRun=true`, which computes the exact identity digest and would-write count without touching prediction rows. Read-only hosted evidence selects 2026-03-22 as the latest official-season source date with zero existing horizon-5 `baseline-moving-average/v0.2` targets: 851 lookback players, 27,359 qualifying source rows, and value-free input-scope digest `0ab825ccbf3a185d1c72f3fc7eb72227`. Keep open for migration-first publication, matching dry-run receipt, separately authorized exact population/rollback, and the natural no-write observation; the one-off must not be represented as a scheduled run (updated 2026-07-30).
  - [x] NEW 9.10 **P1 overprivileged public reader and credential-prefix logging:** `get-predictions-sko` now uses only the existing public read-only server client under the verified public SELECT policy; all service-role resolution, JWT decoding, credential-source warnings, and key-prefix logging are removed. Static scans plus the focused reader suite prove the bounded route no longer references or logs credentials (discovered and completed 2026-07-22).
  - [x] NEW 9.11 **P2 capped and identity-incomplete prediction response:** the reader now enforces strict dates/ranges/positive identifiers/order and bounded pages, selects exact-count deterministic ranges, includes `model_name`, `model_version`, and `updated_at`, and returns explicit page-scoped freshness/coverage/pagination/partial metadata. The dedicated reader suite passes 8/8 and the combined reader/writer group passes 2 files/14 tests plus full TypeScript (discovered and completed 2026-07-22).
  - [x] NEW 9.12 **P2 duplicated unowned generated SKO artifacts:** the owner-authorized historical-only disposition retains the four tracked nested artifacts as bounded evidence, not runtime inputs. Exact hashes preserve their provenance: all three parquet copies differ from the newer canonical-path files while the one-byte timing CSV is identical; targeted scans find no secret/local path. No deletion or promotion is inferred (discovered 2026-07-22; closed 2026-07-28).
  - [x] NEW 9.13 **P1 latent fail-open external pipeline authorization:** inbound Flask auth and the outbound sequencer now both require a nonblank `SKO_PIPELINE_SECRET`; missing config stops before dependency/network work, invalid/missing bearers remain 401, approved stages always forward the bearer, and unknown stages stop before requests. Focused tests pass 6/6; the Production route remains 404 and was not restored or invoked (discovered and completed 2026-07-22).
  - [x] NEW 9.14 **P1 miswired and false-success pipeline surface:** the unrouted placeholder now returns honest 501 `implemented=false` after valid auth instead of 200 accepted, while invalid stages remain 400. The Production mapping remains intentionally untouched/404; NEW 9.3 later closed through owner-authorized historical-only disposition without restoration. Focused Flask/helper tests pass 6/6 (discovered and completed 2026-07-22; disposition updated 2026-07-28).
  - [x] NEW 9.15 **P2 raw dependency error disclosure from the public prediction reader:** `/api/v1/ml/get-predictions-sko` now returns only `prediction_data_unavailable` plus a stable public message, while normalized server diagnostics redact bearer values and URLs. Empty, stale/partial, raw-error exclusion, and sanitized-log regressions pass in the 3-file/24-test API/audit group plus full TypeScript (discovered and completed 2026-07-22).
  - [x] NEW 9.16 **P3 stale privileged-route auth assertion:** the focused writer suite still expected mocked provider error text after the shared boundary began returning generic `Unauthorized.`. The regression now matches the published redacted 401 contract; the 13-test writer cohort passes without changing runtime authorization (discovered and completed 2026-07-30).
