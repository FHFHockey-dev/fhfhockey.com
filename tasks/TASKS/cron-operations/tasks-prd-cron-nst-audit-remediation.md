## Relevant Files

- `web/pages/api/v1/db/cron-report.ts` - Daily cron summary route that correlates scheduled jobs, audit rows, and email output.
- `web/components/CronReportEmail/CronReportEmail.tsx` - Primary daily cron summary email template.
- `web/components/CronReportEmail/CronAuditEmail.tsx` - Audit-focused email template for raw audit run reporting.
- `web/lib/cron/withCronJobAudit.ts` - Shared audit wrapper that currently infers success and failure state for cron routes.
- `web/utils/adminOnlyMiddleware.ts` - Existing admin-or-valid-CRON_SECRET boundary required by privileged scheduled writers.
- `web/__tests__/utils/adminOnlyMiddleware.test.ts` - Verified fail-closed absent/blank-CRON_SECRET helper regressions.
- `web/pages/api/internal/cron-auth-check.ts` and `web/__tests__/pages/api/internal/cron-auth-check.test.ts` - Non-mutating value-free secret-authorization probe used by the P0 rotation gate.
- `web/__tests__/pages/api/v1/db/cron-auth-boundaries.test.ts` - Production-mode season/WGO admin-or-cron route-boundary regression coverage.
- `web/lib/cron/timingContract.ts` - Shared timing envelope utilities used by cron responses and audit rows.
- `web/lib/cron/cronReportTiming.ts` - Timing extraction logic used by the report when reading audit details.
- `web/lib/cron/benchmarkNotes.ts` - Benchmark annotations shown in the cron emails and report tables.
- `web/pages/api/v1/db/update-nst-gamelog.ts` - NST gamelog route and a likely missing-wrapper migration target.
- `web/pages/api/v1/db/update-nst-current-season.ts` - Current-season NST route that must use the new authenticated NST host.
- `web/pages/api/v1/db/update-nst-last-ten.ts` - NST route that must be migrated to the new authenticated host.
- `web/pages/api/v1/db/update-nst-goalies.ts` - NST goalie route that must be migrated and verified.
- `web/pages/api/v1/db/update-nst-team-daily.ts` - NST team-daily route tied to missing-audit observations in the cron report.
- `web/pages/api/v1/db/update-nst-player-reports.ts` - NST player report route that must share the new host and auth flow.
- `web/pages/api/v1/db/check-missing-goalie-data.ts` - NST-backed helper route that must share the new host and auth flow.
- `web/pages/api/Teams/nst-team-stats.ts` - Team stats route behind `update-nst-tables-all` and `update-nst-team-stats-all`.
- `web/pages/api/v1/db/shift-charts.ts` - Cron-scheduled Supabase-writing route with bespoke audit insertion that needs normalization.
- `web/pages/api/v1/db/update-season-stats.ts` - Scoped Supabase-writing cron route that appeared as a missing-audit case.
- `web/__tests__/pages/api/v1/db/update-season-stats.route.test.ts` - Season and dynamic game-stat route regressions for sources, persistence, identity, cursor, PP-TOI, status, and audit behavior.
- `web/pages/api/v1/db/update-rolling-games.ts` - Scoped Supabase-writing cron route that appeared as a missing-audit case and had a recent runtime failure.
- `web/pages/api/v1/db/cron/update-stats-cron.ts` - Repeated audit failure source that likely needs partial-success classification changes.
- `web/pages/api/v1/db/update-stats/[gameId].ts` - Dynamic game-stat writer sharing identity, source, team, skater, goalie, and failure-classification contracts.
- `web/pages/api/v1/db/update-line-combinations/index.ts` - Repeated audit noise source during live-game processing.
- `web/pages/api/v1/db/update-standings-details/index.ts` - Live failing route with NHL API `429` behavior to triage.
- `web/pages/api/v1/db/run-projection-v2.ts` - Live failing route with `422` preflight or pipeline validation behavior.
- `web/pages/api/v1/db/update-sko-stats.ts` - Live failing route with the `assists_5v5` schema mismatch.
- `web/pages/api/v1/db/update-power-rankings.ts` - Live failing route with the `(void 0) is not a function` runtime error.
- `web/pages/api/v1/db/run-projection-accuracy.ts` - Live failing downstream route that depends on a succeeded projection run.
- `web/pages/api/v1/db/update-wgo-goalies.ts` - WGO goalie writer whose bulk and row-level fallback paths must not convert zero persisted rows into route success.
- `web/pages/api/v1/db/update-wgo-skaters.ts` and `web/__tests__/pages/api/v1/db/update-wgo-skaters.types.test.ts` - Exact generated regular-season/playoff insert contracts, target-specific time-field normalization, and focused writer regressions.
- `web/lib/cron/wgoGoaliePersistence.ts` - Bounded bulk/row persistence contract that surfaces incomplete WGO goalie writes as structured actionable failures.
- `web/lib/cron/wgoGoaliePersistence.test.ts` - Focused regression coverage for bulk success, bounded fallback recovery, partial writes, zero persistence, and bounded diagnostics.
- `web/__tests__/pages/api/v1/db/update-wgo-goalies.test.ts` - Handler-level proof that incomplete WGO persistence returns HTTP 500 and writes a truthful error audit with actual persisted-row counts.
- `web/lib/cron/skaterGameStatsPersistence.ts` - Shared bounded parent-repair and full-batch retry contract for skater game statistics.
- `web/lib/cron/skaterGameStatsPersistence.test.ts` - Focused atomic skater-batch, exact-FK, missing-parent, and terminal-failure regressions.
- `web/lib/cron/gameStatsCompleteness.ts` - Shared fail-closed source, team-row, and team-write completeness contract for dynamic, daily, and recent game-stat routes.
- `web/lib/cron/gameStatsCompleteness.test.ts` - Focused regressions for missing/invalid team sources, team-write failure, and absent/empty finished-game player sources.
- `web/lib/cron/statsStatusAdvancement.ts` and its focused test - Exact returned-game-ID proof for normal status advancement and stale quarantine.
- `web/lib/cron/statsUpdateSafety.ts` and its focused test - Shared bounded diagnostic sanitizer and explicit pre-write-only stale-quarantine eligibility contract.
- `web/lib/cron/transactionalGameStatsPersistence.ts` and its focused test - Service-role caller contract for exact parent preflight/repair, one all-table persistence RPC, exact manifest receipts, and bounded atomic quarantine receipts.
- `web/lib/NHL/base.ts`, `web/lib/cron/nonRealizedGameStats.ts`, and focused tests - Typed exact NHL status/URL errors plus the fail-closed three-endpoint 404 corroboration boundary for non-realized schedule rows.
- `supabase/migrations/20260715155738_add_non_realized_game_stats_and_season_selector.sql` and `web/lib/supabase/nonRealizedGameStatsMigration.test.ts` - Additive seven-day zero-stat terminalization and service-role-only current-season discovery RPC contract.
- `web/lib/cron/normalizeDependencyError.ts` - Dependency classification now emits only bounded, sanitized operator details.
- `web/lib/NHL/htmlReports.ts` - Canonical exact-eight-digit NHL HTML game-summary report URL builder.
- `web/__tests__/pages/api/v1/db/update-stats-cron.test.ts` - Route regressions for pending structured failures and stale-game quarantine separation.
- `supabase/migrations/20260714211247_fix_immutable_unaccent_search_path.sql` - Pending fail-closed immutable-unaccent catalog/search-path remediation.
- `web/lib/supabase/immutableUnaccentMigration.test.ts` - Static precondition, postcondition, attribute, and qualified-call migration contract.
- `supabase/migrations/20260715015300_add_transactional_game_stats_manifest.sql` - Additive transactional game-stat persistence, durable outcome/cardinality manifest, legacy RPC hardening, and goalie prune-index migration pending branch/production apply.
- `web/lib/supabase/transactionalGameStatsMigration.test.ts` - Planned static migration security, manifest, atomic-write, quarantine, and access-path contract.
- `web/lib/cron/withCronJobAudit.test.ts` - Unit tests for audit status inference and structured partial-success handling.
- `web/pages/api/v1/db/cron-report.test.ts` - Tests for route-to-job matching, missing-audit handling, and report classification.
- `web/pages/api/v1/db/update-nst-gamelog.test.ts` - Tests for NST configuration failure and authenticated-host usage.
- `web/pages/api/Teams/nst-team-stats.test.ts` - Tests for NST team-stats host migration and audit/report compatibility.
- `tasks/TASKS/cron-operations/cron-schedule.md` - Operator contract for the line-combo, player-underlying, and power-play trigger/scheduled owners.
- `tasks/TASKS/schema-docs/supabase-context/supabase-table-structure.md` - Historical schema snapshot used to reconcile the enabled `lineCombinations` triggers against live catalog evidence.
- `supabase/migration-archive/pre-baseline-20260716/` - Immutable 34-file authoritative-root and 42-version production-ledger archive, checksum ledger, omission manifest, and recovery contract.
- `supabase/migrations/20260716112908_production_schema_baseline.sql` - Credential-free current public-schema baseline with production ACL/default-privilege replay and provider-compatible public type preservation.
- `supabase/migrations/20260716112909_add_line_combinations_source_provenance.sql` - Authoritative-root additive provenance columns and service-role-only atomic line-combination RPC.
- `supabase/migrations/20260716112910_harden_line_combination_trigger_auth.sql` - Three-semantics fail-closed Vault-backed trigger replacement with hidden helper schema and revoked direct execution.

### Notes

- Unit tests should typically be placed alongside the code files they are testing.
- Use `npx vitest run [optional/path/to/test/file]` from `web` to run focused tests under the repository's configured Vitest runner.
- The NST key is not yet available, so task sequencing should separate configuration-failure validation from authenticated live-route validation.

## Tasks

- [x] 1.0 Build the remediation baseline from the March 29 and March 31, 2026 cron emails and map every scoped issue to its owning route, audit source, and failure category.
  - Verification: the paired source list's durable `Failure Inventory` enumerates the hard-route, missing-observation, audit-gap, unknown-status, partial-row, and executive-reporting categories; the shared PRD maps those categories to owning routes/audit sources and explicit remediation requirements (verified 2026-07-15).
- [x] 2.0 Migrate all scoped NST routes to the new authenticated `data.naturalstattrick.com` integration with fail-fast configuration handling and secret-safe logging.
  - Verification: the dedicated NST migration list is fully complete, the active direct ingestion routes resolve `data.naturalstattrick.com` through the shared client while retained proxy/legacy ownership is explicit, and the combined local gate passes the shared client plus scheduled gamelog contracts without secret-bearing output (verified 2026-07-15).
- [ ] 3.0 Normalize audit production and cron-report correlation for cron-scheduled Supabase-writing routes so missing-audit and false-failure cases are eliminated.
- [ ] 4.0 Fix the live non-NST application failures and partial-success classification problems that currently prevent a clean cron health report.
  - [x] NEW 4.1 **P1 immutable-unaccent production failure:** Dedicated production migration `fix_immutable_unaccent_search_path` is ledgered; the exact immutable/strict/parallel-safe/security-invoker/cost/support/empty-search-path/qualified-dictionary contract, three parity probes, and 6/6 dependent-index health pass. Security/performance advisors have no target defect, target-specific Postgres error count is zero, and rollback-only no-op writes exercise all five dependent tables successfully (verified 2026-07-15).
    - [x] NEW 4.1.1 **P1 WGO goalie-write false-success containment:** Bounded bulk/row recovery throws on every residual failure and preserves actual/cumulative persisted counts; the promoted production route returned HTTP 200 for `2026-06-14`, retained the exact two-row date set, and recorded a successful two-row audit (verified 2026-07-15).
  - [x] NEW 4.2 **P1 skater-game-stats ownership failure:** The shared exact-named-FK contract performs a bounded parent lookup, repairs only proven missing players, and retries the complete batch once; promoted production game `2025030416` completed atomically with 36/36 skater rows, 4/4 goalie rows, two team rows, and a successful audit without orphaning or dropped rows (verified 2026-07-15).
    - [x] NEW 4.2.1 **P1 multi-route skater-stat false-success containment:** Dynamic, daily, and recent writers propagate structured batch failures, keep failed games pending, and exclude them from stale quarantine; promoted production game `2025030416` reached exact manifest-v1 completion and the invalid `2025030417` placeholder failed closed at source acquisition with zero writes (verified 2026-07-15).
- [ ] 5.0 Re-run targeted route validation and the end-to-end cron email process, then confirm the post-remediation report is accurate and stable.
- [x] NEW 6.0 **P1 multi-route goalie-stat false-success containment:** Both routes use the exact-named-FK parent-repair/whole-batch retry contract and propagate terminal failures; promoted production game `2025030416` completed with exact 4/4 goalie cardinality while invalid game `2025030417` remained pending with zero rows after a structured upstream failure (verified 2026-07-15).
- [x] NEW 7.0 **P2 regression/tooling reliability:** Replaced optional `.catch(...)` assertions with mandatory rejection capture, aligned this list to Vitest, and confirmed the combined cron-remediation gate passes 8 files/51 tests plus TypeScript, scoped legacy ESLint, Prettier, forbidden-fallback scans, and diff integrity (verified 2026-07-14).
- [x] NEW 8.0 **P1 WGO upstream-fetch/overwrite false-success containment:** Every required page validates HTTP/JSON/data shape before persistence; overwrite writes the complete replacement first and prunes only stale IDs. The promoted `2026-06-14` production probe returned HTTP 200, retained the exact two-row date set, and recorded a successful two-row audit (verified 2026-07-15).
- [x] NEW 9.0 **P1 multi-route team-stat false-success containment:** The nine-category source contract and exactly-two-row write contract fail closed across all writers; promoted production game `2025030416` completed with exact 2/2 team rows, while invalid game `2025030417` remained pending with zero team rows (verified 2026-07-15).
- [x] NEW 10.0 **P1 multi-route player-source false-success containment:** All six home/away arrays plus non-empty processed skater/goalie batches are required. Promoted production game `2025030416` completed with exact 36/36 skater and 4/4 goalie counts; invalid game `2025030417` stayed pending with zero player rows (verified 2026-07-15).
- [x] NEW 11.0 **P1 explicit-success audit precedence:** Explicit `success`/recognized `status` now precedes legacy message heuristics, `Failed: 0` remains successful, and `succeeded` supplies the exact audit row count; included in the 10-file/94-test combined gate (verified 2026-07-14).
- [x] NEW 12.0 **P1 stats-status zero-match false success:** Normal completion, season completion/upsert, and stale quarantine all require `.select("gameId")` plus an exact returned-ID set; zero/partial matches remain pending and fail closed (verified 2026-07-14).
- [x] NEW 13.0 **P1 gap-safe season incremental cursor:** Season processing stops at the first failure, defers later games, and retries the gap on the next run with focused failure-then-success coverage (verified 2026-07-14).
- [x] NEW 14.0 **P1 season PP-TOI folder correctness:** The shared HTML-report builder now preserves the canonical eight-digit season folder and the season route proves the exact URL before an accepted `00:00` fallback (verified 2026-07-14).
- [x] NEW 15.0 **P1 audit-insert returned-error containment:** The shared audit wrapper now inspects a resolved Supabase `{ error }` and emits an operator-visible failure diagnostic; focused returned-error coverage passes (verified 2026-07-14).
- [x] NEW 16.0 **P1 WGO season-lookup false-success containment:** Supabase lookup failures now throw a structured sanitized fetch failure while a genuine absent season remains an explicit zero-update skip (verified 2026-07-14).
- [x] NEW 17.0 **P1 WGO write-diagnostic sanitization:** Bulk, row, recovered-fallback, fetch, and prune diagnostics share bounded URL/Bearer/control-character redaction across logs, HTTP, and audit details (verified 2026-07-14).
- [x] NEW 18.0 **P2 WGO pagination termination:** Date-wide acquisition rejects repeated full pages and any required source still full at the 25-page ceiling with structured audit-failed output (verified 2026-07-14).
- [x] NEW 19.0 **P2 immutable-function catalog precondition:** The migration now fail-closes on pre/post cost, leakproof, support, and configuration drift while explicitly preserving the verified security-invoker/search-path/normalization contract (verified locally 2026-07-14; production apply remains under 4.1).
- [x] NEW 20.0 **P1 requested/landing game-identity boundary:** Both stats writers reject a requested/landing ID mismatch before the first write; the recent cron keeps the requested game pending and non-quarantinable (verified 2026-07-14).
- [x] NEW 21.0 **P2 bounded recent-cron fan-out:** `count` is now a positive integer from 1 through 10, invalid values return caller HTTP 400 before the RPC, and selected concurrent work is capped at ten unique IDs (verified 2026-07-14).
- [x] NEW 22.0 **P2 internal-failure HTTP classification:** Caller input alone uses 4xx; database/RPC/status/quarantine/upstream/internal failures return bounded HTTP 5xx and remain pending/audit-failed (verified 2026-07-14).
- [x] NEW 23.0 **P2 season audit successful-row recognition:** The shared wrapper recognizes `succeeded` as the exact successful-game `rows_affected` count without including failed or deferred work (verified 2026-07-14).
- [x] NEW 24.0 **P1 durable historical-gap recovery:** The season route uses a bounded, ordered, paginated structural completeness scan covering missing/false status, exactly-two team rows, and non-empty skater/goalie batches, then resumes inclusively at the earliest incomplete game; older-gap/later-row, missing-status, and legacy `updated=true` incomplete regressions pass (verified locally 2026-07-14). The separate quarantine/completeness collision is tracked by NEW 31.0.
- [x] NEW 25.0 **P1 bespoke WGO audit-insert containment:** The route delays HTTP output until the manual audit insert resolves; returned or rejected audit failures now produce bounded sanitized HTTP 500 `WGO_GOALIE_AUDIT_WRITE_FAILED` plus a safe operator log instead of false success (verified 2026-07-14).
- [x] NEW 26.0 **P2 player-specific WGO pagination hardening:** The `playerId + date` reader now uses required HTTP/JSON/page-shape validation, repeated-full-page detection, and the shared 25-page ceiling (verified 2026-07-14).
- [x] NEW 27.0 **P1 rejected WGO write sanitization:** Rejected bulk and row upsert promises are normalized into bounded sanitized `WgoGoalieWriteError` details with partial/failed-row accounting (verified 2026-07-14).
- [x] NEW 28.0 **P2 generic stats diagnostic sanitization:** One bounded sanitizer redacts HTML, URLs, bearer-shaped values, and control characters from generic season/recent/dependency failures; raw settled-result logging is removed (verified 2026-07-14).
- [x] NEW 29.0 **P2 cross-table partial-game persistence:** Owner-approved option A routes all three writers through one security-invoker transactional RPC after source materialization and exact parent preflight. The production migration is ledgered and promoted game `2025030416` completed atomically with exact 2/36/4 rows plus the v1 manifest; invalid `2025030417` produced zero cross-table rows and remained pending (verified 2026-07-15).
- [x] NEW 30.0 **P1 stale-quarantine partial-data certification:** Quarantine is now opt-in only through the explicit pre-write `game_not_finished` error class; generic, identity, source, database, status, and post-write failures remain pending, with integrated recent-cron regressions (verified 2026-07-14). The durable outcome collision is tracked by NEW 31.0.
- [x] NEW 31.0 **P1 durable completeness/quarantine outcome collision:** The structural season cursor correctly retries legacy `updated=true` rows missing required stats, while the recent cron intentionally writes the same `updated=true` bit for explicitly safe stale pre-write quarantine. Add a distinct durable quarantine outcome/reason (or another verified exclusion contract) so quarantined/cancelled games cannot become the perpetual earliest incomplete blocker while legacy false-success rows remain detectable; include this in the transactional/status schema checkpoint (discovered 2026-07-14).
  - Production evidence: the additive v1 state machine now distinguishes `complete`, `game_not_finished`, and `schedule_not_realized`; placeholder `2025030417` reached an exact terminal 0/0/0 receipt through the promoted route, current-season routine discovery excludes it at zero pending, historical discovery remains separately explicit at 14,433 rows, and jobs 2/318 were restored only after these gates passed (verified 2026-07-15).
- [x] NEW 32.0 **P2 durable player-batch cardinality certification:** The production RPC recounts all three tables after game-scoped pruning, rejects mismatches, and writes expected/observed counts transactionally. Promoted game `2025030416` completed with exact 2/2, 36/36, and 4/4 receipts; focused mismatch/rollback/selection regressions remain green (verified 2026-07-15).
- [x] NEW 33.0 **P2 single-player WGO identity/query boundary:** Positive safe-integer validation now precedes season lookup/fetch, the normalized NHL ID is used in all three Cayenne predicates and audit context, and mismatched summary/advanced/days-rest rows are excluded while raw-page pagination safeguards remain intact; focused 25/25 and consolidated 99/99 regressions pass (verified 2026-07-14).
- [x] NEW 34.0 **P1 privileged stats-discovery RPC exposure:** Production `public.get_unupdated_games()` now preserves `TABLE(gameid bigint)` as stable fully-qualified security-invoker SQL with an empty search path; `PUBLIC`, anon, and authenticated execution are false, service-role execution is true, and an actual service-role call returns the bounded scalar count successfully (verified 2026-07-15).
- [x] NEW 35.0 **P2 goalie stale-prune access path:** Production migration created `idx_goaliesgamestats_gameid` with `gameId` as the first key; catalog proof is valid/ready/live and the schema-only executable contract already proved its game-scoped use. The immediate INFO-level unused-index advisory is expected before the first promoted writer probe and is not a defect/removal signal (verified 2026-07-15).
- [x] NEW 36.0 **P1 Supabase clean-branch migration reproducibility:** Authoritative commit `09c4d968946898e42a8fd90a82c447a0f278e545` archives the 34-file Git root and exact 42-version production ledger, registers one credential-free current-schema baseline plus two reviewed deltas, and preserves squash-omitted data/configuration ownership explicitly. A replacement data-less branch replayed the complete chain and matched production at 286 tables, 76 views, eight materialized views, 72 sequences, 1,011 constraints, 928 indexes, 285 policies, 94 safe functions, all non-target triggers, relation/schema/routine ACLs, and logical catalog fingerprints; only the explicit pg_cron `jobname::name` compatibility cast and a deparser-only redundant alias differed. Rollback-contained provenance/trigger probes, security/performance advisors, four static tests, formatting, and credential-pattern scans passed. Production history now contains only contiguous versions `20260716112908`–`20260716112910`; the temporary branch was deleted, the guarded branch publication succeeded, and exact Production deployment `dpl_5LADmdbVfdJEzKubKkpR7Yr7zxzk` is READY/current on all five aliases at the same source SHA (verified 2026-07-16).
- [x] NEW 37.0 **P1 deferred transactional-RPC SQL runtime failure:** Six typed-row validation expressions in `20260715015300_add_transactional_game_stats_manifest.sql` called `pg_catalog.coalesce(...)`, but PostgreSQL implements `COALESCE` as a conditional expression rather than a schema-qualified function. Removed only those six invalid qualifications while preserving genuine catalog-function qualification; the recurrence regression forbids their return. Executable PostgreSQL on the schema-only production clone ran the team/skater/goalie typed validators through an exact 2-team/1-skater/1-goalie persistence receipt and rollback-contained failure path. The focused 4-file/49-test gate, implementation-file Prettier, migration SHA check, and scoped diff checks pass (verified 2026-07-14).
- [x] NEW 38.0 **P1 deterministic npm/Vercel dependency boundary:** The clean web payload passed exact npm installation, Supabase/PostgREST 2.89.0, full TypeScript, and 14 Draft Ranker suites/68 tests. The 2,146-file repo-shaped wrapper excludes every forbidden env/cache/pnpm path, is byte-identical to the accepted web payload, and has external manifest SHA-256 `26ad23785f5596c9f6628f581b1f90c21705b5a67b4f05da7bca978ab4301678`. Exact Production-environment staging deployment `dpl_HgjrBeCQVzDTsgryafBLzmXqGyjH` is READY/STAGED with one READY non-skipped build, matching metadata, a clean error scan, and the current Production target/custom aliases unchanged (verified 2026-07-15).
- [x] NEW 39.0 **P2 strict Supabase game-stat write typing:** Removed the open-index `SkaterDbRecord` boundary and payload casts. A discriminated writer now checks regular-season and playoff rows directly against their generated `Insert` contracts, preserving numeric regular-season time fields and normalizing the playoff text-column variants before the exact table upsert. The resolved Supabase/PostgREST 2.110.3 pair passes both focused writer regressions, full TypeScript, Prettier, forbidden-cast scanning, and diff integrity (verified 2026-07-16).
- [x] NEW 40.0 **P1 immutable-unaccent migration convergence drift:** Exact SHA `b2c345b8…00ed2` converged the already-hardened production function through a dedicated migration ledger row without weakening any attribute. Catalog, empty-search-path, qualified function/dictionary, parity, 6/6 dynamic dependent-index, zero target-advisor/error-log, and five-table rollback-only indexed-write proofs pass (verified 2026-07-15).
- [x] NEW 41.0 **P1 concurrent Draft Ranker prop-contract release blocker:** The accepted clean npm payload includes every required ranking-identity caller/module, passes full TypeScript and 14 focused Draft Ranker suites/68 tests, and is frozen byte-for-byte inside the final repo-shaped wrapper manifest (verified 2026-07-15).
- [x] NEW 42.0 **P1 deterministic production-build heap capacity:** `web/package.json` fixes the build heap at 6 GiB; the accepted source compiled without OOM locally and the exact environment-backed staged Vercel build completed READY without skipping the build (verified 2026-07-15).
- [x] NEW 43.0 **P2 Vercel `--skip-domain` alias semantics:** The exact Production-environment stage received two automatic Vercel-generated project aliases even though customer-domain promotion was disabled. The alias registry proves only those two project aliases point to the STAGED artifact, while `fhfhockey.com`, `www.fhfhockey.com`, `fhfhockey.vercel.app`, and the current Production target remain on base deployment `dpl_ALiDv4okqPrv1HZ3oiX7xJXTEbWX`; the release gate therefore keys on customer/current-target invariance rather than an incorrect zero-alias assumption (discovered and verified 2026-07-15).
- [x] NEW 44.0 **P1 persistent pg_cron connecting-run cutover gate:** Jobs 2/318 were quiesced only through `cron.alter_job`. Run `120816` is proven historical orphan metadata: null PID/start/end, no backend, 8,336 later successful job-2 runs, and a later successful 10:40 EDT terminal run. Both jobs are inactive, running-backend and pg_net queue counts are zero, and no cron command/history row was rewritten (verified 2026-07-15).
- [x] NEW 45.0 **P1 exact-promotion authentication boundary:** Chrome promoted only `dpl_HgjrBeCQVzDTsgryafBLzmXqGyjH` without rebuilding; its deployment page shows Production/Current and `fhfhockey.com` plus three additional domains. Missing auth returns 401, canonical Vault auth returns 200, and the WGO and played-game transactional probes pass. Writer 318 was briefly restored, then safely re-quiesced when a fresh selector audit proved that it would hit the same canceled placeholder; NEW 46.0/47.0 now govern writer restoration (verified 2026-07-15).
- [x] NEW 46.0 **P1 canceled/non-realized schedule retry loop:** Exact production probe game `2025030417` returned `structured_upstream_error`; official NHL landing, right-rail, and boxscore endpoints all return 404, the database row is a past type-3 20252026 placeholder, and fail-closed proof leaves it pending with zero 0/0/0 stats. Both job 2 and job 318 select or scan into this row, so either writer would generate a truthful recurring failure. Define and verify a truthful durable terminal outcome/selection contract for confirmed non-realized schedule placeholders without mislabeling them `game_not_finished`, deploy/probe it, and only then restore either writer (discovered 2026-07-15).
  - Verification: additive ledger version `20260715161123`, catalog/ACL/advisor and rollback-only proof, exact triple-404 application request, durable `schedule_not_realized` 0/0/0 receipt, READY exact-manifest build, production promotion `dpl_9uKDA1T3nipJSmnH68NYavgNhh3L`, live missing/current auth probes, and restored-writer activation checks all pass (verified 2026-07-15).
- [x] NEW 47.0 **P1 unbounded historical-backlog activation gate:** Live service-role proof shows `get_unupdated_games()` returns 14,434 pending zero-stat games: one current-season placeholder and 14,433 historical rows reaching back to 2003, including 927 playoff and 12,576 regular-season games. After NEW 46.0 terminalizes `2025030417`, job 2 at four games every 20 minutes would begin an unsupervised multi-season production backfill; the next nine rows are valid 2016 games whose official NHL landing/right-rail/boxscore endpoints all return 200. Define and approve scheduled-writer season/horizon ownership separately from explicit bounded paginated historical recovery, quantify provider/runtime load, and production-probe the bounded selector before either writer is restored (discovered 2026-07-15).
  - Verification: recent job 2 resolves current season `20252026` and returns an empty attempted list through the season-bounded RPC; daily job 318 uses the current-season default with no `seasonId` override; both stored commands are Vault-backed and contain neither the unbounded selector nor a historical override. Jobs 2/318 are active. First natural post-restore job-2 run `145422` succeeded at 17:00 UTC and produced a separate HTTP-200 success audit at 17:00:03; current-season pending and workers/queue are zero, while the 14,433 historical rows remain untouched and separately unapproved (verified 2026-07-15).
- [x] NEW 48.0 **P2 accidental disposable Vercel-project containment:** The owner authorized deletion of only project `prj_jbNTiFnbma6URdABpLfzzRjurCBw`. The signed-in destructive control independently matched that exact ID/name and `No Production Deployment`; Vercel redirected with the exact `projectDeleted` name, the project API now returns 404, and the team project list excludes it. Intended project `prj_LV0wbwH5gRjOsZlowzEFNGRuX7Lw` is intact: `fhfhockey.com`, `www`, `fhfhockey.vercel.app`, and its production alias still resolve exact READY production deployment `dpl_9uKDA1T3nipJSmnH68NYavgNhh3L` with the reviewed base/overlay metadata. An unrelated concurrent Git preview advanced the October branch alias to READY `dpl_7cRo4ExDNYMF7SedYreAD8QiJa5R` without changing production targets. Jobs 2/318 remain active on their Vault-backed current-season-only schedules, latest job-2 runs through `145428` succeeded, current-season pending/workers/queue remain zero, historical pending remains 14,433, and placeholder `2025030417` remains exact terminal 0/0/0 (completed 2026-07-15).
- [x] NEW 49.0 **P1 local/provider cron-secret and exact-environment promotion boundary:** Exact preview `dpl_ArLUeq9eEhsdxKV3KMgYBzzPdTVQ` proved the seven-day terminalization and current-season-only route behavior. Scoped pulls proved current Vercel Preview/Production `CRON_SECRET` values equal, while ignored `web/.env.local` was stale and correctly rejected. Updated only the local ignored value without disclosure; exact 14-file Production artifact `dpl_9uKDA1T3nipJSmnH68NYavgNhh3L` built READY, preserved live aliases until explicit promotion, then became current on `fhfhockey.com`/`www`/project production aliases. Live missing/current auth returned 401/200 and jobs 2/318 were restored with zero current-season pending (verified 2026-07-15).
- [ ] NEW 50.0 **P1 production cron-report zero-inventory false success:** The old packaged route converted a missing schedule file into a false-green zero-inventory report. The stable Next.js 15 tracing/dual-root/fail-closed repair passes focused tests, TypeScript, formatting, exact 94,813-byte trace resolution, bundled-code inspection, and production builds. Authoritative commit `47066fa25fca3ef8b78f98af1a06efbcb064e7c4` and Current Production `dpl_9PEvSsprjbXFG1pZwe9gVbsodDLm` now return HTTP 200/dry-run with 64 scheduled jobs and 64 with activity. Value-free job-234 preflight proved the unchanged `15 21 * * *` command is Vault-backed, authorization-bearing, exact-route, and neither preview nor dry-run; baseline run is `145255`. Job 234 is active with zero queue/live backend, and the existing heartbeat is armed for the first natural nonzero 21:15 UTC report plus successful email/audit proof. That natural observation remains before closure (updated 2026-07-16).
- [x] NEW 51.0 **P1 production release-base regression and unauthorized historical execution:** Deployment `dpl_FiCyDfjVX1Dx14yuSoyySGJvGThQ` at commit `70c88054df6a4609bb419fa286cac6fc79e44c7a` omitted five NEW 46/47 files and regressed scheduled recent discovery to unbounded `get_unupdated_games`; runs `145432`, `145434`, and `145436` completed 12 valid historical games, and displaced run `145440` added four more valid manifests. Retain all 16 exact transactional completions absent explicit destructive rollback. The exact 19-path safety/report repair is now durable as authoritative commit `47066fa25fca3ef8b78f98af1a06efbcb064e7c4` and READY/Current Production deployment `dpl_9PEvSsprjbXFG1pZwe9gVbsodDLm`. First post-restore natural job-2 run `145519` succeeded at 10:00 UTC with a separate HTTP-200 success audit; current-season pending, queue, and live writer backends remained zero, historical pending remained 14,417, and placeholder `2025030417` remained exact terminal 0/0/0 (verified 2026-07-16).
- [x] NEW 52.0 **P0 production database credential exposure containment:** A diagnostic path search printed the ignored local production database connection URL into the private execution transcript. Do not reproduce the value. Under jobs 2/234/318 quiescence, the owner-authorized supported rotation returned HTTP 200, the immediately previous credential was rejected through the routable session pooler, ignored local `SUPABASE_DB_URL` was updated, redundant `DATABASE_PASSWORD` was removed, Vercel Sensitive/Production `POSTGRES_PASSWORD` was updated, all three Supabase-managed URL histories advanced to four Production entries, and the fresh credential authorized through the same pooler after a bounded propagation interval. Secure temporary material was removed. Durable commit `47066fa25fca3ef8b78f98af1a06efbcb064e7c4` then built Production deployment `dpl_9PEvSsprjbXFG1pZwe9gVbsodDLm` with the new Production environment; it is READY/Current, all five public aliases return HTTP 200, missing/current stats auth is 401/200, the selector is an exact zero-game no-op, the report is nonzero, and final queue/backend/current/historical counts are 0/0/0/14,417 (verified 2026-07-16).
- [x] NEW 53.0 **P1 nondurable production release and repeated Git auto-deploy override:** Owner-approved option A advanced authoritative `octoberBranch` by guarded fast-forward from `34da2bcee949f2fc1a5cb1780d765c2aa10fdc93` to exact 19-path commit `47066fa25fca3ef8b78f98af1a06efbcb064e7c4` without caches or unrelated work. Preview and Production builds are READY; Production `dpl_9PEvSsprjbXFG1pZwe9gVbsodDLm` is Current and source-identical, all five aliases return 200, missing/current stats auth is 401/200, current-season selection is a zero-game no-op, and the report has 64 scheduled/64 active jobs with zero unknown/missing-last/missing-audit findings. Jobs 2/318 were restored after value-free command checks. Natural run `145519` then succeeded at 10:00 UTC with a separate HTTP-200 success audit, while current-season pending/queue/live backends remained 0/0/0, historical pending remained 14,417, and the placeholder remained exact terminal 0/0/0. Job 234 was then independently preflighted and activated for NEW 50's separate natural report/email gate (verified 2026-07-16).
- [x] NEW 54.0 **P0 intermediate provider-editor credential rendering and proof-classification correction:** A provider-editor DOM snapshot briefly rendered an intermediate database credential during the authorized cutover. Superseding rotations revoked it immediately; no deployment, writer restoration, queue activity, current-season work, or historical backfill occurred during the incident. Browser-side `ENOTFOUND` results were reclassified as DNS failures rather than authentication evidence. The definitive supported rotation instead proved status-only former-current rejection and fresh-current authorization through the routable session pooler, updated local/Vercel consumers without outputting values, removed all secure temporary material, and preserved jobs 2/234/318 inactive with zero queue/live writer backends and 14,417 historical pending (contained and verified 2026-07-16).
- [x] NEW 55.0 **P1 live trigger bearer residue and broken route authorization:** All three existing `lineCombinations` `AFTER INSERT` semantics are preserved with the same routes, the home-team-only player-underlying bound, and team-specific line-combination behavior. The replacement functions resolve exactly one nonblank canonical `cron_secret` through a hidden privilege-revoked helper, fail closed when it is unavailable, use empty search paths, retain security-definer ownership, and are not directly executable by public API roles. Branch and production proof shows three hardened functions/triggers, zero long bearer literals, zero noncanonical target-auth queue rows or retained response residue, no internal-schema access for anon/authenticated, and 401/200 missing/current route authorization. Production migration `20260716112910` is ledgered and exact deployment `dpl_5LADmdbVfdJEzKubKkpR7Yr7zxzk` is READY/current with no deployment-scoped error logs (verified 2026-07-16).
- [ ] NEW 56.0 **P1 unreferenced public WGO RPC bearer residue:** Baseline sanitization found `public.update_all_wgo_skaters()` contains another hardcoded bearer pattern and retains default direct-execution privileges. Production catalog proof shows no trigger, cron job, view, or other function references it, but repository absence does not prove that an external RPC consumer does not exist. Exclude the credential-bearing body from the clean bootstrap baseline; inventory PostgREST/API/log evidence value-free; then revoke/replace or remove the routine only through a separately approved breaking-contract checkpoint, with zero credential residue and explicit rollback proof. Never print the value or body (discovered 2026-07-16).
- [ ] NEW 57.0 **P1 owner-sensitive public security-definer RPC escalation risk:** Production `public.get_skater_game_score_by_limit(bigint, integer)` is owned by `anon`, is `SECURITY DEFINER`, has a mutable search path, and retains default PUBLIC execution. A no-owner schema dump would recreate it under `postgres`, materially broadening the same public call into a privilege-escalation boundary on clean branches. Exclude it from the supported baseline; complete value-free API/log and external-consumer inventory; then repair, revoke, replace, or remove it only through a separately approved public-contract/security checkpoint with an explicit rollback plan. Do not copy the unsafe production owner or expose the function body (discovered 2026-07-16).
