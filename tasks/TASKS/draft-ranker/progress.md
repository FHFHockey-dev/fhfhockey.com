# Initial Progress State

- **Planning documents:** [Repository audit](./repository-audit.md), [decision record](./decision-record.md), [implementation plan](./implementation-plan.md), [product strategy](./product-strategy.md), and [execution contract](./goal-prompt.md).
- **Overall objective:** Deliver an account-backed personal NHL draft ranker with a persistent top 250, continuous candidate order, verified broader player universe, assisted placement, homepage pairwise refinement, explainable discovery, and privacy-preserving Community Ranking v1.
- **Planning status:** Complete, saved, and reviewed through the approved implementation-goal handoff.
- **Current phase:** Phase 7 — staged production rollout. Product implementation, DR-073 launch QA, staff-stage activation, and the explicitly approved homepage-only observation waiver are complete. The live homepage now shows the account-backed voting surface and opening-night countdown. DR-072 remains active for all broader cohort or secondary-feature expansion; the original 72-hour/50-journey evidence gate has not been declared passed.
- **Goal status:** Active 2026-07-15. Draft Ranker PR [#335](https://github.com/FHFHockey-dev/fhfhockey.com/pull/335) merged as `722f1dff02b7a4b9486836b386c0b576f57c5cfd`. Homepage PR [#338](https://github.com/FHFHockey-dev/fhfhockey.com/pull/338) merged as `3685ce1ef87bf96bbad6f2fc307a971220428615`, and exact reviewed preview `dpl_8sW3z1oABqQGAtxopeSnpLXsyWgp` was promoted into READY production deployment `dpl_FiCyDfjVX1Dx14yuSoyySGJvGThQ`. Production remains at rollout stage `staff` for exactly three verified Supabase admin accounts; only the production homepage flag was newly enabled. Tim Branson remains operator, support owner, and rollback owner. Immediate pre-homepage deployment `dpl_8RitVqdJ9QsRTi11J7xjH66u6cdu` and older retained deployment `dpl_HgjrBeCQVzDTsgryafBLzmXqGyjH` are rollback references.
- **Approved decisions:** DRD-001 through DRD-013, including the six grouped user approvals.
- **Open product decisions:** None for launch planning.
- **Future approval checkpoints:**
  - Any destructive or difficult-to-reverse production migration.
  - Replacement of existing identity, authentication, or homepage architectures.
  - Paid or license-restricted player sources.
  - Material changes to Community Ranking methodology.
  - Conflicting Yahoo or projection ingestion changes.
  - Production rollout.
- **Completed task IDs:** DR-001, DR-002, DR-003, DR-010, DR-011, DR-012, DR-013, DR-020, DR-021, DR-022, DR-023, DR-030, DR-031, DR-032, DR-033, DR-040, DR-041, DR-042, DR-043, DR-050, DR-051, DR-052, DR-060, DR-061, DR-062, DR-063, DR-070, DR-071, and DR-073.
- **In-progress tasks:** DR-072 only. Server-side rollout enforcement, runbooks, launch QA, privacy checks, operational ownership, staff activation, and the scoped homepage launch are complete. The real 72-hour/50-journey staff observation remains in progress for every later expansion; allowlisted, authenticated, and public cohort observations have not begun.
- **Blockers to current work:** No blocker remains for the completed homepage safe spot. The next expansion is still time- and evidence-gated: three staff accounts must produce at least 50 complete journeys, the 72-hour minimum must elapse, and every privacy, ordering, deletion, latency, health, retry, and unexpected-5xx criterion must pass. Discovery, contribution collection, public Community Ranking, beta entitlement, and wider authenticated entitlement remain off.
- **Approval blocker:** The product owner approved the homepage flag as a narrow exception. Any next secondary flag or cohort expansion still requires completed staff evidence, a recorded review, and separate explicit approval.
- **Launch gates:** Dirty-worktree isolation, canonical migration-path confirmation, Supabase security-advisor triage, Postgres patch review, identity backfill reconciliation, and honest community cold-start presentation.
- **Completed planning work:**
  - Read both authority documents in full.
  - Audited repository identity, Yahoo, projection, roster, prospect, authentication, RLS, API, UI, state, export, test, cron, and planning conventions.
  - Inspected relevant live Supabase schema, counts, mappings, functions, policies, and advisors.
  - Resolved architectural decisions A–M.
  - Obtained blanket approval for all recommended product and privacy directions.
  - Produced dependency-ordered tasks DR-001 through DR-083.
  - Saved the approved repository audit, decision record, implementation plan, and initial progress state under `tasks/TASKS/draft-ranker/`.
  - Completed DR-002 and recorded the dirty-worktree boundary, canonical migration path, live migration ledger, live schema counts, ADP quality, and advisor baseline in [execution-boundary.md](./execution-boundary.md).
  - Completed DR-003 and froze the schema, API, RLS, RPC, concurrency, idempotency, deletion, feature-flag, threat-model, and verification contracts in [technical-contract.md](./technical-contract.md).
  - Generated the identity-registry migration through the Supabase CLI; after the approved live apply, aligned its local version to the authoritative ledger as `20260714223013_create_fhfh_player_identity_registry.sql`.
  - Added `fhfhPlayerIdentityMigration.test.ts`; all six focused Vitest tests pass.
  - Parsed all 62 migration statements with `pglast` and confirmed no migration-defined identifier exceeds PostgreSQL's 63-byte limit.
  - Recorded the review scope, security posture, completed/missing validation, and rollback boundaries in [dr-010-migration-review.md](./dr-010-migration-review.md).
  - Applied live migrations `20260714223013_create_fhfh_player_identity_registry` and `20260714223217_add_fhfh_identity_foreign_key_indexes` after approval and a fresh dependency/ledger preflight.
  - Verified five tables, five triggers, 50 constraints, 27 indexes, RLS and restrictive policies on every table, no client CRUD, service-role CRUD, and the fixed-search-path SECURITY INVOKER trigger function.
  - Ran a rolled-back live insert/update/constraint probe with zero residual rows; post-apply advisors report no target security findings and no target unindexed foreign keys. Expected unused-index informational notices remain while the tables are empty.
  - Applied `20260714224230_backfill_fhfh_nhl_player_identities`; reconciled 3,550/3,550 canonical identities, NHL mappings, and aliases, 4,075/4,075 roster-history rows, 1,147/1,147 active/current states, all nine duplicate-name groups, and zero ID mismatches.
  - Applied `20260714224513_stage_yahoo_identity_mapping_review`; staged 2,080 Yahoo keys as `review_required`, created 1,068 deduplicated review items, excluded all six ambiguous Yahoo IDs from mappings, and promoted zero Yahoo evidence automatically.
  - Added two DR-011 migration contract suites; all 14 focused identity tests pass. Both DML migrations were also executed twice inside rolled-back live transactions before apply to prove rerun behavior and zero residue.
  - Replaced the Yahoo mapping job's whole-name 85% and last-name-only fallbacks with a single matcher requiring last name >= 90 and first name >= 50, canonicalizing 38 nickname groups, and rejecting equal-score ties.
  - Added nine focused Python matcher tests; syntax compilation and forward/reverse Michael-to-Mike integration checks pass.
  - Re-evaluated all 1,074 distinct legacy pairs read-only: 1,058 of 1,062 single-candidate pairs pass, four fail, and all six multi-candidate Yahoo IDs remain unresolved. The qualified group contains 1,047 players present in Yahoo 2025 data.
  - Applied `20260715010036_promote_approved_yahoo_identity_matches` after user approval and a twice-run rollback-only rehearsal. The manifest promoted 1,058 distinct Yahoo IDs across 2,072 season-scoped mappings and resolved 1,058 review items; ten review items remain pending by design.
  - Recorded manifest hash `1f34475fea9977c4f827a900e96fa9c031ac195f734811bab0a423ba3545e4f8`, the approved system-review actor, and independent scores on every promoted mapping and resolved review.
  - Reconciled 1,047 verified 2025 players, including 239 with positive preseason ADP and 313 satisfying the approved preseason-or-scalar-fallback seed rule.
  - Re-ran 12 Python matcher/promotion tests and 14 focused Vitest migration tests. Supabase advisors report no identity-table security findings; only expected unused-index informational notices remain.
  - Drafted the 10-table DR-012 ownership/evidence foundation with composite ownership/season/pair foreign keys, continuous sparse ordering, account-deletion cascades, opt-in consent state, and no community aggregate tables.
  - Kept raw authenticated table access read-only so later ranker mutations cannot bypass API/RPC validation, lock versions, audit events, rankability, prompt issuance, or consent checks.
  - Executed the exact DR-012 migration and a two-account RLS/privilege probe inside rolled-back live transactions. All ten tables and 22 policies validated, cross-user reads and direct mutations were denied as designed, and zero schema residue remained.
  - Added eight DR-012 migration-contract tests; all pass.
  - Applied `20260715011329_create_draft_ranker_ownership_foundation` after approval and reconciled ten tables, 104 constraints, 42 initial indexes, six triggers, ten RLS-enabled tables, and 22 owner policies.
  - Verified owner isolation, raw-write denial, uniqueness, canonical evidence, and full ranking-child cascades in a rolled-back two-account live probe with zero residue.
  - Cleared all 20 advisor-reported unindexed foreign keys through additive migration `20260715011557_add_draft_ranker_foreign_key_indexes`; no DR-012 security or unindexed-FK findings remain.
  - Regenerated the FHFH identity and draft-ranker table types from the live schema while preserving unrelated generated-type work.
  - Added shared Draft Ranker API contracts for request IDs, stable data/error envelopes, strict server flagging, Zod validation, owner-safe 404s, conflict codes, and deterministic retry fingerprints.
  - Added the authenticated `GET /api/v1/draft-ranker` bootstrap/status endpoint. It derives ownership only from `requireApiUser`, ignores client-supplied user IDs, and uses owner-filtered service queries.
  - Added 12 DR-013 tests covering 400/401/403/404/405/409/500 behavior, feature-off failure, redaction, owner scoping, validation, and retry hashing; the full TypeScript check passes.
  - Applied service-only invoker RPC migration `20260715012955_initialize_draft_ranker_from_yahoo_adp` and added `POST /api/v1/draft-ranker/initialize` behind the disabled server flag.
  - Live rollback rehearsal produced 313 deterministic entries, a derived 250/63 split, 74 fallback rows, 592 rankable/mapped invalid-ADP identities, and 446 unmapped source rows; retries and payload conflicts behaved correctly with zero residue.
  - Verified zero client/public RPC execution and no Draft Ranker security-advisor findings. Thirty-two focused API/schema/seed tests and TypeScript pass.
  - Applied service-only invoker migration `20260715013836_reorder_draft_ranking_sparse`; the API now supports move-to-rank, insert-above/below, and remove-to-bench with expected-version conflicts and operation replay.
  - A 313-player live rollback rehearsal verified 300→1, 1→251, insert-above, forced midpoint normalization, one normalization event, stale-version conflict, validation, and zero residue. No Draft Ranker advisor findings remain.
  - Added focused reorder migration, contract, server, and route tests; 29 current API/reorder tests and TypeScript pass.
  - Completed DR-022 with an authenticated owner-scoped continuous-list endpoint, account-backed ranking hook, `/draft-rankings` page, paginated top-250/candidate views, numeric movement, accessible insert-above/below selection, drag-to-insert, remove-to-bench, animated layout changes, and stale-version refresh behavior.
  - Added entry-route, server ownership, and ranking-table tests. DR-022 initially passed 40 focused tests and the then-current TypeScript check; DR-023 later expanded the focused suite to 50 passing tests.
  - The browser check caught and corrected a CSS-module purity error. Chrome then verified the real signed-out route at desktop and 390x844 mobile widths: the account gate is legible, centered, consistent with the FHFH dark visual system, and clears the mobile bottom navigation.
  - The in-app browser could not hydrate the local route, and the Next.js development client emitted an existing `isrManifest` full-reload loop while `next build` exceeded the available 4 GB heap. An isolated Chrome/dev-server verification path nevertheless completed the authenticated launch gate.
  - Completed a rollback-only live DR-023 probe: 313 seeded players, 100 randomized moves, total-order validation after every move, 100 complete reorder events, cross-user owner-safe denial, and zero auth/ranker residue.
  - Completed DR-023 using one explicitly approved isolated Supabase Auth account. Chrome verified the real authenticated desktop and 390x844 mobile layouts, 313-player initialization, direct editing, reload persistence, sign-out/sign-in persistence, and rank-250 promotion/displacement while preserving 250 ranked and 63 candidates.
  - Corrected the browser-discovered stale numeric-input state for displaced rows and added a rerender regression test. The current focused Draft Ranker suite passes 50/50 tests.
  - Signed out and deleted the isolated Auth account, then confirmed zero remaining rows across Auth, ranking, entry, event, seed-run, watchlist, pairwise, placement, user, and user-settings records; the temporary credential file was also deleted.
  - Kept production `DRAFT_RANKER_ENABLED` off and reverted the temporary local development-server configuration.
  - At this checkpoint, the repository-wide TypeScript check reported five errors outside Draft Ranker in `PlayerRadarChart.tsx`, `update-wgo-skaters.ts`, and `fetchWigoPlayerStats.ts`. DR-073 later resolved them and restored a passing full build.
  - Completed DR-030 after the Phase 3 checkpoint approval. Applied `20260715024101_add_draft_ranker_player_search`, adding verified lifecycle-aware canonical/alias/external-ID search, explicit non-rankable archived filtering, two trigram indexes, and a service-only editorial player-addition request function that reuses the identity review queue.
  - Added authenticated, feature-flagged search and request endpoints; strict Zod contracts; targeted live generated function types; a 250 ms debounced React Query hook; and an accessible search/request component integrated above the continuous ranking table. No arbitrary or provisional identity creation path exists.
  - Verified service-role-only RPC access, five-per-24-hour user rate limiting, per-user request deduplication, duplicate-name disambiguation, archived filtering, and zero rollback-probe residue. Post-apply advisors report no target security findings; only expected pre-traffic unused-index informational notices remain. Cold live exact-name search measured approximately 154 ms.
  - Seven focused DR-030 suites pass 33/33 tests. Draft Ranker added no TypeScript errors; the five unrelated baseline errors present at that checkpoint were later resolved during DR-073. Chrome rechecked the real signed-out account gate; authenticated search visual verification was deferred to the consolidated Phase 3 browser pass to avoid creating a one-task temporary account.
  - Completed DR-031 by reusing `draft_ranking_watchlist` and applying `20260715025809_add_draft_ranker_player_actions` plus the advisor-driven covering-index migration `20260715030450_add_draft_ranker_player_preference_fk_index`. Dismissed/not-relevant and compare-intent state is separate from ordered candidates and watchlist membership.
  - Added an owner-scoped watchlist/action API, React Query hook, real search-result watch/dismiss/not-relevant/restore controls, and a visible unplaced watchlist panel. Compare-now intent is persisted server-side but its UI is withheld until DR-032 can launch a real assisted-placement session.
  - A live rollback rehearsal verified watch, idempotent replay, dismiss, restore, compare intent, and cross-owner hiding. Live RLS/privilege reconciliation confirms four owner policies, authenticated read-only access, no anonymous/authenticated RPC execution, and service-role-only writes. Security advisors have no target findings and no unindexed target foreign keys remain.
  - Nine focused Phase 3 suites pass 45/45 tests; Draft Ranker added no TypeScript errors, and the five unrelated baseline errors present at that checkpoint were later resolved during DR-073.
  - Completed DR-032 with a pure deterministic placement engine and applied `20260715032036_add_draft_ranker_assisted_placement`, adding persisted engine state and four service-only SECURITY INVOKER lifecycle functions for start, answer, cancel, and atomic confirmation through the existing reorder primitive.
  - Added strict placement contracts, authenticated owner-scoped GET/POST API, server-only interval/anchor computation, targeted generated types, and a React Query hook. Scripted engine, migration, server, and route suites pass 28/28 tests; a full rollback-only live lifecycle probe verified idempotent start, answer persistence, cross-owner hiding, confirmation, watch preservation, compare-intent cleanup, and zero residue.
  - Live catalog checks confirm fixed search paths, no SECURITY DEFINER functions, no anonymous/authenticated execution, and service-role-only execution. Post-apply advisors report no target security findings; only an expected pre-traffic unused player-FK index notice remains. Production `DRAFT_RANKER_ENABLED` remains off.
  - Implemented the DR-033 UI: canonical search and watchlist Place/Re-evaluate actions, seven rough-range starts, deterministic matchup cards, explicit progress/limits, too-close/skip, suggested rank/range/confidence with real neighbors, atomic confirm/cancel, persisted active-session resume, and stale-board revalidation.
  - The real Next.js signed-out route has zero browser console errors at desktop and 390×844; mobile has zero horizontal overflow. Standalone Sass CLI verification is unavailable because the repository's current Sass/Chokidar combination attempts a CommonJS-to-ESM require under Node 22, while the same module stylesheet compiles and renders through the actual Next.js development runtime.
  - Audited the persisted resume path and found that the initial database constraint allowed one active session per player while the UI resumes one per ranking. Applied additive hardening migration `20260715034601_enforce_single_active_draft_placement`: one active session is now enforced per ranking, elapsed sessions expire before replacement, and conflicts return the exact resumable session/player.
  - The exact hardening migration passed a rollback-only live lifecycle rehearsal with zero residue. Live catalog checks confirm a valid partial unique index plus fixed-search-path SECURITY INVOKER/service-only function access; post-apply advisors contain no target security finding. The complete focused Draft Ranker suite now passes 107/107 and TypeScript passes.
  - Closed the launch-universe gap with additive migration `20260715040050_add_verified_2026_prospect`, adding real 2026 No. 3 pick Caleb Malhotra as a verified `active_prospect` with Boston University organization history, Vancouver rights-holder mapping, official NHL provenance, and a resolved editorial-review record. The migration resolves repository IDs instead of hardcoding generated IDs and has four focused contract tests.
  - Completed the approved isolated authenticated DR-033 flow. Search found Caleb Malhotra as a rankable prospect with no Yahoo ID; watch state persisted; placement resumed after a full reload; seven deterministic answers suggested No. 201 with a transparent plausible range and real neighbors; confirmation produced rank 201, version 1, 250 ranked players, 64 candidates, and the expected 250/251 displacement.
  - Corrected the browser-discovered watchlist presentation so watched ranked players remain visible as placed interests with a Re-evaluate action. Desktop and 390×844 mobile layouts were visually verified against the persisted board; the table scrolls inside its card on mobile. No application console errors occurred.
  - Signed out and deleted the approved disposable account, cleared volatile credentials, and verified zero remaining rows across Auth, users/settings, ranking, entry, event, seed, watchlist, placement, player preference, pairwise, and contribution records.
  - The final focused Draft Ranker suite passes 112/112 tests across 22 files and the repository TypeScript check passes. Current Supabase advisors report no target security findings; only expected pre-rollout unused-index informational notices remain. Production `DRAFT_RANKER_ENABLED` remains off.
  - The user approved the Phase 4 homepage-slot product/visual direction before implementation began.
  - Completed DR-040 with versioned explicit contribution consent, immutable consent audit events, idempotent canonical prompt issuance, immutable low/high/too-close/skip submissions, latest-preference supersession, reversal detection, and atomic personal-order alignment. Direct list edits remain excluded from community evidence, and consent withdrawal excludes previously eligible evidence from future aggregates without deleting private history.
  - Applied `20260715042519_add_draft_ranker_pairwise_evidence` after the exact migration and a full two-account lifecycle probe passed inside a rolled-back live transaction. The probe covered canonicalization, duplicate retries, reversals, skips, too-close, revocation, cross-owner hiding, continuous order, and zero residue.
  - Added authenticated consent and response APIs, server-owned policy/source values, strict outcome-only contracts, a user-scoped React Query consent hook, and private-by-default onboarding disclosure behind the separately disabled homepage flag.
  - Live catalog reconciliation confirms RLS plus owner-only authenticated reads on the immutable consent audit, no anonymous reads, no authenticated writes, and service-role-only execution for all three fixed-search-path SECURITY INVOKER functions. Supabase security advisors report no target findings; expected unused-index notices remain before traffic.
  - The current focused Draft Ranker suite passes 146/146 tests across 30 files and the repository TypeScript check passes. Both `DRAFT_RANKER_ENABLED` and `DRAFT_RANKER_HOMEPAGE_ENABLED` remain off.
  - Completed DR-041 with pure versioned `deterministic_v1` matchup selection, the approved 10/5/3/2 twenty-slot policy, Quick Five, five focused review modes, seven-day/30-prompt cooldowns, explicit contradiction revalidation, cutoff validation, and no-more-than-three position-group diversity.
  - Outside-player discovery uses only real continuous candidates; the 10% launch interest group uses real watchlist or verified-prospect signals and backfills safely when those signals are unavailable. Every selected matchup carries a reproducible mode, category, reason code, and human explanation.
  - Added a server-selected authenticated queue endpoint. Clients provide only ranking, expected version, operation ID, and bounded mode; they cannot nominate players, selection reasons, or algorithm versions. Queue issuance retries return the original prompt and conflicting operation reuse is rejected.
  - Added deterministic fixture, server, and API tests for the exact mix, cooldown, contradiction exception, Quick Five, goalie/defense discovery, owner derivation, strict input, independent flags, and idempotent issuance. The focused Draft Ranker suite passes 157/157 tests across 33 files and TypeScript passes.
  - Implemented DR-042 behind the separately disabled homepage flag. When enabled it replaces only the existing transaction-trends slot with one account-backed module; signed-out users get real sign-in/create-account links and trigger no ranking calls, while signed-in users get blind-by-default matchup evidence, six modes, explicit pause, Too Close/Skip, and an animated real ten-player preview linked to the full ranking.
  - The preview uses real account entries, highlights comparison-sourced movement, derives the blue reviewed-area bookmark only from persisted explicit pair preferences, and respects reduced-motion preferences. Changing modes remounts a bounded session, and the ranking/consent React Query caches are keyed by user to prevent cross-account cache reuse.
  - Four homepage component tests cover anonymous no-write behavior, one-card matchup rendering, ten real preview rows, blind evidence, reviewed bookmark, bounded outcomes, and mode changes. The current focused Draft Ranker suite passes 161/161 tests across 34 files; TypeScript and targeted diff checks pass.
  - A real Next.js server with both local flags enabled compiled the homepage successfully and served HTTP 200 with the new flag in SSR props. The only server warnings were existing localhost:3000 season-API fallback behavior and initial file-watcher pressure; the new stylesheet warning was corrected and the final compile was clean.
  - Completed DR-042 visual verification with a real flagged Next.js server in Chrome. Signed-out desktop and 390×844 layouts show one responsive ranker card, one News column to the left of League Separation, the approved navigation, and no mobile horizontal overflow. The only browser warning is the known Next.js development `isrManifest` HMR issue.
  - Created one isolated authenticated visual-test account without sending email, initialized its real 313-player board, and verified the signed-in desktop/mobile matchup and ten-player preview. Choosing Tony DeAngelo over Dylan Cozens moved Tony from No. 250 to No. 249, displaced Dylan to No. 250, loaded the next real matchup, slid the preview window, and persisted the order and reviewed bookmark after reload.
  - Signed out, deleted the isolated account, removed volatile credentials, and verified zero remaining Auth, ranking, entry, event, seed, prompt, comparison, preference, contribution, or rate-event rows for the account.
  - Completed DR-043 and applied `20260715051533_add_draft_ranker_pairwise_rate_controls`. The database serializes quotas per account/action, stores one payload-safe decision per operation, hard-limits queue and response bursts with Retry-After metadata, and flags disabled collection, automated bursts, and repeated-pair targeting without blocking the personal ranking effect.
  - The exact 469-line migration and full live rollback-only lifecycle probe passed before apply; a post-apply transactional probe and catalog reconciliation also passed. The new empty operational table has RLS, one restrictive no-client policy, five indexes, 12 constraints, and service-only CRUD. All three functions are fixed-search-path SECURITY INVOKER with no anonymous/authenticated execution.
  - Supabase advisors report no DR-043 security findings; two expected unused-index informational notices remain before traffic. The full focused/homepage regression pass is 179/179 across 39 files, TypeScript passes, and targeted diff checks are clean.
  - Completed DR-050 by freezing exact discovery sources, season/freshness rules, null behavior, explainable thresholds, and prohibited claims in [discovery-source-contract.md](./discovery-source-contract.md). No upstream Yahoo or projection ingestion was changed.
  - Completed DR-051 with live migrations `20260715055259_create_draft_ranker_discovery_materialization` and `20260715055404_add_draft_ranker_discovery_foreign_key_indexes`, a service-only audited refresh, deterministic fixtures, and source-health reporting. Live run `71d1f4a3-4884-4e00-8cd5-ffad73de1789` inspected 15 real sources and correctly emitted zero suggestions because current projection evidence is unavailable.
  - Completed DR-052 with owner-aware discovery reads, reproducible reason/freshness/source cards, compare/watch/dismiss/not-relevant actions, and explicit empty/stale states. The current empty state says recommendations are withheld instead of inventing data.
  - Completed DR-060 with pinned `regularized-bradley-terry-v1`, decaying Yahoo prior, consent/moderation eligibility, per-user/pair dedupe, neutral previously-undrafted entrants, and exact evidence/admission states.
  - Completed DR-061 with live migrations `20260715061931_create_draft_ranker_community_snapshots` and `20260715062048_add_draft_ranker_community_foreign_key_indexes`, an audited 12:15 UTC refresh, immutable snapshots, and opt-out/deletion rebuild behavior. Live snapshot `2dfdff1e-718c-4fec-946f-fd89f48f2a88` contains 1,148 real players, 313 displayable market seeds, 835 previously-undrafted candidates, and zero accepted comparisons, so it is labeled market-seeded rather than community consensus.
  - Completed DR-062’s public aggregate API and page. Live signed-out verification returned 250 distinct ranks with real identities, headshots, and ADP evidence; signed-in owner context is optional and no user identifier appears in the aggregate payload. DR-073 later completed fresh desktop and 430×932 mobile browser verification.
  - Completed DR-063 with six manipulation/calibration tests and [community-ranking-runbook.md](./community-ranking-runbook.md). Duplicate floods collapse, sparse graphs remain unestablished, consent withdrawal rebuilds, burst evidence is excluded, cutoff admission needs evidence on both sides, and moderated cohorts are removed; distinct coordinated-account risk remains operationally monitored and disclosed.
  - Completed DR-070 with versioned `fhfh-draft-ranking-v1` CSV/JSON, stable IDs, explicit ADP state, optional candidates/watchlist/event summaries, owner-only API access, rate limiting, and ranking-page controls. A real export held 250 top ranks, 64 candidates, one watchlist row, and 13 event summaries with no private comparison evidence.
  - Completed DR-071 with live migration `20260715070405_add_draft_ranker_ordering_repair`, exact-confirmation admin actions, daily 12:45 UTC aggregate health, deterministic/idempotent normalization, immutable before/after audit, identity-review queuing without automatic merge, and community dry runs. Exact count queries prevent high-volume 24-hour rate windows from being truncated.
  - Implemented DR-072’s server-side `off`/`staff`/`allowlist`/`authenticated` rollout policy on every personal API; missing or invalid stages fail closed, allowlists remain server-only, flags are independently reported, and the staged cohort thresholds/rollback steps are frozen in [launch-runbook.md](./launch-runbook.md). No deployed flag was enabled.
  - Ran DR-073’s live disposable-account journey through authentication, 313-player initialization, persisted reorder, real prospect watch, assisted placement, consented comparison, private export, owner-only community delta, opt-out rebuild, and account deletion. Zero rows remained across Auth and every account-owned ranker table.
  - The final focused release suite passes 228/228 tests across 49 files, and the affected WIGO regression suites pass 18/18. Full TypeScript and `npm run build` pass; the production build compiles all 83 static pages and completes sitemap generation.
  - Corrected the repository-wide WIGO type blockers while preserving runtime intent: goalie radar reads now use the actual goalie identifier/season columns and avoid a nonexistent position filter, game-log date/season columns branch by source view, and WIGO skater writes narrow the dynamic destination before applying its generated insert type.
  - Completed DR-073 responsive visual verification with real data. The Community Ranking passed desktop and iPhone 14 Pro Max (430×932) inspection with honest market-seeded wording, real identities/headshots, and no horizontal overflow. The authenticated personal board/export surface passed desktop and 430×932 inspection with privacy wording, three explicit optional-data controls, CSV/JSON actions, and accessible labels.
  - The mobile pass exposed and corrected document overflow from the wide ranking table and fixed bottom navigation. The document and body now both measure 430 pixels while the table remains independently scrollable at 1,050 pixels inside a 428-pixel viewport.
  - Created a second isolated visual account, initialized its real 313-player board, verified the export surface, deleted the account, and confirmed zero Auth, profile, settings, ranking, entry, event, seed, watchlist, pairwise, placement, preference, contribution, or rate-event residue. The local all-flags-on server was stopped and deployed flags remain off.
  - A read-only Vercel production preflight on 2026-07-15 found that the READY production deployment predates Draft Ranker and the live `/draft-rankings` route returns 404. The two newest branch deployments failed; the latest stopped on the `ProcessEnv` typing issue in `rateLimit.ts` that is already corrected in the current workspace, where the full local production build passes. No production deployment or flag change was attempted from the mixed dirty workspace.
  - Rechecked current external state: remote `octoberBranch` is `0ed825fb30f5c6f329cd0dc169d2d9b2ca975e0f`, while this local checkout is sibling checkpoint `92cfab2678d436376961f7abd4c8c9aa414bbd28`; both share merge base `d3746815cf733d4a3e37e720722cad953830e5f1`. The newest remote deployment failed on the same `rateLimit.ts` type line, proving the passing local fix has not reached a READY artifact.
  - Created and validated [release-manifest.md](./release-manifest.md). Its narrow rules classify all 114 current dirty paths as 102 included and 12 explicitly excluded, with zero unclassified. The excluded `.gitignore`, dead-code/super-goal documents, test-result artifacts, and generated Python caches remain outside the release. The manifest's exact 49-file Draft Ranker command passes 228/228 tests, and its broader ten-file WIGO command passes 54/54 tests.
  - Assembled all 102 included paths in a separate clean clone of the current remote commit on local-only branch `codex/draft-ranker-release-20260715`. The transfer contained 52 tracked changes and 50 additions with zero base conflicts or remote collisions; no excluded path transferred. The isolated candidate passes `git diff --check`, 228/228 Draft Ranker tests, 54/54 broader WIGO tests, TypeScript, all 83 static pages, and sitemap generation. No file was staged, committed, pushed, or deployed.
  - Confirmed the GitHub publishing prerequisite is absent: both `gh --version` and `gh auth status` return `command not found`. No installation or authentication change was inferred from product rollout approval.
  - Third consecutive resumed-run blocker audit: `gh` remains unavailable; remote refs contain no `codex/draft-ranker-release-20260715` branch; remote `octoberBranch` remains `0ed825fb30f5c6f329cd0dc169d2d9b2ca975e0f`; and both `web/.env.local` and `web/.env.production` contain zero configured staff or beta cohort IDs. The verified isolated candidate remains local-only and no production state changed.
  - The user installed GitHub CLI 2.96.0 and began `gh auth login`. A fresh `gh auth status` check still reports no authenticated GitHub host, so the interactive browser authorization has not completed. The goal resumed, but no publication action was inferred from an incomplete login prompt.
  - Second consecutive resumed-run authentication audit: `gh auth status` still reports no authenticated GitHub host. The isolated release candidate remains unchanged and local-only; no staging, publication, deployment, or flag mutation occurred.
  - GitHub authentication subsequently completed as `TjsUsername` over HTTPS with `repo` access. The user explicitly authorized publishing the prepared Draft Ranker branch and draft PR; this does not authorize production exposure or infer cohort identities.
  - The final pre-stage guard detected that remote `octoberBranch` advanced from `0ed825fb30f5c6f329cd0dc169d2d9b2ca975e0f` to cleanup commit `5743b6d808ff4cd9dd0cb01fef13863ac95f9f6d`. The stale candidate was not staged. A new clean clone of the exact current remote base accepted all 102 scoped paths with zero collisions and again passed 228/228 Draft Ranker tests, 54/54 broader WIGO tests, TypeScript, all 83 static pages, and sitemap generation.
  - Staged only the classified 102 workspace paths in the refreshed isolated clone. Git represented them as 101 records because the replaced rate-control migration normalized to one rename. The staged diff contained 11,097 insertions and 262 deletions, no prohibited path, and passed `git diff --check`.
  - Committed the product change as `c78059b6de6671a0e594afe1e60d5e42421b5db4` (`feat: complete account-backed draft ranker`), pushed `codex/draft-ranker-release-20260715`, and opened draft PR [#335](https://github.com/FHFHockey-dev/fhfhockey.com/pull/335) against `octoberBranch`.
  - All five reported Vercel checks passed for the product commit. READY preview deployment `dpl_BSTN4KU4zhqKR8UFkRPFwqKHpMiL` returned HTTP 200 for both Draft Ranker pages, returned the expected `draft_ranker_disabled` HTTP 503 response from both checked APIs, kept the homepage module disabled, and produced no error/fatal runtime logs during the smoke window. Production was not promoted and all production flags remain off.
  - The post-publication review checkpoint was re-audited on 2026-07-15. Draft PR #335 is open, mergeable, clean and zero commits behind `octoberBranch`, with all checks green. It has no requested reviewers, submitted reviews, review comments, or non-bot discussion, so the reviewed-artifact gate remains open and no merge or production promotion is authorized by CI alone.
  - The same review-gate condition remained unchanged for three consecutive goal turns. Repository evidence provides no `CODEOWNERS`, visible `octoberBranch` protection rule, or review precedent in the last 20 merged PRs. Two other direct collaborators exist, but assigning either would be external coordination not authorized by the user. The persistent goal is therefore blocked at this checkpoint rather than silently treating CI as review.
  - The product owner subsequently resumed the goal with an explicit approval of PR #335 for an all-flags-off production promotion. That approval covers merging and promoting the exact verified artifact; it does not authorize staff UUID configuration, a non-`off` rollout stage, homepage exposure, or any broader cohort.
  - PR #335 was marked ready and merged with head-SHA protection as `722f1dff02b7a4b9486836b386c0b576f57c5cfd`; its tree `21c199f1f00dce7d856fd92fd5bdcffc0d8c6580` exactly matches the approved PR-head tree.
  - Production inspection found all eight Draft Ranker rollout keys absent, including both cohort lists. The reviewed server policy treats missing or invalid values as fail-closed, so no environment mutation was needed and no user became eligible.
  - READY preview `dpl_5wzgM43KU7Re4FTZZ8GgHhDQYRmw` was promoted into READY production deployment `dpl_2jLGmBmqwYjXiJJVuLjUMYJLrz4D`, which records the exact merge SHA and original preview. Vercel assigned all production aliases without error at `2026-07-15T15:19:26.084Z`; previous deployment `dpl_HgjrBeCQVzDTsgryafBLzmXqGyjH` remains the rollback candidate.
  - Production closed-state smoke passed: both feature pages returned HTTP 200; both checked APIs returned HTTP 503 `draft_ranker_disabled`; homepage props reported `draftRankerHomepageEnabled: false`; rendered browser inspection showed the real signed-out account-backed page and zero Draft Ranker text on the homepage; and deployment-scoped error/fatal logs plus grouped runtime errors were empty.
  - Verified the canonical staff cohort from exact Supabase Auth ownership plus `public.users.role = 'admin'`: three accounts, including the active `TjsUsername` operator. Raw UUIDs were transmitted only to Vercel's sensitive production configuration. The repository records count three and sorted SHA-256 fingerprint `08e5ae064daea8389205cbd9bf54a3ce3a1a31f555e7e51a381ca314a61053dd` instead of identifiers.
  - Set Production-only `DRAFT_RANKER_ENABLED=true`, `DRAFT_RANKER_ROLLOUT_STAGE=staff`, and the exact staff allowlist. Beta, homepage, discovery, contribution, and public Community Ranking controls remain absent/off.
  - Redeployed the reviewed merge artifact without source changes as READY production deployment `dpl_8RitVqdJ9QsRTi11J7xjH66u6cdu`. It records original deployment `dpl_2jLGmBmqwYjXiJJVuLjUMYJLrz4D`, source commit `722f1dff02b7a4b9486836b386c0b576f57c5cfd`, READY time `2026-07-15T16:10:46Z`, and all production aliases without error.
  - Staff-stage smoke passed: anonymous personal API access returned HTTP 401 `authentication_required`; the public aggregate API remained HTTP 503 `draft_ranker_disabled`; both feature pages and the homepage returned HTTP 200; homepage props and rendered content kept the pairwise module absent; production sign-in visually displayed the allowlisted `TjsUsername` account; and no grouped runtime errors or error/fatal deployment logs appeared.
  - The product owner explicitly approved bypassing the 72-hour/50-journey prerequisite for the homepage surface only. This exception does not mark the staff observation complete and does not authorize discovery, contribution, Community Ranking, beta, allowlist, or public expansion.
  - Added a real opening-night countdown above the games area. It reads canonical `public.seasons.startDate = 2026-09-29`, prefers the earliest official NHL `startTimeUTC` when supplied, and transparently falls back to the date while the official puck-drop time is unavailable. No dummy data or database migration was added.
  - PR [#338](https://github.com/FHFHockey-dev/fhfhockey.com/pull/338) passed all five reported checks and merged as `3685ce1ef87bf96bbad6f2fc307a971220428615`. Exact READY preview `dpl_8sW3z1oABqQGAtxopeSnpLXsyWgp` was promoted into READY production deployment `dpl_FiCyDfjVX1Dx14yuSoyySGJvGThQ` from reviewed source commit `70c88054df6a4609bb419fa286cac6fc79e44c7a`.
  - Set only the production-sensitive `DRAFT_RANKER_HOMEPAGE_ENABLED=true`. The rollout stage remains `staff`, the exact three-account cohort and its fingerprint are unchanged, the beta cohort is empty, and discovery, contribution collection, and public Community Ranking remain off.
  - Live visual verification found the countdown and account-backed Personal Draft Ranker visible, Transaction Trends absent, exactly one Latest News panel to the left of League Separation, and no application error. Anonymous personal access remained HTTP 401 `authentication_required`; anonymous Community Ranking remained HTTP 503 `draft_ranker_disabled`; Vercel grouped runtime errors remained empty.
  - The new and immediate prior production artifacts emit the same ten browser hydration warnings. They are documented as an understood pre-existing baseline risk rather than a homepage rollout regression.
  - Homepage rollback is independently safe: disable `DRAFT_RANKER_HOMEPAGE_ENABLED` to restore Transaction Trends without altering staff access. Deployment `dpl_8RitVqdJ9QsRTi11J7xjH66u6cdu` is the immediate pre-homepage artifact reference.
  - Recorded the first post-homepage DR-072 observation checkpoint at `2026-07-15T18:53:35.928548Z`, 9,769 seconds after staff activation. The exact three-account admin cohort remains configured, but production contains zero active Draft Ranker rankings, entries, events, pair prompts, comparisons, rate decisions, or community-eligible comparisons. The volume gate is therefore honestly `0/50` complete journeys; real-traffic latency, retry, and support-rate criteria are unevaluable rather than passed.
  - The same aggregate database checkpoint found zero entry-owner mismatches, zero event-owner mismatches, zero incomplete seed runs, and zero expired active placements. The ten pending identity reviews are the already documented expected quarantine. The latest audited health request remains the successful HTTP 200 run at `2026-07-15T07:16:40.078336Z` with 191 ms duration; no post-activation scheduled health run exists yet because activation occurred after that day's scheduled window.
  - A fresh Supabase advisor review found zero security findings scoped to Draft Ranker or the canonical player-identity tables. The 35 scoped performance notices are unused-index informational findings expected before real traffic, not a reason to remove launch indexes during an empty cohort observation.
  - Vercel reconfirmed production deployment `dpl_FiCyDfjVX1Dx14yuSoyySGJvGThQ` as READY from exact source `70c88054df6a4609bb419fa286cac6fc79e44c7a`, with the reviewed preview recorded as its promotion source and no alias error. From READY through the checkpoint, grouped runtime errors and error/fatal logs were empty. The only 5xx-class requests were two intentional HTTP 503 closed-state probes to `/api/v1/draft-ranker/community`; no unexpected 5xx was observed. Repeated live smoke still returned homepage HTTP 200, anonymous personal HTTP 401 `authentication_required`, and Community HTTP 503 `draft_ranker_disabled`.
  - Completed the first genuine production staff journey from `2026-07-15T19:03:29Z` through `2026-07-15T19:04:50Z` using the allowlisted `TjsUsername` account. The operator signed in through the real production auth page, initialized a 313-player private board from the approved Yahoo seed, returned to the homepage, received an adjacent-rank matchup, submitted neutral `Skip`, received the next matchup, and reloaded successfully. Skip intentionally preserved player order and avoided imposing a personal draft preference.
  - Database reconciliation at `2026-07-15T19:05:31.031518Z` found one active staff-owned ranking, exactly 313 entries, one completed seed run, no duplicate order keys, and lock version zero. The seed inspected 1,494 source rows and persisted 313 verified rankable players in 12 ms, with 592 invalid-ADP, 446 unmapped, and 74 approved fallback rows recorded transparently. Immutable events contain one `ranking_initialized` and one `pairwise_response_recorded`; pairwise evidence contains one completed `skip`, two currently issued prompts after refresh/advance, three allowed rate decisions, one community-suppressed decision, and zero community-eligible comparison. Entry, event, prompt, and comparison ownership mismatches remain zero; incomplete seeds and expired placements remain zero.
  - Production runtime logs independently recorded the initialization as HTTP 201 and every ranking, entries, watchlist, placement, consent, queue, and response request as HTTP 200. The journey produced no runtime error cluster, error/fatal log, or 5xx response. The staff gate now has one distinct staff account with a board and one manually verified complete journey, so the honest volume state is `1/50`; the 72-hour minimum, remaining 49 journeys, latency sample, retry evidence, and next scheduled health run remain open.
  - The product owner subsequently clarified that `TjsUsername` is their personal ranking account and requested a clean reset. The production verification had initialized that account from the real 313-player Yahoo seed and recorded only neutral `Skip` outcomes; lock version remained zero, no personal ordering changed, and no comparison was community eligible. Because the account impact was not made explicit before initialization, this journey is excluded from rollout evidence rather than counted as staff adoption.
  - Reset only the product owner's Draft Ranker state at their explicit request. One ranking and its cascaded entries, seed run, prompts, comparisons, and immutable ranking events were deleted together with 11 account-scoped pairwise rate-control records; there were no contribution-preference or contribution-event rows to remove. The FHFH Auth account, profile, role, and unrelated data were preserved. A hard-refreshed production homepage shows `TjsUsername` signed in with `Create my draft ranking`, no seeded board or matchup, the opening-night countdown present, and no application error. The honest rollout volume returns to `0/50` eligible journeys.
  - Product-owner feedback changed the default personal queue start from the top-250 cutoff to the beginning of the board. Local `deterministic_top_down_v2` now selects current personal ranks 1 and 2 first—the first two Yahoo-ADP-seeded players on a fresh board—and advances to ranks 2 and 3 after the prior adjacent pair is reviewed or skipped. Personal reorders remain authoritative after initialization; static Yahoo ADP does not override later user judgment. Two explicit progression tests plus the server contract update bring the focused suite to 230/230 across 49 files; TypeScript and the production build pass. Publication and live user-owned verification remain pending.
  - An exact captured production `Skip` request was replayed once and both HTTP attempts returned 200, but the product-owner reset removed the underlying comparison/rate ledger before the planned post-replay count reconciliation. This is not claimed as completed storage-level retry evidence; DR-072's retry criterion remains open for a future explicitly disposable or owner-initiated journey.
- **Next executable task:** Publish and verify the reviewed `deterministic_top_down_v2` queue update, without initializing or modifying the product owner's reset board, then continue collecting DR-072 staff traffic, daily health, owner-isolation, ordering, retry, latency, deletion, support, and unexpected-5xx evidence through at least `2026-07-18T16:10:46Z` and 50 eligible account journeys. Do not enable another secondary flag or expand the cohort while either gate is incomplete.
- **First required operational checkpoint:** Review the completed 72-hour/50-journey staff evidence against [launch-runbook.md](./launch-runbook.md), record pass/fail and any understood warnings, then obtain explicit approval before any expansion beyond the already approved homepage-only exception. Phase 8 tasks DR-080 through DR-083 remain explicitly deferred.
