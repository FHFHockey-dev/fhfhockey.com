# Dead Code and Hidden Surface Follow-Through — Tasks

## Relevant Files

- `tasks/TASKS/dead-code-cleanup/prd-dead-code-report.md` - Source audit, candidate clusters, surface classifications, and recommended cleanup order.
- `tasks/TASKS/dead-code-cleanup/prd-file-inventory.md` - Repository file inventory supporting provenance and ownership review.
- `tasks/TASKS/dead-code-cleanup/hidden-product-surface-ownership-matrix.md` - Current route-to-owner/replacement dispositions and exact remaining approval gates.
- `tasks/TASKS/sko-charts/tasks-prd-sko-charts.md` - Owner of SKO-specific quarantine/burn-down decisions.
- `web/package.json` - Current route/build/script/test entrypoint evidence and `knip` tooling.
- `web/pages/` - Next.js route entrypoints that remain URL-reachable even without imports.
- `web/vercel.json`, `web/next.config.js`, `.github/` - External schedules, rewrites, and workflow consumers that import graphs may miss.
- `functions/`, `cms/`, and the retired `webhooks/` lineage - Separate deployable/runtime ownership that must be audited independently.

### Notes

- This list repairs the missing implementation pair for the completed audit; no deletion was authorized by the audit itself.
- Every candidate must be re-verified against current imports, routes, navigation, package scripts, cron/provider configuration, external deployment, dynamic imports, and documentation.
- A Next.js page/API file is a runtime entrypoint even with zero inbound imports.
- Mass deletion, route removal, or uncertain external-consumer cleanup requires the super-goal checkpoint.
- Work in small domain batches with verification between batches; preserve lineage and redirects when product URLs may have bookmarks.

## Tasks

- [x] 1.0 Refresh the dead-code evidence baseline. The current production scan and bounded post-baseline delta review classify every changed/new candidate without promoting raw tool output to a deletion claim (verified 2026-07-25).
  - [x] 1.1 Re-run source inventory/import graph and current `knip --production` using repository conventions; exclude generated/vendor/cache/test false positives explicitly.
  - [x] 1.2 Enumerate pages/API routes, navigation, dynamic links/imports, package scripts, cron schedules, Vercel rewrites, GitHub workflows, separate apps, and documented external callers.
  - [x] 1.3 Compare every 2026 audit candidate to current evidence and classify it as active, hidden supported, admin/operational, compatibility/redirect, quarantine, generated/temp, uncertain, or high-confidence dead.
  - [x] 1.4 Add changed/new candidates and remove disproven claims with evidence; do not carry old `knip` flags forward automatically. Evidence: current `knip` 5.88.1 reports 174 raw unused-file candidates and 127 Sass-alias resolution failures. The post-baseline delta keeps the renamed SKO reader and prediction UI explicitly quarantined under B-SKO, treats test/script-owned Sustainability and operational modules as entrypoint false positives, and removes moved API helpers from dead-code consideration because current imports consume their new library paths. No new deletion claim or file mutation follows from the one-candidate raw-count increase (verified 2026-07-25).

- [x] 2.0 Handle obvious temporary/generated debris separately
  - [x] 2.1 Classify root/temp/debug scripts, duplicated generated-output paths, logs, and test-result artifacts by current package/workflow/documentation usage.
  - [x] 2.2 Move durable findings into existing runbooks/tasks and delete only proven temporary files in small reviewable batches.
  - [x] 2.3 Add/adjust ignore rules for regenerated logs/results only after confirming they are not intentional committed fixtures.
  - [x] 2.4 Verify no package script, workflow, or operator command references removed artifacts.

- [x] 3.0 Resolve empty/stub and development-only routes. Evidence: owner-approved cleanup removes the five unlinked empty/test/prototype routes and their three exclusive zero-consumer implementation files; the supported style-review surface, read-only Trends diagnostics, and quarantined stats legacy route remain intact (verified 2026-07-30).
  - [x] 3.1 Re-verify `FantasyPowerRankings`, `PowerRankings`, `/test`, logo/style/trends testing grounds, placeholders, and xGoals prototypes against current product links and use.
  - [x] 3.2 For each route choose keep/document, protect, redirect, move to tests/docs, or delete and record the replacement/consumer evidence.
  - [x] 3.3 Obtain approval before removing a route family or externally bookmarkable surface with uncertain consumers. Evidence: the owner-approved plan explicitly removes only pure empty/test/prototype routes while retaining legacy products as noindex quarantine pages; the exact five-route deletion follows that boundary (verified 2026-07-30).
  - [x] 3.4 Verify routing, sitemap/navigation, build, and direct replacement links after each batch. Evidence: exact route/config scans retain every approval-gated route and canonical replacement link, none of the 20 deleted paths reappears, the exact `589b0241d` Production build is READY, and the bounded public shell cohort returns seven HTTP 200 responses. The local Yahoo recovery changes four existing page/API implementations but no route topology or deployment config (verified 2026-07-30).

- [x] 4.0 Resolve hidden legacy product surfaces through owning initiatives. The retained legacy families now carry visible noindex quarantine notices and canonical replacement links, while the active Trends Debug page and API fail closed behind existing administrator authorization (verified 2026-07-29).
  - [x] 4.1 Reconcile buy-low/sell-high, legacy goalies/team stats/projections/FORGE/SKO/debug/twitter routes against current Variance, Underlying Stats, FORGE, Trends, Lines, and admin replacements. Evidence: the exact ownership matrix classifies all nine route families as supported, redirect compatibility, historical quarantine, or approval-gated legacy; current link/source scans and 4 files/28 tests pass without changing a route (verified 2026-07-29).
  - [x] 4.2 Prefer explicit redirects or quarantine notices when lineage/bookmarks remain useful; do not leave duplicate live product implementations ambiguous. Evidence: `/buyLowSellHigh`, `/trueGoalieValue`, `/teamStats*`, and `/projections` retain bookmark-compatible behavior with one shared visible legacy notice, `noindex,nofollow`, and exact links to Underlying Stats, Variance, or FORGE. Existing `/goalies` redirect and `/skoCharts` quarantine remain unchanged (verified 2026-07-29).
  - [x] 4.3 Move active debug/operator behavior behind authenticated admin ownership or extract it into repeatable tests/runbooks. Evidence: `/trendsDebug` renders the validation console only after resolved admin identity, so loading/non-admin shells initialize no console data hooks; its server payload route is wrapped by the existing fail-closed `adminOnly` boundary (verified 2026-07-29).
  - [x] 4.4 Synchronize deletion/quarantine tasks with the relevant source initiative before editing files. Evidence: the matrix assigns Variance, Underlying Stats, FORGE, B-SKO, rolling-player-metrics, and Lines/GDL ownership, preserves B-DEAD 3.3/4.2/4.3 and 5.2–5.4 gates, and makes no runtime edit (verified 2026-07-29).

- [x] 5.0 Remove dead component/utility clusters only after route decisions. Evidence: exact revalidation confirms all 20 deleted files remain absent and consumer-free across runtime, scripts, tests, dynamic imports, workflows/configuration, and separate roots; every retained original candidate is explicitly active, operational/manual, or owner-quarantined rather than deletion-eligible (verified 2026-07-30).
  - [x] 5.1 Re-verify legacy SKO/prediction, WiGO, goalie/team, old upsert, DRM wrapper, PPTOI, FORGE card, retired NHL API, formatter, and generic utility clusters.
  - [x] 5.2 Confirm no active route, script, test fixture, dynamic import, separate app, cron, or external caller uses each cluster. Evidence: exact deleted path/basename/symbol scans remain empty for all 20 removed files. The only similar hits are the separately retained current `PPTOIChart.tsx` and unrelated Sustainability CSS class names; active, operational/manual, and owner-quarantined candidates remain classified outside the deletion set (verified 2026-07-30).
  - [x] 5.3 Delete targeted proven-unused clusters with any required import/style/test cleanup and preserve documentation lineage. Evidence: exact runtime/script/config/separate-root scans prove zero consumers for 20 retired UI/utility files, removed in bounded 14-file/1,313-line and six-file/668-line batches; active WiGO, `dateUtils`, current PPTOI, and every uncertain operational/quarantine cluster remain intact (verified 2026-07-29).
  - [x] 5.4 Verify type/build/unit/runtime paths owned by each domain after its batch. Evidence: deleted-path/symbol scans are empty, full TypeScript passes, and current Variance plus Underlying Team Stats suites pass 2 files/8 tests without a build (verified 2026-07-29).

- [ ] 6.0 Audit admin/operational and separate-project security/ownership
  - [ ] 6.1 Verify `/db`, admin CSV/alias tools, cron APIs, webhooks, `functions`, and CMS are authenticated, deployed, documented, and owned or explicitly retired.
  - [x] 6.2 Do not delete operational routes based on app import graphs; correlate with cron inventory, provider callbacks, external scheduler, and deployment configs.
  - [x] 6.3 Add security/remediation tasks for reachable hidden tools lacking adequate authorization. Evidence (2026-07-25): bounded route/separate-root review registered NEW 8.1–8.3 before remediation, separating the locally repaired standalone webhook and `/db` shell defects from their deployment/ownership gate.

- [x] 7.0 Final cleanup verification and synchronization
  - [x] 7.1 Re-run import/knip/route inventories and explain remaining intentional flags/false positives. Evidence: current `knip` 5.88.1 reports 155 unused-file candidates, down from 174 before the bounded removals; all 164 unresolved imports are known Sass alias-analysis false positives, while scripts, tests, framework/config entrypoints, manual operational modules, quarantined surfaces, and separate apps remain excluded from deletion claims (verified 2026-07-30).
  - [x] 7.2 Run targeted tests, type/build checks, and direct route/navigation smoke verification proportional to removed batches. Evidence: the cleanup cohorts retain 30 focused tests plus full TypeScript; the later exact Production build is READY, seven public route shells return 200, and both protected rolling routes return 401 without authorization. No local or hosted build was added for this documentation/package-contract follow-through (verified 2026-07-30).
  - [x] 7.3 Confirm no unresolved import, missing Sass module, package-script failure, route dead end, cron gap, or deployment entrypoint removal. Evidence: exact recovery `96ccea804` is READY/Production, all seven public shells return 200, both protected rolling routes return 401, populated Start Chart desktop/mobile and bounded restored-reader surfaces pass, and deployment-scoped runtime-error/5xx queries are empty (verified 2026-07-30).
  - [x] 7.4 Update the audit PRD, file inventory, this list, owning initiative lists, and master ledger with final dispositions/evidence. The later guarded recovery receipt closes 7.3 and parent 7.0 without changing migration or writer state (verified 2026-07-30).

## NEW Tasks

- [ ] NEW 8.0 Append every newly discovered candidate, hidden consumer, security gap, uncertain external dependency, and cleanup optimization here before closure.
- [x] NEW 8.1 **P0 — Fail the standalone line-combination webhook closed and stop disclosing dependency failures.** The Express service now rejects missing/blank configuration, malformed schemes, wrong tokens, and non-exact bearer values with fixed HTTP 401; exact bearer comparison is timing-safe. Mutation failures return fixed HTTP 500 text instead of raw exception messages. The Next.js counterpart also stops logging the Puppeteer endpoint and returning raw messages/stacks. Focused standalone auth tests pass 2/2, both JavaScript files parse, full web TypeScript and scoped ESLint pass, the new helper/test format cleanly, and diff integrity is clean; the two retained legacy route/app files preserve their pre-existing whole-file format (discovered and locally remediated 2026-07-25).
- [x] NEW 8.2 **P1 — Gate the `/db` operational shell itself, not only selected cards and downstream APIs.** The page now consumes the explicit auth-loading state, renders no operational UI to signed-out/non-admin users, and skips its public-table/local-resume reads unless the resolved user is an administrator. Downstream CSV, alias, cron, and mutation APIs retain their independent server authorization. Full TypeScript, scoped ESLint, and diff integrity pass while preserving the page's pre-existing whole-file format (discovered and locally remediated 2026-07-25).
- [ ] NEW 8.3 **P1 — Resolve and verify the deployed operational ownership boundary.** The standalone `webhooks` service is explicitly retired; exact local/live inventory finds only the canonical Next.js route. Current Production proves signed-out `/db` settles on an administrator-required shell with zero controls/tables, missing webhook auth returns 401 before work, the functions health owner returns 200, and CMS redirects to `/studio` then requires a Sanity login provider with zero editor surfaces. The malformed-bearer probe discovered bounded error drift now locally fixed under NEW 8.9. Keep this row open only until that exact local fix is published and Production malformed auth returns the same fixed 401 without screenshot/upload/Discord work (partially reconciled 2026-07-30).
- [x] NEW 8.4 **P3 — Correct the stale `utils/dateUtils.ts` deletion candidate before cleanup.** Exact source review finds `usePlayerWeeklyStats` actively importing `getWeekStartDates`; the file remains tracked and is removed from the dead-code claim. This consumer does not justify retaining the separate zero-consumer utilities deleted under 5.3 (discovered and reconciled 2026-07-29).
- [x] NEW 8.5 **P3 — Reconcile the imported source header after NEW 8.4.** The source and master both contained 39 mirrored rows while the master section label still declared 38. The canonical header and inventory now report the exact 40-row ledger including this reconciliation, with 26 verified and 14 open (discovered and reconciled 2026-07-30).
- [x] NEW 8.6 **P2 — Declare active direct imports instead of relying on transitive package installation.** The final `knip` scan found `@sanity/client` used by `createComment`, `progress` used by the retained WGO operator, and `domhandler` types used by two active NST routes without direct manifest ownership. `package.json` and the canonical lock now declare their already-locked versions; offline lock validation reports zero vulnerabilities, manifest/lock parity is 3/3, and the rerun has zero unlisted dependencies (discovered and closed 2026-07-30).
- [x] NEW 8.7 **P3 — Quarantine the retained `/statsPlaceholder` route.** The owner chose bookmark-compatible retention rather than deletion. The page now uses the shared visible legacy notice, `noindex,nofollow`, and an exact `/underlying-stats` replacement link without changing its historical data behavior (closed 2026-07-30).
- [x] NEW 8.8 **P2 — Reconcile the active-migration allowlist with scheduler ownership.** The standalone-retirement verification exposed that reviewed migration `20260730091500_consolidate_scheduler_ownership.sql` was active but absent from the exact migration inventory test. The allowlist now includes it and the focused four-test baseline contract passes without applying a migration (discovered and closed 2026-07-30).
- [x] NEW 8.9 **P1 — Bound shared administrator-authentication failures.** Value-free Production probing found malformed bearer input returned Supabase JWT parser detail even though missing auth failed safely. The shared middleware now requires one exact bearer shape and maps both malformed and rejected tokens to the same fixed 401 `{ message: "Unauthorized.", success: false }`; five focused tests and scoped lint pass. Publication and Production malformed-auth proof remain owned by NEW 8.3 (discovered and locally closed 2026-07-30).

## Completion Reconciliation — July 2026

- A current `rg --files` inventory counts 3,401 tracked paths, including 2,050 web source/style files, 331 page entrypoints, and 256 API entrypoints. Temporary-cache `knip` 5.88.1 completed its production scan and reported 173 unused-file candidates; its nonzero exit is the expected findings result. The report explicitly excludes tests, operational scripts, framework/config entrypoints, separate apps, Sass alias failures, and other runtime/tool false positives instead of converting the raw total into deletion claims.
- The current entrypoint boundary includes 48 package scripts, 20 Vercel cron definitions, the separately verified 64-job pg_cron inventory, one GitHub workflow, and the retained `functions` (53 files) and `cms` (22) roots. The former standalone `webhooks` root is retired after exact local and live Vercel ownership proof; the canonical Next.js webhook remains operational. Navigation, dynamic/detail routes, rewrites/sitemap, provider callbacks, and documented external callers remain first-class consumer evidence.
- Exact tracking and ignore checks prove the 24 previously removed generated-state files remain untracked while their classes are narrowly ignored: Playwright results, all three Supabase CLI temp roots, Python bytecode, Codex build outputs, dependency caches, the local dev/SOS/Yahoo logs, and team-underlying audit JSON. Producers for the SOS log and team-underlying JSON remain intact; no package/workflow/operator consumer reads the removed outputs. The four duplicated nested SKO outputs remain intact as owner-authorized historical evidence with exact hash/size and secret/path-scan provenance under B-SKO 8.4/NEW 9.12.
- The owner-approved route cohort removes `/FantasyPowerRankings`, `/PowerRankings`, `/test`, `/testLogoMaker`, and `/xGoalsPage` after exact navigation, rewrite, sitemap, workflow, cron, and runtime-link scans found no consumer. Their exclusive `LogoMaker` and league xGoals prototype files are also removed. `cssTestingGrounds` remains the supported style-review surface, `statsPlaceholder` remains a noindex legacy quarantine page, and `trendsTestingGrounds` remains read-only quarantined diagnostics.
- Current `knip` reconfirms every named legacy component/utility family as an import-graph candidate, including SKO/prediction, WiGO, goalie/team utilities, old upserts, DRM wrapper, PPTOI, FORGE card, retired NHL API, duplicate formatter, and generic utilities. That closes re-verification only: external/manual callers, route decisions, targeted deletion, and post-delete tests remain open. Operational routes and separate apps are protected from import-graph deletion by the canonical 64-plus-20 scheduler/config/provider inventory.
- This bounded reconciliation originally closed 11/35 rows. Later evidence closes baseline parent 1.0 and generated-debris parent 2.0; the retained nested SKO outputs now have an explicit historical-only disposition under B-SKO. Route, hidden-product, component-deletion, security, and final-verification parents remain open behind their explicit approval, consumer, and runtime gates.
- The 2026-07-25 delta reconciliation closes 1.4 and parent 1.0. A fresh `knip` 5.88.1 production scan reports 174 raw candidates and 127 unresolved Sass-alias imports; neither raw class is a deletion list. Candidate-affecting changes preserve the renamed SKO reader/prediction cluster as explicit quarantine, preserve tests/scripts/operational modules as entrypoints, and disprove deadness for API helpers moved into imported library paths. B-DEAD is 14/35 with 21 open. No route, component, utility, dependency, build, deployment, or external state changed.
- The 2026-07-25 operational-security cohort closes 6.3 plus local NEW 8.1/8.2 after repairing the standalone webhook bearer/error boundary, removing Puppeteer endpoint/raw-error disclosure from its Next.js counterpart, and gating `/db` plus its initial reads on resolved administrator state. NEW 8.3 preserves exact deployment, value-free runtime, standalone owner/retirement, functions receipt, and live CMS access-policy proof. B-DEAD is 17/38 with 21 open; no external system changed.
- The 2026-07-29 hidden-product reconciliation closes 4.1/4.4 with an exact nine-family route-to-owner matrix. `/goalies`, `/skoCharts`, `/FORGE`, `/trendsDebug`, and `/twitterEmbeds` retain their verified redirect, quarantine, or supported ownership; four unresolved legacy families spanning five routes remain unchanged behind 3.3/4.2/4.3. Four current route suites pass 28/28. B-DEAD is 19/38 with 19 open; no route or external state changed.
- The 2026-07-29 hidden-product implementation closes 4.2/4.3 and parent 4.0. Five retained legacy routes across four families use one shared visible noindex quarantine notice with exact canonical replacement links, while the Trends Debug page/API require resolved administrator authorization and denied shells start no console I/O. Three focused files pass 22/22 tests, full TypeScript and scoped zero-error lint pass with one pre-existing image warning. B-DEAD is 22/38 with 16 open; redirect/removal and component-deletion gates remain separate.
- The 2026-07-29 targeted cluster cleanup closes 5.3/5.4 and P3 NEW 8.4. Fourteen exact zero-consumer retired UI/utility files (1,313 lines) are removed; `dateUtils` is retained after its active `usePlayerWeeklyStats` consumer disproves the stale candidate. Deleted-symbol scans, full TypeScript, and 2 files/8 current replacement-route tests pass. Broader 5.2 stays open for quarantined and operational/manual-caller clusters. B-DEAD is 25/39 with 14 open.
- The follow-on WiGO cleanup removes six additional exact zero-consumer legacy files (668 lines): three obsolete chart implementations, TeamNameSVG, the unused three-year fetch helper, and retired rating constants. The nine-file/28-test active WiGO suite and full TypeScript pass. The cumulative targeted removal is 20 files/1,981 lines; B-DEAD remains 25/39 because this strengthens already-closed 5.3 without overstating external/manual consumer row 5.2.
- The 2026-07-30 final-verification cohort closes 3.4, 7.1, 7.2, 7.4, and P2 NEW 8.6. Current `knip` falls to 155 raw candidates, reports 164 Sass-only unresolved aliases and zero unlisted dependencies after three active transitive imports gain direct manifest ownership. The exact READY Production build and seven public 200/two protected 401 probes close the cleanup-specific route/build smoke boundary. Row 7.3 and parent 7.0 remain open because the separately owned Start Chart/Yahoo reader still returns 500 on Production until the verified local recovery is published. B-DEAD is 31/41 with ten open.
- The guarded recovery publication closes 7.3 and parent 7.0. Exact `96ccea804` is READY/Production as `dpl_HCFwiK4yAPeXUG3QzC3R28NtYvsc`; seven public shells return 200, protected rolling routes return 401, populated Start Chart desktop/mobile and restored reader surfaces pass, and bounded runtime-error/5xx queries are empty. B-DEAD is 33/41 with eight open; no migration or writer ran.
- The 2026-07-30 standalone-service disposition retires the six-file Express duplicate. The canonical Production trigger already targets `/api/v1/webhooks/on-new-line-combo`; no runtime/config/CI consumer points at the separate root; the only Vercel team contains six named web/CMS/functions projects and no webhook project. NEW 8.3 stays open only for the remaining grouped Production denial plus functions/CMS ownership receipt. The same verification registers and closes NEW 8.8 by adding the reviewed scheduler-ownership migration to the exact allowlist. B-DEAD is 35/43 with eight open; imported actionable completion is 4,868/4,987 with 119 open = 97.61%.
- The owner-approved pure-route cohort removes five empty/test/prototype routes and three exclusive zero-consumer implementation files while preserving every supported or quarantined surface. Source/master 3.0 and 3.3 close. B-DEAD is 37/43 with six open; imported actionable completion is 4,870/4,987 with 117 open = 97.65%.
- The operational ownership cohort proves the current signed-out `/db`, functions, and CMS live boundaries and missing-auth canonical webhook denial. Malformed webhook auth exposed Supabase parser detail, so P1 NEW 8.9 registers and closes the local fixed-response repair while NEW 8.3 retains exact publication/proof. B-DEAD is 38/44 with six open; imported actionable completion is 4,871/4,988 with 117 open = 97.65%.
