# Date Range Matrix Refactor — Completion Tasks

## Relevant Files

- `tasks/TASKS/dead-code-cleanup/prd-drm-refactor.md` - Source PRD, architecture contract, hotfixes, performance guidance, and rollout plan.
- `web/pages/drm.tsx` - Active `/drm` route, page-owned Option-A fixed-window resolver, exact-inclusive Custom mode, scope reporting, and unsupported-card containment.
- `web/components/DateRangeMatrix/useDateRangeMatrixData.ts` - Unified raw/aggregated data hook and direct fail-closed typed aggregate projection.
- `web/components/DateRangeMatrix/useDateRangeMatrixData.test.tsx` - Regular/playoff mapping, request ordering, invalidation, rejection, and transition-render masking regressions.
- `web/components/DateRangeMatrix/useTOIData.tsx` - Generated-schema raw reader, deterministic pagination, strict normalization, player/pair-game aggregation, fallback metadata, and genuine coverage contract.
- `web/components/DateRangeMatrix/useTOIData.test.ts` - Raw pagination, later-page rejection, mirrored-pair, partial-coverage, fallback, identity, JSON, and season-type regressions.
- `web/components/DateRangeMatrix/DateRangeMatrixView.tsx` - Presentational view wrapper with explicit result-state rendering.
- `web/components/DateRangeMatrix/DateRangeMatrixView.test.tsx` - Loading, empty, error, and partial-coverage view regressions.
- `web/__tests__/pages/drm.test.tsx` - Aggregate/date ownership, Option-A filter stability, exact Custom transitions, active-preset, synchronous card-scope invalidation, and timezone-safe calendar regressions.
- `web/components/DateRangeMatrix/fetchAggregatedData.test.ts` - Deterministic pagination, exact fixed/post-filter game-ID scope, exact skater/goalie totals, injured-player appearances, unavailable/contradictory-row handling, fail-closed page/identity/fallback, and bounded-filter regressions.
- `web/components/DateRangeMatrix/utilities.test.ts` - Explicit team/season/type-bound completed-window ownership, deterministic hard bounds, ledger validation, and database-error regressions.
- `web/components/DateRangeMatrix/index.tsx` - Core matrix renderer and legacy wrapper.
- `web/components/DateRangeMatrix/DateRangeMatrixForGames.tsx` - Specialized raw-source wrapper and dead-code candidate.
- `web/components/DateRangeMatrix/fetchAggregatedData.ts` - Complete paginated aggregate reader with strict typed row/pair reduction, exact returned-row scope revalidation, and unique matched-game/card ownership.
- `web/components/DateRangeMatrix/lineCombinationHelper.ts` - Shared lines/pairs derivation with pre-existing changes.
- `web/components/DateRangeMatrix/lineCombinationHelper.test.ts` - Sole current focused regression file; one helper happy-path test.
- `web/components/DateRangeMatrix/drm.module.scss` and `index.module.scss` - Page/matrix responsive, datepicker, control, scope-status, and grid styling.
- `web/components/DateRangeMatrix/TeamDropdown.tsx` - Physical selector that must coexist with TeamSelect.
- `web/components/DateRangeMatrix/LinePairGrid.tsx` - Pure canonical-scope consumer of the mapped roster, derived line/pair layout, and exact card metrics.
- `web/components/DateRangeMatrix/LinePairGrid.test.tsx` - Exact supported card values, hidden unsupported values, explicit unavailable coverage, and synchronous loading/empty/error/zero-game reset regressions.
- `web/components/DateRangeMatrix/PlayerCardDRM.tsx` and `GoalieCardDRM.tsx` - Props-only exact-stat cards with no independent global/date-only reads.
- `web/styles/LinePairGrid.module.scss` - Supported goalie-stat layout and scoped unavailable-state styling.
- `web/components/DateRangeMatrix/utilities.ts` - Shared `PlayerData` contract plus explicit bounded completed-team-game resolver.
- `web/lib/supabase/pagination.ts` - Shared ordered short-page and bounded-filter pagination helpers used by the aggregated reader.
- `web/pages/api/v1/db/shift-charts.ts` and `web/lib/supabase/Upserts/supabaseShifts.js` - Current full relationship-writer evidence owned by NEW 20.0; the route now exposes a named caller-owned-audit delegate while preserving the direct route's existing auth/manual-audit contract under NEW 51.0.
- `web/pages/api/v1/db/update-shifts.ts` and `web/__tests__/pages/api/v1/db/shift-charts.audit.test.ts` - Job-16 compatibility adapter and one-call/one-row success/failure audit regressions under NEW 51.0.
- `web/lib/projections/ingest/shifts.ts` and `web/pages/api/v1/db/ingest-projection-inputs.ts` - Incompatible partial `shift_charts` producer/preflight evidence owned by NEW 20.0.
- `web/pages/api/v1/db/update-PbP.ts`, `web/pages/api/v1/db/ingest-projection-inputs.ts`, `web/__tests__/pages/api/v1/db/update-PbP.test.ts`, and `web/__tests__/pages/api/v1/db/ingest-projection-inputs.test.ts` - Authenticated one-release compatibility adapter, named unwrapped canonical transaction handler, duplicate-control rejection, strict calendar-range validation, and bounded/exact/legacy-contract regressions under NEW 27.0/52.0/53.0.
- `web/lib/projections/derived/buildStrengthTablesV2.ts`, `buildGoalieGameV2.ts`, and `web/pages/api/v1/db/build-projection-derived-v2.ts` - Player/team/goalie materialization stages that must publish one exact source-versioned game result under NEW 23.0/28.0.
- `web/lib/supabase/Upserts/nhlRawGamecenter.mjs` - Immutable raw-snapshot producer whose normalized per-game scopes require exact correction semantics under NEW 29.0.
- `supabase/migrations/20260720105524_add_projection_materialization_transactions.sql` - Additive service-only input/relationship/derived manifests, durable pipeline state, shared locks, and transactional exact-replacement RPCs under NEW 20/23–42.
- `web/lib/supabase/projectionMaterializationMigration.test.ts` - Static schema, privilege, uniqueness, named-conflict-target, and forbidden-positional-target guards for the additive migration.
- `web/lib/projections/ingest/projectionInputPersistence.ts`, `relationshipMaterialization.ts`, `derived/projectionDerivedPersistence.ts`, and `projectionPipelineState.ts` - Typed transactional clients, receipt validation, source/version compare-and-swap, queue selection, and durable operation-state contracts.
- `web/pages/api/v1/db/run-rolling-forge-pipeline.ts`, `shift-charts.ts`, and focused transactional/auth tests - Scheduled coordinator, canonical relationship writer, audit truth, and deployment-gated orchestration evidence.
- `web/lib/NHL/server/index.test.ts` and `web/__tests__/pages/api/v1/db/shift-charts.queue.test.ts` - Latest-started offseason season selection and older-algorithm shared relationship-queue route evidence under NEW 33/37.
- `web/lib/cron/combinedCronInventory.ts` and `web/lib/cron/combinedCronInventory.test.ts` - Fail-closed canonical pg_cron-plus-Vercel inventory, provider-specific schedule validation, canonical-route enforcement, exact coordinator-mode budget selection, calendar-overlap detection, domain classification, and scheduled runtime-cap evidence under NEW 49/50/54/55.
- `tasks/TASKS/cron-operations/cron-schedule.md` and `web/vercel.json` - Authoritative pg_cron and Vercel schedule/function-duration inputs consumed by the combined inventory.
- `web/lib/rollingPlayerOperationalPolicy.ts` and `web/pages/api/v1/db/run-projection-v2.ts` - Shared projection-route default budget contract used by runtime execution and the scheduler audit under NEW 50.

### Notes

- This list repairs the missing task pair. The target implementation is clean in the shared worktree and traces to committed partial work at `e0a43320291b54a497c3fcd28da989225ddc31fc`; preserve unrelated dirty work outside the target paths.
- The unified hook/view are claimed implemented, but parity, stability, pagination, and runtime behavior remain unverified.
- Keep both team selectors per the PRD unless a later approved product decision changes scope.
- Delete `DateRangeMatrixForGames` only after current consumers are proven absent and its unique use case is intentionally retired.
- Dependency order after the producer audit: completed reader work → immutable-source identity and exact normalized scope (NEW 25/27/29/40) → canonical locked relationship replacement (NEW 20/26) → one cross-provider schedule owner and platform-safe runtime (NEW 38/49/50) → atomic player/team/goalie derived replacement (NEW 23/28) → durable cross-day cursor scheduling (NEW 24/39) → one truthful audit owner (NEW 41/51) → supported-baseline/runtime proof → bounded history repair/canary → natural evidence → legacy-owner retirement → duplicate wrapper/UX work. This evidence-based refinement keeps all writers on one per-game lock/version contract before any status or cursor can claim completion.
- The exact reviewed 23-path NEW 12/17/18 implementation, regression, style, source-pair, and canonical-control slice is committed on `octoberBranch` as `21b5043ca3aac791ce24ed6b42d7c61607a80395`; its path count/name/status and empty post-commit index were verified before NEW 10.0 began.

## Tasks

- [x] 1.0 Reconcile the committed partial implementation with the PRD architecture. Evidence: route/hook/view/raw/aggregated/renderer/style/test ownership and the exact `e0a43320291b54a497c3fcd28da989225ddc31fc` baseline are mapped; every verified defect is appended below as NEW 8.0–16.0 (2026-07-18).
  - [x] 1.1 Inspected status/history and mapped the active `/drm` route, unified hook/view, legacy/raw wrappers, aggregated reader, renderer, helper, styles, and sole focused test to the PRD.
  - [x] 1.2 Verified the current contracts and recorded the missing error/coverage/source states, incorrect playoff bucket, stale-request risks, raw-schema drift, and numeric-type drift as NEW 8.0–11.0/16.0.
  - [x] 1.3 Identified triplicated line/pair derivation, duplicate date-range ownership, the dormant mixed-source default wrapper, and the unconsumed raw wrapper; targeted disposition remains under NEW 12.0/13.0.
  - [x] 1.4 Confirmed every DRM target path was clean before edits, preserved all unrelated shared changes, and recorded the committed partial baseline and dependency reorder in the canonical diary.

- [x] 2.0 Make raw and aggregated data paths complete, stable, and parity-testable. Both strict readers share canonical scope/identity semantics, deterministic complete grouping, pagination, stable request ownership, explicit coverage states, and one bounded URL-backed aggregate/raw comparison path; the group-2 verification and production-mode browser gate pass (verified 2026-07-21).
  - [x] 2.1 Verify raw shift-chart and aggregated inputs share canonical player/team/date/mode semantics and identity mapping. Both readers use the selected canonical team/season/type/date/mode scope, strict numeric/player identity validation, shared roster/group contracts, and whole-game partial containment; focused reader and source-switch regressions plus production-mode historical URL proof pass (verified 2026-07-21).
  - [x] 2.2 Add explicit pagination for any potentially complete-table Supabase reads and verify returned counts until a short page. Raw, aggregate, fallback, and displayed-card readers now use deterministic short-page/chunked retrieval; later-page failure and complete-count regressions pass in the focused 9-file/115-test DRM group (verified 2026-07-21).
  - [x] 2.3 Stabilize hook dependencies and memoize aggregated rows to eliminate update-depth loops without hiding real data changes. Stable defaults, memoized request identities/rows, stale-response rejection, and rerender regressions pass in the focused 9-file/115-test DRM group (verified 2026-07-21).
  - [x] 2.4 Centralize lines/pairs calculation for both sources and test ordering, thresholds, multi-position/goalie handling, missing rows, and ties. The unified hook owns the shared pure helper; canonical score/ID tie-breaks, complete three-forward/two-defense thresholds, four-line/three-pair caps, authoritative forward classification, goalie/unknown exclusion, incomplete-group omission, caller immutability, and input-order invariance pass focused regressions (verified 2026-07-21).
  - [x] 2.5 Add a bounded QA comparison path for representative teams/date ranges and document expected source differences rather than forcing false equality. The visible URL-backed source selector preserves exact team/season/type/timeframe/date/mode scope while switching aggregate/raw. Raw QA reduces shift rows and intentionally has no line/goalie stat cards; aggregate reads materialized rows and scoped cards; honest partial/empty coverage may differ by source completeness. Same-scope page regression and production-mode aggregate-to-raw browser proof pass with zero console/runtime errors (verified 2026-07-21).
  - [x] 2.6 Return explicit loading, empty, partial, stale, and error state plus source/coverage metadata. The unified hook/view expose idle/loading/success/empty/partial/error states, stale-request containment, exact source identity, coverage metadata, and visible diagnostics; focused hook/page/view tests pass in the 9-file/115-test DRM group (verified 2026-07-21).

- [x] 3.0 Finish page/view wiring and controls. All five children are evidence-closed by the unified immutable hook/view contract, synchronized selectors, deterministic URL/season/source ownership, portal-safe DatePicker behavior, and stale/mixed-result regressions; the focused control group passes 5 files/41 tests (verified 2026-07-21).
  - [x] 3.1 Ensure `drm.tsx` uses the unified hook and props-only view without duplicate derivation or unstable object construction. The page calls one `useDateRangeMatrixData` owner, forwards its canonical roster/lines/pairs/state/coverage into props-only renderers, and retains stable memoized aggregate/request identities under NEW 9/18/56 (verified 2026-07-21).
  - [x] 3.2 Preserve both horizontal TeamSelect and physical TeamDropdown with synchronized canonical abbreviation state and accessible labels. Both selectors read/write the same sanitized URL-backed team identity; native label/ARIA and canonical-or-empty selection regressions pass (verified 2026-07-21).
  - [x] 3.3 Keep date range, season type, mode, and source/QA URL state deterministic and restorable. Exact URL hydration/canonicalization, historical-season lookup, selected-season raw/aggregate constraints, and production-mode historical/default browser proof pass under NEW 16 (verified 2026-07-21).
  - [x] 3.4 Fix datepicker clipping with portal and/or scoped overflow/z-index behavior that works on desktop and mobile. NEW 15's local portal hooks, themed in-viewport calendar, Escape focus restoration, responsive breakpoints, and six-width Chromium proof close the contract (verified 2026-07-21).
  - [x] 3.5 Verify invalid ranges, no schedule/data, team changes, source changes, and rapid control changes do not leave stale or mixed results. Exact page/hook regressions cover missing/reversed ranges, empty/error/partial states, team/source/season changes, late aggregate and rolling responses, filter invalidation, and manual-Custom races; the focused 5-file/41-test group passes (verified 2026-07-21).

- [x] 4.0 Complete presentation, accessibility, and performance polish. Token-aligned active styles, responsive controls/cards, local matrix overflow, sticky headers, tooltip/ARIA/focus semantics, logical roving focus, memoized transforms, stable request props, and representative real-roster/request-count/rerender evidence pass without a speculative virtualization rewrite (verified 2026-07-21).
  - [x] 4.1 Align typography, spacing, colors, controls, page header, loading/empty states, and focus styles with current FHFH tokens without redesigning the matrix. NEW 15/62 retain the existing layout while making responsive surfaces, portal calendar, active selector/tooltip/position styles, and visible focus use shared tokens; focused tests, Prettier, and three-module Sass compilation pass (verified 2026-07-21).
  - [x] 4.2 Add horizontal-scroll affordance, readable sticky layers, contrast/tooltips/aria labels for matrix cells, and keyboard-visible focus. The matrix owns local overflow/scrollbar gutter, layered sticky headers, accessible row/column/cell names and tooltips, one logical roving tab stop, arrow navigation, and reorder-safe focus; NEW 14/15/58/59 tests and six-width Chromium proof pass (verified 2026-07-21).
  - [x] 4.3 Memoize measured heavy transforms and stable props; remove/gate debug logs and avoid speculative virtualization below demonstrated roster-size need. Canonical request identity, roster/group derivation, sort/ATOI transforms, visible line/pair/card slices, and page aggregate props are memoized; NEW 63 stabilizes the dormant wrapper's empty ATOI identity and exact roster dependencies. Request-count/rerender tests prove mode-only and stable rerenders do not refetch, zero lint findings remain, active failure diagnostics stay intentional, and real NHL roster evidence does not justify virtualization (verified 2026-07-21).
  - [x] 4.4 Measure representative load/interaction/render behavior and append any verified bottleneck as a `NEW` task. Representative production-backed PHI season rendering, six responsive widths, logical-focus reorder, stable-rerender/mode/source/filter request counts, and same-scope aggregate/raw interaction were measured through browser and regression evidence. No remaining local render/fetch bottleneck was demonstrated; the only newly verified styling drift was registered and closed as NEW 62 (verified 2026-07-21).

- [ ] 5.0 Resolve specialized wrapper and duplicate-code disposition
  - [x] 5.1 Search all imports/routes/tests/docs for `DateRangeMatrixForGames` and identify any planned game-detail use. Repository-wide source inventory finds only the wrapper's own definition and task/documentation references: zero runtime import or route consumer and no recorded planned game-detail use. Disposition/removal remains separately open under 5.2/NEW 13 (verified 2026-07-21).
  - [ ] 5.2 Keep it as a thin typed wrapper when a supported consumer exists; otherwise record the retirement decision and remove only after approval if route/product scope changes.
  - [ ] 5.3 Reconcile `useTOIData`, LinePairGrid fetches, and legacy default wrapper paths with the unified contract and remove only proven duplicates.

- [x] 6.0 Run targeted and integrated verification. All four children now pass: retained automated/type/style/build evidence plus the final production-mode browser sweep cover representative regular/playoff, aggregate/raw, all three layouts, both team selectors, Custom/date/error/empty/loading/partial states, DatePicker portal/focus, logical grid navigation, and six responsive widths with zero browser warnings/errors (verified 2026-07-21).
  - [x] 6.1 Run focused helper/hook/component tests, TypeScript checks, and SCSS/build validation using current package commands. The complete DRM group passes 10 files/135 tests; full TypeScript, changed-code Prettier, zero-finding isolated lint, three-module bundled-Node-24 Sass compilation, and the successful 83-page production build/static generation gate pass (verified 2026-07-21).
  - [x] 6.2 Browser-verify representative regular/playoff date ranges, raw/aggregated QA, modes, both selectors, datepicker, loading/empty/error, and responsive widths. Production-mode Chromium verified the populated PHI 2025-26 regular aggregate and raw scopes, historical EDM 2024-25 playoff Custom empty scope, deterministic reversed-range error, line-combination/total-TOI/full-roster layouts, physical dropdown and horizontal selector synchronization, themed in-viewport DatePicker with Escape focus return, and 1440/1008/1007/768/390/320 widths with four/two/one control columns and zero document overflow (verified 2026-07-21).
  - [x] 6.3 Confirm no maximum-update-depth error, duplicate fetch storm, missing player, mixed source, stale result, console error, or sticky/overflow regression. Aggregate/raw transitions retained matching URL/control/content state; every populated row/column header was named, exactly one roving cell advanced logically by keyboard, sticky headers and matrix-local overflow held at all six widths, the earlier TOI invariants became truthful 76-row aggregate and 535-row raw partial notices, and browser diagnostics contained zero warnings/errors. Existing deferred-response and request-count regressions supply deterministic stale-result and duplicate-fetch proof without production fault injection (verified 2026-07-21).
  - [x] 6.4 Update the PRD, this list, Relevant Files, dead-code disposition, and master ledger with parity/performance/test evidence. The source PRD/list, master PRD/list, final summary, and diary agree on 61/89 before this row's closure, zero-consumer wrapper retention pending explicit retirement approval, group-2/group-4 evidence, verification results, and exact publication receipts (verified 2026-07-21).

## NEW Tasks

- [ ] NEW 7.0 Append every verified parity defect, pagination gap, stale-state issue, consumer conflict, accessibility problem, and optimization discovered during execution here before closure.
- [x] NEW 8.0 **P1 — Fix active playoff and aggregate value mapping.** The page now passes explicit season type; the unified mapper selects `regularSeasonData` or `playoffData`, converts `totalTOI` and relationship values to finite numeric contracts, derives canonical team/franchise identity, rejects invalid player IDs, and keeps a stable default aggregate reference. Focused regular/playoff/identity/hook regressions pass 4/4; the combined DRM helper group passes 2 files/5 tests, full TypeScript and scoped ESLint pass, and the changed hook/new test pass Prettier (2026-07-18).
- [x] NEW 9.0 **P1 — Prevent stale or permanently loading DRM results.** Exact request identities now suppress late raw/date/aggregate completions and synchronously mask prior roster/derived state during changed-input renders; invalid inputs and failed/empty last-N lookups clear old dates/data; the hook/view expose deterministic loading/empty/error/partial/stale/source/coverage metadata. Focused hook/view/page/helper verification passes 4 files/14 tests, full TypeScript and scoped diff checks pass, scoped legacy ESLint has zero errors and only three pre-existing page dependency warnings, and the changed hook plus all three new tests pass Prettier (2026-07-18).
- [x] NEW 10.0 **P1 — Rebuild the promised raw `shift_charts` source against the generated schema.** The reader now uses generated row types, a narrow expected-type-or-null team/date select, deterministic `id`-ordered inclusive short pages, later-page rejection, strict identities/clocks/JSON/unique player-games/mirrors, whole-game partial coverage for incomplete producer rows, optional consistent home/away metadata, post-fallback canonical same/mixed categorization, and exact roster/pair/GP/TOI/ATOI/team/coverage output. The unified hook passes season type and exposes genuine partial coverage. Verification passes 4 files/51 tests, full TypeScript, scoped legacy ESLint, Prettier, scoped diff integrity, and two independent final reviews; no schema or external state changed (2026-07-18).
- [x] NEW 11.0 **P1 — Make the active aggregated reader complete and fail closed.** The page now owns one explicit team/season/date/season-type request; the generated-schema reader uses the shared ordered short-page helper, constrains `team_id`/`season_id`/`game_type` plus exact dates and optional filters, and rejects any page error. Player fallbacks use ordered 200-ID chunks and reject any failed chunk; database failures in last-N date resolution propagate to the page error state; nullable player identities reject instead of becoming silent partial success; and the unused duplicate player reader is removed. The focused DRM/pagination group passes 7 files/31 tests, full TypeScript and scoped diff checks pass, scoped legacy ESLint has zero errors and the same three pre-existing page dependency warnings, the two new tests plus existing page test pass Prettier, and independent final review found no NEW 11 blocker (2026-07-18).
- [x] NEW 12.0 **P2 — Encode owner-approved Option-A last-N plus home/opponent semantics.** L7/L14/L30 now resolve the selected team's exact completed-game IDs first from a page-owned team/season/type-bound WGO request; deterministic `date`/`game_id`/`id` ordering, exact `0..N-1` range, strict ledger validation, and insufficient/error fail-closed behavior define the fixed window. The fully paginated aggregate reader receives those exact IDs and only then applies Home/Away/Opponent, while unique filtered IDs drive truthful `x matching games within last N team games` copy. Manual date edits enter visible exact-inclusive Custom; team/filter changes preserve it, season/type or preset selection exits it, missing/reversed ranges issue no request, active-preset re-clicks are safe no-ops, and paired local-calendar parse/format logic passes full-ISO boundaries in New York and Auckland. The page unmounts unsupported line/goalie cards for Custom/loading/error/empty without claiming NEW 17 or NEW 18 complete. Verification: 7 files/43 focused tests, page 11/11 in both `America/New_York` and `Pacific/Auckland`, full TypeScript, bundled-runtime Sass, Prettier, scoped diff integrity, scoped legacy ESLint with zero errors and the same three pre-existing hook warnings, and independent data/logic re-review pass (2026-07-18).
- [ ] NEW 13.0 **P2 — Consolidate data and derivation ownership.** Remove write-only page state and triplicated line/pair work, make `LinePairGrid` a canonical props consumer, and retain/remove the two dormant wrappers only through the recorded consumer/approval disposition.
- [x] NEW 14.0 **P2 — Restore selector and matrix accessibility.** Team/date/layout controls now use native labeled semantics; carousel/timeframe/mode controls expose button/pressed state; the matrix exposes named grid/row/header/cell/value structure with one logical roving tab stop, all four arrows plus Home/End, pointer/focus header parity, and visible focus/tooltips. Evidence: integrated 4 files/23 tests, full DRM 11 files/124 tests, full TypeScript, targeted Prettier, Sass, dual-timezone page runs, zero-error scoped lint with five pre-existing warnings, diff integrity, and independent no-finding review pass. Exact checkpoint `46400a3639557b6742af14a6f55339abcfa3d393` is published with fresh local/tracking/live equality (completed 2026-07-21). NEW 15 remains separately open.
- [x] NEW 15.0 **P2 — Repair responsive and DatePicker styling contracts.** Each picker now carries local input/wrapper/calendar hooks into the portal with only library descendants global; controls use real four/two/one-column breakpoints, LinePairGrid collapses at tablet/handset widths, parent grids permit child shrinkage, and the matrix owns readable minimum tracks plus local overflow. Team/date/layout controls have visible three-pixel focus rings. Real Chromium proves zero body overflow at 1440/1008/1007/768/390/320 px, themed in-viewport portal rendering with Escape focus restoration, correct control/card collapse, matrix-local scrolling, sticky headers, and logical keyboard navigation. Evidence: full DRM 11 files/124 tests, TypeScript, Prettier, bundled-Node-24 Sass, zero-error scoped lint with five pre-existing warnings, and diff integrity pass (completed 2026-07-21).
- [x] NEW 16.0 **P2 — Validate and restore deterministic URL/season/source state.** A Pages-Router-safe query-state owner now hydrates direct URLs after mount, preserves adjacent keys and popstate, and prevents pre-hydration defaults from erasing restored dates. Team, mode, season, season type, timeframe, source, venue, opponent, and calendar values sanitize deterministically; exact historical-season lookup fails closed and selected season constrains both raw and aggregate reads. Production-mode Chromium restores an Edmonton 2024–25 playoffs/raw/custom URL with exact dates and zero console/runtime errors, canonicalizes a fully malformed URL to safe current-season defaults, and renders the default route without a TOI overlay. Evidence: 14 files/146 tests, full TypeScript, production build/static generation, live HTTP-200 server, and browser verification pass (completed 2026-07-21).
- [x] NEW 17.0 **P1 — Align visible LinePairGrid skater/goalie card metrics to the canonical matrix scope.** Exact skater/goalie rows are now fetched only for the aggregate reader's post-filter matched IDs and each player's exact scoped appearances through ordered, bounded, inclusive pagination. Missing, duplicate, malformed, unsafe-identity, and internally contradictory rows stay unavailable instead of becoming zero. Skaters retain exact G/A/PTS/PPP/SOG/HITS/BLKS/+/-; goalies retain exact GP/SV/weighted SV%/weighted GAA while unsupported Record/QS%/SO/GS and all Custom cards are hidden. Filtered, sparse-opponent, injured-player, exact-value, corrupt-row, Custom-hide, and bounded-query regressions pass (2026-07-18).
- [x] NEW 18.0 **P1 — Clear stale LinePairGrid results across unresolved and empty scopes.** One canonical team/season/type/timeframe/date/game-ID/venue/opponent key masks mismatched page results on the first render; late aggregate completions remain sequence-rejected; `LinePairGrid` and both cards are pure props consumers with no retained async state and return empty for incomplete/loading/error/empty/zero-game contracts. Focused A-to-B loading, page-filter invalidation, nonzero-to-zero, empty, and error regressions pass. Combined evidence for NEW 17/18: 8 files/56 tests, 12/12 page tests in New York and Auckland, full TypeScript, bundled-Node Sass, Prettier, diff integrity, zero scoped ESLint errors with the same three pre-existing page warnings, and two independent final audits pass (2026-07-18).
- [x] NEW 19.0 **P1 — Make the active aggregate reduction as strict as the rebuilt raw contract.** Generated `shift_charts` row types now feed strict identity/date/team/season/type/clock/JSON/line-pair normalization; duplicate row/player-game identities, missing or contradictory mirrors, unknown partners, unsafe appearance bounds, metadata drift, and returned rows escaping fixed-game/venue/opponent scope reject before fallback or card reads. One unordered pair/game fact produces symmetric zero-aware totals, canonical same/mixed buckets, player-relative percentages, exact GP/game IDs, and rounded ATOI; the downstream mapper directly projects the typed contract without coercion or row dropping. The raw reader shares the padded integer/`M:SS`/`H:MM:SS` grammar, while its nullable-partial coverage versus the aggregate query's exact non-null scope remains honestly documented under NEW 20. Verification passes 9 DRM/shared-pagination files/115 tests, 3 direct files/82 tests, Auckland page 12/12, full TypeScript, zero-error scoped legacy ESLint with three pre-existing page warnings, Prettier, diff integrity, React review, and two independent final audits (2026-07-18).
- [ ] NEW 20.0 **P1 — Reconcile the two active `shift_charts` producer contracts and repair incomplete history.** Local containment now uses generated-schema completeness plus the exact paginated raw-shift player manifest, separates projection strength ownership from DRM relationship fields, normalizes numeric assignments/game types, deterministically pages positions, handles playoff OT, validates every shift-bearing PBP timeline and interval fail-closed, and invalidates only strength-owned columns before exact current-player replacement. The bounded inventory remains 14,976 relationship-incomplete rows/392 games and 12,279 current-season strength-incomplete rows/329 games. Keep open until one authoritative relationship materializer, transactional/versioned producer boundary, bounded history repair, production deployment, and rollback evidence are approved and verified. Raw/aggregate readers must continue exposing incomplete rows as partial/error rather than truthful zero. [Discovered during NEW 10/19; shared B-DRM/FORGE/cron dependency; local evidence 2026-07-19.]
- [x] NEW 21.0 **P2 — Close raw-reader input, clock, and fallback-name boundary gaps.** Team metadata now uses the same optional-safe uppercase/trimmed identity as the reader, reduced clocks must remain safe integers, game length must be positive, and fallback surnames preserve every token after the given name. Focused lowercase/invalid-input, overflow, zero-length, and surname-particle regressions pass within the 4-file/51-test group; full TypeScript and independent delta review pass (2026-07-18).
- [ ] NEW 22.0 **P0 — Contain legacy `shift_charts` service-role credential logging.** The value-bearing URL/key log calls are removed without accessing or reproducing the secret, and local writers now fail closed. Exact tracked-source/config/history inventory finds zero runtime, package, workflow, Vercel, migration, import, loader, child-process, or historical invoker; every known cron/coordinator/manual caller uses the independent authenticated route and never loads the self-executing file. Keep open only for value-free untracked/manual/external execution and retained-log inventory between 2024-10-09 and 2026-07-19. If execution or retention is proven, rotate the production credential and update every consumer under a separately approved rollback-tested migration; otherwise advance self-execution retirement through its disposition gate. [Discovered during NEW 20; shared B-DRM/B-CLEAN security dependency; tracked inventory verified 2026-07-21.]
- [ ] NEW 23.0 **P1 — Fail the derived-strength pipeline closed on incomplete player rows.** Local builders now reject incomplete clocks, identities, situations, PBP, and exact raw-player coverage; clear the requested game’s prior team/player derived scope before rebuilding; stop the team/goalie dependency chain after failure; and retain the failed date as the resume cursor. Focused stale-player, nullable-metric, zero-clock, stage-order, and cursor regressions pass. Keep open until an exact durable derived-status/version gate, transactional replacement/serialization, production deployment, and bounded stale-history proof prevent partial multi-game/date reads. [Discovered during NEW 20; shared B-DRM/FORGE dependency; local evidence 2026-07-19.]
- [ ] NEW 24.0 **P1 — Give scheduled projection ingestion a durable completed-game window and cursor.** Local routes now default to the prior completed UTC slate, process every same-date game unless an explicit bound is supplied, accept deterministic date/game cursors, retain the exact failed date/game, and pass greater-than-six-game continuation regressions. Keep open because the cursor is response-only and the production jobs still require coordinated durable cursor storage/commands, deployment, bounded retry/runtime proof, and natural-run evidence. [Discovered during NEW 20; shared B-DRM/FORGE/cron dependency; local evidence 2026-07-19.]
- [ ] NEW 25.0 **P1 — Replace PBP presence with completeness and freshness semantics.** Final metadata, positive unique event IDs, more than terminal-only coverage, exactly one `game-end`, prevalidated shifts, exact PBP replacement, source fingerprints, shared per-game lock/versioned RPC, rollback/concurrency, deployed one-game production publication, and physical no-DML replay now pass. Keep open because active job 104 can still bypass the manifest through NEW 27 and no bounded historical/backfill verification has run. [Discovered during NEW 20; shared B-DRM/FORGE dependency; updated evidence 2026-07-21.]
- [ ] NEW 26.0 **P1 — Exact-replace relationship-owned `shift_charts` scope on corrected source snapshots.** Shared-lock/current-input CAS, relationship-only field clearing, strength preservation, exact current-player replacement/count receipt, source-correction/removal, rollback, idempotency, and one bounded production canary/replay now pass. Keep open for bounded affected-history repair, active canonical scheduler proof, natural writer evidence, and final legacy-owner retirement under NEW 20/27/29/38. [Discovered during the authorized Option-A producer audit; root dependency NEW 20; updated evidence 2026-07-21.]
- [ ] NEW 27.0 **P1 — Eliminate the active legacy PBP writer collision.** Exact authoritative checkpoint `58cca4e31e44981df3e3089b6ebd6f0cb1d20139` makes `update-PbP` one outer `adminOnly` plus audited compatibility adapter: `recent`, bounded/start-only/current-day, and exact ten-digit game requests delegate to the named unwrapped canonical input transaction; exact IDs resolve through the authorized request client and select only that game; invalid methods/IDs/lookups fail closed. The auth inventory is 20 admin-or-cron / 1 exact-cron-only / 31 unprotected and the focused 7-file/55-test group plus TypeScript passes. `games=all` intentionally remains the authenticated legacy writer for one release. Keep open for value-free/manual/untracked caller inventory, a replacement bounded backfill decision, guarded deployment, and job-104 previous-slate/exact-game/auth/non-writing/timing probes under temporary deactivation because its 100-second caller timeout is shorter than the canonical handler's 270-second default; require rollback/reactivation gates and final legacy retirement. [Discovered during the authorized Option-A producer audit; published remediation 2026-07-21.]
- [ ] NEW 28.0 **P1 — Exact-replace goalie-derived scope in the same source-versioned transaction as player/team output.** The current goalie builder silently skips malformed facts, can report selected games as processed with zero writes, upserts without deleting stale game rows, and does not prove PBP completeness or source generation. Compute explicit valid rows or a durable justified no-goalie state, then atomically exact-replace player, team, and goalie game scopes under one lock with an expected-input-version compare-and-swap and exact receipt counts. Add stale-goalie, zero-row, corrected-source, late-failure rollback, idempotency, and version-race regressions plus bounded history proof. [Discovered during the authorized Option-A producer audit; root dependency NEW 23, 2026-07-20.]
- [ ] NEW 29.0 **P1 — Make normalized Gamecenter scopes exact and version-addressable.** Immutable raw payloads already carry content hashes, but normalized roster/PBP/shift writes are upsert-only, so upstream removals can survive and poison the expected player/event manifest used by DRM, xG, underlying stats, and team-unit TOI. Under the same per-game lock, bind normalization to the latest immutable raw hashes, exact-replace each normalized game scope, publish parser/source fingerprints and exact counts, and prove correction-with-removal, bounded affected-history repair, downstream xG/TOI compatibility, rollback, and idempotency. [Discovered during the authorized Option-A producer audit; root dependency NEW 25; cross-initiative finding, 2026-07-20.]
- [x] NEW 30.0 **P0 — Put every activated projection materialization route behind the cron/admin boundary.** The exact isolated Production-environment artifact at authoritative commit `a2af6daa24e8f7e5f49990437a93c35650e39151` is READY with no custom alias movement. Input, derived, and rolling routes each reject anonymous and deliberately invalid requests with 401, accept the current header-only cron credential far enough to return the intentional unsupported-method 405, and persist the exact value-free 2×401 plus 1×405 audit groups. No materialization work ran during the proof (verified 2026-07-21).
- [x] NEW 31.0 **P1 — Make input, relationship, and derived idempotency physically truthful.** On the supported-baseline branch, input, relationship, and derived exact replays returned before DML with unchanged stored scopes, versions, completion timestamps, and zero prune counts; exact replacement pruned stale owner rows; A→B→A reversion repaired reused logical identities as new versions; forced late-status failures rolled back all three scopes; and a real two-session shared-lock race committed one input winner while the contender rejected without mutation. Final zero-residue, advisor, and branch-deletion proof passed (2026-07-20).
- [ ] NEW 32.0 **P1 — Add expected input-version CAS to every dependent transaction.** Relationship and derived clients/RPCs carry and verify the input hash plus version under the shared game lock and final update predicate. Isolated ABA/version-race proof and the deployed production canary/replays pass. Keep open solely until NEW 27/29 contain every physical source writer that can otherwise mutate inputs without advancing the canonical version. [Discovered during NEW 28 contract review; updated evidence 2026-07-21.]
- [ ] NEW 33.0 **P1 — Keep the scheduled relationship queue on the authoritative latest-started season during the offseason.** The route uses shared server `getCurrentSeason()`; direct server and implicit-route tests prove a future season is excluded and the latest-started season ID reaches the paginated game query after the current season has ended. Keep open until a deployed bounded implicit-queue probe proves the same season with unchanged exact schedules. [Discovered during scheduled-queue review 2026-07-20; local behavioral proof 2026-07-21.]
- [x] NEW 34.0 **P2 — Close durable pipeline-state replay and validator gaps.** Client key validators match the three SQL contracts, null transition/status values fail closed, and both client and RPC now treat a completed operation as an exact no-mutation receipt. On the isolated branch, acquire/advance/complete and acquire/fail/reacquire/advance/complete paths advance exact revisions; competing lease, cursor regression, range change, owner mismatch, and stale revision all reject without mutation; a real two-session race commits exactly one revision advance; and no probe lease remains running. Evidence: focused static/client tests plus authorized data-less temporary branch, 2026-07-20.
- [x] NEW 35.0 **P1 — Enforce mixed-owner `shift_charts` and first-adoption integrity.** Strength readers exclude only all-null relationship-only rows while retaining partial strength rows; input SQL requires two teams, deletes fully unowned debris, and invalidates legacy relationship/derived scopes even when creating the first status row. Disposable-branch exact replacement pruned withdrawn owner rows; A→B→A correction/removal, forced late rollback, and current-version derived continuity passed. The bounded production canary adopted a legacy strength-only two-team scope, completed input → relationship → derived at v1/v1/v1 with zero non-candidate mutation in the bounded canary window, and exact replays performed zero DML with unchanged digests, status, and timestamps. [Completed 2026-07-21.]
- [x] NEW 36.0 **P0 — Prove the additive migration compiles before any production apply.** The complete additive migration compiles and applies against the supported public-schema baseline; duplicate-column and named-conflict guards, capture/input/relationship/derived replay, rollback, stale-version, ABA, concurrency, ACL/RLS/index, and generated-type gates pass. Temporary fixtures and diagnostic objects were removed, final candidate-specific advisors passed, and the disposable branch was deleted; B-CRON-NST NEW 60 separately owns the missing authoritative `analytics` baseline (2026-07-20).
- [ ] NEW 37.0 **P2 — Make relationship scheduling algorithm-version aware.** Local status reads include the stored relationship algorithm; a shared-selector route test proves a completed v2 row is queued and reaches the per-game processing path under the current v3 materializer. Keep open for a value-free legitimate candidate preflight plus one bounded deployed implicit-queue writer proving v3 publication, unchanged input identity, truthful audit, and zero unrelated mutation. [Discovered 2026-07-20; local behavioral proof 2026-07-21.]
- [ ] NEW 38.0 **P1 — Put the canonical relationship transaction in the active schedule and enforce input → relationship → derived order.** Local `run-projection-v2` now executes canonical input → relationship → derived with fail-stop/status gating, and job 16's `update-shifts?action=all` target is a compatibility adapter rather than a separate legacy implementation. Keep open because production schedules still run job 16 before input and cross-provider ownership is unresolved under NEW 49/50; require one authoritative cutover, rollback metadata, exact natural-order evidence, and final owner retirement through NEW 20/26/27. [Discovered 2026-07-20; local-order correction 2026-07-21.]
- [ ] NEW 39.0 **P1 — Make scheduled backlog/cursor identity durable across explicit coordinator arguments and day boundaries.** Implicit coordinator runs now preserve the input durable queue, resume the oldest unfinished input backlog, avoid explicit date/`maxGames` bypass, and stop downstream stages on failure. Keep open for an explicit decision whether per-game manifest-driven downstream recovery satisfies the required cross-stage lifecycle or a shared operation identity is still needed, followed by deployed next-day recovery and natural-run proof. [Discovered 2026-07-20; local-state correction 2026-07-21.]
- [x] NEW 40.0 **P1 — Bind normalized source hashes to the latest immutable Gamecenter snapshot inside the game lock.** All three locked materialization RPCs validate exact current snapshot-head/raw IDs, hashes, and versions; stale or mismatched provenance rejects without mutation, while A→B→A correction/removal and current-version relationship/derived publication pass. Final cleanup/advisor/branch-deletion proof passed; NEW 29 remains open for bounded history and broader xG/TOI compatibility (2026-07-20).
- [ ] NEW 41.0 **P2 — Keep cron audit outcomes truthful for rejected relationship requests.** Local `shift-charts` handling now derives audit failure from every final HTTP status at or above 400, with runtime coverage for GET 405, invalid-game 400, and invalid-limit 400. Keep open for exact deployment and natural audit evidence. [Discovered during final audit-truth review; locally remediated 2026-07-20.]
- [x] NEW 42.0 **P1 — Eliminate runtime conflict-target ambiguity from table-valued projection RPCs.** All four affected branch functions now use their exact named constraints (eight named targets and zero positional targets), the focused static guard passes, and executable capture/input/relationship/derived initial plus exact-replay probes pass. Late-status failpoints prove full rollback, stale raw/input-version calls leave state unchanged, correction/reversion reaches exact versions 1→2→3, an ABA probe rejects the reused fingerprint at the stale input version, and a real two-session race commits exactly one version-4 winner while the blocked contender resumes with `PROJECTION_INPUT_CAS_MISMATCH`. Evidence: authorized data-less temporary branch, 2026-07-20.
- [x] NEW 43.0 **P2 — Make a completed durable pipeline operation terminal at the SQL RPC boundary.** `advance_projection_pipeline_state_v1` now returns the exact stored completed receipt for a same-range acquire without changing revision, cursor, lease, error, or timestamp; a changed range still rejects with unchanged state. The static regression passes, the corrected replacement function is applied only on the isolated branch, full success/failure recovery paths pass, and a real two-session revision race commits one winner while the blocked contender rejects stale revision. Evidence: authorized data-less temporary branch, 2026-07-20.
- [x] NEW 44.0 **P1 — Revoke inherited destructive `service_role` privileges from projection state tables.** The migration now explicitly revokes all privileges from `service_role` on each new state table before restoring only `SELECT`, `INSERT`, and `UPDATE`. Static ordering guards pass; branch catalog proof shows exact narrow ACLs, forced RLS/no policies, browser denial, service-only invoker RPCs, and eight named/zero positional targets; runtime proves browser table/RPC denial, valid service-role terminal RPC execution, raw service-role update/delete denial, immutable-trigger update/delete rejection, unchanged raw rows, and zero running probe leases. Evidence: authorized data-less temporary branch, 2026-07-20.
- [x] NEW 45.0 **P2 — Give the exact raw-snapshot foreign key a linter-recognized covering index.** The migration and isolated branch now use the exact ordered `(raw_payload_id, game_id, endpoint, payload_hash)` child index for `nhl_raw_snapshot_head_exact_payload_fkey`; the focused static guard and catalog definition pass, and the targeted unindexed-foreign-key advisor entry is absent. Fresh-branch unused-index informational notices remain expected until real workload and do not justify removing required integrity indexes. [Verified on the authorized Option-A branch, 2026-07-20.]
- [x] NEW 46.0 **P1 — Make the canonical migration executable through the authoritative CLI transaction boundary.** The first guarded production `db push` failed before schema/history mutation because its top-level `LOCK TABLE` lacked an explicit transaction. Commit `d8e98d9570989e4840a9172980ccb3abf36fb9ce` added and statically guarded one full-file `BEGIN`/`COMMIT`, passed the exact one-file dry run, and was fast-forward published. The canonical production apply then recorded exact history version `20260720105524`; 3/3 forced-RLS state tables, 5/5 valid indexes, 4/4 enabled raw triggers, 7/7 invoker/empty-path functions, exact narrow ACLs, 5,680/5,680 valid heads, zero pre-canary status/pipeline rows, and zero candidate advisor warning/error all pass (2026-07-21).
- [x] NEW 47.0 **P2 — Reconcile the frozen cron-auth inventory after projection-route hardening.** The inventory now records input, derived, and the one-release `update-PbP` compatibility adapter behind the shared cron/admin boundary. The reviewed split is 20 admin-or-cron / 1 exact-cron-only / 31 unprotected; all three routes are absent from the explicit unprotected set and the focused inventory suite passes (discovered and kept reconciled 2026-07-21).
- [x] NEW 48.0 **P1 — Fail relationship publication closed when Yahoo name matching cannot supply a complete player position.** Exact commit `4f8ba12ae7d7622299f3fccf6ec2cfbf2b63cd64` publishes the raw-PBP-bound roster fallback, canonical algorithm-v3 payload, pre-calculation/client rejection, and legacy-compatible relationship-column trigger. Production records migration `20260721013821`; the strict unique-date first-adoption selector still found 343 plays, 846 source shifts, 38 players/two teams, two goalies, complete current-hash roster coverage, and exactly three Yahoo-unmatched fallbacks after an immediate gate found zero writer/lock overlap. The isolated canary completed input 382, relationship 38, and derived 42 verified rows; exact replays performed zero DML with unchanged table digests, versions, fingerprints, and timestamps. Final manifests are complete at v1/v1/v1, invalid candidate positions and bounded-window non-candidate mutations are zero, the 32,397 legacy-invalid rows are unchanged, every stage has two successful audits, and bounded build/runtime error evidence is empty. The exact-SHA Production target is READY without alias error; all five project domains resolve it and the live boundary returns 401/401/405 for missing/invalid/current authorization. Branch invalid/valid/service-role/legacy-update proof, local 5-file/57-test and 42-file/321-test groups, TypeScript/lint, advisors, cleanup, and branch deletion remain green. [Completed 2026-07-21; root dependencies NEW 20/26/29 remain independently open.]
- [ ] NEW 49.0 **P1 — Reconcile cross-provider scheduler ownership before canonical projection activation.** The local fail-closed combined inventory now reads the canonical 64 active pg_cron rows plus 20 Vercel rows, applies strict provider-specific cron grammar and pg_cron calendar semantics, classifies shared pipeline domains, and proves the same-minute Vercel rolling-coordinator versus pg_cron job-308 projection collision without exposing hosts or credentials. Keep open to select one authoritative natural writer, remove or reorder overlaps with exact rollback metadata, prove zero concurrent writers after deployment, and capture one natural ordered run. [Discovered during post-canary schedule audit; local inventory verified 2026-07-21.]
- [ ] NEW 50.0 **P1 — Align naturally scheduled projection work with the deployed platform duration limit.** One shared runtime constant now proves the coordinator and direct projection route default to 270 seconds, while the Vercel Pages function cap is 240 seconds; the combined inventory identifies the coordinator plus active direct jobs 308/393 as lacking audit-flush headroom unless an exact bounded override is present. Keep open to reduce or split bounded work with headroom, or remove the affected route from natural Vercel execution while retaining bounded/manual use; prove every retained nontrivial schedule terminates and persists its final audit. [Discovered during post-canary schedule audit; local inventory verified 2026-07-21.]
- [ ] NEW 51.0 **P2 — Establish one audit owner for relationship compatibility calls.** Exact local checkpoint `3e74ac295373af6dfe8efea1c8983403b4531cac` keeps the outer `withCronJobAudit(adminOnly(...))` `update-shifts` adapter as job 16's sole auth/audit owner and delegates through an explicit caller-owned-audit `shift-charts` handler; the default direct route retains its existing `adminOnly` and manual-audit behavior. Focused success and invalid compatibility regressions each assert exactly one truthful insert, existing direct-route audit coverage remains green, and the broader six-file/29-test audit/queue/transaction/coordinator group plus full TypeScript, Prettier, and diff integrity pass. Keep open until the exact deployment and next bounded/natural invocation prove one call produces one truthful row, downstream of NEW 49/50. [Discovered during post-canary schedule audit; local remediation published 2026-07-21.]
- [x] NEW 52.0 **P1 — Reject repeated compatibility query controls before selecting a writer.** `update-PbP` now rejects array/repeated `gameId`, `startDate`, `endDate`, and `games` values with HTTP 400 before canonical or legacy work can run. Parameterized regression coverage proves both writers remain untouched for every repeated control. [Discovered during independent NEW 27 review; completed 2026-07-21.]
- [x] NEW 53.0 **P2 — Validate canonical projection calendar ranges strictly before source work.** The canonical input handler now accepts only real `YYYY-MM-DD` calendar dates in nondecreasing order. Malformed, nonexistent, and reversed ranges fail with HTTP 400 before Gamecenter fetches, raw capture, or persistence; parameterized regressions cover each class. [Discovered during independent NEW 27 review; completed 2026-07-21.]
- [x] NEW 54.0 **P2 — Reject noncanonical scheduled route aliases before classification.** Scheduled routes now reject every raw whitespace character and fail when WHATWG URL normalization changes the raw pathname, including direct, parent, and percent-encoded dot-segment aliases. Exact regressions prove each malformed path fails before domain classification; the final four-file scheduler group passes 66/66, full TypeScript and Prettier pass, diff integrity is clean, and independent re-review found no residual P0–P3 issue. [Discovered and completed 2026-07-21.]
- [x] NEW 55.0 **P2 — Derive coordinator runtime evidence from its exact scheduled mode.** Coordinator schedules now require exactly one supported `mode`, reject missing/repeated/unsupported values, and select the shared daily-incremental, overnight, or targeted-repair pipeline budget. Exact overnight/targeted regressions close the underreporting path while preserving the current daily 270-second versus 240-second finding; the final four-file scheduler group passes 66/66, full TypeScript and Prettier pass, diff integrity is clean, and independent re-review found no residual P0–P3 issue. [Discovered and completed 2026-07-21.]
- [x] NEW 56.0 **P2 — Render forward lines and defense pairs independently.** The unified hook is now the sole canonical group owner in all three modes; the helper derives computed scores from cloned players; and `LinePairGrid` consumes readonly groups, caps lines/pairs independently, and renders grouped forward/defense columns without recomputation or caller mutation. Zero-line/three-pair and one-line/two-pair regressions pass. Evidence (2026-07-21): focused 4 files/36 tests and full DRM 8 files/114 tests pass; TypeScript, targeted Prettier, bundled-Node-24 Sass compilation, dual-timezone 13-test page runs, diff integrity, and legacy ESLint with zero errors/the same three pre-existing page warnings pass; independent review found no P0–P3 issue. Exact published checkpoint `fb4e5fd778954546db174ff546dd099752cf9324` has fresh local/tracking/live equality. NEW 13 remains open only for the separately approval-gated dormant-wrapper disposition.
- [x] NEW 57.0 **P2 — Preserve caller roster order during matrix sorting.** Every active total-TOI sort now copies before sorting; an exact regression proves the caller roster order remains unchanged while the rendered order follows ATOI. Included in the NEW 14 verification group (completed 2026-07-21).
- [x] NEW 58.0 **P2 — Keep missing self-cell ATOI meaning truthful.** The accessible formatter accepts only clock seconds `00`–`59`; absent and malformed values, including `20:99`, announce exactly `N/A ATOI`. Included in the NEW 14 verification group (completed 2026-07-21).
- [x] NEW 59.0 **P2 — Preserve logical roving focus across matrix reorder.** Roving/focused/hovered state, refs, and header highlighting now use row/column player IDs. The focus-then-rerender regression proves the same logical cell retains browser focus and `tabIndex=0`, exactly one tab stop remains, and ArrowRight follows the new order. Included in the NEW 14 verification group (completed 2026-07-21).
- [x] NEW 60.0 **P1 — Keep invalid appearance/game-length source rows from taking down otherwise usable DRM scopes.** Both raw and aggregate readers now exclude an entire game when an appearance exceeds game length or a relationship exceeds either recorded appearance; no TOI is capped or fabricated and strict row/scope/clock/identity/mirror validation remains. Exact input/roster/skipped coverage reaches the hook and page, usable games/cards/matrix remain visible under an explicit partial status, and game-local pair facts commit only after every appearance bound passes. Evidence (2026-07-21): production-backed PHI season renders a matrix plus `76 rows were skipped` with no runtime dialog/console error and zero body overflow; affected-plus-valid and affected-only regressions pass in both readers; full DRM 11 files/129 tests, TypeScript, Prettier, Sass, scoped lint with zero errors/five pre-existing warnings, and diff integrity pass. The bounded production inventory remains 98 initial appearance/game-length offenders across 77 games/29 teams; historical producer repair remains owned by NEW 20/25–29 and requires separate production authority.

- [x] NEW 61.0 **P2 — Make line/pair grouping deterministic when canonical scores tie.** Canonical combo-score/player-ID ordering and shared-TOI/canonical partner tie-breaks now make equivalent raw/aggregate rosters input-order invariant without mutating callers. Exact regressions prove complete-group thresholds, existing four-line/three-pair caps, authoritative multi-position forward handling, goalie/unknown exclusion, and incomplete-group omission; the group-2 and full DRM gates pass (discovered and completed 2026-07-21).
- [x] NEW 62.0 **P2 — Remove active DRM color-token drift before closing presentation polish.** Active matrix tooltip, selector focus/option, and player-position badge styles now use existing FHFH surface/text/border tokens without redesign. Three target Sass modules compile under bundled Node 24; focused page/view/matrix/card coverage passes 4 files/35 tests and Prettier is clean (discovered and completed 2026-07-21).
- [x] NEW 63.0 **P2 — Stabilize the dormant DRM wrapper's empty ATOI identity and roster memo dependencies.** One immutable typed empty ATOI identity now preserves the conversion memo and the aggregate roster memo depends only on its actual aggregate input. Isolated legacy lint has zero findings; the complete DRM group passes 10 files/135 tests, full TypeScript and Prettier pass, and base 4.3/4.4 closure remains supported (discovered and completed 2026-07-21).

Publication evidence: exact checkpoint `36b3c38bf1db495b411b64fdf5dc8545ca9b434a` publishes the fail-closed 84-job combined inventory, shared projection-route budget, and closed NEW 54/55 validation without changing a provider owner or schedule. A normal push plus fresh fetch proves local/tracking/live `octoberBranch` equality and zero divergence; NEW 49/50 remain open for the authorized ownership/runtime cutover and deployed natural evidence.
