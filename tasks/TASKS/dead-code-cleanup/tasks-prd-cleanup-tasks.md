# Codebase Cleanup and Warning Reduction — Tasks

## Relevant Files

- `tasks/TASKS/dead-code-cleanup/prd-cleanup-tasks.md` - Source PRD, baseline warning goals, progress claims, and hook-audit examples.
- `web/package.json` - Authoritative lint/type/build/test commands and dependency versions.
- `web/components/common/OptimizedImage.tsx` and `OptimizedImage.test.tsx` - Enforced sized-or-fill API, keyed one-attempt fallback state, caller error composition, alt handling, and direct regressions.
- `web/lib/images.ts` - Canonical tracked local team-logo fallback.
- `web/components/HomePage/HomepageGamesSection.tsx` and its focused test - Migrated normal-game logo contract and partial-upstream-game crash boundary.
- `web/components/HomePage/HomepagePlayoffBracket.tsx`, module styles, and focused test - Playoff logo sizing, containment, optimization, sentinel, and failed-source contract.
- `web/components/HomePage/HomepageStandingsInjuriesSection.tsx` and its focused test - Migrated standings/injury logo source, size, loading, and fallback evidence.
- `web/components/TeamDashboard/TeamLeaders.tsx` and focused test - Intentional raw player-headshot source validation, intrinsic sizing, loading, and terminal fallback contract.
- `web/components/TeamDashboard/TeamDashboard.tsx` - Duplicate leaders-query/data-owner follow-up discovered during the TeamLeaders audit.
- `web/components/StatsPage/MobileTeamList.tsx` and `MobileTeamList.test.tsx` - Mobile stats-logo primary/fallback behavior without nonexistent paths.
- `web/pages/stats/index.tsx` and `web/__tests__/pages/stats/index.test.tsx` - Desktop stats-logo wrapper migration and failed/blank-logo evidence.
- `web/pages/index.tsx` - Image-migration pilot and current pre-existing homepage changes.
- `web/components/DraftDashboard/ProjectionsTable.tsx` - Hook-dependency audit candidate.
- `web/components/GameGrid/GameGrid.tsx` - Hook/image audit candidate.
- `web/components/GameGrid/Header.tsx` - Weekly Game Grid sortable column-header semantics and interaction ownership.
- `web/components/GameGrid/DateRangeTeamGrid.tsx` - Date-range metric sortable column-header semantics and interaction ownership.
- `web/components/GameGrid/TransposedGrid.tsx` - Contextual accessible naming for transposed day-exclusion switches.
- `web/components/GameGrid/DesktopMasterTable.tsx` - Stable accessible naming for the desktop master day-inclusion switches.
- `web/components/GameGrid/Toggle/Toggle.tsx` - Shared focusable-switch label and description ownership.
- `web/components/GameGrid/Switch/Switch.tsx` and `Switch.module.scss` - Native switch keyboard-repeat handling and the decorative sort-arrow direction contract.
- `web/components/GameGrid/SortableHeaders.test.tsx` - Focused native-button, column-header, sort-state, repeat-key, and day-switch regressions.
- `web/components/GameGrid/SwitchToggle.test.tsx` - Direct shared Switch/Toggle state, pointer, and keyboard regressions.
- `web/components/GameGrid/GameGrid.module.scss` and `web/components/GameGrid/DateRangeTeamGrid.module.scss` - Layout-preserving native sort-action presentation and forced-colors focus fallbacks.
- `web/pages/index.tsx`, `web/pages/stats/index.tsx`, and `web/pages/projections/index.tsx` - Original four-directive representative stale-suppression cohort.
- `web/pages/shiftChart.js` - Two active hook suppressions retained with concrete mount/playback state-write justifications.
- `web/components/TeamScheduleCalendar/TeamScheduleCalendar.tsx` - Complex hook-dependency candidate.
- `web/components/TeamDashboard/TeamDashboard.tsx` - Complex state/dependency candidate with pre-existing changes.
- `web/components/WiGO/` - Targeted hook warning candidates.
- `.gitignore` - Prevents dependency caches, Codex Next build outputs, Python bytecode, and legacy web-root Supabase CLI internal state from being staged again.
- `web/.gitignore` - Prevents generated Playwright `test-results` and `playwright-report` output from recurring in Git while CI keeps its separate ephemeral failure uploads.
- `supabase/.temp/cli-latest` and `supabase/.temp/linked-project.json` - Pre-existing Supabase CLI internal-state files removed from tracking while their physical ignored copies remain available to the local CLI.
- `supabase/supabase/.temp/*` and `web/supabase/.temp/*` - Eleven additional duplicated Supabase CLI internal-state files removed from tracking while every physical ignored copy remains available locally.
- `web/test-results/.last-run.json` and the three tracked `web/test-results/rankings-*/error-context.md` files - Stale generated Playwright outputs removed from tracking while their physical ignored copies remain available locally.
- `.tmp-next-dev.log`, `web/lib/supabase/sosStandings.log`, `web/tmp/team-underlying-audit*.json`, and `yahoo_historical.log` - Seven write-only/generated development, audit, and ingestion outputs removed from tracking while their ignored physical copies remain local.
- `tasks/TASKS/rules/process-task-list.mdc` - Governing explicit-path staging and coherent checkpoint-commit discipline added after the cache incident.
- `web/lib/supabase/Upserts/Yahoo/__pycache__/*.pyc` - Three pre-existing tracked Python bytecode artifacts removed forward-only while their source scripts remain intact.
- `web/lib/supabase/Upserts/supabaseShifts.js` - Legacy self-executing ingestion candidate whose value-bearing Supabase credential logs are contained under shared B-DRM NEW 22; external invocation/retirement remains evidence-gated.

### Notes

- This list repairs the missing task pair for the cleanup PRD.
- Re-measure the current baseline; the 2025 warning count and completed claims are not current evidence.
- Preserve the heavily dirty working tree and avoid cleanup edits overlapping active features until the relevant diff is understood.
- Do not add suppressions merely to reduce counts. Every suppression needs a local reason and evidence that dependencies are intentionally stable.
- Treat B-DRM NEW 22 as the single countable security root for the legacy shift script; this cleanup list records the shared ownership without duplicating its checkbox.
- Image migration is behavior/layout work, not a mechanical warning deletion; verify sizing, loading priority, remote domains, and CLS.

## Tasks

- [x] 1.0 Establish current warning/error baseline and ownership. Evidence: the declared, strict, and full-core paths plus ownership/dependency graph are recorded below; the selected semantic ARIA scope is verified without suppression (2026-07-22).
  - [x] 1.1 Discover the repository's actual lint/type/build commands and run the narrowest baseline that reports file/rule/count without auto-fix. Evidence: deprecated/conflicting `next lint`, missing ESLint-9 flat config, explicit legacy-config/no-fix fallback, direct TypeScript, Vitest, Prettier, and bundled-Node-24 Sass paths were verified (2026-07-22).
  - [x] 1.2 Record errors and warnings by rule/domain and separate pre-existing dirty-feature ownership from safe cleanup candidates. Evidence: the pre-change snapshot was 0/132 across 1,497 files; hook, image, ARIA, anonymous-export, strict-directive, toolchain, and dirty-feature owners are explicit under NEW 18–26 (2026-07-22).
  - [x] 1.3 Verify the PRD's completed ARIA, key/ref, OptimizedImage, and prior hook claims against current code/tests. Evidence: valid shared switch state, current key/ref cleanup, and resolved historical hook examples were distinguished from the still-open wrapper/fallback, stale-directive, and 64-warning hook debt instead of trusting the old PRD claims (2026-07-22).
  - [x] 1.4 Select a low-risk first-pass scope capable of at least a 25% warning reduction in that scope without behavior changes. Evidence: all five Game Grid ARIA-role warnings formed one semantic scope and are eliminated 5/5 (100%) with pointer/keyboard/state parity (2026-07-22).

- [x] 2.0 Fix correctness and accessibility warnings first. Evidence: native column-header/action ownership, switch naming, repeat-key protection, clipped/high-contrast focus, and render-safe state propagation pass focused, lint, type, Sass, and clean-browser gates (2026-07-22).
  - [x] 2.1 Fix invalid keys, refs, DOM attributes, roles, labels, and ARIA state using semantic behavior rather than suppression. Evidence: `aria-sort` remains on native headers; one labeled native button owns each sort; decorative indicators are hidden; day/preseason switches are named; zero ARIA-role warning remains (2026-07-22).
  - [x] 2.2 Preserve refs needed by cleanup functions through stable local captures and verify lifecycle behavior. Evidence: direct stats inspection confirms hover/leave timeout refs are copied/read during unmount cleanup and cleared, while the mobile scroll listener and locally owned `scrollTimeout` are both removed/cleared; no behavior rewrite or suppression was introduced (2026-07-22).
  - [x] 2.3 Add/update focused tests only for meaningful interaction or accessibility regressions. Evidence: two focused files pass six tests covering shared switches, header/date-range sort state, Enter/Space/pointer activation, repeat suppression, independent preseason control, and stable Header/Transposed/DesktopMasterTable day naming and state transitions (2026-07-22).

- [ ] 3.0 Resolve React hook dependency warnings safely
  - [ ] 3.1 Inspect each missing dependency for stale-closure correctness; include, move, memoize, or restructure it based on actual data flow.
  - [ ] 3.2 Remove unnecessary dependencies only when they are not read and removal cannot leave stale derived state.
  - [ ] 3.3 Stabilize helpers/objects at the correct ownership boundary instead of wrapping everything in `useCallback`/`useMemo` mechanically.
  - [ ] 3.4 For intentional omissions, add the narrowest single-line disable plus a concrete local justification and regression/direct verification evidence.
  - [ ] 3.5 Verify no render loop, duplicate request, stale state, or lost cleanup is introduced.

- [ ] 4.0 Verify and complete the image optimization pilot
  - [x] 4.1 Audit `OptimizedImage` API, fallback, dimensions/aspect ratio, alt behavior, priority/loading, error state, and Next image configuration. Evidence: the public union and runtime guard require either an explicit width/height pair or `fill`; ordinary image props and empty decorative alt text are preserved; primary/fallback changes reset a keyed one-attempt fallback state; caller handlers cannot bypass it; empty, self-referential, and failed fallbacks terminate without retry; the canonical default is a tracked local asset. Eleven direct wrapper tests, the complete five-file/20-test image/stats group, full TypeScript, and zero-finding scoped lint pass (verified 2026-07-22).
  - [x] 4.2 Verify already migrated homepage/standings/game/mobile logos against layout and network behavior. Evidence: incomplete normal-game rows are omitted before team metadata lookup; complete-game behavior remains covered; playoff team logos use contained 3:2 boxes, `HOME`/`AWAY` sentinels use the tracked fallback instead of invalid requests, and current bracket assets use source-aware intrinsic dimensions plus 420px/120px responsive `sizes` through the optimizer. Real-wrapper component tests cover source/size/loading/fallback/candidate behavior, while direct 390×844 and 1440×900 home/stats checks show zero broken images or horizontal overflow; the final homepage proof renders all 32 standings logos with 22×22 attributes, lazy loading, HTTPS sources, zero failures, and zero sentinel URLs. Four focused files pass 19 tests, full TypeScript passes, changed-scope lint has zero errors and only the identical pre-existing TeamDashboard hook warning, Prettier/Sass/diff integrity pass (verified 2026-07-22).
  - [ ] 4.3 Migrate only the approved pilot Game Grid/Team Leaders/homepage occurrences that have known dimensions and no incompatible external source.
  - [ ] 4.4 Document when raw `<img>` remains intentional and defer broad migration until pilot evidence supports it.

- [ ] 5.0 Reduce repetitive low-risk noise without speculative refactors
  - [ ] 5.1 Consolidate truly duplicate warning patterns into existing helpers when ownership and behavior are identical.
  - [ ] 5.2 Remove dev-only logs or gate them explicitly; do not hide operational error context needed in production.
  - [ ] 5.3 Tune ESLint rules only when the rule is systematically incompatible with repository architecture and targeted fixes cannot express intent.
  - [ ] 5.4 Avoid data-fetch, caching, bundle, or component architecture rewrites in this initiative.

- [ ] 6.0 Verify results and document the reusable pattern
  - [x] 6.1 Re-run scoped and repository lint/type checks and report exact pre/post errors and warnings by rule. Evidence: the changed/new TypeScript scope is 0 errors/0 warnings and full TypeScript passes. The baseline-compatible directory lint invocation is 1,499 files/0 errors/127 warnings (64 hooks, 59 raw images, four anonymous defaults, zero ARIA); the extension-complete JS/JSX/TS/TSX core is 1,500/0/129 (64 hooks, 61 raw images, four anonymous defaults, zero ARIA), with the sole delta `components/PoissonDistributionChart.jsx` and its two image warnings. Only five-to-zero ARIA is compared like-for-like with the 1,497-file directory-form pre-change snapshot (2026-07-22).
  - [ ] 6.2 Verify touched pages/components directly, including loading/error/responsive and image layout behavior.
  - [ ] 6.3 Confirm zero new errors and no warning-count improvement achieved solely by broad ignores.
  - [x] 6.4 Update the source PRD, this list, Relevant Files, and master ledger with evidence and remaining follow-ups. Evidence: all canonical controls agree on the corrected semantic-accessibility, stale-suppression, wrapper/stats-fallback, and bounded migrated-logo/TeamLeaders results. Base 4.2 plus NEW 25/46–49/51–54 are closed at 60/87 with 27 open; NEW 7/19–20/26/40–42/45/50/55 and the broader image/hook/noise/toolchain/integrated rows remain explicit (2026-07-22).

## NEW Tasks

- [ ] NEW 7.0 Append every verified warning cluster, unsafe suppression, behavior regression, ownership conflict, and broader follow-up discovered during execution here before closure.
- [x] NEW 8.0 **P1 Git-history dependency-cache contamination:** remove only accidental `.pnpm-store/**` and `.npm-cache/**` payloads from the four post-`3d6d5ca` `oh my` commits and the current index without losing any non-cache commit, staged, unstaged, or untracked goal work; rewrite the pushed branch only after explicit owner approval. Evidence: guarded remote rewrite, owner-run mixed reset, exact saved-work reconciliation, current/remote ref parity, and reachable-history checks all pass; the physical local caches remain present and ignored, never deleted (2026-07-15).
  - [x] NEW 8.1 Freeze the old local/remote commit IDs and value-free path/blob manifests that exclude both cache roots, then prove the rewrite boundary starts after `3d6d5ca` without treating that base as a reset target. Evidence: frozen base `3d6d5ca`, remote lease `357d69c78`, old local head `4d738d18f`, verified 330 MiB rescue bundle, base-applicable 2.2 MiB binary non-cache patch, staged/unstaged/untracked path lists, and two retained untracked snapshots; the refreshed 413-file archive has no AppleDouble members and round-trips with zero path/type/size/hash mismatch (2026-07-15).
  - [x] NEW 8.2 Perform the cache-only rewrite in an isolated clone, retain every non-cache tree entry from all four commits (including the first commit's real source work and the local fourth commit's six control artifacts), and add root ignore rules for both cache directories. Evidence: isolated candidate `d3746815c` contains three commits after the untouched base, zero cache paths, the preserved 181-path remote delta plus six-path local control delta, and only a final two-rule `.gitignore` commit; no shared ref, index, worktree, or remote was mutated (2026-07-15).
  - [x] NEW 8.3 Compare pre/post non-cache manifests and commit/path inventories before any remote mutation; fail closed on any unexplained non-cache difference. Evidence: remote head versus rewritten first commit and old local head versus rewritten second commit each have zero non-cache path differences; the final candidate differs only by `.gitignore`, descends from `3d6d5ca`, has zero reachable cache paths and zero blobs over 100 MiB, and its isolated worktree is clean. The original untracked snapshot remains retained while a refreshed `COPYFILE_DISABLE=1` archive and 413-record SHA-256 sidecar preserve the newer task-document state (2026-07-15).
  - [x] NEW 8.4 After explicit approval, update `octoberBranch` with `--force-with-lease` against the recorded remote head, never an unguarded force push, and verify the two cache roots are absent from rewritten reachable history. Evidence: owner supplied the exact authorization; three stable pre-push fingerprints, a 422-file untracked round-trip, exact working-tree reconstruction, and dry-run lease validation passed. GitHub accepted only `357d69c78 -> d3746815c` under the exact lease; a fresh bare clone resolves to the candidate with zero reachable/current cache paths and zero blobs over 100 MiB (2026-07-15).
  - [x] NEW 8.5 Reconcile the current worktree with the rewritten remote using an index-only/mixed operation that leaves working files untouched; prove all previously staged, unstaged, and untracked non-cache work remains present before optional local cache deletion. Evidence: the owner-run mixed reset succeeded; `HEAD`, tracking, and live remote agree at descendant checkpoint `92cfab267`, with zero staged/tracked/current/reachable cache paths and zero over-100-MiB blobs. All 426 saved untracked files are exact in `HEAD`, the saved 13-path tracked patch is fully present, newer Draft Ranker edits remain untouched, and a fresh private 5-tracked/1-untracked safeguard round-trips without mismatch (2026-07-15).
- [x] NEW 9.0 **P1 checkpoint-commit discipline:** require explicit-path staging and coherent verified checkpoint commits before switching initiatives or entering provider/production/migration/deployment/history-sensitive work; inspect unexpectedly large staged sets and exclude dependency caches, generated output, secrets, local metadata, and unrelated work. Evidence: governing `process-task-list.mdc` now defines the checkpoint cadence, large-stage stop condition, dirty-tree exception record, and recovery-manifest fallback (2026-07-15).
- [x] NEW 10.0 **P1 generated-build checkpoint contamination:** remediate the pushed `92cfab267` checkpoint's 148 `web/.next-codex-*` build files (219,880,152 blob bytes), one Python bytecode file, and newly added local Supabase project-ref file without changing any of its other 289 non-target paths or overwriting newer Draft Ranker work. Evidence: guarded option-A rewrite, fresh-clone integrity proof, owner-run local mixed reset, ref/index/tree/history/blob checks, and full safeguarded-path reconciliation all pass; local generated directories remain physically present but ignored and untracked. Exact eight-path checkpoint `5743b6d80` is fast-forward published and independently clone/fsck verified (2026-07-15).
  - [x] NEW 10.1 Prove the checkpoint descends from the cache-clean candidate, reconcile all saved work, inventory the generated-only path/byte boundary, and run a value-free credential scan before proposing remote action. Evidence: ancestry/preservation pass; all cache counts remain zero; high-confidence private-key/provider-token/credentialed-URL scans are clean, with only the expected public browser Supabase `anon` credential identified in compiled output. Exact equality scanning of 24 unique sensitive values from all 12 local env files across all 24 compressed webpack packs (642,337,912 decompressed bytes) completed with zero failures and zero matches (2026-07-15).
  - [x] NEW 10.2 Add narrow prevention rules for `.next-*`, `__pycache__/`, `*.py[cod]`, and `/supabase/.temp/project-ref`, then verify each currently published generated path class matches an ignore rule. Evidence: `git check-ignore --no-index -v` resolves all four classes to the new root rules and `.gitignore` passes `git diff --check` (2026-07-15).
  - [x] NEW 10.3 After explicit owner choice, either perform the recommended isolated, exact-lease history rewrite that removes the 220-MB generated payload from reachable history or make a forward-only cleanup commit that leaves those blobs in history; re-verify every non-target path, current worktree safeguard, remote ref, scoped generated/cache absence, and blob-size boundary before resuming production work. Owner-authorized option A moved only `92cfab267 -> 0ed825fb3`. Fresh-clone and post-reset proof agree: local/tracking/live refs are `0ed825fb3`, staged/scoped/cache counts are zero, maximum reachable blob is 13,264,504 bytes, no blob exceeds 100 MiB, all 62 tracked and 49 untracked safeguarded paths are present as exact content, intentional deletion, or verified newer forward edits, and the 148 physical build files remain ignored/untracked (2026-07-15).
- [x] NEW 11.0 **P2 pre-existing Python bytecode debt:** forward-delete the three older tracked `__pycache__/*.pyc` files already present in parent `d3746815c`, verify their source scripts regenerate bytecode only locally under the new ignore rule, and do not broaden the current single-tip P1 rewrite into older history without a separate explicit approval. Evidence: only the three Yahoo `__pycache__` files are deleted, all three source scripts remain present and compile successfully with bytecode redirected to a disposable local prefix, every target resolves to the `__pycache__/` ignore rule, scoped diff integrity passes, older history is unchanged, and checkpoint `5743b6d80` publishes exactly these three deletions plus five reviewed control files (2026-07-15).
- [x] NEW 12.0 **P1 divergent-branch and dirty-overlay reconciliation:** preserve every local and remote non-transient change, reject stale regressions, merge without rewrite, verify the candidate, publish through a normal push, and synchronize the shared checkout without losing ignored/local work. Evidence: isolated merge `803f30291` retains the local DRM and 52-commit remote histories; 135 focused tests, TypeScript, formatting, diff integrity, compile, ancestry, and fsck pass; normal publication reaches `8ad7329e9`. The owner-run mixed reset plus exact tracked/untracked provenance audit established a clean post-reconciliation baseline at `8ad7329e9` with zero ahead/behind, empty index/worktree before the resulting control-only diff, three supported active migrations, 34 byte-identical archived historical migrations, `pull.ff=only`, and the retained recovery stash (verified 2026-07-19).
- [x] NEW 13.0 **P2 pre-existing Supabase CLI internal-state tracking:** remove `supabase/.temp/cli-latest` and `supabase/.temp/linked-project.json` from Git tracking without deleting the physical local files or rewriting history. Evidence: current official Supabase local-development guidance classifies `.temp/` as CLI internal state that must not be committed; both files were already covered by `supabase/.gitignore`, their value-free shapes are local version/link metadata, Git records only those two forward deletions, and both physical ignored files remain present for the local CLI (2026-07-21).
- [x] NEW 14.0 **P2 stale Playwright generated-artifact tracking/publication reconciliation:** remove the four canonical `web/test-results` artifacts from Git tracking without deleting their physical local evidence, changing CI failure-artifact uploads, or rewriting history. Evidence: the exact `.last-run.json` plus three rankings `error-context.md` files are forward-deleted from the index, all four physical copies remain present and resolve to `web/.gitignore`, no non-test consumer references their exact paths, and `git ls-files web/test-results` is empty (2026-07-21).
- [x] NEW 15.0 **P2 remaining Supabase CLI internal-state tracking:** remove the nine duplicated `supabase/supabase/.temp` files and two `web/supabase/.temp` files from Git tracking without deleting physical local state or rewriting history, and prevent the previously unignored web-root variant from recurring. Evidence: official guidance classifies every `.temp/` path as non-committable CLI state; all eleven files entered through generic bulk commits, total only 475 bytes, have no runtime/build/tool consumer, and duplicate metadata blobs across roots. Exact index-only removal leaves zero matching tracked paths, all eleven physical copies remain present, and all eleven resolve to repository ignore rules (2026-07-21).
- [x] NEW 16.0 **P2 tracked generated log/audit outputs:** remove the one local dev-server log, one legacy SOS writer log, four write-only team-underlying audit JSON outputs, and one historical Yahoo ingestion log from Git tracking while preserving their physical local copies and source producers. Evidence: the seven files total 1,323,007 bytes, each entered in one generic historical commit, none is a runtime/build/test input, exact narrow ignore rules cover every path, index-only removal leaves zero matching tracked paths, and all seven physical copies remain present and ignored (2026-07-21).
- [x] NEW 17.0 **P1 Yahoo historical-log sensitivity and Git-history disposition:** determine value-free whether the retained historical blob contains an active or high-confidence credential before deciding whether a risky history rewrite is justified. Evidence: bounded scans report zero private keys, JWTs, GitHub/AWS tokens, userinfo URLs, bearer values, sensitive assignments/query parameters, or email-like strings; none of 20 current local secret values across eight ignored environment files occurs in the blob. The 1,454 URL-shaped strings contain no sensitive query key, no code reads the log, and its single old blob is only 908,233 bytes, so forward untracking plus ignore is sufficient and no history rewrite/credential action is justified by current evidence (2026-07-21).
- [x] NEW 18.0 **P2 invalid sortable-column role semantics:** all five invalid role/`aria-sort` warnings are eliminated without suppression; native column headers retain `aria-sort`, labeled native buttons own sorting, and the final full-core run contains zero `jsx-a11y/role-supports-aria-props` warning (verified 2026-07-22).
- [ ] NEW 19.0 **P2 non-durable lint entrypoint:** the declared `next lint` command is deprecated and fails in the nested isolated checkout because both inner and enclosing legacy configs register the Next plugin; direct ESLint 9 also fails without legacy mode because no flat config exists. Preserve rule parity while moving to one reproducible repository-owned ESLint CLI/config entrypoint, and prove normal-root plus isolated-checkout execution before retiring the value-free legacy workaround (discovered 2026-07-21).
- [ ] NEW 20.0 **P1 dependency/toolchain authority drift:** npm and pnpm lockfiles resolve different Next/ESLint/TypeScript-ESLint versions, no `packageManager` establishes authority, `.nvmrc` Node 22 differs from Rankings CI Node 20/npm, `next` floats while `eslint-config-next` is pinned older, and `pnpm-workspace.yaml` retains placeholder build approvals. Select one supported install/runtime contract through an explicit strategy checkpoint, align CI/local versions and lock authority without losing dependency work, then prove clean install, lint, type, test, and build reproducibility (discovered 2026-07-21).
- [x] NEW 21.0 **P2 stale and unjustified lint suppressions:** removed exactly four unused directives from homepage/stats/projections without runtime edits; retained the two active `shiftChart` exhaustive-deps suppressions with concrete mount-only/state-writing justifications. The four-page strict rerun reports zero errors/unused directives and six unrelated warnings; one homepage regression, full TypeScript, JS syntax, and diff integrity pass. NEW 40–42 retain the expanded lint-policy/config/formatting findings rather than overclaiming global cleanup (verified 2026-07-22).
- [x] NEW 22.0 **P1 OptimizedImage fallback/API contract defect:** the wrapper now exposes a statically and dynamically enforced width/height-versus-fill union, forwards ordinary props without surrendering managed source/error behavior, preserves explicit empty alt text, resets state when either source changes, and performs at most one deterministic fallback attempt before a terminal state while notifying the caller after internal state ownership. Eleven direct regressions plus the five-file/20-test group, full TypeScript, zero-finding scoped lint, targeted formatting, diff integrity, and independent React/final review pass (verified 2026-07-22).
- [x] NEW 23.0 **P2 nonexistent stats-logo fallback:** mobile and desktop stats logo paths now share the tracked `/teamLogos/FHFH.png` fallback through `OptimizedImage`; blank abbreviations start at that asset and failed primaries transition to it without constructing `/teamLogos/default.png`. Two focused stats regressions are included in the five-file/20-test group; full TypeScript and scoped lint pass (verified 2026-07-22).
- [x] NEW 24.0 **P2 stale cleanup PRD evidence:** the PRD now distinguishes historical warning snapshots from the 1,497-file/132-warning pre-change directory baseline, the current baseline-compatible 1,499-file/127-warning rerun, and the extension-complete 1,500-file/129-warning core audit; it removes duplicate/obsolete next-step claims, records wrapper/hook gaps as open, and reports only verified ARIA/switch/ref outcomes (verified 2026-07-22; exact denominators reconciled under NEW 44).
- [x] NEW 25.0 **P2 TeamLeaders image-pilot source-contract gap:** the intentionally raw leader headshot now accepts only safe root-relative paths or HTTPS sources from three explicit NHL hosts, uses 80×80 intrinsic/container sizing plus lazy/async/no-referrer behavior, and owns a deduplicated trusted-source → official-CMS → tracked-local-placeholder → terminal-removal sequence. Direct allowlist, invalid-source, attribute, fallback, loading/error/order, and lifecycle regressions are included in the four-file/19-test group; full TypeScript and scoped lint pass (verified 2026-07-22).
- [ ] NEW 26.0 **P1 current hook-dependency warning debt:** the current core baseline has 64 `react-hooks/exhaustive-deps` warnings, with the representative historical cohort still carrying 15 warnings across Draft Dashboard, shift chart, Game Grid, Team Dashboard, Team Schedule Calendar, and projections. Process each by actual data flow with focused stale-state/render/request/cleanup evidence; do not bulk-add dependencies or suppressions (discovered 2026-07-21).
- [x] NEW 27.0 **P3 Switch/Toggle direct-regression gap:** direct tests prove both shared controls expose `role="switch"`/`aria-checked` and support pointer, Enter, and Space activation without `aria-pressed` misuse (verified 2026-07-22).
- [x] NEW 28.0 **P2 sortable-header action semantics:** each sortable native header now owns `aria-sort` while one labeled native button owns activation; direction indicators are decorative, preseason remains independent, and component plus browser pointer/Enter/Space evidence passes (verified 2026-07-22).
- [x] NEW 29.0 **P2 clipped sortable-action focus evidence:** both sort-button families use inset focus treatment; real-browser screenshots of the first Team and last Score controls inside the actual overflow-clipped schedule table show an intact visible ring (verified 2026-07-22).
- [x] NEW 30.0 **P2 unnamed preseason switch:** Toggle forwards label/description to its focusable switch, and the preseason control is uniquely exposed as `Hide preseason games` independently of Team sorting (verified 2026-07-22).
- [x] NEW 31.0 **P3 native keyboard-evidence gap:** a clean isolated browser tab proves Team Enter → ascending, Space → descending, pointer → ascending, and Week Score Space → descending with unique native controls and zero error logs after remediation (verified 2026-07-22).
- [x] NEW 32.0 **P2 held-key repeat activation:** supported keydowns prevent native default before repeat rejection; focused tests assert repeated Enter/Space are default-prevented and cause no additional state/callback transition, while clean-browser non-repeat activation remains exact (verified 2026-07-22).
- [x] NEW 33.0 **P2 forced-colors focus fallback:** both sort-button families retain the ordinary inset ring and compile an inset `CanvasText` outline with box shadow disabled under `forced-colors: active`; bundled Node 24 Sass compilation and exact CSS inspection pass (verified 2026-07-22).
- [x] NEW 34.0 **P2 unnamed day-exclusion switches:** Header and TransposedGrid switches expose contextual day-specific names on their focusable control; NEW 36 subsequently corrected those names to the stable `Include … games` on-state contract while preserving this naming closure (verified 2026-07-22).
- [x] NEW 35.0 **P2 render-phase parent sort update:** Header computes the next sort state before updating local and parent state, preserving default/toggle direction behavior; the final fresh browser interaction sequence logs zero errors and no cross-component render warning (verified 2026-07-22).
- [x] NEW 36.0 **P1 inverted day-switch announcement:** Header and TransposedGrid use the stable `Include … games` on-state name and `aria-checked=true` only when included; focused tests prove included/excluded updater transitions without changing filtering behavior (verified 2026-07-22).
- [x] NEW 37.0 **P2 sort-arrow/ARIA direction contradiction:** the decorative indicator now maps ascending to the existing up-arrow class and descending to down, uses each column's default while inactive, and passes direct data/class assertions in both active directions (verified 2026-07-22).
- [x] NEW 38.0 **P2 shared-switch held-key repeat activation:** both reusable controls prevent native default before rejecting repeated Enter/Space keydowns; focused tests prove repeats are canceled with no extra callback while pointer and ordinary keyboard activation remain exact (verified 2026-07-22).
- [x] NEW 39.0 **P1 DesktopMasterTable day-switch name/state contradiction:** the third shared Toggle caller now uses the same stable `Include … games` on-state name as Header/TransposedGrid; a direct regression proves checked included state, the exclusion updater, rerendered unchecked state, and an unchanged accessible name (verified 2026-07-22).
- [ ] NEW 40.0 **P2 core unused-suppression expansion:** strict `pages`/`components`/`lib` lint reports 41 unused directives rather than the four-item representative NEW 21 cohort. After removing those original four, inventory and disposition the remaining 37 rule-policy-dependent directives (30 `no-console`, six `no-alert`, one `no-unused-expressions`) without bulk deletion or silently changing the lint contract; coordinate any rule-policy decision with NEW 19/20 (discovered 2026-07-22).
- [ ] NEW 41.0 **P2 out-of-core hook suppression/config drift:** `hooks/useVORPCalculations.ts` has two additional unused `no-console` directives plus four directives referencing unregistered TypeScript-ESLint rules. Decide the intended hook lint/rule contract under NEW 19/20, then remove or replace only behavior-neutral directives with direct type/lint evidence (discovered 2026-07-22).
- [ ] NEW 42.0 **P3 legacy page formatting mismatch:** direct Prettier checks already fail on the three representative NEW 21 pages. Preserve the directive-only diff now; scope any whole-file normalization separately with reviewed churn and behavior evidence rather than mixing it into suppression cleanup (discovered 2026-07-22).
- [x] NEW 43.0 **P3 stale publication-label evidence:** canonical controls label checkpoint `36b3c38bf1db495b411b64fdf5dc8545ca9b434a` as historical scheduler/generated-state evidence and now identify exact published NEW 21 checkpoint `11c51736c764b3df7b2d0da7920c93c0f2a91337` as the latest B-CLEAN publication (verified 2026-07-22).
- [x] NEW 44.0 **P2 full-core lint evidence-count drift:** every current control now records both reproducible legacy-config/no-auto-fix denominators: the baseline-compatible directory invocation is 1,499 files/0 errors/127 warnings (64 hooks, 59 raw images, four anonymous defaults, zero ARIA), while explicit `--ext .js,.jsx,.ts,.tsx` is the extension-complete core at 1,500/0/129 (64 hooks, 61 raw images, four anonymous defaults, zero ARIA). The exact delta is the sole core `.jsx` file, `components/PoissonDistributionChart.jsx`, with two image warnings; only the stable five-to-zero ARIA result is compared like-for-like (verified 2026-07-22).
- [ ] NEW 45.0 **P2 residual nonexistent team-logo fallback debt:** audit and remediate the remaining `/teamLogos/default.png` literals and dynamic `"default"` team-logo paths across TopMovers, the quarantined Forge TopMoversCard, Trends/placeholder, TeamStandingsChart, PlayerPickupTable, and OpponentMetricsTable. Classify active versus zero-consumer ownership, use a proven local fallback only where behavior is known, add focused evidence, and do not fold TeamLeaders NEW 25 or broad image migration into this task (discovered 2026-07-22).
- [x] NEW 46.0 **P1 normal-game partial-data crash:** `HomepageGamesSection` now rejects incomplete home/away abbreviations before either metadata lookup, and a focused regression proves the partial row is omitted while the complete card still renders (verified 2026-07-22).
- [x] NEW 47.0 **P2 playoff-logo layout/optimization contract:** playoff team logos now use contained 3:2 boxes; missing-team sentinels render the tracked fallback without `HOME`/`AWAY` requests; current bracket, conference-final, and Cup-final assets use their measured intrinsic dimensions; and the proven current NHL-host assets no longer force `unoptimized`. Direct component and network/layout evidence passes (verified 2026-07-22).
- [x] NEW 48.0 **P2 migrated-logo verification gap:** homepage game/standings tests now exercise the actual `OptimizedImage` wrapper with a minimal `next/image` DOM boundary, and new playoff regressions prove dimensions, responsive candidates, optimizer eligibility, sentinels, and fallbacks. The four-file/19-test group plus direct 390×844/1440×900 checks prove zero broken images/overflow; the final homepage run shows 32/32 standings logos, zero sentinel URLs, and zero failures (verified 2026-07-22).
- [x] NEW 49.0 **P2 TeamDashboard duplicate leaders query/data ownership:** the unread parent leader interfaces/state/WGO query and duplicate invalid SCSS declarations are removed; the rendered `TeamLeaders` child remains the sole fetch/render owner. Loading/error/empty/category-order coverage passes, and a semantic-normalized diff plus independent review finds no other TeamDashboard behavior change (verified 2026-07-22).
- [ ] NEW 50.0 **P2 residual StatsPage player-headshot contract:** `LeaderboardCategory`, `BSH`, and `Goalie` repeat the unconstrained DB/CMS raw-image fallback with no intrinsic dimensions, deterministic local fallback, or focused tests. Audit and repair that shared cohort separately from TeamLeaders NEW 25 and broad migration (discovered 2026-07-22).
- [x] NEW 51.0 **P2 TeamLeaders unresolved-input infinite loading:** missing team/season input now terminates loading with a stable explicit selection error and performs no database query; a direct regression proves the spinner cannot persist (verified 2026-07-22).
- [x] NEW 52.0 **P2 TeamLeaders stale async result ownership:** an effect-local request owner now blocks older or unmounted team/season success, failure, and finalization from overwriting current state; focused deferred-promise tests prove stale success and stale failure are both ignored (verified 2026-07-22).
- [x] NEW 53.0 **P3 TeamLeaders image-metadata failure observability:** optional player-image query failures retain leader rendering through the deterministic CMS/local sequence and emit one fixed value-free diagnostic without upstream details; direct partial-success evidence passes (verified 2026-07-22).
- [x] NEW 54.0 **P2 optimized playoff-asset responsive-candidate gap:** the optimized bracket and conference/Cup assets now declare their 420px responsive and 120px capped display widths through `sizes`. Direct DOM assertions plus the public `getImageProps` contract prove the sized bracket gains a 640px candidate and the series art a 128px candidate that their unsized native-width forms omit; the final four-file/19-test group passes (verified 2026-07-22).
- [ ] NEW 55.0 **P2 TeamDashboard unresolved-season infinite loading:** the parent dashboard initializes loading but starts/finalizes its data effect only when team and effective-season inputs are truthy; an unavailable current-season result therefore leaves the whole dashboard spinner permanent. Define a loading-versus-terminal season status boundary and add direct parent-level evidence without conflating it with the now-fixed TeamLeaders child state (discovered 2026-07-22).

Publication evidence: exact checkpoint `36b3c38bf1db495b411b64fdf5dc8545ca9b434a` contains the reviewed NEW 13–17 forward cleanup and prevention rules; a normal push plus fresh fetch proves local/tracking/live `octoberBranch` equality and zero divergence. Every one of the 24 physical local artifacts remains present and ignored.

Latest publication evidence: exact 17-path checkpoint `a1948f78baa16797a29ae98bf0c7796782efc2db` contains the reviewed base 4.2 plus NEW 25/46–49/51–54 implementation, two new test files, and synchronized controls. The four-file/19-test, TypeScript, scoped lint, Prettier, Sass, browser/network, React/independent review, exact-path/generated/cache/binary/mode/size/value-free, parity, and stable-ID gates passed; a normal non-force push plus fresh fetch proves local/tracking/live equality and zero divergence. It supersedes exact wrapper/stats checkpoint `d5cc06cf57c26ac59f5ff9b5db1de8bf1e67f313` as the latest B-CLEAN publication.
