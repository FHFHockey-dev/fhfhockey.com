## Relevant Files

- `.gitignore` - Prevents the removed Yahoo OAuth token export from being tracked again.
- `web/lib/supabase/Upserts/yahooAuth/token.json` - Deleted tracked Yahoo credential/token artifact; external credentials and tokens were confirmed rotated/re-authorized on 2026-07-11.
- `migrations/20260326_create_auth_user_settings_platform.sql` - Creates the MVP user tables, deferred-provider tables, indexes, constraints, triggers, and RLS policies described in the PRD.
- `migrations/20260327_encrypt_connected_account_tokens_with_vault.sql` - Moves provider token material out of plaintext columns and into Supabase Vault-backed secret references.
- `supabase/migrations/20260713055508_auth_user_settings_platform_baseline.sql` - Canonical Supabase migration copy of the production auth/settings baseline.
- `supabase/migrations/20260713055537_encrypt_connected_account_tokens_with_vault.sql` - Canonical Supabase migration copy of the Vault token-storage baseline.
- `supabase/migrations/20260713055538_expose_connected_account_token_rpc.sql` - Canonical Supabase migration copy of the service-role token-upsert wrapper.
- `supabase/migrations/20260713060931_harden_auth_provider_ownership_and_refresh_tokens.sql` - Applied idempotent ownership-constraint, covering-index, and service-role token-read hardening migration, ordered after its Vault/wrapper prerequisites.
- `supabase/migrations/20260713080449_optimize_auth_rls_initplans.sql` - Applied semantics-preserving optimization for all 36 auth-table owner RLS policies.
- `web/lib/supabase/database-generated.types.ts` - Generated Supabase types that must be updated after the schema migration so app code can use the new tables safely.
- `web/lib/supabase/client.ts` - Browser Supabase client used by auth UI flows and client-side account/settings requests.
- `web/lib/supabase/index.ts` - Existing shared Supabase wrapper that may need consolidation or cleanup for global auth/session handling.
- `web/lib/supabase/public-client.ts` - Public client used in read-only contexts; should remain safe and not gain authenticated user-only responsibilities.
- `web/lib/supabase/server.ts` - Server-side Supabase client wrapper that will need clear separation between service-role and user-session usage.
- `web/lib/supabase/server.test.ts` - Verifies privileged client creation is lazy, stable, and cannot fall back to a public key.
- `web/lib/integrations/yahoo/discovery.ts` - Persists per-user Yahoo discovery data and selects the current NHL game from the canonical `yahoo_game_keys` contract with a safe user-payload fallback.
- `web/lib/integrations/yahoo/discovery.test.ts` - Covers unordered user-game selection against the canonical Yahoo season plus league/team payload normalization.
- `web/lib/integrations/yahoo/refresh.ts` - Implements audited manual Yahoo refresh cooldown, in-flight dedupe, stale-run recovery, Vault token loading, and sync finalization.
- `web/lib/integrations/yahoo/refresh.test.ts` - Covers refresh blocking, cooldown, stale-run recovery, successful audit finalization, and failure-state persistence.
- `web/lib/integrations/fantrax/config.ts` - Defines the credential-free Fantrax manual-import provider key and bounded payload limits.
- `web/lib/integrations/fantrax/manualImport.ts` - Normalizes CSV/JSON and provides the shared parameterized owner-scoped league/team/default/active-context persistence, pagination, dedupe, cooldown, and failure mechanics used by manual-import providers.
- `web/lib/integrations/fantrax/manualImport.test.ts` - Covers multi-league JSON, grouped CSV rosters, validation, in-flight dedupe, cooldown timing, and stale-run recovery.
- `web/lib/integrations/espn/config.ts` - Defines the credential-free ESPN provider key.
- `web/lib/integrations/espn/manualImport.ts` - Applies ESPN-specific parsing/messages and provider configuration to the shared manual-import mechanics without credentials, cookies, scraping, or private endpoints.
- `web/lib/integrations/espn/manualImport.test.ts` - Covers ESPN multi-league JSON/CSV normalization, provider-specific blocking, and owner/provider-qualified active-context writes.
- `web/lib/integrations/patreon/config.ts`, `oauth.ts`, `sync.ts`, and focused tests - Define the least-scope Patreon API v2 contract, signed state, configured-campaign identity/member/tier normalization, stable-ID anti-sharing, Vault-backed rotating tokens, generic entitlement materialization, guarded sync state, and disconnect cleanup.
- `web/pages/api/v1/account/yahoo/refresh.ts` - Authenticated manual Yahoo refresh API route with method enforcement and `Retry-After` behavior.
- `web/__tests__/pages/api/v1/account/yahoo/refresh.test.ts` - Route coverage for authentication, method handling, success, conflict, cooldown, and unexpected errors.
- `web/pages/api/v1/account/fantrax/import.ts` - Authenticated GET/import/default/active-context API for the manual Fantrax foundation.
- `web/__tests__/pages/api/v1/account/fantrax/import.test.ts` - Route coverage for owner state, imports, default/active selection, cooldowns, and method enforcement.
- `web/pages/api/v1/account/espn/import.ts` - Authenticated ESPN state/import/default/active-context API with method and `Retry-After` behavior.
- `web/__tests__/pages/api/v1/account/espn/import.test.ts` - Route coverage for ESPN authentication, import, context selection, cooldown timing, and method enforcement.
- `web/pages/api/v1/account/patreon/*` and `web/__tests__/pages/api/v1/account/patreon/routes.test.ts` - Authenticated Patreon state/connect/resync/disconnect APIs plus signed callback materialization and route/error/cooldown coverage.
- `web/lib/cron/withCronJobAudit.test.ts` - Tests for audit-log persistence and timing capture in cron/API wrappers that now use the explicit service-role client.
- `web/__tests__/pages/api/v1/db/update-wgo-averages.route.test.ts` - Route test covering structured dependency-error behavior after the API moved to the explicit service-role client.
- `web/contexts/AuthProviderContext/index.tsx` - Global auth state provider with session-first user publication, optional role enrichment, centralized fail-closed sign-out, and targeted local FHFH auth reset.
- `web/pages/_app.tsx` - App root where auth-aware provider wiring must become global so the header can react to session state on every route.
- `web/components/Layout/Header/Header.tsx` - Header entry point for the logged-out `Sign-in / Sign-up` button and logged-in avatar menu.
- `web/components/Layout/Header/Header.module.scss` - Header styling updates for the new auth CTA and avatar/menu affordances.
- `web/__tests__/components/Layout/Header.test.tsx` - Component tests for logged-out header CTA rendering, modal launch, and logged-in header state handoff.
- `web/components/auth/AuthModal.tsx` - Modal container for sign-in, sign-up, forgot-password, and verification guidance flows.
- `web/components/auth/AuthModal.module.scss` - Styles for the header-triggered auth modal shell.
- `web/components/auth/AuthModal.test.tsx` - Component tests for auth modal rendering, mode switching, and main interaction states.
- `web/components/auth/AuthForm.tsx` - Form logic for Google/email auth and recovery, including credential-free same-origin return-path construction.
- `web/components/auth/AuthForm.module.scss` - Styles for the in-modal Google and email/password auth form states.
- `web/__tests__/components/auth/AuthForm.test.tsx` - Component tests for auth branches plus OAuth/sign-up/recovery return-path containment.
- `web/components/auth/UserMenu.module.scss` - Styles for the logged-in avatar trigger and account tray/menu.
- `web/components/auth/UserMenu.tsx` - Logged-in avatar trigger and tray/menu for sign out, account settings, and league settings entry points.
- `web/__tests__/components/auth/UserMenu.test.tsx` - Component tests for avatar menu actions and authenticated header states.
- `web/pages/auth/index.tsx` - Existing auth page that should be reduced to a compatible fallback or redirect-oriented surface after the modal flow is introduced.
- `web/pages/auth/callback.tsx` - OAuth and verification callback handler with a safe title, synchronous URL/history scrub, sanitized returns, and fail-closed navigation.
- `web/pages/auth/Callback.module.scss` - Styles for the callback status page that handles OAuth and email-link completion states.
- `web/__tests__/pages/auth/callback.test.tsx` - Route tests for callback exchanges, one-time credential consumption, safe title/history state, generic errors, and fail-closed behavior.
- `web/pages/auth/reset-password.tsx` - Password reset page with safe callback consumption, exactly-once recovery processing, and password update UX.
- `web/pages/auth/ResetPassword.module.scss` - Styles for the dedicated reset-password recovery page.
- `web/__tests__/pages/auth/reset-password.test.tsx` - Tests for reset token containment, Strict Mode exactly-once handling, fail-closed behavior, validation, and successful reset.
- `web/lib/supabase/auth-callback-location.ts` - Structured callback capture, synchronous visible/Next-history scrub, safe return-path sanitizer, and full-navigation fallback.
- `web/lib/supabase/auth-callback-location.test.ts` - Focused callback-location, stale-history, unsafe-return, and navigation-fallback regressions.
- `tasks/TASKS/auth-user-settings-platform/docs/auth-provider-manual-config.md` - Manual Supabase and Google Cloud configuration checklist for the implemented auth flows.
- `web/pages/account/index.tsx` - Account settings route entry point for authenticated users.
- `web/__tests__/pages/account/index.test.tsx` - Smoke tests for the authenticated account route shell and unauthenticated fallback prompt.
- `web/components/account/AccountSettingsPage.tsx` - Main account settings UI for profile, league defaults, saved teams, Yahoo/manual-import provider controls, and the production-verified Patreon connection panel.
- `web/components/account/AccountSettingsPage.module.scss` - Styles for the authenticated account-settings shell and section navigation.
- `web/__tests__/components/account/AccountSettingsPage.test.tsx` - Component tests for account settings sections, profile editing, empty states, and guarded actions.
- `web/components/account/FantraxImportPanel.tsx` - Shared configurable credential-free CSV/JSON import panel with stored league/team status, default/active selection, and retry/failure UX.
- `web/components/account/FantraxImportPanel.module.scss` - Responsive Neon Noir styles shared by the manual-import provider panels.
- `web/components/account/FantraxImportPanel.test.tsx` - Fantrax component coverage for load/import/default/active and failure-state behavior.
- `web/components/account/EspnImportPanel.tsx` - ESPN-specific configuration wrapper for the shared manual-import panel.
- `web/components/account/EspnImportPanel.test.tsx` - ESPN UI coverage for load/import/default/active switching and failure-state preservation.
- `web/components/account/PatreonConnectionPanel.tsx` and `.test.tsx` - Account-settings-only Patreon connection, generic eligibility/tier/sync status, guarded actions, and loading/error/unconfigured regressions.
- `web/lib/user-settings/defaults.ts` - Central default values for profile/settings rows seeded from the existing fantasy config system.
- `web/lib/user-settings/defaults.test.ts` - Unit tests for default settings generation and schema-safe fallback values.
- `web/lib/user-settings/ensureUserRecords.ts` - Auth-time helper that creates or backfills missing `user_profiles` and `user_settings` rows for newly signed-in users.
- `web/lib/user-settings/mappers.ts` - Mapping helpers between persisted user settings and the existing fantasy config shapes already used in the app.
- `web/lib/user-settings/mappers.test.ts` - Unit tests for mapping between database rows and app-facing settings objects.
- `web/lib/projectionsConfig/fantasyPointsConfig.ts` - Existing reference for default fantasy scoring that should inform seeded account settings without changing Draft Dashboard behavior.
- `web/pages/api/` - Existing API route area that may need a small authenticated settings endpoint layer if direct client-table access is not sufficient for some account operations.
- `tasks/TASKS/rules/fhfh-styles.md` - Local FHFH design-system rules for applying the Neon Noir / Cyberpunk visual language to auth and account surfaces.
- `tasks/TASKS/auth-user-settings-platform/prd/prd-auth-user-settings-platform.md` - Source PRD this task list implements.
- `tasks/TASKS/auth-user-settings-platform/tasks-prd-auth-user-settings-platform.md` - Canonical implementation task list for the feature.

### Notes

- Unit tests should typically be placed alongside the code files they are testing.
- This repo currently uses Vitest rather than Jest, so run targeted tests with `npx vitest run [optional/path/to/test/file]`.
- Regenerate `web/lib/supabase/database-generated.types.ts` after the migration is applied.
- Do not modify `web/components/DraftDashboard/DraftDashboard.tsx` in this feature pass.
- Do not modify `web/pages/api/v1/db/manual-refresh-yahoo-token.ts` in this feature pass.

## Tasks

- [x] 1.0 Build the Supabase-backed user data foundation for profiles, settings, saved teams, and future connected-account support
  - [x] 1.1 Create a migration for `user_profiles`, `user_settings`, and `user_saved_teams` with UUID-based ownership keyed to `auth.users.id`.
  - [x] 1.2 Add the deferred-design tables needed for future provider support, including `connected_accounts`, provider preference records, external league/team metadata tables, sync-run tracking, and Patreon/site entitlement placeholders.
  - [x] 1.3 Add indexes, uniqueness constraints, and ownership constraints needed for default-team selection, provider identity uniqueness, and future Patreon anti-sharing enforcement.
  - [x] 1.4 Add RLS policies so authenticated users can only read and mutate their own profile, settings, saved teams, and future provider-owned rows.
  - [x] 1.5 Ensure provider token storage is modeled as service-role-only or otherwise not client-readable, even if full provider token handling is deferred.
  - [x] 1.6 Define sensible database defaults for fantasy scoring, league type, roster config, UI preferences, and active-context placeholders using the existing fantasy config references.
  - [x] 1.7 Apply the migration in the appropriate environment and regenerate `web/lib/supabase/database-generated.types.ts`.

- [x] 2.0 Refactor app-wide auth state and Supabase client handling so authenticated session state is available globally and safely
  - [x] 2.1 Audit the current Supabase wrappers in `web/lib/supabase/` and choose a clear responsibility split for browser auth, public read-only access, and service-role server access.
  - [x] 2.2 Expand `AuthProviderContext` to expose a richer authenticated user shape, including ID, email, display name, avatar URL, verification-aware metadata, and admin role where available.
  - [x] 2.3 Update `web/pages/_app.tsx` so auth-aware context is available globally rather than only on `/auth` and `/db` routes.
  - [x] 2.4 Ensure auth state initializes correctly on first page load and responds to Supabase auth state changes without flicker or route-specific assumptions.
  - [x] 2.5 Add helper logic for creating or reconciling app-owned profile/settings rows after successful authentication if they do not already exist.
  - [x] 2.6 Verify that existing admin-only flows depending on the current `public.users` role table still work after the auth refactor.

- [x] 3.0 Implement the header authentication entry flow with a logged-out sign-in/sign-up button and a logged-in avatar menu
  - [x] 3.1 Update `Header.tsx` to render a logged-out `Sign-in / Sign-up` CTA to the right of `.bmcWrap`.
  - [x] 3.2 Add the logged-in avatar trigger in the same header area, using a safe avatar fallback strategy when no provider image exists.
  - [x] 3.3 Build the logged-in tray or menu with at least `Account Settings`, `League Settings`, and `Sign Out`.
  - [x] 3.4 Wire the logged-out CTA to open the auth modal and wire the logged-in actions to the correct routes or placeholders.
  - [x] 3.5 Update header styles so the new auth UI works in the existing desktop/mobile header system without disrupting current navigation behavior.
  - [x] 3.6 Add component tests covering logged-out, logged-in, and sign-out/menu interaction states.

- [x] 4.0 Implement the core auth UX flows for Google sign-in, email/password, email verification, callback handling, and password reset
  - [x] 4.1 Build an `AuthModal` that supports sign-in, sign-up, and forgot-password modes without requiring a dedicated page for normal entry.
  - [x] 4.2 Build an `AuthForm` that supports Google OAuth initiation and email/password sign-in/sign-up submission through Supabase Auth.
  - [x] 4.3 Add clear UX states for email verification required, existing-account collisions, OAuth errors, invalid credentials, and recovery-email success.
  - [x] 4.4 Implement the Supabase callback route for OAuth code exchange and verification-related redirects.
  - [x] 4.5 Implement the reset-password page so recovery links can land on a dedicated route and allow secure password updates.
  - [x] 4.6 Rework the existing `/auth` page into a compatible fallback surface or redirect-oriented helper page that does not conflict with the new modal-first UX.
  - [x] 4.7 Add route/component tests for callback handling, password reset flow, and auth-form state transitions.
  - [x] 4.8 Document the manual provider configuration required in Supabase and Google Cloud, including site URL, redirect allow list, callback URL, and OAuth client settings.

- [x] 5.0 Build the MVP Account Settings experience and design-safe placeholders for league settings, connected accounts, provider refresh controls, and Patreon entitlements
  - [x] 5.1 Create the authenticated `/account` route and main `AccountSettingsPage` shell.
  - [x] 5.2 Add profile management UI backed by `user_profiles`, including display name and avatar display state.
  - [x] 5.3 Add league-default settings UI backed by `user_settings`, using the persisted fantasy scoring and roster-config mapping helpers without touching Draft Dashboard.
  - [x] 5.4 Add the MVP saved-teams UI backed by `user_saved_teams`, including create, edit, list, and default-selection behavior for manual saved teams.
  - [x] 5.5 Add non-implemented but explicit connected-account sections for Yahoo, Fantrax, Patreon, and ESPN so the architecture is visible without shipping the real sync flows yet.
  - [x] 5.6 Add design placeholders for future provider features including multiple linked leagues/teams, default team selection, active-context switching, refresh-on-login preference, manual refresh, cooldown messaging, and in-flight dedupe states.
  - [x] 5.7 Add Patreon entitlement placeholders on the account page that make clear Patreon is connected from account settings rather than used as primary site login.
  - [x] 5.8 Add guarded authenticated-page behavior for unauthenticated users and loading/error states for missing profile/settings rows.
  - [x] 5.9 Add component tests for account settings rendering, saved-team CRUD states, default-team behavior, and connected-account placeholder visibility.

- [x] 6.0 Enforce cross-table ownership consistency for provider-linked records discovered during schema design. Reopened and closed 2026-07-13: production now has eleven validated composite ownership foreign keys and covering child indexes; rollback-only probes reject cross-owner writes and accept legitimate same-owner writes.
  - [x] 6.1 Add composite foreign keys, constraints, or trigger-based validation so provider-linked rows cannot reference `connected_account_id`, `external_league_id`, or `external_team_id` owned by a different `user_id`.

- [x] 7.0 Harden provider secret storage before any real Yahoo, Fantrax, Patreon, or ESPN tokens are written
  - [x] 7.1 Choose and implement an at-rest encryption strategy for `private.connected_account_tokens` instead of relying on plain text token columns.

- [x] 8.0 Migrate ambiguous Supabase imports to explicit client roles discovered during the wrapper audit
  - [x] 8.1 Replace server-side and API-route imports of `lib/supabase` with explicit browser, public, authenticated-token, or service-role clients based on actual access requirements.

- [x] NEW 9.0 Complete the manual Supabase email-auth delivery configuration required for sign-up verification and password recovery. Custom SMTP, successful Supabase handoff, and recipient-side confirmation/recovery receipt are verified; link execution remains separately open under NEW 10.4/14.5 (completed 2026-07-18).
  - [x] NEW 9.1 Enable `Email` auth in the Supabase dashboard for the target environment.
  - [x] NEW 9.2 Confirm that `Confirm email` is enabled so password sign-up requires verification before protected settings access.
  - [x] NEW 9.3 Verify the Supabase auth email templates preserve the app-provided redirect target so verification and recovery links return to the implemented callback/reset flow.
  - [x] NEW 9.4 Verify the Supabase `Site URL` and redirect allow-list entries for localhost, production, and any required preview URLs.
  - [x] NEW 9.5 Configure a working SMTP sender or otherwise verify outbound email delivery in the active Supabase environment so sign-up and recovery emails actually arrive. Custom SMTP is enabled; bounded confirmation/recovery requests returned HTTP 200 with successful Auth handoff and no SMTP error; a read-only Outlook Inbox listing found the exact signup-confirmation and password-reset deliveries at the recorded 2026-07-14 handoff time. Link opening and lifecycle verification remain separate under NEW 10.4/14.5 (completed 2026-07-18).

- [ ] NEW 10.0 Complete the remaining manual auth-provider configuration and verification checklist documented in `tasks/TASKS/auth-user-settings-platform/docs/auth-provider-manual-config.md`
  - [x] NEW 10.1 Confirm required deploy environment values exist and are correct: `NEXT_PUBLIC_SITE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, and `NEXT_PUBLIC_SUPABASE_PUBLIC_KEY`. Signed-in Vercel CLI inspection confirmed all three names are present and scoped to Production, Preview, and Development without exposing values; the authenticated production account/session and owner-scoped Supabase reads prove the deployed URL/key pair is functional against the expected project, while the canonical production auth/account flow proves the site origin is correct (2026-07-13).
  - [x] NEW 10.2 Verify Google Cloud OAuth client configuration, including authorized JavaScript origins and the hosted Supabase callback URI.
  - [ ] NEW 10.3 Run the documented manual verification checklist for Google sign-in on localhost and production. Production evidence on 2026-07-14: after the targeted local-auth reset, Google account selection completed the hosted Supabase callback and restored authenticated `/account`; the verification session was then explicitly logged out as part of NEW 45 containment. Localhost remains unverified.
  - [ ] NEW 10.4 Run the documented manual verification checklist for email sign-up verification, callback completion, and password-recovery flow.
  - [ ] NEW 10.5 Verify preview-deployment auth behavior if preview auth support is required.

- [x] NEW 11.0 Complete Supabase Vault readiness checks before any live Yahoo, Fantrax, Patreon, or ESPN tokens are written. Production Vault availability, access grants, and statement-logging posture were verified without reading secret values (2026-07-13).
  - [x] NEW 11.1 Confirm the `vault` schema and extension are available in each target Supabase environment before applying `20260327_encrypt_connected_account_tokens_with_vault.sql`. The active production project has the `supabase_vault` extension and `vault` schema (2026-07-13).
  - [x] NEW 11.2 Verify after migration that `anon` and `authenticated` do not have direct access to `vault.secrets` or `vault.decrypted_secrets`. Production privilege inspection found no `PUBLIC`, `anon`, or `authenticated` grants (2026-07-13).
  - [x] NEW 11.3 Review database statement-logging settings for environments that will write provider tokens so plaintext token values are not exposed in logs during Vault writes. Production uses `log_statement=ddl` and does not duration-log ordinary statements (2026-07-13).

- [x] NEW 12.0 Recover and re-apply the Vault token-storage migration in environments where the first run failed partway through. The active production schema is fully converged even though canonical migration-ledger repair remains separately open under NEW 35 (2026-07-13).
  - [x] NEW 12.1 Apply the corrected `20260327_encrypt_connected_account_tokens_with_vault.sql` migration after pulling the fix for the missing-column rerun case. Resulting production tables/functions are live and 4/4 current token rows reference Vault secrets (2026-07-13).
  - [x] NEW 12.2 Verify that `private.connected_account_tokens` now has `access_token_secret_id` and `refresh_token_secret_id` and no longer has plaintext `access_token` or `refresh_token` columns. Verified directly in production without reading values (2026-07-13).

- [x] NEW 13.0 Tighten service-role client initialization and configuration guarantees after the explicit-import refactor. Strict local initialization and deployed all-environment configuration are both verified (2026-07-13).
  - [x] NEW 13.1 Confirm `SUPABASE_SERVICE_ROLE_KEY` is configured in every environment that will run service-role API routes and cron jobs. Signed-in Vercel inspection confirmed the variable is present for all environments without exposing its value (2026-07-13).
  - [x] NEW 13.2 Replace the current test-safe service-role fallback with strict lazy initialization: imports remain side-effect free, privileged use requires both canonical environment values, and public/test-key fallbacks are rejected. Verified by focused Supabase/Yahoo tests and full TypeScript validation on 2026-07-11.

- [ ] NEW 14.0 Resolve custom SMTP delivery failure for Supabase Auth emails
  - [x] NEW 14.1 Verify the custom SMTP configuration saves successfully and is accepted by Supabase without provider-auth errors.
  - [x] NEW 14.2 Check junk/spam/quarantine and recipient-side filtering for verification and reset emails. A bounded read-only Outlook Inbox listing found both exact production signup-confirmation and password-reset deliveries at 2026-07-14 17:48 UTC, proving recipient-side Inbox delivery without relying on junk/quarantine. No message link, identifier, address, or mailbox state was accessed or changed (completed 2026-07-18).
  - [x] NEW 14.3 If delivery still fails, retry with the alternate Namecheap SMTP submission port and security mode combination supported by Private Email.
  - [x] NEW 14.4 Confirm the configured sender mailbox is allowed to send via the chosen SMTP credentials and is not blocked by provider policy.
  - [ ] NEW 14.5 Send a fresh sign-up verification and password reset email and confirm end-to-end delivery. One bounded plus-address confirmation and one exact-account recovery request returned HTTP 200 at 2026-07-14 17:48 UTC; Auth logs recorded `user_confirmation_requested` and `user_recovery_requested` without SMTP errors. Inbox receipt, link clicks, callback/reset rendering, and test-user cleanup remain open.

- [x] NEW 15.0 Fix recovery-link handling so password reset links open the reset screen instead of silently signing the user in. Reconciled after all four implementation subtasks, runtime verification 16.0, redirect hardening 18.0/20.0, and 13 focused callback/reset/form tests passed on 2026-07-11.
  - [x] NEW 15.1 Route hash-based Supabase recovery sessions through `/auth/reset-password` in the callback handler.
  - [x] NEW 15.2 Add a regression test covering recovery links that arrive with `access_token` and `refresh_token` in the hash.
  - [x] NEW 15.3 Route code-based Supabase recovery callbacks through `/auth/reset-password` instead of treating them like standard OAuth sign-in.
  - [x] NEW 15.4 Add a regression test covering recovery links that arrive with `code=...&type=recovery`.

- [x] NEW 16.0 Verify the recovery-flow fix against a runtime that actually includes the patched callback handler
  - [x] NEW 16.1 Run the updated local app or deploy the latest callback fix before re-testing password recovery.
  - [x] NEW 16.2 Re-test the forgot-password flow and confirm the recovery link lands on `/auth/reset-password` with the password form visible.

- [ ] NEW 17.0 Rotate sensitive credentials that were exposed during auth setup and debugging
  - [ ] NEW 17.1 Rotate the Supabase service-role key and any other exposed Supabase secrets.
  - [ ] NEW 17.2 Rotate exposed third-party credentials, including Yahoo, Resend, database, and Google service-account secrets.
  - [ ] NEW 17.3 Update local and deployed environment configuration with the rotated values and verify dependent integrations still work.

- [x] NEW 18.0 Remove the remaining ambiguity from Supabase password-recovery redirects
  - [x] NEW 18.1 Change forgot-password emails to target `/auth/reset-password` directly instead of routing recovery through the generic callback page.
  - [x] NEW 18.2 Teach `/auth/reset-password` to accept recovery payloads delivered as `code`, `token_hash`, or hash-based session tokens.
  - [x] NEW 18.3 Add regression coverage for direct reset-page recovery links so future auth changes do not silently fall back to site-root sign-in behavior.

- [x] NEW 19.0 Harden homepage server-render fallbacks so upstream NHL outages do not make local development look broken
  - [x] NEW 19.1 Remove the stale JS season helper that could shadow the maintained TypeScript season helper on case-insensitive filesystems.
  - [x] NEW 19.2 Stop homepage server-side fetches from assuming every upstream response is valid JSON.
  - [x] NEW 19.3 Stop the homepage future-game search loop after the first upstream failure instead of retrying multiple slow external dates in sequence.

- [x] NEW 20.0 Fix the Supabase redirect allow-list mismatch that is still collapsing localhost recovery links back to the site root
  - [x] NEW 20.1 Replace the current localhost wildcard redirect entry with `http://localhost:3000/**` so nested auth routes like `/auth/reset-password` are allowed.
  - [x] NEW 20.2 Add exact localhost and production auth redirect entries for `/auth/callback` and `/auth/reset-password` so password recovery does not depend on wildcard behavior.
  - [x] NEW 20.3 Re-test forgot-password after the redirect allow-list fix and verify the recovery email now contains `/auth/reset-password` in `redirect_to`.

- [x] NEW 21.0 Eliminate the duplicate browser Supabase auth client that can interfere with recovery-session state changes
  - [x] NEW 21.1 Make `lib/supabase` re-export the shared browser client from `lib/supabase/client` instead of constructing a second GoTrue instance.

- [x] NEW 22.0 Replace the hanging password-update client call with a direct Auth API request that either succeeds or surfaces a real error
  - [x] NEW 22.1 Submit reset-password updates to `/auth/v1/user` with the active recovery access token instead of relying on the hanging `supabase.auth.updateUser(...)` path.
  - [x] NEW 22.2 Add an explicit timeout and direct error parsing so the reset screen no longer stalls indefinitely on "Updating your password now."

- [x] NEW 23.0 Restyle the auth surfaces to match the FHFH Neon Noir design system in `tasks/TASKS/rules/fhfh-styles.md`
  - [x] NEW 23.1 Update the auth modal, auth form, callback page, and reset-password page to use the FHFH accent typography, neon-glow interaction states, and glass/panel treatments.
  - [x] NEW 23.2 Replace generic auth-page spacing, borders, and buttons with the standardized FHFH panel, ghost-button, and accent-button patterns.
  - [x] NEW 23.3 Apply the same FHFH styling language to the account shell and connected-account cards so auth/account surfaces feel visually coherent.
  - [x] NEW 23.4 Add or update component/page tests only where styling changes require markup changes or new accessible labels.

- [ ] NEW 28.0 Investigate and eliminate the remaining `Multiple GoTrueClient instances detected` warnings still emitted in unrelated tests so the browser/client auth surface uses one clear singleton path everywhere.
  - [x] NEW 28.1 Disable automatic URL auth-payload processing in the main browser client because `/auth/callback` and `/auth/reset-password` already handle those flows explicitly.
  - [x] NEW 28.2 Give the read-only browser public client its own storage key so it does not contend with the primary authenticated browser client.
  - [ ] NEW 28.3 Verify in production that the warning no longer appears and that auth remains usable after Yahoo redirect round-trips. Partial evidence on 2026-07-13: the authenticated `/account` surface remained usable after the bounded Yahoo refresh and browser logs contained only Apollo's informational DevTools message, with no `Multiple GoTrueClient instances detected` warning. A true Yahoo OAuth redirect round-trip was not reissued and remains required or explicitly excepted.

- [x] NEW 24.0 Implement the Yahoo Fantasy connected-account flow and first-pass league sync foundation. All 10 child rows now close with production OAuth/discovery/manual-refresh evidence, full-field standings/team metadata, saved/default/active context, and owner-scoped cached opponent-roster access (2026-07-13).
  - [x] NEW 24.1 Create the Yahoo connect/disconnect OAuth flow from account or league settings without coupling Yahoo to core site login.
  - [x] NEW 24.2 Store per-user Yahoo connection metadata in the connected-account model while keeping token material in the private token store.
  - [x] NEW 24.3 Build the first Yahoo league/team discovery sync so users with multiple Yahoo leagues can choose a default team and active league context.
  - [x] NEW 24.4 Add guarded refresh controls, cooldown enforcement, and sync-run dedupe to avoid rapid repeated Yahoo sync attempts. Verified in production on 2026-07-13: one authenticated refresh returned HTTP 200 and refreshed 39 teams across five leagues; the UI immediately disabled the action and showed completed/next-eligible state; exactly one `provider_sync_runs` row is `manual`/`completed`, finished at 10:07:15 UTC with cooldown until 10:12:15 UTC and a cleared dedupe key; the targeted route error cluster is empty. Temporary validation branch `5799c88f-514b-4520-9883-3b13f5d99797` was deleted and confirmed absent after verification.
  - [x] NEW 24.5 Keep the existing shared Yahoo refresh path untouched in `web/pages/api/v1/db/manual-refresh-yahoo-token.ts`. Verified on 2026-07-11 that the route has no working-tree diff; the per-user account flow remains isolated under `/api/v1/account/yahoo/*`.
  - [x] NEW 24.6 Drive the `League Settings` tab from synced Yahoo league scoring and roster data for the selected active Yahoo league instead of showing only generic user defaults.
  - [x] NEW 24.7 Add Yahoo league/team dropdown switchers to `League Settings` and `Saved Teams`, backed by `user_provider_preferences.active_context`, so users can quickly swap active leagues without reconnecting.
  - [x] NEW 24.8 Support saving imported Yahoo teams into `user_saved_teams` using synced `external_teams.roster_snapshot` plus linked `external_league_key` and `external_team_key`. The account UI now inserts or refreshes the provider-backed saved team with Yahoo roster/settings provenance and preserves default-team semantics; component tests and `tsc --noEmit` pass (2026-07-11).
  - [x] NEW 24.9 Sync and expose Yahoo league standings plus all league teams metadata so the account UI can show the full league field, not only the current user’s owned teams. Discovery now merges league teams/standings, marks ownership, persists opponent metadata without fetching opponent rosters, and the account UI labels full-field rows while restricting default/context actions to owned teams; 20 focused tests and `tsc --noEmit` pass (2026-07-11).
  - [x] NEW 24.10 Add on-demand or cached views for other Yahoo teams’ rosters within a synced league. Added an authenticated owner-scoped roster endpoint with a 15-minute database cache, secure Vault-backed token access/refresh persistence, provider-failure isolation, and an accessible opponent roster viewer with loading/empty/error states. Focused verification passes 3 files/22 tests plus `tsc --noEmit` and scoped diff checks (2026-07-13).

- [x] NEW 25.0 Implement the Fantrax connected-account and league-import foundation. Owner-approved option A is implemented without credentials, scraping, a schema migration, deletion, or production action; four focused files pass 27/27 tests plus `tsc --noEmit` and scoped diff checks (2026-07-14).
  - [x] NEW 25.1 Confirm the viable Fantrax integration path for this project (official API, partner access, or manual/import fallback) before coding against an unstable assumption. The owner approved option A on 2026-07-14: accept owner-supplied CSV/JSON through the shared provider tables; do not store Fantrax credentials, scrape, or use an unofficial authenticated session.
  - [x] NEW 25.2 Add the Fantrax connection architecture to account settings using the shared connected-account model. The account panel and authenticated API use `connected_accounts`, `external_leagues`, `external_teams`, `user_provider_preferences`, and `provider_sync_runs`; all reads/writes remain owner-qualified and existing RLS/composite ownership contracts require no migration.
  - [x] NEW 25.3 Implement the first Fantrax league/team discovery or import flow with support for multiple leagues and default-team selection. Bounded CSV/JSON normalization supports stable or name-derived keys, league settings/metadata, grouped roster rows, additive idempotent upserts, explicit/default-first selection, and later in-place default changes while preserving an existing choice unless a new import marks a replacement.
  - [x] NEW 25.4 Add provider-specific sync-state, cooldown, and failure messaging for Fantrax so repeated retries stay controlled. Imports record running/completed/failed state, unique in-flight dedupe, stale-run recovery, a 15-second retry cooldown with `Retry-After`, and accessible loading/success/error UI; complete reads use verified range pagination.

- [x] NEW 26.0 Implement the ESPN connected-account placeholder-to-integration upgrade path. Owner-approved option A is complete locally with a credential-free ESPN manual-import wrapper over the verified provider-neutral persistence/UI mechanics; 7 focused files pass 39/39 tests plus TypeScript, Sass, React review, formatting, and scoped diff checks (2026-07-14).
  - [x] NEW 26.1 Decide whether ESPN support will use an authenticated wrapper, manual league import, or a server-side adapter before exposing a live connect button. The owner approved option A on 2026-07-14: use owner-supplied CSV/JSON through the shared provider model and store no ESPN credentials/cookies or use scraping/private endpoints.
  - [x] NEW 26.2 Add ESPN connected-account persistence and active-context selection using the shared provider model. The authenticated API uses the existing owner-qualified tables, provider-specific `espn` rows, complete paginated state reads, verified default/active team mutations, and no schema migration.
  - [x] NEW 26.3 Support multiple ESPN leagues/teams and in-place switching of the active league context without forcing users off their working page. The account panel renders all stored leagues/teams and independently changes active context or default team in place with accessible team-specific controls.

- [x] NEW 27.0 Implement Patreon account linking and entitlement materialization without coupling Patreon to site auth. The local API v2 foundation is complete; the combined Fantrax/ESPN/Patreon/account regression group passes 11 files/54 tests plus TypeScript, Sass, formatting, React/security review, and scoped diff validation, including an explicit successful active generic-entitlement write. Live configuration/deployment/tier policy remain separate under NEW 41–42 (2026-07-14).
  - [x] NEW 27.1 Add the Patreon OAuth connect/disconnect flow from account settings only. Signed 15-minute state, bounded same-origin next paths, exact callback derivation, authenticated connect/disconnect APIs, and cascade-backed Vault cleanup are implemented without adding Patreon to site sign-in.
  - [x] NEW 27.2 Sync Patreon member identity and entitlement state into `user_entitlements` while preventing one Patreon identity from being reused across multiple site users. The flow filters to `PATREON_CAMPAIGN_ID`, uses stable user/member IDs, preflights both unique owners, converts `23505` races to 409, rolls back new partial accounts, and stores only generic site-owned eligibility/tier metadata.
  - [x] NEW 27.3 Surface Patreon-linked access state and perk eligibility in account settings without making Patreon a primary sign-in provider. The Patreon panel shows connection, provider status, generic eligibility, tier labels, last sync/error state, and an explicit no-concrete-feature-mapping boundary.
  - [x] NEW 27.4 Add a manual re-sync path and support-facing status messaging for Patreon entitlement drift or billing-state changes. Manual re-sync has in-flight dedupe, stale recovery, 60-second cooldown/`Retry-After`, provider 429 propagation, access-token refresh, and immediate persistence of rotated refresh tokens before follow-up reads.

- [x] NEW 29.0 Remove Yahoo OAuth secrets and token artifacts from tracked files, rotate the exposed Yahoo app credentials/tokens, and replace repo-local secret storage with env-only or vault-backed handling.
  - [x] NEW 29.1 Delete `web/lib/supabase/Upserts/yahooAuth/token.json` from the tracked codebase and add its exact path to `.gitignore`; repository removal is complete, while credential invalidation remains open under 29.2.
  - [x] NEW 29.2 Rotate the Yahoo consumer key/secret, revoke/re-authorize exposed Yahoo tokens, and update local/deployed secret stores. User supplied the required exact confirmation on 2026-07-11; safe local inspection confirmed non-empty `YFPY_CONSUMER_KEY` and `YFPY_CONSUMER_SECRET` configuration without reading values.

- [x] NEW 30.0 Complete the manual Yahoo app configuration for the new per-user OAuth callback flow. The production-only HTTPS validation path is selected and verified: the live callback completed authorization-code exchange, persisted the connected account/token references, and discovered 39 teams across five leagues through the exact callback origin/path (2026-07-14 reconciliation).
  - [x] NEW 30.1 Verify the Yahoo app redirect URI allows `/api/v1/account/yahoo/callback` for local development. Direct `http://localhost` Yahoo callback testing is intentionally not supported because Yahoo requires HTTPS; this row is superseded by the deployed-HTTPS plan in 30.4 rather than misrepresenting localhost as allowed.
  - [x] NEW 30.2 Verify the Yahoo app redirect URI allows `/api/v1/account/yahoo/callback` for production. The successful production Yahoo OAuth callback and discovery sync prove `https://fhfhockey.com/api/v1/account/yahoo/callback` is accepted by the configured Yahoo app.
  - [x] NEW 30.3 Confirm the Yahoo app redirect URI matches the exact origin/path currently used by the running environment, including scheme, host, port, and no accidental trailing-slash mismatch. `buildYahooCallbackUrl` derives the forwarded HTTPS origin plus the fixed path with no trailing slash; successful production code exchange/discovery proves the exact registered value matches.
  - [x] NEW 30.4 Adjust the local Yahoo testing plan because Yahoo requires `https` redirect URIs; use a trusted HTTPS origin for local callback testing or validate Yahoo only against a deployed HTTPS environment. Selected the deployed-production HTTPS validation path; localhost remains useful for non-provider unit/UI work but is not represented as a valid Yahoo redirect.
  - [x] NEW 30.5 Configure the Yahoo OAuth client credentials in the deployed environment so production `/api/v1/account/yahoo/connect` can build the authorization URL. The route's supported `YFPY_CONSUMER_KEY`/`YFPY_CONSUMER_SECRET` names are present across all Vercel environments; values were not read (2026-07-13).

- [x] NEW 31.0 Expose a service-role-only PostgREST wrapper for encrypted connected-account token writes so Yahoo callback sync can persist provider tokens in production.
  - [x] NEW 31.1 Apply the migration that exposes `public.upsert_connected_account_tokens_secure(...)`. Production function inspection confirms the wrapper is live, `SECURITY DEFINER`, search-path pinned, revoked from public client roles, and executable only by `service_role` (2026-07-13).
  - [x] NEW 31.2 Redeploy the app so the Yahoo callback uses the public wrapper RPC instead of trying to call the `private` schema directly. Production deployment `dpl_4RHw9oZqG5foXzmABrPRv6rcwoho` built the current dirty-tree implementation, reached READY, and is aliased to `fhfhockey.com` (2026-07-13).

- [x] NEW 32.0 Investigate why Yahoo discovery is finding leagues but zero owned teams after a successful OAuth callback.
  - [x] NEW 32.1 Validate the shape returned by `yahoo.user.game_teams(...)` for the connected Yahoo account and adjust the team flattening logic if the current parser is reading the wrong nesting level.
  - [x] NEW 32.2 Verify the connected-account UI can show discovered Yahoo teams and default-team selection once owned-team parsing is corrected. The account component suite verifies connected metadata, league/team rows, and default-team state using normalized Yahoo payloads (2026-07-11).
  - [x] NEW 32.3 Limit Yahoo league discovery to the latest/current Yahoo NHL game season by default instead of importing every historical league the user has ever had.
  - [x] NEW 32.4 Derive Yahoo `league_key` from `team_key` when the team payload omits it, and filter synced leagues to the owned-team league set when team discovery is present.
  - [x] NEW 32.5 Use `public.yahoo_game_keys` as the bounded authoritative current Yahoo NHL game/season source for provider discovery, with an explicit `user.games()` max-season fallback when the canonical row is unavailable or absent from the connected account. Source provenance is recorded in account metadata; focused tests and `tsc --noEmit` pass (2026-07-11).

- [ ] NEW 33.0 Stop auth/session hydration from blocking the account UI on secondary role lookups after provider redirects.
  - [x] NEW 33.1 Make the auth context publish the authenticated user immediately from the Supabase session before waiting on the optional `public.users` role query.
  - [x] NEW 33.2 Prevent the header from flashing the logged-out CTA while auth is still resolving.
  - [x] NEW 33.3 Add a logged-out mobile-menu auth CTA that opens the shared `AuthModal` so mobile users have the same sign-in/sign-up entry point as desktop.
  - [x] NEW 33.4 Add signed-in mobile-menu account controls with avatar/context plus direct links to account settings, league settings, and sign-out.
  - [ ] NEW 33.5 Verify in production that `/account` no longer hangs on "Loading account settings..." after Yahoo redirect round-trips.

- [x] NEW 34.0 Replace the current "clear browser history" auth recovery workaround with a targeted local FHFH auth-session reset. Production verification completed on 2026-07-14: the targeted reset reported success, preserved the separately signed-in Supabase dashboard, and a full Google callback restored authenticated `/account`; the verification session was subsequently revoked for NEW 45 containment.
  - [x] NEW 34.1 Add a browser-auth reset helper that clears only the FHFH/Supabase auth storage keys instead of wiping unrelated site logins.
  - [x] NEW 34.2 Reset stale local Supabase auth state before starting fresh sign-in attempts so hung modal submissions do not require full browser-history clearing.
  - [x] NEW 34.3 Add a visible `Reset Local Auth` action to the auth form as a user-facing recovery tool.
  - [x] NEW 34.4 Make the auth modal body scroll safely on small screens so sign-in/sign-up flows remain usable on mobile Safari without trapping content below the viewport.
  - [x] NEW 34.5 Verify in production that users can recover from stale local auth state without clearing unrelated browser sessions. The live Reset Local Auth action cleared only FHFH state, left the signed-in Supabase dashboard intact after reload, and allowed a complete Google redirect back to authenticated `/account` on 2026-07-14.

- [x] NEW 35.0 Reconcile the production auth/settings schema with canonical Supabase migration history so disaster recovery and new environments do not depend on unregistered root-level SQL files.
  - [x] NEW 35.1 Inventory production extension, table, Vault-reference, function, privilege, and migration-ledger state without reading token values. Evidence: Vault and the token schema/functions are live, all four token rows use secret references, and none of the three auth migrations appear in the production migration ledger (2026-07-13).
  - [x] NEW 35.2 Move or regenerate the auth/settings migrations under the canonical `supabase/migrations/` workflow and prove they converge idempotently against an isolated database/branch before production ledger repair. The complete chain applied and re-applied idempotently on data-less branch `5799c88f-514b-4520-9883-3b13f5d99797`; constraint, ownership, RLS, RPC, and advisor probes passed (2026-07-13).
  - [x] NEW 35.3 Reconcile production migration history without replaying destructive DDL, then verify the recorded versions, schema, privileges, and generated types agree. The three already-live baselines were registered history-only, the two deltas were applied, all five exact local versions are present, and production-generated types include the secure token getter (2026-07-13).

- [x] NEW 36.0 **P1 integrity:** Apply and verify the missing provider ownership constraints in production so privileged writes cannot create cross-user connected-account, league, team, sync, token, preference, or entitlement references.
  - [x] NEW 36.1 Prove the current production gap and preflight existing rows. Evidence: only single-column foreign keys and no ownership trigger are live; all eleven cross-owner mismatch counts are currently zero (2026-07-13).
  - [x] NEW 36.2 Add the narrow idempotent composite ownership constraints after required parent uniqueness, using `NOT VALID`/validation sequencing where appropriate to avoid unsafe locking. The bounded migration is applied in production with parent uniqueness, eleven validated composite foreign keys, covering indexes, and the service-role-only token-read wrapper (2026-07-13).
  - [x] NEW 36.3 Verify cross-owner writes are rejected, legitimate same-owner writes and Yahoo callback sync still pass, advisors remain clear for the auth tables, and generated types are unchanged or regenerated as required. Rollback-only production probes reject cross-owner writes and pass same-owner and authenticated RLS reads/writes; target auth security lints, unindexed-FK lints, and RLS-initplan lints are zero, and types/tests/typecheck pass (2026-07-13).

- [x] NEW 37.0 **P2 performance:** Close auth-table advisor findings exposed by isolated migration validation without changing ownership semantics or deleting indexes based on a fresh/idle branch.
  - [x] NEW 37.1 Add covering child indexes for all eleven composite provider-ownership foreign keys and verify the advisor no longer reports unindexed auth ownership keys. Branch and production advisors report zero target-table unindexed-FK findings (2026-07-13).
  - [x] NEW 37.2 Change the 36 existing owner RLS policy expressions from per-row `auth.uid()` evaluation to the equivalent scalar init-plan form `(select auth.uid())`, preserving every command, `USING`, and `WITH CHECK` boundary. All 36 policies use the scalar form in branch and production, with rollback-only RLS isolation probes passing (2026-07-13).
  - [x] NEW 37.3 Re-run security/performance advisors plus the cross-owner/same-owner probes; record fresh-branch `unused_index` notices as insufficient removal evidence rather than deleting operational indexes speculatively. Target auth security/unindexed-FK/initplan findings are zero; only expected fresh/idle unused-index notices remain and no index was speculatively removed (2026-07-13).

- [x] NEW 38.0 Deploy and production-verify the new owner-scoped Yahoo opponent-roster route and cache before closing its release evidence. Production deployment `dpl_CmirzkNJK8DfqdnojEDvTiUKHZ71` is READY and aliased to `fhfhockey.com`; the route is present in the successful build. Exactly one authenticated provider load for Adorable Puppies returned 21 players and changed safe metadata from `not_fetched` to `visibility=league`, `source=on_demand`, `fetched_at=2026-07-14T14:05:03.891Z`. One immediate collapse/reopen reported `Loaded the cached roster`, retained the same fetch timestamp/player count, and therefore did not issue a second Yahoo provider fetch. Vercel recorded exactly two HTTP-200 route invocations and no target runtime-error cluster. No token value was read (2026-07-14).

- [x] NEW 39.0 Add the missing explicit ESPN manual-import operational contract: bounded validation, provider-isolated running/completed/failed sync state, in-flight dedupe, stale-run recovery, 15-second cooldown/`Retry-After`, paginated complete reads, and loading/empty/error/success UI. The shared mechanics and ESPN wrappers pass the 7-file 39-test group plus TypeScript/Sass/React/diff verification; no deployment or production import occurred (2026-07-14).

- [x] NEW 40.0 Implement the explicit Patreon API v2 operational contract: signed account-settings-only OAuth state; `identity`-scope member/tier normalization for the configured FHFH campaign; stable Patreon user/member anti-sharing identifiers; Vault-backed rotating tokens; owner-qualified status, entitlement, sync-run, cooldown, and failure state; and cascade-backed secret cleanup on disconnect. Evidence is shared with NEW 27.0–27.4: the combined provider/account group passes 11 files/54 tests plus TypeScript, Sass, formatting, React/security review, and scoped diff checks, including an explicit successful active generic-entitlement write; no production action occurred when this local task originally closed (2026-07-14).

- [x] NEW 41.0 Decide and configure the product mapping from verified Patreon campaign/tier IDs to concrete FHFH feature access before granting tier-specific perks. Owner-approved option A on 2026-07-14 establishes a documented generic-supporter-only exception: the integration may materialize `patreon_supporter` eligibility and tier metadata but grants no concrete FHFH feature. Any future tier-specific perk requires a new product decision.

- [ ] NEW 42.0 Configure and production-verify Patreon after a separate checkpoint: register/inspect the v2 OAuth client and exact callback URL, add `PATREON_CLIENT_ID`, `PATREON_CLIENT_SECRET`, and `PATREON_CAMPAIGN_ID` to deployed secrets, deploy, run one bounded connect/resync/disconnect probe, and verify duplicate-identity rejection plus Vault-secret cleanup without reading token values.
  - [x] NEW 42.1 Reconcile live prerequisites without exposing values. Official current Patreon API v2/Vercel/Supabase guidance was checked; production callback is exactly `https://fhfhockey.com/api/v1/account/patreon/callback`; Vercel project/deployment linkage is healthy but has no `PATREON_*` variables; production baseline is zero Patreon accounts/entitlements/token rows/referenced Vault secrets/sync runs; the Vault cleanup trigger, provider/member uniqueness indexes, and entitlement RLS are live (2026-07-14).
  - [x] NEW 42.2 Sign in to the Patreon account that owns the FHFH campaign, enable its creator account if required, and create or reconcile a v2 OAuth client with the exact production callback. The owner completed sign-in/creator enablement; Codex created the API v2 `FHFHockey` client with callback exactly `https://fhfhockey.com/api/v1/account/patreon/callback`, resolved the single `FHFHRadio` creator campaign through the authenticated v2 API, captured required values only in transient secure runtime state, and removed the mode-0600 temporary creator-token bridge without printing or committing credentials (2026-07-14).
  - [x] NEW 42.3 Add `PATREON_CLIENT_ID`, sensitive `PATREON_CLIENT_SECRET`, and `PATREON_CAMPAIGN_ID` to the Vercel production environment without printing or committing values. All three variables were transferred directly from transient secure runtime state, saved as Sensitive and Production-only, and verified by names/scope plus Vercel's success notice; no value was printed or written to the repository (2026-07-14).
  - [x] NEW 42.4 Deploy the current verified implementation to production, confirm READY/alias state, and verify the Patreon account API reports configured without target runtime errors. Deployment `dpl_46Zukj28HpTpMyHwudhmzKvgkGzc` reached READY and was aliased to `fhfhockey.com`; the authenticated live Patreon panel reported `Auth: verified`, `Connection: Not linked`, and an enabled `Connect Patreon` action with no configuration error (2026-07-14).
  - [ ] NEW 42.5 Run one authenticated Patreon connect and one guarded resync, recording only safe identity/status/tier/count/timestamp evidence and verifying a generic supporter entitlement with no concrete feature grant. The identity-only OAuth exchange and guarded resync both completed successfully at 2026-07-14 17:27/17:29 UTC, persisted one connected account, one Vault-backed token row/two secret references, and two completed sync runs, and granted no concrete feature. The campaign-owning creator identity is not an active paid member of its own campaign, so it correctly produced zero active generic entitlements; positive-member production evidence remains unresolved under NEW 44.0.
  - [x] NEW 42.6 Verify duplicate stable Patreon identity rejection with one bounded rollback-safe production probe or equivalent two-account API evidence; do not retain synthetic rows. A two-account insert attempt using the live stable Patreon identity was rejected by the production unique constraint inside an exception-guarded block, and the post-probe query found exactly one legitimate Patreon account and zero retained synthetic probe rows (2026-07-14).
  - [x] NEW 42.7 Disconnect the linked Patreon account and prove account/entitlement/token/sync cleanup plus deletion of the referenced Vault secret IDs without reading token values; inspect target Vercel/Supabase errors afterward. The authenticated UI disconnect returned to `Connection: Not linked`; production then contained zero target account, entitlement, token, and sync rows and zero Vault secrets under the two recorded connected-account secret names. Target Vercel Patreon routes had no runtime error cluster; Supabase API logs showed successful 200/204 operations and no Patreon-specific database error (2026-07-14).

- [x] NEW 43.0 **P1 Patreon production auth handoff:** Remediate the live Patreon panel's missing bearer-token headers. Deployment `dpl_5enipuFW2p7KzFQtUW4EQQ24uwtw` reached READY/alias with configured secrets, but the authenticated account shell received HTTP 401 from `/api/v1/account/patreon` because the panel omitted the Supabase access token required by `requireApiUser`. Added the same session-token boundary used by Yahoo/Fantrax/ESPN to status/connect/resync/disconnect, covered session-expiry and header behavior, redeployed as `dpl_46Zukj28HpTpMyHwudhmzKvgkGzc`, and verified the configured authenticated live state before resuming OAuth (completed 2026-07-14).

- [ ] NEW 44.0 **P2 Patreon active-supporter production evidence gap:** Resolve the remaining positive-entitlement requirement in NEW 42.5. The authorized campaign-owner OAuth probe proved the correct inactive path because Patreon does not report the creator identity as an active paid member of its own campaign. The owner's later confirmation that Patreon is signed in and the creator account is enabled reconfirms completed NEW 42.2 but cannot convert the campaign owner into an active paid member or satisfy this positive-evidence row. The positive local contract explicitly passes normalization plus a successful `patreon_supporter` active-row write with `generic_entitlement_only=true` and no feature grant inside the 4-file/16-test focused Patreon suite. Either run one bounded connect/resync/disconnect with a real active FHFH patron identity and repeat cleanup, or obtain a separate explicit owner-approved active-supporter evidence exception accepting the negative production lifecycle plus this verified active-member local contract as sufficient evidence; the already-approved generic-only product exception does not itself waive this evidence requirement. Never synthesize a production entitlement or grant a concrete feature (reconfirmed 2026-07-14).

- [x] NEW 45.0 **P1 OAuth callback/reset fragment/title containment:** Prevent Google/Supabase OAuth access, refresh, or provider-token fragments from surviving in callback/reset tab titles or any observable post-return URL/title/history state, while preserving authenticated return-path behavior. Deployment `dpl_BXxbdjaCrj85guU33rGgS9UoaaS7` is READY and aliased to production; one fresh Google return was synchronously reduced to the credential-free callback path before Chrome metadata exposure, completed an authenticated `/auth` fallback and clean `/account` handoff, and retained no sensitive marker in visible query/hash/title/body state. Exact Next-history scrubbing remains covered by the 33-test focused local group because the Chrome evaluation surface does not expose `history`. The Google connection was removed through the signed-in account controls, leaving zero matching connections, and a guarded production cleanup found and deleted exactly one probe `auth.sessions` row plus its refresh token. Vercel recorded one callback HTTP 200 with zero target error clusters/error-fatal/5xx rows; Supabase recorded the expected authorize/callback/user lifecycle with information-level 302/302/200 responses and no warning/error/4xx/5xx. The originally surfaced access JWT passed its fixed 2026-07-14 14:49:58 EDT expiry and was never reused (completed 2026-07-14).
  - [x] NEW 45.1 Contain the verification-session exposure without persisting token values: Google provider-token revocation returned HTTP 200, Supabase local/reset and final probe-session logout calls returned HTTP 204, and the mode-0600 transient revocation bridge was removed. The already-issued short-lived access JWT passed its fixed 2026-07-14 14:49:58 EDT expiry and was never reused or recorded.
  - [x] NEW 45.2 Give `/auth/callback` and `/auth/reset-password` safe explicit document titles and synchronously consume/scrub sensitive callback query/hash material before asynchronous session processing or navigation can leave it as the tab title/history-facing state. Implemented safe SSR/client titles and noindex/no-referrer metadata; structured capture scrubs the visible pathname and Next `history.state` before any auth call without waiting for router readiness; AuthForm and both routes sanitize same-origin return paths; scrub failures stop processing and route to credential-free `/auth` with a full-navigation fallback (2026-07-14).
  - [x] NEW 45.3 Add focused callback regressions proving hash credentials are consumed once, the visible URL/title are scrubbed, code/token-hash paths retain behavior, and no credential value reaches logs or rendered text. The 4-file focused group passes 33/33 tests (13 helper, 8 callback, 7 reset, 5 AuthForm), including Strict Mode exactly-once processing, stale Next-state cleanup, router-not-ready handling, rejected-error sentinels, unsafe/nested returns, and fail-closed navigation; project TypeScript, zero-warning scoped lint, and scoped `git diff --check` pass (2026-07-14).
  - [x] NEW 45.4 Deploy and safely production-verify the bounded fix. Deployment `dpl_BXxbdjaCrj85guU33rGgS9UoaaS7` is READY/aliased; a fresh Google redirect completed authenticated fallback/account handoff with clean visible path/query/hash/title/body state and a callback URL already scrubbed before Chrome metadata exposure. The provider connection was removed with zero matching connections remaining; the sole session created in the bounded probe window and its refresh token were deleted with guarded one-row evidence. Target Vercel and Supabase lifecycle logs contain no error cluster, warning/error, 4xx, or 5xx, and the originally surfaced JWT is past fixed expiry without reuse (2026-07-14).

- [ ] NEW 46.0 **P2 OAuth PKCE migration dependency:** Replace browser OAuth's JavaScript-default implicit fragment flow with PKCE only after production confirmation/recovery templates and the mailbox-backed email flow are made PKCE-compatible; update the template/callback contract and verify Google, sign-up confirmation, and recovery end to end rather than switching `flowType` speculatively (discovered 2026-07-14).

- [x] NEW 47.0 **P2 expired-session recovery reachability:** When the production Patreon API correctly returns `Invalid or expired session`, the account/auth shells must stop presenting the stale user as verified, and visible Sign Out must clear local FHFH auth state even if the remote session is already invalid. Deployment `dpl_2yad83EZq4A5oiQNovUPocLVxQMg` reached READY and aliased to `fhfhockey.com`; the same Chrome profile that had rendered the stale verified shell reached the signed-out fallback form, and a fresh boolean-only reset proof confirmed the `/auth` path, fallback heading, reset confirmation, signed-out header, visible reset action, and absence of the stale signed-in branch. Deployment-scoped runtime evidence contained two HTTP-200 requests and no `/auth` error cluster. The separate browser-autofill credential-containment incident is tracked under P1 NEW 48.0 (completed 2026-07-14).
  - [x] NEW 47.1 Centralize visible sign-out through the auth context so it clears the rendered user immediately and always invokes the targeted local Supabase reset even when the remote session is expired, rejected, or wedged. `AuthProviderContext` now fails closed before the bounded local-scope reset, and Header/UserMenu/`/auth` all use that shared action (2026-07-14).
  - [x] NEW 47.2 Keep `Reset Local Auth` visible on the stale signed-in `/auth` branch and add focused regressions for context state clearing, targeted storage preservation, header/mobile/menu actions, and fallback-page reachability. The signed-in branch now exposes both recovery actions; 5 files/15 tests, `tsc --noEmit`, scoped ESLint with zero errors, and scoped diff checks pass, including rejected and resolved-error sign-out cases with unrelated storage preserved (2026-07-14).
  - [x] NEW 47.3 Deploy the bounded recovery fix and verify the already-stale production shell reaches a signed-out recoverable auth form without browser-storage inspection; inspect the target runtime/auth error boundary afterward. Production `dpl_2yad83EZq4A5oiQNovUPocLVxQMg` is READY with five aliases and no alias error. After an autofill snapshot incident was contained separately under NEW 48.0, a fresh value-free boolean proof confirmed the fallback/reset state and deployment-scoped logs showed two HTTP 200s with no `/auth` runtime error cluster (2026-07-14).

- [x] NEW 48.0 **P1 browser-autofill credential exposure containment:** The owner confirmed the FHFH password was rotated, affected sessions were contained, and the saved Chrome credential was updated or removed. A fresh production check then used only value-free boolean assertions—no snapshots, input-value reads, cookies, or browser-storage inspection—and proved `/auth`, the fallback/reset controls, and the signed-out header were reachable while the stale signed-in branch was absent. Both originally visible fields had been cleared immediately, the exposed value was never reused or repeated in repository controls, and the verification tab was finalized (completed 2026-07-14).

- [x] NEW 49.0 Reconcile stale A-AUTH documentation and control paths against the current implementation and verification evidence without closing any external/provider gate: repair the canonical PRD, task-list, and manual-config references; record production Google as verified with localhost still open, production SMTP handoff as verified with then-open Outlook receipt and confirmation/recovery lifecycle, preview auth as conditional, direct reset-route and credential-scrub behavior as implemented, and PKCE as deferred under NEW 46.0. At this documentation-only checkpoint all pre-existing 21 unchecked rows remained open; path, link, count, and scoped diff checks passed with no external mutation. NEW 50.0 later closes only the independently observed receipt rows (completed 2026-07-18).

- [x] NEW 50.0 Reconcile the already-requested production confirmation/recovery deliveries through a bounded read-only Outlook Inbox check: the exact signup-confirmation and password-reset messages are present at the recorded 2026-07-14 handoff time, closing only delivery/receipt rows 9.0/9.5/14.2. Link opening, callback/reset rendering, password update, cleanup, localhost, preview, and PKCE gates remain open; no message identifier, address, link, credential, mailbox state, provider state, session, or production state was accessed or changed (completed 2026-07-18).

- [ ] NEW 51.0 **P1 legacy mailbox plaintext-credential retention:** a bounded Sent Items verification for an unrelated support reply unexpectedly exposed older messages containing plaintext credential material. Do not repeat, copy, test, or reuse any value. Treat the already-rotated FHFH password family as contained under NEW 48, identify the owner/system for every other affected credential without persisting values, verify each is retired or rotate and contain it, then obtain explicit owner approval before deleting the source messages and any recoverable mailbox copies. A follow-up metadata-only inventory found four candidate messages—one `FHFH` subject from 2025 and three `fhfhockey.com` developer-account subjects from 2022—sent only to a Gmail-domain recipient; no body, value, message ID, address, or link was read or persisted. Metadata cannot identify the exact provider/system or prove retirement, so obtain an owner-supplied redacted system/owner/status mapping before any rotation or deletion. No credential, session, provider, or message changed (updated 2026-07-21).
