## Relevant Files

- `tasks/prd-sustainability-trends-audit.md` - Source PRD that defines the audit scope, validation method, and consolidation goals.
- `tasks/tasks-prd-sustainability-trends-audit.md` - Execution checklist for the audit work.
- `tasks/TASKS/three-pillars-analytics/sustainability/sustainability-trends-audit-report.md` - Canonical working audit worksheet, glossary, family index, findings, validation evidence, and consolidation register.
- `tasks/TASKS/three-pillars-analytics/sustainability/sustainability-trends-inventory.json` - Deterministic tracked-file inventory with stable audit IDs, family memberships, surface kinds, runtime-status classifications, and inclusion evidence.
- `web/scripts/audit-sustainability-trends-inventory.mjs` - Reproducible generator for the tracked-file inventory; combines explicit PRD seeds, relevant tracked paths/SQL, recursive local imports, and related tests without inferring production ownership from names alone.
- `tasks/TASKS/three-pillars-analytics/sustainability/sustainability-trends-dependency-map.json` - Deterministic first dependency/table map covering local imports, literal API edges, literal Supabase objects/RPCs, SQL declarations, ownership classes, and explicit dynamic/unknown boundaries.
- `web/scripts/audit-sustainability-trends-dependencies.mjs` - Reproducible generator for the first dependency and data-object ownership map.
- `web/pages/trends/index.tsx` - Primary trends dashboard page and likely consolidation anchor for trend-related UI.
- `web/pages/trends/placeholder.tsx` - Older or alternate trends dashboard implementation that may duplicate current logic.
- `web/pages/trendsSandbox.tsx` - Sandbox page for player trend and sustainability exploration with direct Supabase reads.
- `web/pages/trendsTestingGrounds.tsx` - Manual testing page for player trend metric rebuilds and fetches.
- `web/pages/trends/player/[playerId].tsx` - Player-specific trend page that reads rolling metrics and related trend outputs.
- `web/pages/underlying-stats/index.tsx` - Team power rankings and underlying-stats UI backed by team rating calculations.
- `web/pages/start-chart.tsx` - Start-chart UI using projections, matchup context, and goalie probabilities.
- `web/pages/FORGE.tsx` - FORGE dashboard UI for skater and goalie projections, uncertainty, and accuracy trends.
- `web/pages/stats/player/[playerId].tsx` - Adjacent player stats page with rolling-average logic that may overlap conceptually with trend systems.
- `web/pages/projections/index.tsx` - Adjacent projections surface that may share projection/baseline assumptions.
- `web/pages/api/v1/trends/player-trends.ts` - Player trend metric fetch/rebuild endpoint that upserts `player_trend_metrics`.
- `web/pages/api/v1/trends/skater-power.ts` - Skater trend percentile endpoint for the trends dashboard.
- `web/pages/api/v1/trends/team-power.ts` - Team trend percentile endpoint for the trends dashboard.
- `web/pages/api/v1/trends/team-ctpi.ts` - Team CTPI endpoint used alongside trend dashboards.
- `web/pages/api/v1/trends/team-sos.ts` - Team strength-of-schedule endpoint used alongside trend dashboards.
- `web/lib/trends/playerTrendRebuildScope.ts` - Shared seven-day player-trend repair-window contract used by manual and scheduled rebuild routes.
- `web/__tests__/pages/api/v1/trends/player-trends.test.ts` - Focused fail-closed auth, bounded-scope, and valid server-to-server rebuild regressions.
- `web/__tests__/pages/api/v1/trends/team-power.test.ts` - Multi-page deterministic source-read and second-page category/ranking regression.
- `web/pages/api/v1/sustainability/trends.ts` - Sustainability leaderboard endpoint backed by `sustainability_scores` and `player_baselines`.
- `web/pages/api/v1/sustainability/trend-bands.ts` - Sustainability trend-band read/recompute endpoint backed by `sustainability_trend_bands`.
- `web/pages/api/v1/sustainability/rebuild-priors.ts` - Sustainability prior rebuild endpoint.
- `web/pages/api/v1/sustainability/rebuild-window-z.ts` - Sustainability z-window rebuild endpoint.
- `web/pages/api/v1/sustainability/rebuild-score.ts` - Sustainability score rebuild endpoint backed by `sustainability_scores`.
- `web/pages/api/v1/sustainability/rebuild-trend-bands.ts` - Bulk sustainability trend-band rebuild endpoint.
- `web/pages/api/v1/db/sustainability/rebuild-baselines.ts` - Baseline rebuild endpoint that writes `player_baselines`.
- `web/pages/api/v1/start-chart.ts` - Start-chart data endpoint reading `player_projections`, `goalie_start_projections`, and team ratings.
- `web/lib/projections/startChartFantasyScoring.ts` - Versioned Start Chart adapter over the shared default skater fantasy-point contract.
- `web/__tests__/pages/start-chart.test.tsx` and `web/__tests__/pages/api/v1/start-chart.test.ts` - UI contract-display and API formula regressions for Start Chart scoring.
- `web/pages/api/v1/db/update-start-chart-projections.ts` - Legacy start-chart builder that writes `player_projections`.
- `web/pages/api/v1/db/update-rolling-player-averages.ts` - Rolling-average trigger endpoint for `rolling_player_game_metrics`.
- `web/lib/supabase/Upserts/fetchRollingPlayerAverages.ts` - Main rolling-average builder and upsert path for `rolling_player_game_metrics`.
- `web/pages/api/v1/db/update-team-power-ratings.ts` - Team power ratings builder writing `team_power_ratings_daily`.
- `web/pages/api/v1/db/update-team-power-ratings-new.ts` - Alternate/new team power ratings builder writing `team_power_ratings_daily__new`.
- `web/lib/teamRatingsService.ts` - Shared team-rating fetch layer used by multiple pages and APIs.
- `web/lib/power-ratings.ts` - Core team power rating math helpers used by the ratings jobs.
- `web/pages/api/v1/db/update-goalie-projections.ts` - Legacy goalie-start projection trigger using RPC.
- `web/pages/api/v1/db/update-goalie-projections-v2.ts` - Current goalie-start projection builder writing `goalie_start_projections`.
- `web/pages/api/v1/db/run-projection-v2.ts` - FORGE projection run endpoint orchestrating projection generation.
- `web/pages/api/v1/db/run-projection-accuracy.ts` - FORGE accuracy and calibration endpoint writing multiple accuracy tables.
- `web/pages/api/v1/projections/players.ts` - Read endpoint for `forge_player_projections`.
- `web/pages/api/v1/projections/teams.ts` - Read endpoint for `forge_team_projections`.
- `web/pages/api/v1/projections/goalies.ts` - Read endpoint for `forge_goalie_projections`.
- `web/lib/trends/playerTrendCalculator.ts` - Core player trend metric definitions and rolling average calculations.
- `web/lib/trends/teamMetricConfig.ts` - Team trend metric definitions and category setup.
- `web/lib/trends/skaterMetricConfig.ts` - Skater trend metric definitions and category setup.
- `web/lib/trends/teamPercentiles.ts` - Team trend percentile calculation logic.
- `web/lib/trends/utils.ts` - Shared trend utility logic and baseline/streak helpers.
- `web/lib/sustainability/bandService.ts` - Sustainability trend-band orchestration and storage helpers.
- `web/lib/sustainability/bandCalculator.ts` - Sustainability band calculation logic and metric-specific baseline handling.
- `web/lib/sustainability/bands.ts` - Sustainability metric definitions and supported metric list.
- `web/lib/sustainability/priors.ts` - Sustainability prior calculation and upsert logic.
- `web/lib/sustainability/windows.ts` - Sustainability window/z-score calculation and upsert logic.
- `web/lib/sustainability/score.ts` - Sustainability score calculation and upsert logic.
- `web/lib/baselines/aggregations.ts` - Legacy baseline aggregation logic used by baseline rebuilds.
- `web/lib/projections/run-forge-projections.ts` - Main FORGE engine implementation and projection writes; the old `runProjectionV2.ts` shim has been removed.
- `web/lib/projections/goaliePipeline.ts` - Goalie pipeline stage spec and table ownership hints.
- `web/lib/projections/queries/skater-queries.ts` - FORGE skater query layer reading rolling metrics and sustainability bands.
- `web/lib/projections/queries/goalie-queries.ts` - FORGE goalie query layer reading goalie-start projections and related inputs.
- `web/lib/projections/queries/team-context-queries.ts` - FORGE team context query layer for matchup/context adjustments.
- `web/pages/api/v1/db/calculate-wigo-stats.ts` - Legacy WIGO builder writing `wigo_recent` and `wigo_career`.
- `web/tests/api/dashboard-endpoints.test.ts` - Existing endpoint contract tests covering trend and sustainability responses.
- `web/tests/api/dashboard-endpoints-invariants.test.ts` - Existing invariant tests for dashboard trend/sustainability payloads.
- `web/tests/api/dashboard-endpoints-4xx.test.ts` - Existing 4xx coverage for trend/sustainability endpoints.
- `web/tests/audit/dashboard-data-audit.spec.ts` - Existing audit-style tests that may inform regression coverage.
- `web/tests/audit/dashboard-freshness-audit.spec.ts` - Existing freshness audit tests relevant to snapshot/fallback behavior.
- `web/tests/audit/dashboard-performance-audit.spec.ts` - Existing performance audit tests for dashboard endpoints.
- `web/tests/audit/dashboard-reliability-audit.spec.ts` - Existing reliability audit tests for trend/sustainability endpoints.
- `web/sql/tests/sko_trends_validation.sql` - SQL validation asset for sustainability/trend outputs.
- `web/sql/functions/analytics.rpc_sko_player_series.sql` - SQL function related to sustainability/trend payload composition.
- `web/lib/supabase/database-generated.types.ts` - Reference for known tables, views, and RPC names used in the audit.

### Notes

- This task list is for executing the audit defined in the PRD, not for implementing the final consolidated engine yet.
- The audit should proceed one system family at a time and should produce artifacts as it goes instead of waiting until the end.
- Existing tests should be reused where useful, but the audit will also define additional regression coverage for approved formulas and data paths.
- SQL validation scripts and API verification steps should be treated as first-class audit outputs, not optional notes.

## Tasks

- [x] 1.0 Build the master audit inventory and system-family map. The worksheet/glossary, deterministic inventory/classification, first dependency map, and conservative data-object ownership map are complete; family-level audits retain all dynamic and unknown evidence (verified 2026-07-22).
  - [x] 1.1 Read the PRD and create a working audit worksheet that mirrors the required output sections: page purpose, helpers, APIs, source tables, target tables, validation steps, overlap findings, and consolidation recommendation. The canonical incremental report includes stable audit IDs, runtime status, trigger/auth/freshness/formula fields, evidence states, finding links, and guarded recommendation criteria (verified 2026-07-22).
  - [x] 1.2 Build the full in-scope file inventory covering pages, APIs, helper libraries, background jobs, SQL assets, and tests for trend, sustainability, baseline, rolling-average, projection, and power-rating logic. The reproducible tracked-file manifest contains 1,052 stable-ID entries across pages, APIs/jobs, helpers/data, SQL/migrations, tests, components, scripts, styles, stories, and documentation; two consecutive runs produced SHA-256 `dc7fa2f3a95444a04f2a30b9fefc8b228257d80f54824c8ca138da7e7bb859b4` (verified 2026-07-22).
  - [x] 1.3 Group the inventory into system families: trends, sustainability, team power/underlying stats, start-chart/rolling metrics, FORGE, and legacy/adjacent systems. Multi-family membership is preserved rather than forcing false exclusivity: trends 99, sustainability 230, team power 38, start/rolling 102, FORGE 191, and legacy/adjacent 412 memberships (verified 2026-07-22).
  - [x] 1.4 Mark each inventoried file as current production, active experiment, legacy, adjacent dependency, or unclear. All 1,052 entries are classified: 5 current production, 7 active experiment, 78 legacy, 815 adjacent dependency, and 147 unclear; production is asserted only for scheduled routes in `web/vercel.json`, while unresolved ownership remains explicitly `unclear` for family-level audit (verified 2026-07-22).
  - [x] 1.5 Create the first dependency map linking each page to its APIs, each API to its helper modules, and each helper/job to its read and write tables. The deterministic map records 753 surfaces, 1,872 direct local dependency edges, 263 literal API edges (245 resolved directly to tracked route files), 395 referenced/declared data objects, and 52 dynamic references across 31 files that remain explicit family-audit work (verified 2026-07-22).
  - [x] 1.6 Create a table ownership map that classifies each relevant Supabase table/view as raw source, derived aggregate, snapshot, cached output, presentation output, or unknown/mixed. All 395 static objects are classified conservatively: 107 raw source, 11 derived aggregate, 33 snapshot, 1 cached output, 10 presentation output, and 233 unknown/mixed; each object retains read/write/declaration/invocation evidence and unknowns are not promoted by naming alone (verified 2026-07-22).
  - [x] 1.7 Create the audit glossary template that will be reused for each system to define “trend,” “baseline,” “projection,” “typical performance,” sample windows, and output type. The report also requires snapshot, sustainability, power, and accuracy/calibration semantics so cross-family terms cannot be compared without units, population, horizon, provenance, and leakage boundaries (verified 2026-07-22).

- [x] 2.0 Audit the trends and sustainability systems end to end. Static ownership, canonical-player traces, definition drift, and findings are completely recorded; NEW 2/7/8 remain explicit remediation work (verified 2026-07-22).
  - [x] 2.1 Audit `web/pages/trends/index.tsx`, `web/pages/trends/placeholder.tsx`, `web/pages/trendsSandbox.tsx`, `web/pages/trendsTestingGrounds.tsx`, and `web/pages/trends/player/[playerId].tsx` for user-facing outputs, imports, dependency paths, and overlap. The canonical report distinguishes the linked `/trends` movement hub and active player detail from the older placeholder, linked experimental sandbox, and unlinked testing surface; it records each surface's visible outputs, direct Supabase/API ownership, URL state, and overlap (verified 2026-07-22).
  - [x] 2.2 Audit `web/pages/api/v1/trends/player-trends.ts`, `web/pages/api/v1/trends/skater-power.ts`, `web/pages/api/v1/trends/team-power.ts`, `web/pages/api/v1/trends/team-ctpi.ts`, and `web/pages/api/v1/trends/team-sos.ts` for read paths, write paths, parameters, fallback behavior, and consuming pages. The report records methods, parameters, current-season resolution, pagination/cache/fallback behavior, tables, consumers, and failures; P0 NEW 1 and P1 NEW 3 preserve the unauthenticated mutator and unpaginated team-power discoveries (verified 2026-07-22).
  - [x] 2.3 Audit `web/lib/trends/playerTrendCalculator.ts`, `web/lib/trends/teamPercentiles.ts`, `web/lib/trends/teamMetricConfig.ts`, `web/lib/trends/skaterMetricConfig.ts`, and related trend utilities for actual formulas, windows, percentile logic, and hidden assumptions. The report records cumulative/sample variance, complete-only 3/5/10 windows, rank-percentile/weighted category math, 1/3/5/10/20 skater windows, partial sandbox rolling windows, streak thresholds, and fixed SKO/luck constants; P1 NEW 2 preserves the expected-shooting formula mismatch (verified 2026-07-22).
  - [x] 2.4 Audit `web/pages/api/v1/sustainability/trends.ts`, `web/pages/api/v1/sustainability/trend-bands.ts`, `web/pages/api/v1/sustainability/rebuild-priors.ts`, `web/pages/api/v1/sustainability/rebuild-window-z.ts`, `web/pages/api/v1/sustainability/rebuild-score.ts`, `web/pages/api/v1/sustainability/rebuild-trend-bands.ts`, and `web/pages/api/v1/db/sustainability/rebuild-baselines.ts`. The report records each read/write contract, parameters, prerequisites, paging/chunking, observability, and persistence ownership; P0 NEW 4 preserves the six unauthenticated writer contracts plus GET-triggered band mutation (verified 2026-07-22).
  - [x] 2.5 Audit `web/lib/sustainability/*` and `web/lib/baselines/*` to document metric definitions, baseline derivation, prior logic, z-score windows, band logic, score construction, and upsert targets. The report captures pooled F/D Beta priors, three-season posteriors, 3/5/10/20 empirical-Bayes windows, clipped luck/skill score construction, metric-specific band families/intervals, and the older baseline-window layer without importing draft-only claims (verified 2026-07-22).
  - [x] 2.6 Produce the trends/sustainability read-write table map for `player_trend_metrics`, `sustainability_priors`, `sustainability_player_priors`, `sustainability_window_z`, `sustainability_scores`, `sustainability_trend_bands`, and `player_baselines`. The report names the canonical role plus exact reader/writer family for all seven objects (verified 2026-07-22).
  - [x] 2.7 Run player-level validation traces for the selected canonical players by pulling raw stats, rebuilding trend/sustainability outputs, and comparing stored rows, API payloads, and visible UI values. Read-only traces for Nick Suzuki, Tyson Hinds, Rasmus Dahlin, and Josh Norris reproduce Sustainability scores exactly (including clipping), compare raw/current trend rows with the public API, and record NEW 7/8 for playoff-unit/history corruption and false source-cutoff freshness (verified 2026-07-22).
  - [x] 2.8 Record definition drift, naming collisions, duplicate formulas, and preferred source-of-truth candidates across trend and sustainability systems. The report now separates movement, empirical-Bayes sustainability, raw rolling windows, projection adjustment, and UI presentation semantics; canonical ownership and NEW 2/7/8 decision boundaries are explicit (verified 2026-07-22).

- [x] 3.0 Audit the team power, underlying stats, and matchup-context systems end to end. Static ownership, ANA/VGK traces, path dispositions, and formula classifications are completely recorded; NEW 6/9 remain explicit remediation work (verified 2026-07-22).
  - [x] 3.1 Audit `web/pages/underlying-stats/index.tsx` and the team-power portions of `web/pages/trends/index.tsx` and `web/pages/trends/placeholder.tsx` for displayed metrics, dependency paths, and duplicated concepts. The report separates canonical persisted-rating diagnosis, canonical movement context, and the legacy alternate Trends surface (verified 2026-07-22).
  - [x] 3.2 Audit `web/pages/api/v1/db/update-team-power-ratings.ts`, `web/pages/api/v1/db/update-team-power-ratings-new.ts`, `web/lib/teamRatingsService.ts`, and `web/lib/power-ratings.ts` for formulas, input tables, target tables, fallback logic, and authoritative status. The canonical writer/service/math chain and no-write `__new` quarantine are mapped; P0 NEW 5 preserves missing writer auth and P1 NEW 6 preserves the PDO unit/default ambiguity (verified 2026-07-22).
  - [x] 3.3 Audit related matchup and team-context endpoints and helpers, including CTPI and SOS paths, to document how they are calculated and where they enter downstream pages. CTPI persisted-first/raw fallback, its component weights, SOS latest-snapshot opponent records, and both dashboard consumers are recorded (verified 2026-07-22).
  - [x] 3.4 Produce the team power/system-family map for `team_power_ratings_daily`, `team_power_ratings_daily__new`, and any supporting team gamelog or WGO tables/views. The report distinguishes the canonical rating snapshot, quarantined alternate target, in-memory trend percentiles, CTPI daily/raw inputs, SOS standings, and paginated NST/WGO sources (verified 2026-07-22).
  - [x] 3.5 Run team-level validation traces for the selected canonical teams by pulling raw game logs, recomputing team ratings where feasible, and comparing persisted rows and UI outputs. Read-only ANA/VGK traces reconstruct the 25-entry EWMA inputs and verify the live Start Chart rating cards; both persisted game-date snapshots precede final source ingestion and are carried forward unchanged, registered as NEW 9 (verified 2026-07-22).
  - [x] 3.6 Document whether the old and new team-power paths are redundant, divergent, or intentionally separate. The canonical `team_power_ratings_daily` writer remains authoritative; `update-team-power-ratings-new` is an explicit no-write quarantine stub, while trend-category percentiles and CTPI are intentionally separate presentation models (verified 2026-07-22).
  - [x] 3.7 Record approved, suspect, and legacy formulas for offense, defense, pace, special teams, trend10, CTPI, and SOS-related outputs. Current EWMA/100±15 ratings, category percentiles, CTPI, and standings SOS are recorded; the `__new` path is legacy and missing-PDO fallback remains suspect under NEW 6 (verified 2026-07-22).

- [x] 4.0 Audit the start-chart, rolling-average, and legacy baseline systems end to end. Static ownership, rolling-to-Start-Chart traces, and the UI-wrapper classification are completely recorded; NEW 10/11 remain explicit remediation work (verified 2026-07-22).
  - [x] 4.1 Audit `web/pages/start-chart.tsx` for its displayed outputs, fallback logic, and dependencies on projections, team ratings, and goalie-start probabilities. The report records the slate/date/team controls, skater and goalie outputs, explicit requested/resolved-date fallback, canonical FORGE skater source, goalie-start context, CTPI context, and source/freshness metadata (verified 2026-07-22).
  - [x] 4.2 Audit `web/pages/api/v1/start-chart.ts` and the retired `web/pages/api/v1/db/update-start-chart-projections.ts` contract to document how projections are built, read, and surfaced. The active API reads latest succeeded `forge_player_projections` plus `goalie_start_projections`; repository implementation evidence proves the former `player_projections` writer was removed after its caller audit and is not an active side channel (verified 2026-07-22).
  - [x] 4.3 Audit `web/pages/api/v1/db/update-rolling-player-averages.ts` and `web/lib/supabase/Upserts/fetchRollingPlayerAverages.ts` to document how `rolling_player_game_metrics` are calculated, refreshed, truncated, and reused downstream. The report records the scheduled/audited wrapper, canonical WGO appearance spine, paginated NST/WGO/roster/line inputs, strength/date/season scope, guarded cleanup/upsert modes, and player-detail/FORGE reuse (verified 2026-07-22).
  - [x] 4.4 Audit adjacent legacy baseline-style systems including `web/pages/api/v1/db/calculate-wigo-stats.ts`, `wigo_recent`, `wigo_career`, `wgo_skater_stats_last_30`, and nearby average/recent-performance outputs. These remain explicitly legacy/adjacent baselines rather than canonical rolling or trend writers (verified 2026-07-22).
  - [x] 4.5 Produce the read-write map for `rolling_player_game_metrics`, retired `player_projections`, `goalie_start_projections`, `wigo_recent`, `wigo_career`, and nearby legacy derived tables. The report names canonical/retired ownership and active readers/writers without treating historical documentation references as runtime routes (verified 2026-07-22).
  - [x] 4.6 Run validation traces that start from raw player gamelogs, rebuild rolling averages, and confirm how those values propagate into start-chart projections and related outputs. Raw last-five EV shots/TOI for the four canonical skaters disagree with date-scoped persisted windows after daily history resets; FORGE consumes those rows and reports zero shot evidence for all four. Start Chart resolves the same canonical run and the live UI/API values agree; NEW 10/11 preserve rolling-history and fantasy-scoring drift (verified 2026-07-22).
  - [x] 4.7 Document whether start-chart logic should survive as part of the future unified engine, survive as a UI-specific wrapper, or be retired in favor of FORGE equivalents. Start Chart survives as a UI-specific slate wrapper over canonical FORGE: its read API resolves succeeded `forge_runs`, reads `forge_player_projections`, joins goalie-start/team context, and performs presentation-only aggregation; the legacy `player_projections` materializer is deleted and explicitly disabled by compatibility/pipeline contracts (verified 2026-07-22).

- [x] 5.0 Audit the FORGE projection stack, accuracy reporting, and consolidation path. Static ownership, canonical traces, overlap, and consolidation guidance are completely recorded; NEW 13 remains explicit remediation work after the bounded NEW 12 adapter repair (verified 2026-07-22).
  - [x] 5.1 Audit `web/pages/FORGE.tsx` and its related read APIs to document which outputs are shown, how uncertainty is exposed, and what parts are unique versus overlapping with other pages. The report records player/team/goalie projections, interval and confidence drivers, requested/resolved-date health, calibration context, and links into player trends and Start Chart (verified 2026-07-22).
  - [x] 5.2 Audit `web/pages/api/v1/projections/players.ts`, `web/pages/api/v1/projections/teams.ts`, and `web/pages/api/v1/projections/goalies.ts` for query behavior, filters, and table dependencies. These compatibility readers use bounded schemas and latest-succeeded-run resolution over canonical `forge_*_projections`, with explicit replacement metadata where a `/forge/*` reader owns the richer contract (verified 2026-07-22).
  - [x] 5.3 Audit `web/pages/api/v1/db/run-projection-v2.ts`, `web/lib/projections/run-forge-projections.ts`, `web/lib/projections/goaliePipeline.ts`, and the projection query/helpers layer to document the full FORGE pipeline from inputs to outputs. The eight-stage dependency chain from roster/schedule through ingest, relationships, derived strength, goalie priors, projections, and accuracy is explicit (verified 2026-07-22).
  - [x] 5.4 Audit the retired `web/pages/api/v1/db/update-goalie-projections.ts`, active `web/pages/api/v1/db/update-goalie-projections-v2.ts`, and `web/pages/api/v1/db/run-projection-accuracy.ts` to document goalie start probabilities, projection accuracy baselines, and calibration outputs. The v2 writer is the sole supported start-probability path; the old writer was deleted after a clean caller audit (verified 2026-07-22).
  - [x] 5.5 Produce the read-write map for FORGE tables, including `forge_runs`, `forge_player_game_strength`, `forge_team_game_strength`, `forge_goalie_game`, `forge_player_projections`, `forge_team_projections`, `forge_goalie_projections`, `forge_projection_results`, `forge_projection_accuracy_daily`, `forge_projection_accuracy_player`, `forge_projection_accuracy_stat_daily`, and `forge_projection_calibration_daily`. The report maps each object to the exact pipeline stage and principal reader/writer family (verified 2026-07-22).
  - [x] 5.6 Run canonical player/team/goalie validation traces that compare raw inputs and intermediate pipeline assumptions against final FORGE outputs and stored accuracy rows. Exact run `eca9c5b6-e7a1-4d71-9350-296553abe065` reconciles player/team totals within rounding and Lukas Dostal's saves plus goals allowed to shots against; live canonical readers return the same run. NEW 12 records confidence-driver JSON nesting drift; NEW 13's broad 1,674-of-8,002 result gap is refined to a 1,344-of-7,523 canonical latest-run gap with explicit zero/partial-date and stale-run evidence (verified 2026-07-22).
  - [x] 5.7 Build the final overlap matrix across trends, sustainability, start-chart, team power, FORGE, and legacy systems to identify duplicated concepts, conflicting formulas, and stale paths. The report matrix names authoritative owner, intentional adapters, duplicate concepts, and disposition for all six families (verified 2026-07-22).
  - [x] 5.8 Produce the final consolidation recommendation that identifies the preferred shared engine pieces, deprecated files, required regression tests, and the path toward one future dashboard page with multiple tabs/views. The recommendation keeps one shared data/model layer with FORGE projection ownership, preserves Trends/Start Chart as views, retains the Sustainability model as a distinct signal, and gates retirement/repair work behind remaining NEW 2/6–10/13 plus focused regression/backfill evidence; NEW 11's versioned Start Chart adapter satisfies its scoring prerequisite (verified 2026-07-22).

- [x] NEW 1.0 **P0 unauthenticated privileged trend rebuild:** `POST /api/v1/trends/player-trends` now requires a timing-safe fail-closed `CRON_SECRET`, resolves the current season server-side, requires exact season-start source history, limits writes to the current seven-day repair window, caps optional player scope at 250 IDs, and returns bounded diagnostics. The browser Testing Grounds is read-only; focused missing-config, retired/missing bearer, invalid scope, and valid bounded-repair regressions pass (verified 2026-07-22).
- [ ] NEW 2.0 **P1 expected-shooting formula/identity drift:** `playerTrendCalculator` publishes metric key `expected_shooting_pct` / label `Expected Shooting %` but calculates `shots / ixG` and rejects `ixG=0`; the canonical contextual-ranking definition calculates `individual xG / individual unblocked attempts * 100`. Decide the canonical denominator/unit/key and historical/versioned repair boundary before changing persisted `player_trend_metrics` or user-visible interpretation.
- [x] NEW 3.0 **P1 team-power silent season truncation:** every dynamic source now range-paginates in deterministic date/game/team order until a short page while retaining exact season/date bounds. A route-level 1,001-row fixture proves the second-page team reaches the final offense ranking (verified 2026-07-22).
- [x] NEW 4.0 **P0 unauthenticated Sustainability writer family:** all six rebuild routes now use fail-closed `adminOnly` inside the existing audit wrapper; mixed `trend-bands` keeps ordinary GET public and routes POST/GET-recompute through the same authorization boundary. Focused authorization/audit tests, TypeScript, production Vault-backed caller inspection, exact production deployment `dpl_5uhfJXZagLhhhDLq2brn56xJpBTa`, and value-free 200 public / 401 missing-invalid / 400 current-auth input-validation probes pass without executing a write (verified 2026-07-22).
- [x] NEW 5.0 **P0 unauthenticated canonical team-rating writer:** `/api/v1/db/update-team-power-ratings` now uses fail-closed `adminOnly` inside the existing audit wrapper without changing GET/POST scope or the quarantined `-new` path. The unchanged Vault-backed scheduled caller, focused tests, exact READY Production deployment, and value-free DELETE probes prove 401 missing/invalid and 405 current-auth method validation without executing a write (verified 2026-07-22).
- [ ] NEW 6.0 **P1 missing-PDO unit/default ambiguity:** canonical `calculateEwma` substitutes `100` when PDO is null while the same code notes NST is likely expressed near `1.0`; a missing value can therefore dominate league z-scores and `variance_flag`. Prove the source unit and null prevalence, select neutral fallback versus fail-closed handling, and define bounded historical recompute/version evidence before changing persisted ratings.
- [ ] NEW 7.0 **P1 player-trend playoff unit and cumulative-history corruption:** `wgo_skater_stats_playoffs.toi_per_game` is seconds while the shared calculator consumes minutes. Forward rebuilds now pass playoff-only rows through an explicit source adapter that converts seconds to minutes; a mixed regular/playoff regression reproduces `11.970075` shots/60 and retains the regular row in a two-game accumulator. Keep the root open to define versioned bounded repair for already-stored 60×-low values and reset cumulative samples before rewriting history.
- [ ] NEW 8.0 **P1 Sustainability source-cutoff provenance misstates freshness:** `score.ts` recorded the requested snapshot as the `player_stats_unified` cutoff even when inputs were months older. Forward score construction now reads the latest selected appearance date and uses explicit provenance v2 to separate requested snapshot, observed raw cutoff, derived window-z snapshot, season scope, and observed age in days; focused as-of/version regressions pass. Keep the root open for explicit bounded repair policy for already-stored v1 false provenance before treating historical score freshness as trustworthy.
- [ ] NEW 9.0 **P1 team-rating final-source schedule inversion:** sampled ANA/VGK game-date ratings were written at 09:15 UTC before final NST rate rows arrived around 09:55 UTC; the next-day exact-date gate carried the stale rating forward instead of recomputing the completed game. Read-only production evidence now proves the owning NST ingest runs active at 09:55 UTC before the active no-parameter rating caller at 10:15 UTC, so ordering is already correct. The local forward repair makes only that default caller recompute the prior date before today's carry-forward; explicit dates/ranges, smoke tests, and empty-table backfills keep their former scope, and the focused final-source/carry regression passes. Keep the root open to version and backfill affected historical snapshots and verify the deployed natural dependency.
- [ ] NEW 10.0 **P1 rolling-player date-scoped refresh resets history and contaminates FORGE:** daily bounded calls fetched only the requested source dates while recomputing cumulative/last-N fields from that truncated slice. Canonical skaters fall from season histories to one-to-three-game samples, raw last-five values disagree, and FORGE records zero shot evidence with low-sample shrinkage. The local forward repair keeps date-scoped player selection and writes bounded but reads each selected player's complete prior selected-scope history through the requested end date; response telemetry exposes that history-read mode and focused source/write-window tests pass. Keep the root open to repair persisted primary/support rows and prove raw→rolling→FORGE reconstruction before backfill.
- [x] NEW 11.0 **P1 Start Chart fantasy-point contract drift:** `fhfh-default-skater-v1` now adapts the shared default weights (G `3`, A `2`, PPP `1`, SOG `0.2`, HIT `0.2`, BLK `0.25`) for Start Chart. The API computes all six available canonical FORGE components at read time and returns the contract; the UI renders that returned version/weight set. API/UI regressions pass 2 files/3 tests plus full TypeScript. No historical row is rewritten because Start Chart fantasy points were an unpersisted response projection over stored raw components (verified 2026-07-22).
- [x] NEW 12.0 **P2 FORGE confidence-driver extraction drops modeled context:** the canonical reader now extracts PP opportunity, matchup, and rest from `uncertainty.model.skater_selection`, retains top-level fallback compatibility for older rows, and the existing focused route regression covers every nested driver (verified 2026-07-22).
- [ ] NEW 13.0 **P1 FORGE result/accuracy coverage and run-version gap:** the original all-succeeded-run population counted 8,002 player-games and 1,674 missing results. Canonical latest-succeeded-run plus exact same-date eligibility refines this to 7,523 player-games: 6,179 have a matching result and 1,344 are missing, comprising 912 rows on 18 zero-result dates and 432 across three partial dates. The 149 additional stored matches in the broad population belong outside the latest-run population, and surviving rows on all three partial dates reference older runs after later reruns became canonical. The one active accuracy caller explicitly uses offset 0. A forward-only selected-run diagnostic now exposes/persists total, same-date eligible, matched, missing, wrong-date, invalid-identity, and match-rate fields with focused coverage. Keep the root open to define latest-run/source-run authority, stale-row handling, and bounded backfill/idempotent rerun rules before treating player accuracy as comprehensive evidence.
