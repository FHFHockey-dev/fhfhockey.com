# Yahoo Ingestion and NHL Identity Mapping — Audit, Remediation, and Rollout Tasks

**2026-07-31 Yahoo reader publication and cache-retirement receipt:** Commit `fdfbb2764d656be37b47be27de64d0e50f25f19f` was promoted without a second build as READY deployment `dpl_EphZQcV7FFUHah3sVfYWuzkcVj8D`. Value-free Production probes returned Start Chart 200, ownership trends 200 with 1,058 mapped/436 unmapped players, and ownership snapshots 200; the bounded one-hour Production error-log query was empty. Migration `20260731022805_revoke_legacy_yahoo_read_cache.sql` then applied under exact linked history. The legacy materialized cache remains service-readable only (`anon=false`, `authenticated=false`, `service_role=true`), both replacement views remain security-invoker and browser-readable, and targeted advisor readback found no new canonical-reader issue. Rows 7.1, 8.2, and NEW 9.6 close. Provider/league equivalence, Python retirement, historical backfill, and end-to-end/provider gates remain open.

## Relevant Files

- `tasks/TASKS/draft-dashboard-yahoo/prd-yahoo-audit.md` - Source PRD/audit, schema proposals, priority findings, and historical-ingestion addendum.
- `tasks/TASKS/draft-dashboard-yahoo/tasks-prd-draft-dashboard.md` - Downstream Draft Dashboard consumer requirements and missing-player diagnostics.
- `tasks/TASKS/auth-user-settings-platform/tasks-prd-auth-user-settings-platform.md` - Per-user Yahoo connection/token architecture and completed tracked-secret remediation.
- `tasks/TASKS/draft-dashboard-yahoo/yahoo-current-inventory.md` - Current ownership, schema, route/schedule/caller, configuration, artifact, dependency, and gap inventory.
- `web/lib/integrations/yahoo/` - Shared server-side Yahoo configuration, OAuth, discovery, and provider adapter code.
- `web/lib/integrations/yahoo/playerWriter.ts` - Typed Yahoo player transform, deduplication, and atomic persistence contract.
- `web/lib/integrations/yahoo/writerPermissions.test.ts` - Focused cross-runtime writer, permission, generated-type, and omission contracts.
- `web/lib/supabase/Upserts/Yahoo/yahooAPIgameIds.py` - Historical game metadata/week ingestion path.
- `web/lib/supabase/Upserts/Yahoo/yahooApiPlayerKeys.py` - Historical player-key ingestion path.
- `web/lib/supabase/Upserts/Yahoo/yahooAPI.py` - Player detail/draft/ownership batch path.
- `web/lib/supabase/Upserts/Yahoo/yahooHistoricalOwnership.py` - Paginated, adaptive historical ownership backfill tool.
- `web/lib/supabase/Upserts/Yahoo/populate_yahoo_nhl_mapping.py` - Yahoo-to-NHL mapping implementation candidate.
- `web/lib/supabase/yahooUniformNumbers.py` - Standalone uniform-number path to merge or quarantine.
- `web/pages/api/v1/db/update-yahoo-players.ts` - Current TypeScript batched player writer/RPC caller.
- `web/pages/api/v1/db/update-yahoo-weeks.ts` - Current matchup-week ingestion route.
- `web/pages/api/v1/db/manual-refresh-yahoo-token.ts` - Global Yahoo token refresh route requiring strict internal authorization.
- `web/lib/standardization/nameStandardization.ts` - Existing TypeScript player normalization/alias source.
- `web/lib/supabase/Upserts/Yahoo/player_name_normalization_spec.json` - Cross-runtime normalization artifact if current and generated safely.
- `web/lib/supabase/database-generated.types.ts` - Current Yahoo table/RPC schema evidence.
- `migrations/` and `web/supabase/migrations/` - Yahoo schema/mapping/history migration evidence; reconcile duplicate migration roots before changes.

### Notes

- This list repairs the PRD-only Yahoo audit pair and converts recommendations into executable verification/remediation tasks.
- The previously tracked Yahoo token JSON was deleted and ignored; credentials were confirmed rotated/re-authorized on 2026-07-11. Do not recreate JSON credential artifacts. Use server-only environment or approved encrypted/Vault-backed storage.
- Keep global ingestion credentials separate from per-user connected-account tokens and ownership.
- Prefer one authoritative writer per domain while preserving compatibility readers during staged migration.
- All Yahoo/provider calls, production cron changes, live token checks, database application, and controlled account events use the super-goal checkpoint protocol.
- Complete-table Supabase/PostgREST reads must paginate until a short page or use a verified aggregate/RPC.

## Tasks

- [x] 1.0 Establish the authoritative current Yahoo inventory and contracts
  - [x] 1.1 Re-inventory every Python/TypeScript route, shared helper, table, RPC, migration, cron entry, UI consumer, environment name, and generated artifact related to Yahoo.
  - [x] 1.2 Record stable ownership for game metadata/weeks, player keys, player details, ownership history, draft history, uniform numbers, NHL mapping, global token refresh, and per-user league sync.
  - [x] 1.3 Reconcile live/generated schema with the PRD's inferred tables and identify stale names such as `_mat`, duplicated week storage, or conflicting migration roots.
  - [x] 1.4 Build a dependency graph from credentials → games/weeks → player keys → player details/history → mapping → downstream projections/Draft Dashboard.
  - [x] 1.5 Convert every verified P0–P3 gap into a `NEW` task with current evidence rather than relying on the 2025 audit state.
    - Evidence (1.0–1.5, 2026-07-22): `yahoo-current-inventory.md` records the complete current owner/schema/RPC/migration/cron/caller/environment/artifact set, explicit legacy/canonical boundaries, dependency graph, and every current gap under NEW 9.1–9.7. No provider or database operation was executed.

- [x] 2.0 Secure credentials, routes, and provider interaction
  - [x] 2.1 Verify the removed token JSON remains absent/ignored and scan tracked Yahoo code/artifacts for credential-shaped values without printing secrets.
  - [x] 2.2 Centralize global Yahoo credential configuration in server-only code with explicit required-variable validation and redacted logging.
  - [x] 2.3 Keep per-user connected-account tokens in the approved encrypted service-role-only path and prove browser/RLS clients cannot read them.
  - [x] 2.4 Protect manual refresh, ingestion, backfill, mapping mutation, and sync routes with internal secret/admin authorization plus method/rate controls.
  - [x] 2.5 Return minimal refresh metadata rather than provider payloads and avoid logging token/key fragments.
    - Evidence (2.0/2.4, 2026-07-23): the three global provider routes are GET/POST bounded behind fail-closed admin/cron authorization and shared transient/rate-limit handling; sheet sync remains exact-cron-only with fixed value-free failure output. Historical backfill and mapping/uniform maintenance writers now default to no write unless their exact opt-in variable is set, require server-only credentials, and have no browser/scheduled caller. Focused auth/inventory/source contracts plus Python maintenance tests pass without provider/database execution.
    - Evidence (2.1–2.3/2.5/2.6, 2026-07-22): the deleted token artifact remains absent and exactly ignored; tracked-value shape scans are clean; the production-verified connected-account wrapper remains service-role-only with owner-scoped browser access; and the global refresh route returns only minimal metadata. `globalCredentials.ts` is the sole active global-route owner for exact-column reads, complete nonblank validation, and fail-closed refreshed-token persistence. All three routes import it and expose fixed value-free failures; focused helper plus authorization/missing-config/provider-redaction tests pass 2 files/13 tests. Unscheduled Python alternatives retain explicit compatibility configuration but are not canonical scheduled/browser credential owners.
  - [x] 2.6 Add focused authorization, missing-config, redaction, and provider-error tests.

- [x] 3.0 Consolidate game metadata, weeks, and player-key ingestion
  - [x] 3.1 Choose and document one canonical normalized weeks representation while preserving a compatibility view/read path during cutover.
  - [x] 3.2 Consolidate game/week ingestion into idempotent versioned writes with change detection, bounded concurrency, retries, and partial-result reporting.
  - [x] 3.3 Parameterize game/league/season selection; remove hard-coded current IDs from canonical jobs while retaining explicit maintenance overrides.
  - [x] 3.4 Fetch all player keys through verified Yahoo/provider pagination and paginate all Supabase source reads; do not assume 2,000 or a large limit is complete.
  - [x] 3.5 Mark absent keys inactive only after a complete successful scope snapshot so partial provider responses cannot falsely deactivate players.
  - [x] 3.6 Add deterministic fixtures/tests for multi-season weeks, unchanged skips, additions/removals, partial batches, retries, and completeness counts.
    - Evidence (3.1, 2026-07-23): `yahoo_matchup_weeks` is the canonical one-row-per-game/week representation and active route writer; `yahoo_game_keys.game_weeks` remains a read-compatible legacy field only until the lifecycle cohort removes its unscheduled writer. No second canonical projection was introduced.
    - Evidence (3.4, 2026-07-25): the active player route now owns deterministic Yahoo game-player collection pagination at the provider's 25-row page boundary, continues until an explicit short terminal page, sorts exact keys, and rejects malformed, duplicated, repeated, or safety-bound-exhausted pages before persistence. It then reads only the reconciled active game scope through the existing stable-order Supabase paginator. Deterministic two-page, malformed, repeated, and nonterminating regressions pass; read-only Production aggregates prove all 2,827 stored keys match the exact game-scoped syntax across only game 453 (1,333) and game 465 (1,494). No provider call or database mutation ran.
    - Evidence (3.0/3.2/3.3/3.5/3.6, 2026-07-30): the scheduled metadata/week route now defaults to Yahoo's canonical `nhl` alias, retains only a validated explicit maintenance override, normalizes a complete game/week response, and calls service-only migration `20260730195000`. Its transaction serializes per game, versions the receipt, change-detects metadata/weeks, removes absent weeks only after complete validation, and physically skips unchanged rows. Exact local first-run/replay/correction/removal/duplicate-failure/two-session/ACL/down-reapply/zero-residue proof passes. The already-verified player-key transaction deactivates absent keys only after complete provider pagination. Helper fixtures cover seasons, ordering, invalid/duplicate/partial inputs, retries, counts, and exact receipts. NEW 9.5 retains Production application/provider/legacy-owner gates.

- [x] 4.0 Complete player detail plus ownership/draft history persistence
  - [x] 4.1 Verify efficient multi-key batching and defensive extraction across supported Yahoo response shapes without debug single-key restrictions.
  - [x] 4.2 Separate fetch, transform, persistence, and telemetry responsibilities while retaining a shared row contract across Python and TypeScript maintenance paths.
    - Evidence (4.0/4.2, 2026-07-25): `playerWriter.ts` owns typed Yahoo response transformation, exact-key deduplication, generated-RPC persistence, and receipt validation; the route retains provider fetch/retry and response/audit telemetry. Generated Production types include `upsert_yahoo_players_atomic(Json[]) -> Json` and the route has no RPC `any` escape hatch. The opt-in Python compatibility path now pages only explicit game scope, batches provider reads, preserves null/zero plus explicit snapshot status, uses only the canonical atomic RPC, and has no import-time client, fixed IDs/path, legacy RPC, or direct-table fallback. Focused contracts pass 8/8, Python compilation and targeted ESLint pass; full TypeScript reaches only the unrelated pre-existing GameGrid `isDesktop` ordering error.
  - [x] 4.3 Create/verify normalized daily ownership history and, if required, draft-analysis history with stable player/date/source/version identity and query indexes.
    - Evidence (4.1/4.3, 2026-07-22): the active TypeScript writer uses 25-key Yahoo batches and array/object/primitive ownership extraction; generated types and production-ledger migrations verify normalized ownership/draft-history tables, stable per-player date/timestamp identities, source fields, and query indexes. NEW 9.3 keeps atomic writer/history semantics open.
  - [x] 4.4 Make the canonical RPC/writer atomically upsert latest-compatible `yahoo_players` fields and idempotently append the scoped history snapshot.
  - [x] 4.5 Preserve an explicit missing/omitted status when a snapshot ran but Yahoo omitted a player; do not convert missing values to fabricated zero ownership.
  - [x] 4.6 Merge uniform-number extraction into the canonical detail pipeline or prove the standalone tool remains necessary; quarantine hard-coded path behavior.
  - [x] 4.7 Define evidence-based retention/aggregation only after measuring volume and consumers; destructive pruning requires approval.
  - [x] 4.8 Add RPC/integration tests for atomic latest+history writes, duplicate date reruns, response-shape variation, omissions, partial failures, and rollback.
    - Evidence (4.4/4.5/4.8, 2026-07-25): authorized migrations `20260723113533_make_yahoo_player_writer_atomic.sql` and `20260725200808_fix_yahoo_player_writer_captured_at.sql` are applied with exact isolated-plan history. Live privilege/search-path proof passes. A value-free Production transaction verifies latest plus explicit omission, duplicate ownership/draft replay, and forced late numeric-failure rollback; the outer rollback leaves zero latest/ownership/draft sentinel rows. Focused route/migration response-shape and permission contracts pass 4/4.
    - Evidence (4.7, 2026-07-23): read-only production aggregates measure 488,901 ownership-history rows across 1,548 players from 2024-10-04 through 2026-07-22, 32,017 daily rows across 1,333 players through 2025-04-17, and zero draft-history rows. Existing latest/timeline and normalized-history consumers require preservation, so the approved policy is no destructive pruning; retain raw normalized snapshots and reconsider aggregation only after the reader cutover and an explicit storage/performance trigger.
    - Evidence (4.6, 2026-07-23): the scheduled TypeScript detail payload is the canonical uniform-number owner. No import, route, or schedule calls `yahooUniformNumbers.py`; the duplicate now has no machine-specific path/current IDs or import-time clients and requires explicit `YAHOO_UNIFORM_NUMBERS_MAINTENANCE=1` plus game/league/server credentials. Static maintenance contracts and Python compilation pass without execution.

- [x] 5.0 Build authoritative Yahoo-to-NHL identity mapping and review
  - [x] 5.1 Define a generated, versioned cross-runtime normalization specification for punctuation, Unicode/diacritics, suffixes, aliases, whitespace, and canonical IDs.
  - [x] 5.2 Apply deterministic exact/alias matches before fuzzy candidates and use team, position cluster, active status, and provider IDs only as explicit scored evidence.
  - [x] 5.3 Persist authoritative mappings with confidence, method, review status, effective dates, lineage/supersession, source version, and reviewer notes.
  - [x] 5.4 Persist unmatched/conflicting candidates with reasons, attempts, candidate scores, source identities, and timestamps rather than dropping them.
  - [x] 5.5 Support incremental mapping for new/changed players and an explicit full recompute that never overwrites approved manual mappings silently.
  - [x] 5.6 Provide or verify an authorized review/approve/reject/reassign workflow and downstream cache/materialized-view refresh.
  - [x] 5.7 Reconcile mapping completeness against the full Yahoo and NHL player universes using paginated reads and report goalie/skater/team/position segments.
  - [x] 5.8 Add normalization, deterministic ranking, ambiguity, false-positive, manual override, supersession, and incremental-update tests.
    - Evidence (5.1/5.4/5.6/5.8, 2026-07-22): the shared JSON/Python normalizer, unmatched table, FHFH identity review queue, guarded promotion generator/migration, lineage/provenance fields, and matcher/promotion/migration/Draft Dashboard collision tests preserve unresolved identities and deterministic review. Full incremental recompute behavior and current segmented completeness stay open.
    - Evidence (5.2/5.3, 2026-07-23): exact/manual/alias candidates precede thresholded fuzzy matching; same-name candidates can only be disambiguated by explicit team, position-cluster, active-state, or provider-id evidence and equal best scores remain unresolved. The canonical FHFH external-identity/review contract persists context/season scope, method, confidence, verification/supersession state, provenance/version hashes, actors, timestamps, merge lineage, and resolution notes. Focused matcher and immutable-ledger tests pass 19/19 without a provider/database operation.
    - Evidence (5.7, 2026-07-23): complete server-side read-only aggregates reconcile all 2,827 Yahoo players to 2,092 mapped/735 unmapped (skaters 1,882/628; goalies 210/107), with every Yahoo team and display-position segment recorded. The full 3,555-row NHL universe reconciles to 1,070 mapped/2,485 unmapped, including all position and team-id segments; inactive/no-team historical players explain much of the intentionally broader NHL denominator. This is measurement, not permission to auto-promote mappings.
    - Evidence (5.0/5.5, 2026-07-23): the promotion generator now has explicit `incremental` and `recompute-all` modes. Incremental comparison emits only new/changed candidates, reports removals for review without deleting/superseding them, requires an exact full baseline plus expected pending count, and writes the full current manifest for the next comparison. Recompute-all selects the complete manifest, while generated SQL aborts on conflicting verified identities and only updates review-required or exact same-manifest rows. Focused identity tests pass 22/22 without generating/applying SQL or touching a provider/database.

- [ ] 6.0 Add operational scheduling, retries, and observability
  - [x] 6.1 Define exact schedules and dependency gates for token freshness, games/weeks, keys, details/history, mapping, and downstream consumers.
  - [x] 6.2 Implement bounded exponential backoff with jitter and `Retry-After` handling for 429/transient responses plus fail-fast handling for auth/schema/systemic errors.
  - [x] 6.3 Persist run identity, component, parameters, start/end, processed/succeeded/failed/omitted counts, rate-limit events, retries, and partial-success state.
  - [ ] 6.4 Make historical backfill resumable/idempotent with bounded date/player scopes and machine-readable telemetry; do not treat local log files as the sole operational record. The legacy endpoint does request genuine week-scoped ownership, but the canonical catalog contains no league identity: every `yahoo_game_keys.leagues` value is an empty array, connected-account storage contains nine distinct 2025 league keys, and it contains no older-season league. Do not choose one arbitrarily or port the fixed 2024/2025 IDs; require controlled provider discovery/equivalence evidence before finalizing the durable TypeScript cursor (verified read-only 2026-07-30).
  - [x] 6.5 Alert on stale last success, repeated ownership failure, mapping coverage regression, unmatched growth, rate-limit saturation, token failure, and schema drift.
  - [x] 6.6 Update cron inventory/reporting so scheduled route names, audit rows, expected tables, and partial failures correlate accurately.
    - Evidence (6.6, 2026-07-22): the canonical 64-job schedule and cron-report maps record the Yahoo weeks/player/sheet jobs, exact routes/tables/order, shared audit wrapper ownership, and partial/failure classification.
    - Evidence (6.2–6.3, 2026-07-23): all three active global Yahoo provider routes use one bounded three-attempt exponential-jitter retry owner, honor bounded `Retry-After`, retry only 429/5xx/network-transient failures, and fail fast on auth and other provider errors. Player/week responses expose processed/succeeded/failed/omitted, retry, rate-limit, completeness, and partial state; the existing audit wrapper durably records route/component, parameters, timing/run row identity, response counts, and failure classification. Focused tests pass 3 files/21 tests plus full TypeScript without provider/database execution.
    - Evidence (6.1, 2026-07-30): the exact local ownership migration retains metadata/weeks first, makes self-discovered player detail second, disables the duplicate standalone sheet trigger, and leaves export receipt-gated inside the player lifecycle. Provider retry, complete metadata/week receipt, complete key snapshot, atomic player/history receipt, mapping, and export gates are explicit. Production cutover/natural evidence remains consolidated under NEW 9.5.
    - Evidence (6.5, 2026-07-30): the canonical player route records nonsensitive mapped/unmatched counts beside its existing retry/rate-limit receipt and classifies OAuth/token versus schema failures without returning provider details. The daily cron report compares durable player-ingestion audit observations and emits machine-readable warnings for stale success, two consecutive failures, mapping regression, unmatched growth, rate-limit saturation, token failure, and schema drift. Pure classification plus report integration pass 2 files/22 tests, full TypeScript, and scoped ESLint.

- [ ] 7.0 Verify downstream compatibility and user-facing states
  - [x] 7.1 Verify projections and Draft Dashboard consumers retain latest ADP/round/drafted/ownership fields while historical timelines use normalized history. Production deployment `dpl_EphZQcV7FFUHah3sVfYWuzkcVj8D` returns value-free 200 responses for Start Chart, ownership trends, and ownership snapshots after the canonical reader cutover; no bounded Production error logs occurred (closed 2026-07-31).
  - [x] 7.2 Verify missing/unmatched players surface through diagnostics and review state rather than silently disappearing.
  - [x] 7.3 Verify matchup-week, goalie, shift, roster, connected-account, and league/team consumers use the canonical identities and current-season selection.
  - [x] 7.4 Provide loading, empty, stale, partial, rate-limited, unmapped, and provider-unavailable states with last-success/source metadata.
  - [x] 7.5 Add compatibility/contract tests around the stable latest-player view/API and representative downstream queries.
    - Evidence (7.2–7.5, 2026-07-22): A-DRAFT exposes bounded unmapped/conflict/current-game-missing diagnostics without dropping projections; current matchup/week, goalie/shift, roster, account, and league/team consumers use current-season and owner-scoped identities; connected-account and dashboard surfaces preserve loading/empty/error/partial/rate-limit/provider states; focused mapping, pagination, ownership, discovery, roster, and migration contracts pass.
    - Local cutover receipt (7.1, updated 2026-07-31): all named read-only consumers now select `yahoo_players_with_normalized_history` with the stable `ownership_timeline:normalized_ownership_timeline` alias, and mapping readers select `yahoo_nhl_player_map_read`; hosted parity is exact for latest fields and mapping rows. Eight focused files/47 tests, TypeScript, and scoped ESLint pass. Keep 7.1 open for exact deployed parity and postdeploy controls.

- [ ] 8.0 Execute staged verification and synchronize the initiative
  - [x] 8.1 Run unit/fixture tests without live Yahoo dependency, targeted database/RPC checks, and safe local dry runs with bounded samples.
    - Evidence (8.1, 2026-07-22): deterministic Python matcher tests pass 10/10; focused Vitest route-inventory, auth-boundary, mapping, ownership, account-provider, and migration checks pass 16 files/60 tests; full TypeScript and Python compilation pass. Generated promotion SQL remains review/apply-separated and no live Yahoo or database call occurred.
  - [x] 8.2 Verify schema indexes, RLS/API exposure, row identities, and complete pagination using current Supabase docs/changelog and advisors where available. Connected catalog/advisors and the disposable local stack prove the canonical RLS/identity/index/grant state; Production migration `20260731022805` applies after populated parity, revokes browser SELECT on the legacy cache while retaining service access, and leaves both replacement readers security-invoker/browser-readable. Targeted post-migration ACL/relation/advisor readback passes (closed 2026-07-31).
  - [ ] 8.3 Run staging/provider controlled verification only through a checkpoint; record counts without storing credentials or sensitive provider payloads.
  - [ ] 8.4 Reconcile one bounded end-to-end path from Yahoo source through history/mapping to Draft Dashboard/player visibility.
  - [ ] 8.5 Update the PRD, this list, auth/provider tasks, Draft Dashboard list, cron docs, runbook, and master ledger with final evidence.
    - Applied migration/readback receipt (8.2, updated 2026-07-31): complete read-only catalog evidence proves `yahoo_names.player_id` is nonblank and unique across 1,333/1,333 rows, while the 1,857-row legacy Yahoo/NHL map has 771 duplicate ID-pair groups, 12 null Yahoo IDs, and 1,857 distinct full rows, so imposing a guessed map key would corrupt valid variants. Current Supabase guidance requires explicit Data API grants and RLS; connected advisors identify the expected legacy materialized-view exposure plus two exact duplicate-index pairs. Applied predeploy migration `20260726000603_harden_yahoo_read_surfaces.sql` fail-closes on the names identity/RLS/primary-key contracts, adds the unique names identity and positions read policy, removes three redundant indexes covered by canonical primary keys, reduces browser grants to select-only compatibility, and exposes one security-invoker base-table view. The local consumer cutover now uses both canonical readers and passes eight focused files/47 tests, TypeScript, and scoped ESLint. Postdeploy-only migration `20260731022805_revoke_legacy_yahoo_read_cache.sql` remains gated on deployed parity and then advisor proof; 8.2 stays open for that publication sequence.

## NEW Tasks

- [ ] NEW 9.0 Append every verified defect, schema drift, provider/manual dependency, unmatched identity class, and optimization discovered during execution here before closure.
- [x] NEW 9.1 **P0 unauthenticated global Yahoo maintenance routes:** `/api/v1/db/manual-refresh-yahoo-token`, `/api/v1/db/update-yahoo-weeks`, and `/api/v1/db/update-yahoo-players` now use the shared fail-closed admin/cron boundary. Exact commit `b4aeea8f9af6229aa64f75e4b311b1218a5afb4c` is READY/Production as `dpl_4q8GM1wqTbHR3NbnxRxAakdLEvJr`; all three routes return 401 for missing/invalid authorization and current Vault-backed authorization reaches safe 405 method validation through unsupported PUT. No provider or writer executed, and the bounded relevant runtime-error query is empty (closed 2026-07-23).
- [x] NEW 9.2 **P0 public-key fallback in mapping writer:** `populate_yahoo_nhl_mapping.py` allowed `NEXT_PUBLIC_SUPABASE_PUBLIC_KEY` as a fallback for persistence. The writer now requires `SUPABASE_SERVICE_ROLE_KEY`; a focused source-contract test prevents regression and no mapping/database operation ran (closed 2026-07-22).
- [x] NEW 9.3 **P1 canonical player/history writer contract drift:** migrations `20260723113533` and `20260725200808` are applied; the security-invoker/service-role-only writer validates the full batch, atomically writes latest plus idempotent daily ownership/draft history, preserves genuine zero values, represents omissions explicitly, and rolls back on late failure. Production runtime/zero-residue proof, generated RPC typing, typed TypeScript transform/persistence ownership, and the opt-in Python compatibility contract close 4.0/4.2/4.4/4.5/4.8 (closed 2026-07-25).
- [x] NEW 9.4 **P1 split global credential/configuration ownership:** one server-only `globalCredentials.ts` owner now performs every active global route's exact-column credential read, complete-row validation, and fail-closed token update. The three scheduled/manual routes contain no direct credential-table access or public key/token fallback and emit only fixed stage diagnostics/responses. Per-user OAuth remains separately owner-scoped; unscheduled Python maintenance alternatives are explicitly compatibility/quarantine paths rather than canonical global credential owners. Focused helper/static-route plus authorization/missing-config/provider-redaction tests pass 2 files/13 tests, full TypeScript and complete lint pass, and no credential value, provider call, or database operation was used (verified 2026-07-22).
- [ ] NEW 9.5 **P1 incomplete ingestion lifecycle and fixed-season scheduling:** canonical metadata/weeks now defaults to Yahoo's `nhl` alias and atomically versions a complete normalized snapshot through service-only migration `20260730195000`; canonical player detail owns bounded complete Yahoo provider pagination plus deterministic active-scope Supabase paging and lifecycle-specific alert telemetry. Fresh local execution proves both snapshot RPCs' service-only grants, rollback-zero residue, idempotent replay, correction/removal, and two-session per-game serialization. An exact 12-job fixture proves the applied scheduler migration deactivates six duplicate owners, retains five distinct jobs, removes fixed `gameId=465`, and makes sheet export receipt-gated inside the player lifecycle. The required Production predeploy migrations are applied and read back in the hosted ledger. Keep open for controlled provider runtime/league discovery, legacy Python ownership retirement, and resumable historical backfill; do not select one of nine connected-account 2025 leagues as the global history owner without equivalence proof (updated 2026-07-31).
- [x] NEW 9.6 **P1 schema/read cutover and security proof drift:** local migrations `20260725235646_add_normalized_yahoo_ownership_reader.sql` and predeploy-safe `20260726000603_harden_yahoo_read_surfaces.sql` supply security-invoker compatibility views for canonical daily ownership and the RLS-protected Yahoo/NHL base map; the hardening migration also removes three exact redundant indexes covered by canonical primary keys. The isolated code cutover routes all read-only consumers through both views, aliases normalized history to the stable timeline field, and passes eight focused files/47 tests, TypeScript, and scoped ESLint. Commit `fdfbb2764d656be37b47be27de64d0e50f25f19f` is promoted as READY `dpl_EphZQcV7FFUHah3sVfYWuzkcVj8D`; populated Start Chart/trends/snapshot probes are 200 with empty bounded Production error logs. Postdeploy migration `20260731022805` is applied and read back: canonical views are security-invoker/browser-readable, the legacy materialized cache is service-only, and the 1,857-row non-unique legacy map is explicitly retained rather than assigned a fabricated key (closed 2026-07-31).
- [x] NEW 9.7 **P2 archived-migration test fixture drift:** the Yahoo identity-review and Draft Ranker Yahoo-seed tests still resolved their SQL from the removed active migration root after the clean-baseline archive. Both now read the exact immutable production-ledger copies; the focused suites pass without restoring duplicate active migrations (closed 2026-07-22).
- [x] NEW 9.8 **P0 publicly executable Yahoo player writer:** authorized migration `20260723040553_restrict_legacy_yahoo_player_writers.sql` is applied with exact local/remote history alignment. Live privilege proof returns `anon=false`, `authenticated=false`, and `service_role=true` for both `upsert_players_batch` overloads and `upsert_yahoo_players_v3`; the security advisor no longer reports browser execution of the `SECURITY DEFINER` writer. The remaining mutable-search-path advisories are separately non-exposure hardening, not an execute-grant bypass (closed 2026-07-23).
- [x] NEW 9.9 **P1 exact-cron sheet sync exposed downstream error text:** `/api/internal/sync-yahoo-players-to-sheet` retained exact fail-closed cron-secret authorization but returned and logged raw caught error messages. It now emits fixed value-free failure copy; focused source/auth-inventory tests pass and no sheet/provider/database call ran (closed 2026-07-23).
- [x] NEW 9.10 **P1 mapping dry-run wrote unmatched review rows:** `match_players` inserted unmatched rows before the explicit `YAHOO_MAPPING_WRITE_ENABLED=1` boundary, so the default analysis path could still mutate. Matching is now side-effect-free and unmatched persistence executes only inside the exact write opt-in; focused source/behavior tests pass without a database call (closed 2026-07-23).
- [x] NEW 9.11 **P2 archived identity-promotion fixture recurrence:** the focused Yahoo promotion test still resolved its committed manifest from the removed active migration tree despite the earlier fixture-drift closure. It now reads the exact immutable production-ledger artifact and passes together with the canonical provenance/review contract test (closed 2026-07-23).
- [x] NEW 9.12 **P0 Production atomic Yahoo writer rejects draft-history batches:** guarded migration `20260725200808_fix_yahoo_player_writer_captured_at.sql` is applied and history-aligned, replacing only the ambiguous local variable through fail-closed definition checks. Production catalog proof confirms the repaired variable, security-invoker mode, fixed `pg_catalog` search path, browser denial, and service-role execution. A value-free transaction passes latest/omission, duplicate ownership/draft replay, and forced late-failure rollback; the outer rollback leaves zero sentinel rows. Focused contracts pass 4/4 (closed 2026-07-25).
- [x] NEW 9.13 **P1 Python Yahoo maintenance writer fabricated zero observations and bypassed the canonical transaction:** `yahooAPI.py` defaulted missing draft/ownership values to zero, called the ambiguous legacy RPC, and fell back to a direct `yahoo_players` upsert from import-time fixed configuration. It is now inert without exact `YAHOO_PLAYER_MAINTENANCE_WRITE_ENABLED=1`, requires explicit game/league/season/server configuration, paginates the exact game scope, batches provider reads, preserves null/genuine-zero and observed/omitted semantics, and persists only through the typed canonical atomic writer with receipt validation. Focused source/behavior contracts pass 8/8 and Python compilation passes without provider/database execution (opened and closed 2026-07-25).
- [x] NEW 9.14 **P1 migration-before-code invariant was not enforced at publication:** guarded recovery commit `96ccea804b90b5a6f482de45f6b7931253725311` is READY/Production as `dpl_HCFwiK4yAPeXUG3QzC3R28NtYvsc`. Populated Start Chart API and exact 1440×900/390×844 browser proof pass; ownership trends returns structured 200, and Variance, Player Pickup/Game Grid, Command Center, and projections reader surfaces render without application errors. Deployment-scoped error and 5xx queries are empty. No migration or writer ran; migration-first normalized cutover remains under NEW 9.6 (closed 2026-07-30).
