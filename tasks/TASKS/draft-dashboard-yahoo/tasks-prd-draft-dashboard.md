# Fantasy Hockey Draft Dashboard — Implementation and Completion Tasks

## Relevant Files

- `tasks/TASKS/draft-dashboard-yahoo/prd/prd-draft-dashboard.md` - Source PRD for dashboard functionality, immediate initiatives, weekend priorities, backlog, constraints, and success criteria.
- `tasks/TASKS/draft-dashboard-yahoo/prd/prd-draft-dash-debug.md` - Separate embedded debug/performance checklist; synchronize overlapping fixes without duplicating implementation.
- `tasks/TASKS/draft-dashboard-yahoo/docs/DraftDashboard_Audit.md` - Historical dashboard implementation/quality audit used as evidence, not automatic completion proof.
- `tasks/TASKS/draft-dashboard-yahoo/draft-dashbord-audit.md` - Earlier dashboard inventory and supporting evidence.
- `tasks/TASKS/draft-dashboard-yahoo/prd-yahoo-audit.md` - Yahoo ingestion/mapping audit that owns upstream provider-data remediation.
- `web/components/DraftDashboard/DraftDashboard.tsx` - Main dashboard state and three-panel orchestration.
- `web/components/DraftDashboard/DraftSettings.tsx` - Draft, scoring, source, and roster settings surface.
- `web/components/DraftDashboard/ImportCsvModal.tsx` - Session-local projection CSV import flow.
- `web/__tests__/components/DraftDashboard/ImportCsvModal.test.tsx` - Modal accessibility, clear/reset, Escape, and focus-restoration regressions.
- `web/components/DraftDashboard/ProjectionsTable.tsx` - Available-player list, filtering, source/value controls, sorting, and diagnostics.
- `web/components/DraftDashboard/DraftBoard.tsx` - Pick grid, ownership, keeper/trade presentation, and draft sequence.
- `web/components/DraftDashboard/MyRoster.tsx` - Current-user roster and keeper configuration surface.
- `web/components/DraftDashboard/SuggestedPicks.tsx` - Need/value/risk recommendation surface.
- `web/components/DraftDashboard/ComparePlayersModal.tsx` - Player comparison workflow.
- `web/components/DraftDashboard/DraftSummaryModal.tsx` - Draft summary/export workflow.
- `web/pages/draft-dashboard.tsx` - Route-level SSR loading fallback and client mount boundary.
- `web/__tests__/pages/draft-dashboard.test.tsx` - Server-rendered loading-state regression.
- `web/hooks/useProcessedProjectionsData.tsx` - Projection ingestion, normalization, source blending, and derived data.
- `web/hooks/useProjectionSourceAnalysis.ts` - Per-source coverage/quality analysis.
- `web/hooks/useVORPCalculations.ts` - VORP/VONA/VBD, replacement pools, availability, and position grouping.
- `web/hooks/useVORPCalculations.test.tsx` - Deterministic grouped/split replacement, utility, multi-position, run, and personalized-fill regressions.
- `web/lib/draftDashboard/forwardGrouping.ts` - Shared persisted grouping, effective-roster, eligibility, pool, and roster-allocation contract.
- `web/lib/draftDashboard/forwardGrouping.test.ts` - Grouping, dedupe, roster preservation, allocation, and preference-persistence regressions.
- `web/__tests__/components/DraftDashboard/SuggestedPicks.test.tsx` - FWD label/filter and stale split-preference component regression.
- `web/lib/draftDashboard/keepers.ts` - Versioned keeper validation, migration, transactional import, materialized-pick, and sequence contract.
- `web/lib/draftDashboard/keepers.test.ts` - Keeper identity/bounds/conflict, persistence migration, reset, batch, and next-pick regressions.
- `web/__tests__/components/DraftDashboard/KeeperWorkflow.test.tsx` - Draft Board keeper ownership and accessible bulk-validation feedback regressions.
- `web/lib/draftDashboard/pickTrades.ts` - Versioned snake-aware pick identity, validation/migration, owner resolution, batch, and next-turn contract.
- `web/lib/draftDashboard/pickTrades.test.ts` - Snake-round, bounds, owner, completed-pick, keeper, migration, edit, multiple-trade, and forecast regressions.
- `web/components/DraftDashboard/ManageTradesModal.tsx` - Accessible single/bulk/edit/remove/reset trade-management workflow.
- `web/__tests__/components/DraftDashboard/ManageTradesModal.test.tsx` - Modal focus, edit, bulk feedback, and reset-confirmation regressions.
- `web/hooks/usePlayerRecommendations.ts` - Canonical VBD/VONA/need/risk recommendation scoring and numeric explanation contract.
- `web/hooks/usePlayerRecommendations.test.tsx` - VONA, grouped-FWD need, ordering, explanation, and empty-state regressions.
- `web/lib/draftDashboard/sourceAccuracy.ts` - Retrospective Total/Per-Game source comparison and coverage calculations.
- `web/components/DraftDashboard/ProjectionSourceAccuracy.tsx` - Leakage-isolated source accuracy/coverage view.
- `web/__tests__/components/DraftDashboard/ProjectionSourceAccuracy.test.tsx` - Total/Per-Game, no-weight-mutation, and empty-state regressions.
- `web/lib/draftDashboard/previousSeasonTotals.ts` - Bounded, ordered, batched prior-season totals fetch and latest-season selection contract.
- `web/lib/draftDashboard/previousSeasonTotals.test.ts` - Chunking, pagination, and deterministic latest-row regressions.
- `web/lib/draftDashboard/summaryConfiguration.ts` - Reproducible draft-configuration summary and CSV-payload privacy boundary.
- `web/lib/draftDashboard/summaryConfiguration.test.ts` - Settings coverage and custom-source row/header exclusion regressions.
- `web/lib/draftDashboard/keyboardShortcuts.ts` and focused test - Shared guard preventing global shortcuts from firing inside form, link, button, editable, or modal interactions.
- `web/lib/draftDashboard/draftWorkflow.integration.test.tsx` - Representative import/weight/group/keeper/trade/draft/undo/recommendation/summary contract workflow.
- `web/lib/draftDashboard/projectionFreshness.ts` and focused test - Truthful upstream-unknown, retained-cache-stale-risk, and custom-import timestamp notices.
- `web/__tests__/components/DraftDashboard/ComparePlayersModal.test.tsx` - Comparison focus, trap, close, and restoration regressions.
- `web/__tests__/components/DraftDashboard/DraftSettingsScoring.test.tsx` - Independent goalie/skater expansion and duplicate-safe add/remove/reset regressions.
- `web/__tests__/components/DraftDashboard/DraftSummaryModal.test.tsx` - Configuration rendering and custom-source privacy regressions.
- `web/lib/standardization/` - Shared player-name and CSV-column normalization helpers.
- `web/lib/supabase/pagination.ts` - Shared ordered page-loop and bounded filter-chunk helpers used by dashboard reads.
- `web/lib/supabase/pagination.test.ts` - Range, retry, short-page, limit, and bounded filter-chunk regressions.
- `web/lib/draftDashboard/yahooMapping.ts` - Fail-closed current-game Yahoo identity resolver and aggregate diagnostics.
- `web/lib/draftDashboard/yahooMapping.test.ts` - Same-name goalie/skater/position/team/composite-key mapping regressions.
- `web/lib/draftDashboard/projectionCrosscheck.ts` - Complete multi-source missing-player comparison helper.
- `web/lib/draftDashboard/projectionCrosscheck.test.ts` - Multi-page comparison and partial-error regressions.
- `web/lib/draftDashboard/csvImportContract.ts` - Verified official-source intersection and canonical CSV identity/stat contract.
- `web/lib/draftDashboard/csvImportContract.test.ts` - Official-source intersection and header-standardization regressions.
- `web/lib/draftDashboard/csvImportValidation.ts` - Safe numeric coercion, identity validation, dedupe, and accepted/skipped reporting.
- `web/lib/draftDashboard/csvImportValidation.test.ts` - Valid, duplicate, missing-identity, unsafe-numeric, and normalized-identity regressions.
- `web/lib/draftDashboard/csvImportSession.ts` - In-memory/tab-scoped v3 CSV persistence boundary and legacy migration.
- `web/lib/draftDashboard/csvImportSession.test.ts` - Session-only, independent-tab, migration, and clear regressions.
- `web/lib/draftDashboard/customProjectionSources.ts` - Fail-closed custom CSV registration into the canonical processed-projection source list.
- `web/lib/draftDashboard/customProjectionSources.test.ts` - Selected/disabled/player-type/custom-ID integration regressions.
- `web/lib/draftDashboard/sourceControlPreferences.ts` - Official-only scalar v4 preference persistence and v2/v3 migration.
- `web/lib/draftDashboard/sourceControlPreferences.test.ts` - Defaults, scalar clamping, custom exclusion, migration, and invalid-payload regressions.
- `web/lib/draftDashboard/sourceWeights.ts` - Shared effective-share, normalization, zero-sum, and weighted-projection contract.
- `web/lib/draftDashboard/sourceWeights.test.ts` - Arbitrary/disabled/zero-sum/missing-value weighting regressions.
- `web/lib/draftDashboard/sourceRankImpact.ts` - Deterministic per-player-type projection ranking and material source-change deltas.
- `web/lib/draftDashboard/sourceRankImpact.test.ts` - Independent pool, threshold, direction, and exact-rank regressions.
- `web/hooks/useProjectionSourceAnalysis.ts` - Non-persisting scalar compatibility analysis hook; obsolete percent-style writer removed.
- `web/lib/draftDashboard/projectionInclusionDiagnostics.ts` - Raw-to-normalized-to-processed accounting with bounded identity samples.
- `web/lib/draftDashboard/projectionVisibility.ts` - Shared exact-token position/search/drafted/favorites visibility predicates and reason counts.
- `web/lib/draftDashboard/projectionRowSelection.ts` - Current-team-aware deterministic duplicate official-source selection.
- `web/lib/draftDashboard/fantasyPointRecalculation.ts` - Cached combined-stat fantasy score/per-game/difference recalculation.
- `web/__tests__/components/DraftDashboard/ProjectionsTable.test.tsx` - Drafted visibility, hide behavior, reason counts, and source-inclusion rendering regression.
- `web/scripts/audit-draft-projection-inclusion.ts` - Re-runnable ordered/paginated live official-source identity/duplicate/conflict audit.

### Notes

- This list repairs the missing task-list pair for `prd-draft-dashboard.md`. All rows begin unchecked until current code and runtime behavior are verified against the PRD.
- Keep Yahoo ingestion/mapping remediation in its dedicated initiative; this list owns dashboard consumption, diagnostics, and user-facing fallback behavior.
- Imported CSV data is session-local and must never be written to Supabase or require `AdminOnly`.
- Source-control preferences and other durable UI preferences may use local storage, but imported projection rows must remain tab/session scoped and clearable.
- Preserve the separate embedded tasks in `prd-draft-dash-debug.md`; when one code change satisfies both lists, attach the same verification evidence and synchronize both.
- Add tests for non-trivial parsing, weighting, replacement calculations, pick ownership, keeper allocation, and exclusion logic. Direct visual verification is sufficient for low-risk styling only.
- Yahoo ownership boundary (reconciled 2026-07-12): `B-YAHOO` owns provider credentials, game/season discovery, ingestion freshness, authoritative mapping rows, and unmatched/manual-review persistence. `A-DRAFT` owns complete bounded reads, current-game consumer selection, collision-safe identity use, and honest loading/error/stale/partial diagnostics. Missing or ambiguous Yahoo detail must leave Yahoo-only fields unavailable without dropping otherwise valid projection players.

## Tasks

- [x] 1.0 Verify the claimed dashboard baseline and establish a current implementation map
  - [x] 1.1 Map the PRD's claimed completed layout, draft management, history/undo, search, roster, leaderboard, projection, VORP/VONA/VBD, keyboard shortcut, scoring-manager, persistence, and responsive behaviors to current files and tests. Evidence map: `DraftDashboard.tsx` owns draft/session/history/shortcut orchestration; `DraftSettings.tsx` owns scoring/source/roster/keeper/trade controls; `DraftBoard.tsx` and `MyRoster.tsx` own board/leaderboard/roster/search; `ProjectionsTable.tsx` owns filtering/value/diagnostics; `useProcessedProjectionsData.tsx`, `useVORPCalculations.ts`, and `SuggestedPicks.tsx` own data/value/recommendations; component CSS modules own responsive behavior. No focused legacy dashboard tests were found, so existing 2.6/3.6/4.5/5.4/6.6/7.5/8.5/10.1 rows remain the required regression work (2026-07-12).
  - [x] 1.2 Identify claims that are incomplete, stale, contradicted, or still unverified and append each non-duplicate gap as a `NEW` task with evidence. Evidence: SSR blank state was reproduced/closed as NEW 11.6; live Yahoo/pagination defects were closed as NEW 11.1–11.5; source-preference persistence split-brain is NEW 11.7; the explicit modal-pattern versus current inline-settings conflict is NEW 11.8. Remaining client-interaction claims are not accepted from code presence and stay open in their existing implementation/verification groups (2026-07-12).
  - [x] 1.3 Reconcile dashboard-owned data requirements with the Yahoo ingestion/mapping initiative and document which loading/error/stale states must handle upstream gaps. The Notes section records the ownership boundary; dashboard reads now fail closed for ambiguous/current-game-missing Yahoo identities, preserve non-Yahoo projections, expose aggregate partial-coverage diagnostics, and surface query failures while `B-YAHOO` retains provider/ingestion/manual-review ownership (2026-07-12).
  - [x] 1.4 Confirm all large projection/player reads use explicit pagination or a provably bounded API contract and add remediation tasks for incomplete coverage. Evidence: live player master is 3,336 rows and now uses ordered short-page completion; live auxiliary skater scope is 761 IDs/~7.8 KB and now uses 200-value chunks; the 1,409-row Yahoo map and projection cross-checks use ordered range loops; 16 focused pagination/mapping/cross-check tests and TypeScript pass (2026-07-12).

- [x] 2.0 Verify and finish session-local projection CSV import. Evidence: all 2.1–2.6 contracts are implemented; the consolidated CSV/route group passes 20/20 focused tests plus TypeScript and scoped diff validation (2026-07-12).
  - [x] 2.1 Verify `ImportCsvModal` is accessible from the dashboard header and supports keyboard focus management, drag/drop, file selection, preview, mapping, validation, cancellation, and clearing. Evidence: header trigger and modal wiring exist; named modal, focus trap, Escape, opener restoration, dropzone/file input, first-50 preview, editable header/player mapping, validation alert, cancel, close/reset, and explicit Clear file behavior are implemented. Focused component tests 2/2 and combined A-DRAFT group 19/19 plus TypeScript pass (2026-07-12).
  - [x] 2.2 Reuse shared name/column standardization and enforce the minimum common metric set: player name, team, position, goals, assists, plus the verified intersection baseline required by current sources. Evidence: `csvImportContract` derives skater/goalie minimums from stat keys supported by every official source, retains canonical identity columns, and passes 4/4 contract/header tests (2026-07-12).
  - [x] 2.3 Report parsed, accepted, skipped, duplicate, and invalid rows with actionable reasons; safely coerce numeric fields and preserve unknown columns only as non-critical metadata. Evidence: strict finite-number coercion, required identity/stat validation, canonical ID/name-team-position dedupe, parser warnings, row counts/reasons, and accepted-only import pass 4/4 helper plus 2/2 modal tests (2026-07-12).
  - [x] 2.4 Store imported rows in memory with tab-scoped `sessionStorage` fallback under a versioned key; prove no API/Supabase writes and no cross-tab durable leakage. Evidence: dashboard state owns the active list, `draft.customCsvList.v3` is the sole row fallback, v1/v2 migrate, clear removes all versions, a separate simulated tab reads no rows, local-storage writes are absent, and 4/4 session tests plus TypeScript pass (2026-07-12).
  - [x] 2.5 Register `custom_csv` as an ephemeral projection source and merge it through the same processed-projection and source-analysis contracts as official sources. Evidence: `buildActiveProjectionSources` admits only selected `custom_csv*` entries of the active player type, creates the same `ProjectionSourceConfig` contract used by official sources, keeps rows in the in-memory bypass path, and passes selected/disabled/type/ID regressions (2026-07-12).
  - [x] 2.6 Add or update focused parser/mapping/session persistence tests, including malformed CSV, duplicate players, multi-position values, missing required columns, and clear/reset behavior. Evidence: 20/20 consolidated tests cover parser warnings, missing headers/stats, safe coercion, canonical-ID/name dedupe, multi-position preservation, modal clear/reset/focus/Escape, v1/v2 migration, v3 clear, separate-tab isolation, registration, and SSR resilience (2026-07-12).

- [x] 3.0 Verify and finish projection source toggles and weights. Evidence: 3.1–3.6 and NEW 11.7 are complete; consolidated A-DRAFT tests pass 49/49 plus TypeScript/diff checks, and repeated representative cached weight recomputation measures 11.36–13.37 ms (2026-07-12).
  - [x] 3.1 Render every official source plus `custom_csv` when present with enabled state and a validated `0.0–2.0` weight control in `0.1` steps. Evidence: official config defaults and selected `custom_csv*` registration share the active-source contract; DraftSettings renders both player types/custom entries with toggle, 0–2 numeric input, 0.1 step, effective share, and bounded debounced updates; focused config/registration tests pass (2026-07-12).
  - [x] 3.2 Default to all available sources enabled with normalized equal weights and persist only source-control preferences under a versioned local-storage key. Evidence: official sources default selected at equal scalar weight 1, `draft.sourceControls.v4` persists only known official IDs, filters custom session IDs, clamps 0–2, safely migrates v2/v3 arrays, and passes 4/4 preference tests (2026-07-12).
  - [x] 3.3 Centralize weight normalization and zero-sum behavior so aggregation, VORP/VONA/VBD, source analysis, ranks, and Suggested Picks use the same effective shares. Evidence: shared `sourceWeights` drives DraftSettings shares/normalization and processed-stat weighted averages; disabled/zero weights contribute zero, all-zero yields null rather than fabricated zero, missing source values renormalize over contributors, and downstream VORP/rank/suggestion consumers use the same processed player values. Focused tests pass 3/3 (2026-07-12).
  - [x] 3.4 Show effective share per source and an honest rank-impact indicator when source changes materially alter a player's result. Evidence: source cards/chips use the shared normalized share contract including pending edits; completed source-control recomputes compare deterministic skater/goalie projection ranks, show accessible up/down badges only at an absolute three-rank threshold, and disclose exact previous/current pool ranks. Focused rank tests pass 2/2 (2026-07-12).
  - [x] 3.5 Memoize recomputation around relevant source/weight/data changes and verify the PRD's approximately 200 ms desktop interaction target on representative data. Evidence: the hook reuses complete raw projection/actual/Yahoo inputs when only scalar weights change and invalidates on source membership, season, custom-source version, or refresh. Deterministic 1,000-player × 24-stat × 8-source cached recompute plus pool-ranking runs measured 11.36–13.37 ms; cache-key regressions pass (2026-07-12).
  - [x] 3.6 Add focused tests for normalization, disabled sources, zero sum, arbitrary weights, imported-source inclusion/removal, persistence migration, and downstream recalculation. Evidence: preference/weight/custom-source/rank suites cover all named cases, including explicit removal and 20→25 downstream projection recalculation; the consolidated A-DRAFT group passes 48/48 plus TypeScript/diff validation (2026-07-12).

- [x] 4.0 Diagnose and close missing-player defects. Evidence: 4.1–4.6 and NEW 11.9–11.12 are complete; live official-source coverage/duplicate evidence, 63/63 focused regressions, TypeScript, and scoped diff checks pass (2026-07-12).
  - [x] 4.1 Add a development/diagnostic view that reconciles total source rows, normalized players, included players, and exclusions by reason. Evidence: the diagnostics panel now reports raw/valid/unique/processed/invalid/duplicate/missing counts by player type, bounded identity samples, Yahoo unresolved ID samples, and shared position/search/drafted/favorites exclusion counts over the complete pool (2026-07-12).
  - [x] 4.2 Audit fuzzy matching, canonical name normalization, multi-position parsing, goalie detection, drafted-player filtering, search terms, ADP/null handling, season totals, and pagination as independent exclusion causes. Evidence: custom-only fallback/name normalization remains bounded and visible; exact eligibility-token predicates cover multi-position/goalie/grouped-forward cases; the table now receives drafted and available players; search/favorites/drafted reasons share the render predicates; null ADP remains visible; season expansion does not filter the pool; all official reads are ordered/paginated. Focused and live audits pass (2026-07-12).
  - [x] 4.3 Persist or display unmatched source/player-master identities through the Yahoo mapping review contract instead of silently dropping them. Evidence: invalid source identities and valid IDs missing after processing render as bounded samples, Yahoo unmapped/current-game-missing/unresolved IDs expose bounded review samples, and the UI states that B-YAHOO owns authoritative mapping/manual-review persistence (2026-07-12).
  - [x] 4.4 Reproduce each verified missing-player case, implement the narrowest root-cause fix, and keep the exclusion reason visible when omission is intentional. Evidence: live paging reproduced conflicting duplicate-ID rows in six skater sources and identical DTZ goalie duplicates; selection now normalizes team aliases, prefers the player's current team, then GP/stable order. The stale named-player console probes were not valid exclusion evidence and were removed; intentional UI filters now retain reason counts (2026-07-12).
  - [x] 4.5 Add regression tests for affected identities plus normalization, multi-position, null ADP, source-toggle, drafted, and search-filter behavior. Evidence: duplicate/current-team/team-alias tests, canonical CSV identity tests, multi-position/goalie/grouped-forward visibility, null ADP, custom source inclusion/removal/toggle, drafted hide, search/team search, pagination, and component diagnostics are covered in the 63/63 group (2026-07-12).
  - [x] 4.6 Verify corrected players appear with the right source, stats, position eligibility, value metrics, and availability state. Evidence: deterministic duplicate-row tests preserve selected team/stat rows, shared visibility tests preserve complete eligibility, weighted/scoring tests recompute value outputs, and the component regression proves drafted rows appear until explicitly hidden and remain diagnosed (2026-07-12).

- [x] 5.0 Verify and finish the grouped-forwards option. Evidence: 5.1–5.4 and NEW 11.13–11.14 are complete; the consolidated A-DRAFT suite passes 64/64 plus TypeScript and scoped diff validation (2026-07-12).
  - [x] 5.1 Add or verify a persisted, explained `groupForwards` setting that switches C/LW/RW replacement pools to FWD without changing goalie or defense pools. Evidence: the v1 preference safely loads/saves only `split`/`fwd`; toggling derives FWD from preserved custom C/LW/RW counts while D/G remain unchanged, and grouped total edits redistribute deterministically without losing the split configuration (2026-07-12).
  - [x] 5.2 Centralize grouped-position mapping in `useVORPCalculations` and apply it consistently to full/available pools, starters, utility allocation, replacement indices, VORP/VONA/VBD, and run forecasts. Evidence: shared normalization/grouping builds deduplicated full/available pools, effective starters, FWD utility/replacement/rank/expected-run inputs, personalized fills, and FWD best-position metrics; synthetic values prove the rank-four baseline is 70 rather than the duplicated 80 (2026-07-12).
  - [x] 5.3 Update table bands, sorting, tooltips, Draft Board, roster visuals, and labels to reflect FWD when active and separate positions when inactive. Evidence: Projections Table display/filter/run forecast, Suggested Picks filters/cards/need adjustment, roster needs/progress/cards, slot movement, team allocation, and Draft Summary use the same FWD/D/G contract; split mode retains C/LW/RW/D/G (2026-07-12).
  - [x] 5.4 Add deterministic synthetic-pool tests for grouped and ungrouped calculations, utility allocation, multi-position eligibility, scarcity, and toggle persistence. Evidence: 9 focused helper/hook tests cover dedupe, effective roster counts, proportional edits, grouped allocation, persistence, grouped/split replacements, utility, run estimates, and personalized fills; a component regression proves stale C filters become FWD and filter the recommendation cards correctly (2026-07-12).

- [x] 6.0 Implement and verify keeper preloading with forfeited-pick semantics. Evidence: 6.1–6.6 and NEW 11.15–11.17 are complete; consolidated A-DRAFT verification passes 72/72 plus TypeScript, Sass compilation, and scoped diff checks (2026-07-12).
  - [x] 6.1 Define a stable keeper contract with canonical player ID, owning team ID, kept round/pick, validation state, and persistence version. Evidence: `KeeperEntry` v1 stores `status=valid`, canonical string IDs, round/pick-in-round/global-pick identity, team ownership, and legacy migration with duplicate player/pick collapse (2026-07-12).
  - [x] 6.2 Provide an accessible single/bulk keeper import flow with player/team/round validation, duplicate prevention, and clear conflict messaging. Evidence: controlled single-player input returns visible status/alert feedback; JSON or headered/headerless CSV batches validate transactionally and report row-specific errors without partial writes (2026-07-12).
  - [x] 6.3 Add keepers to the correct roster, mark the corresponding pick as completed/forfeited and owned by that team, and remove the player from all availability/recommendation pools. Evidence: valid keepers materialize as typed completed picks on their selected team, Draft Board keeper ownership overrides underlying original/traded display ownership, drafted-ID filtering removes the player from table/recommendations/VONA, and the board shows a keeper badge/forfeited tooltip (2026-07-12).
  - [x] 6.4 Ensure keeper picks update replacement values, roster progress, turn sequence, contribution graphs, Draft Board visuals, undo/reset behavior, and summary/export output consistently. Evidence: materialized picks feed existing team/roster/progress/value paths; next-open-pick skips consecutive completed keeper slots; reset rematerializes keepers rather than splitting configuration from availability; summary labels and excludes keepers from steal/reach highlights; remaining baseline changes while full baseline stays stable (2026-07-12).
  - [x] 6.5 Define and test conflicts among keepers, traded picks, invalid rounds, duplicate players, and already-completed draft state. Evidence: validation rejects unknown player/team, out-of-range round/pick, duplicate keeper player/pick, already-drafted player, and completed pick; keeper team owns the forfeited display while the underlying trade remains intact for restoration after keeper removal; team-count and roster-round reductions are blocked while incompatible keepers exist (2026-07-12).
  - [x] 6.6 Add focused state/calculation/component tests and run an integrated keeper smoke scenario. Evidence: 12 keeper/helper/hook/component assertions cover versioning, migration, conflicts, transactional import, materialization/reset/removal, sequence skipping, board ownership, accessibility feedback, availability removal, and remaining/full FWD replacement; consolidated suite passes 72/72 (2026-07-12).

- [x] 7.0 Implement and verify custom draft-pick trades. Evidence: 7.1–7.5 and NEW 11.18–11.19 are complete; consolidated A-DRAFT verification passes 78/78 plus TypeScript, three touched Sass compilations, and scoped diff checks (2026-07-12).
  - [x] 7.1 Define a versioned pick-ownership mapping keyed by stable global pick identity with original and current team ownership. Evidence: `PickTradeEntry` v1 stores valid status, round/pick/global identity, snake-derived original owner, and current owner; legacy maps/arrays migrate through validation into sorted unique entries (2026-07-12).
  - [x] 7.2 Build an accessible Manage Trades modal supporting single and bulk entries, validation, edit/remove, reset, and conflict display. Evidence: focus-managed/trapped/Escape/backdrop modal supports controlled single upsert, transactional JSON/CSV input with row feedback, edit/remove, two-step reset, and keeper-at-pick warnings; component regression passes (2026-07-12).
  - [x] 7.3 Apply ownership changes to Draft Board, current turn, My Roster, keeper allocation, upcoming owner sequence, recommendations, run/risk forecasts, and summary/export behavior. Evidence: one resolver drives current turn and Draft Board; My Roster receives that turn; next-pick risk skips completed keeper turns and honors trades; drafted team assignment follows current owner; keeper display ownership takes precedence without deleting the underlying trade; summaries/bookmarks expose trades (2026-07-12).
  - [x] 7.4 Persist trades locally without corrupting older saved settings and recompute only caches dependent on pick order/ownership. Evidence: local session, v2 snapshot, and portable bookmark persist v1 entries plus a legacy compatibility map; restore migrates either shape; trade changes update owner/turn/forecast/board/summary state without invalidating projection-source processing caches (2026-07-12).
  - [x] 7.5 Add tests for traded snake-round picks, keeper/trade conflicts, multiple trades, undo/reset, persistence, visuals, and downstream calculations. Evidence: focused helper/component tests cover snake original owners, invalid/no-op/completed picks, keeper precedence, multiple transactional trades, same-pick edits, legacy migration, trade-aware next-turn forecasts, board ownership, modal edit/remove/reset/focus/bulk feedback; trade configuration remains independent of draft undo and has explicit reset (2026-07-12).

- [x] 8.0 Verify and finish Suggested Picks and projection-source accuracy surfaces. Evidence: 8.1–8.5 and NEW 11.20–11.21 are complete; consolidated A-DRAFT verification passes 86/86 plus TypeScript, four touched Sass compilations, and scoped diff checks (2026-07-12).
  - [x] 8.1 Verify Suggested Picks ranks only available eligible players using the documented need-adjusted VBD, VONA, and next-pick risk inputs. Evidence: the available-player prop remains the sole input pool; canonical score combines 70% VBD/30% VONA, optional normalized roster/category need, and a bounded gone-by-next-owned-pick risk boost; grouped FWD eligibility and trade/keeper-aware next-pick timing are applied, and new sessions default to canonical rank (2026-07-12).
  - [x] 8.2 Provide concise explanations and draft/compare actions whose state updates remain consistent with the table, roster, and board. Evidence: cards show numeric VBD, VONA, need, baseline, and gone-risk tags; Draft uses the shared draft mutation; Compare maintains a two-player queue and opens the existing shared comparison modal against the complete player pool; placeholder View/More controls are removed (2026-07-12).
  - [x] 8.3 Build or finish a per-source accuracy/coverage view with clear Total versus Per-Game semantics and no retrospective data leakage into draft-time weighting. Evidence: the new table reports accuracy, matched comparison coverage, and counts per contributing source; Total compares loaded totals, Per Game divides counting stats by source-projected versus actual GP while leaving existing rates unchanged; copy disclaims preseason-vintage inference and retrospective use (2026-07-12).
  - [x] 8.4 Keep manual source weights distinct from evidence-based source quality until a documented automatic weighting policy is approved. Evidence: accuracy calculations are a pure read-only projection/actual analysis with no source-control callback, slider, persistence write, or ranking input; the panel explicitly states it never changes manual weights or draft-time projections (2026-07-12).
  - [x] 8.5 Add focused ranking/explanation/empty-state tests for Suggested Picks and accuracy summaries. Evidence: 9 focused hook/helper/component tests cover VONA ordering, FWD need, risk rank, numeric explanations, working actions, recommendation empty state, Total/Per-Game math, no weight control, and accuracy empty state; consolidated suite passes 86/86 (2026-07-12).

- [x] 9.0 Complete remaining dashboard productivity and scoring backlog. Evidence: 9.1–9.5 and NEW 11.22–11.24 close with 102/102 consolidated A-DRAFT tests, final 9/9 focused tests, TypeScript, four Sass compilations, scoped diff checks, and desktop/tablet/mobile browser layout measurements (2026-07-12).
  - [x] 9.1 Verify or implement prior-season inline row expansion without issuing one network request per row. Evidence: one shared current-player batch uses unique 200-ID chunks, ordered inclusive page loops, deterministic latest rows, and shared loading/error/cache state; expanding multiple rows issues one table batch in regression coverage (2026-07-12).
  - [x] 9.2 Bring the goalie dynamic scoring-stat manager to parity with the completed skater manager, including duplicate safeguards and independent expansion state. Evidence: goalie/skater managers independently expand and support duplicate-safe add, edit/remove, and reset; GA/GAA labels remain distinct and SHP is not a goalie default; focused component regression passes (2026-07-12).
  - [x] 9.3 Verify Compare Players modal data, selection limits, reset behavior, accessibility, and downstream state consistency; synchronize the embedded debug checklist. Evidence: table and Suggested Picks use the shared complete-player modal; table selection is capped at exactly two with visible status and Clear; initial focus, Tab trap, Escape/backdrop close, and opener restoration pass focused tests; the embedded checklist is synchronized (2026-07-12).
  - [x] 9.4 Verify Draft Summary captures teams, picks, keepers, trades, source settings, custom projections metadata, and value metrics without exposing session CSV contents unexpectedly. Evidence: summary/bookmark v3 captures all named draft/value/source settings and sanitized custom metadata while excluding CSV headers/rows; helper and modal privacy regressions pass (2026-07-12).
  - [x] 9.5 Decide from measured layout evidence whether JavaScript expand-button width detection is necessary; retain CSS-only behavior when it remains correct. Evidence: CSS-only full-subgroup row behavior measures 95% width with no subgroup/grid overflow at 2056px, 900px, and 520px; no JavaScript detector was added (2026-07-12).

- [x] 10.0 Run integrated correctness, performance, accessibility, and resilience verification. Evidence: 36 files/114 tests, TypeScript, Sass, diff checks, HTTP-200 route readback, warm-load/weight/filter measurements, and the representative contract workflow pass; browser localhost interaction was policy-blocked, so responsive evidence reuses the already recorded 2056/900/520 measurements rather than bypassing the browser boundary (2026-07-12).
  - [x] 10.1 Add/update tests for parsing, identity reconciliation, source weighting, VORP/VONA/VBD, grouped forwards, keepers, trades, recommendations, persistence, and meaningful failure branches. Evidence: consolidated A-DRAFT selection passes 36 files/114 tests, including one cross-contract workflow (2026-07-12).
  - [x] 10.2 Verify loading, empty, upstream-error, stale-data, partial-source, invalid-import, and no-enabled-source states without presenting fabricated zero values. Evidence: settled-source, table-state, CSV-validation, and freshness tests pass; official upstream timestamps are explicitly unknown, refresh-failed retained data is labeled potentially stale, and custom notices use only real import timestamps (2026-07-12).
  - [x] 10.3 Verify keyboard operation, focus trapping/restoration, shortcut input safety, labels/descriptions, table semantics, color-independent state cues, and responsive/mobile usability. Evidence: shared shortcut guard blocks form/link/button/editable/dialog targets; modal/table/component regressions verify focus/labels/status text; prior 2056/900/520 browser measurements plus Sass compilation cover responsive behavior (2026-07-12).
  - [x] 10.4 Run a representative integrated draft: import CSV, adjust weights, toggle grouped forwards, preload keepers, apply trades, draft/undo players, compare players, inspect suggestions, and review the summary. Evidence: the new cross-contract workflow executes import, weights, FWD grouping, keeper/trade ownership, draft/undo, recommendations, and privacy-safe summary in one run; focused UI tests cover working draft/compare actions and exactly-two modal behavior (2026-07-12).
  - [x] 10.5 Measure initial load, projection recompute, filtering/search, and large-table interaction; remediate verified bottlenecks without speculative architecture replacement. Evidence: clean dev compile/route returned HTTP 200; warm SSR requests measured 0.780/0.213/0.216s, 1,000×24×8 source recompute/rank measured 12.69ms, and 50 filter/search passes over 10,000 players measured 71.14ms total/1.423ms each (2026-07-12).
  - [x] 10.6 Synchronize all overlapping rows in `prd-draft-dash-debug.md`, this task list, and the master ledger and record verification evidence. Evidence: completed bookmark/multi-CSV debug rows and all group-10/NEW source rows are mirrored in the master ledger; SHA remains explicitly open under A-DRAFT-DEBUG (2026-07-12).

## NEW Tasks

- [x] NEW 11.0 Append every verified implementation gap, regression, open question, manual/provider dependency, and optimization discovered during execution here before closing the initiative. Evidence: all findings through shortcut safety NEW 11.27 were appended before remediation; only the explicit settings-boundary decision NEW 11.8 remains (2026-07-12).
- [x] NEW 11.1 **P1 correctness:** Resolve duplicate Yahoo-map candidates against current-game player details before assigning ADP/position data. Live evidence found 1,409 mapping rows for 761 projected skater IDs, 656 duplicate groups, four conflicting non-null Yahoo-ID groups, and two ID-versus-null groups. The resolver now fails closed on ambiguous/current-game-missing candidates and selects Connor Murphy `5381`, Sebastian Aho `6777`, and Elias Pettersson `7520` from current-game type/position/team evidence; focused regressions pass (2026-07-12).
- [x] NEW 11.2 **P2 reliability/performance:** Bound every auxiliary player-ID `.in(...)` filter in `useProcessedProjectionsData.tsx` and deterministically paginate every multi-page Yahoo-map/player read. All actual-stat, mapping, composite-key, and numeric-ID filters now use unique 200-value chunks plus ordered short-page loops; shared chunk/page regressions pass (2026-07-12).
- [x] NEW 11.3 **P2 stale-state/observability:** Replace the invalid Yahoo player-key prefix fallback with an explicit current-game detail contract and surface coverage/conflict diagnostics without unconditional production debug payloads. `CURRENT_YAHOO_GAME_ID` is centralized, map IDs are normalized independently of player keys, prior-game fallback is removed, and the existing diagnostics panel reports selected/unmapped/conflicting/current-game-missing counts (2026-07-12).
- [x] NEW 11.4 **P2 completeness/reliability:** Make the CSV import player-master lookup deterministic and bounded. The live 3,336-row player master now loads by ordered short pages without the 50,000-row cap; last-name fallback uses bounded ordered chunks/pages; failures surface in the modal and named-player production logs are removed (2026-07-12).
- [x] NEW 11.5 **P2 diagnostic correctness:** Paginate the Projections Table missing-player cross-check for every configured source rather than relying on current sub-1,000-row table sizes. The extracted helper groups source ownership, uses ordered short-page reads, fails on partial errors, and passes a synthetic three-page regression (2026-07-12).
- [x] NEW 11.6 **P2 resilience/accessibility:** Replace the route-level `return null` before client mount with a meaningful server-rendered loading state. `/draft-dashboard` now SSRs a named `main`, `aria-busy=true`, and truthful loading status without fabricated data. Server-render regression, TypeScript, and a no-script browser readback pass with no overlay/console warning (2026-07-12).
- [x] NEW 11.7 **P1 persistence correctness:** Reconcile projection-source preference persistence into one active contract. `useProjectionSourceAnalysis.ts` owns `draft.sourceControls.v3` percent-style local storage but has no consumers; the live dashboard owns scalar `0.0–2.0` controls, persists them only inside the tab-scoped resume snapshot, and omits them from `draftDashboard.session.v1`. Acceptance: one versioned local-storage contract for official skater/goalie preferences, safe migration from v2/v3/legacy session shapes, custom CSV IDs remain session-only, reload restores without a draft-resume decision, and focused migration/zero-sum/custom-source tests pass. Evidence: the compatibility hook no longer writes storage; v4 is the sole durable official-only owner, preserves old effective proportions, filters custom IDs, restores independently of draft resume, and passes focused migration/weighting tests plus TypeScript (2026-07-12).
- [x] NEW 11.8 **P2 product/UX conflict checkpoint:** Owner approved option A on 2026-07-13: retain the completed/tested inline/collapsible `DraftSettings` workstation, supersede the old all-settings-modal sentence for A-DRAFT, and leave any future presentation reconsideration to `B-DRAFT-STYLE`. Workflow-specific CSV/trades/compare/summary dialogs remain modal; no broad UI rewrite was required.
- [x] NEW 11.9 **P2 production observability/privacy:** Remove remaining unconditional per-render/per-player Draft Dashboard debug logs and named-player traces; retain bounded development-only phase/diagnostic logging and user-visible aggregate diagnostics instead. Evidence: render payload, Yahoo detail, Marchenko, Jack Hughes, and bookmark logs are removed; development-only aggregate/phase logs and user diagnostics remain (2026-07-12).
- [x] NEW 11.10 **P1 duplicate-source identity correctness:** Resolve duplicate official projection rows deterministically against the player's current team, then games played and a stable tie-breaker. Live paginated evidence found conflicting duplicate IDs in AG (2), DFO skaters (7), DTZ skaters (13), FHFH (4), Kubota (8), and Laidlaw (2); DTZ goalie duplicates are projection-field-identical but still require deterministic collapse. Evidence: abbreviation/full-name/nickname team normalization and deterministic selection pass 4/4 tests; live audit is re-runnable (2026-07-12).
- [x] NEW 11.11 **P1 stale scoring correctness:** Replace the placeholder fantasy-point-only fast path that updates a cache key and returns without recalculating player scores. Recompute projected/actual/per-game/difference fields and round summaries from cached combined stats whenever scoring settings change. Evidence: the cache path recalculates/clones player scores and round summaries without refetching; projected-only inputs no longer fabricate actual zero; 3/3 focused tests pass (2026-07-12).
- [x] NEW 11.12 **P1 stale round-summary correctness:** Include team count in processed/full cache identity so changing league size cannot reuse round boundaries and summaries generated for a different number of teams. Evidence: base/full cache identity and snapshots now include team count; changes rebuild from cached raw inputs under the new round boundary (2026-07-12).
- [x] NEW 11.13 **P1 grouped-forward calculation correctness:** Stop deleting split roster counts while VORP still reads them; deduplicate multi-position forward pools, use FWD for full/available replacement and VONA ranks, and apply FWD personalized-fill/expected-run semantics without changing D/G. Evidence: shared pool/effective-roster/allocation helpers and deterministic hook tests prove the corrected contract; D/G baselines remain 40/20 in the synthetic pool (2026-07-12).
- [x] NEW 11.14 **P2 grouped-forward product consistency:** Preserve custom C/LW/RW counts across FWD toggles and expose derived FWD counts/needs/progress/filters/recommendation labels consistently across settings, roster, table, and Suggested Picks. Evidence: all named surfaces now receive or derive the grouping mode; FWD component filtering and preference/roster preservation regressions pass (2026-07-12).
- [x] NEW 11.15 **P1 keeper state correctness:** Replace silent keeper pick/player replacement and reset inconsistency with one versioned validated keeper contract. Reject invalid player/team/round/pick, duplicate player, occupied pick, and already-completed non-keeper conflicts; materialize keepers as explicitly typed completed picks, preserve them coherently across draft reset, and remove only the matching keeper draft entry. Evidence: keeper v1 validation/materialization/migration and reset/remove wiring pass focused and consolidated verification (2026-07-12).
- [x] NEW 11.16 **P2 keeper import/ownership UX:** Add accessible transactional bulk keeper input with row-specific validation feedback and ensure Draft Board ownership/tooltips, roster/summary presentation, and persistence identify the keeper team even when the underlying original/traded pick owner differs. Evidence: JSON/CSV bulk UI uses status/alert output; transactional tests and keeper-over-trade board ownership regression pass (2026-07-12).
- [x] NEW 11.17 **P1 replacement-baseline correctness:** Honor the persisted `remaining` versus `full` baseline mode in `useVORPCalculations`. Keeper/drafted removal must change replacement values in remaining-pool mode while full-pool mode stays stable; add deterministic evidence for both paths. Evidence: a materialized keeper removes the 100-point forward; remaining FWD replacement moves 70→60 while full remains 70 (2026-07-12).
- [x] NEW 11.18 **P1 trade ownership correctness:** Replace unchecked `round-pick → team` mutation with a versioned stable trade contract that derives global pick/original owner across snake rounds, validates owner/bounds/completed state, migrates legacy overrides, and drives current turn/board/keeper/downstream ownership from one resolver. Evidence: the shared v1 contract/resolver drives all named consumers and passes focused snake/migration/conflict/forecast regressions (2026-07-12).
- [x] NEW 11.19 **P2 trade management UX:** Replace the incomplete inline-only controls with an accessible Manage Trades workflow supporting single and transactional JSON/CSV bulk entry, visible validation/conflict warnings, edit/remove/reset, and truthful keeper-at-pick precedence. Evidence: modal component and integrated quick-control feedback pass accessibility/edit/bulk/reset/keeper-warning checks (2026-07-12).
- [x] NEW 11.20 **P1 recommendation ranking correctness:** Make the canonical Suggested Picks rank explicitly combine VBD, VONA, positional/category need, and probability of being gone by the next owned pick; honor grouped FWD eligibility, default new sessions to this canonical rank, and expose numeric reason evidence instead of generic tags. Evidence: canonical score/explanation contracts and deterministic hook/component ordering tests pass (2026-07-12).
- [x] NEW 11.21 **P2 source accuracy/action completeness:** Implement the missing per-source Total/Per-Game retrospective accuracy/coverage view with explicit leakage warning and no weight mutation; replace Suggested Picks placeholder actions with working draft/compare behavior and test empty/explanation/action states. Evidence: pure accuracy analysis/read-only panel plus shared draft/compare wiring and focused action/empty tests pass (2026-07-12).
- [x] NEW 11.22 **P1 prior-season query scalability:** Replace per-expanded-player Supabase reads with bounded ID chunks plus deterministic page loops and a shared latest-season cache so expanding many rows cannot issue one request per row or truncate multi-page results. Evidence: shared batch helper and table wiring use 200-ID chunks, ordered short-page loops, deterministic latest rows, and one multi-expanded-player batch; focused tests pass (2026-07-12).
- [x] NEW 11.23 **P2 comparison workflow correctness/accessibility:** Enforce the documented two-player selection limit with visible feedback and give Compare Players initial focus, focus trap, Escape/backdrop close, and opener focus restoration across both table and Suggested Picks entry points. Evidence: exactly-two cap/status/Clear and complete modal focus lifecycle pass focused table/modal regressions (2026-07-12).
- [x] NEW 11.24 **P2 summary reproducibility/privacy:** Add keeper/trade, source enabled/weight, custom-source metadata, grouping/baseline/need/value settings to Draft Summary/bookmark evidence without embedding imported CSV row contents or other session-only payloads. Evidence: one sanitized builder drives summary/bookmark v3 and tests prove metadata inclusion with CSV header/row exclusion (2026-07-12).
- [x] NEW 11.25 **P1 partial-source resilience:** Per-source settled reads preserve healthy sources, fail only when every enabled source fails, and the dashboard retains a healthy/previously loaded player-type pool when the other type refreshes or errors; focused regressions pass (2026-07-12).
- [x] NEW 11.26 **P2 state truthfulness/freshness:** Explicit no-enabled/partial/upstream-error notices and the bounded freshness helper label refresh-failed retained data as potentially stale, expose upstream timestamp ownership, and use only real custom import times; focused regressions pass (2026-07-12).
- [x] NEW 11.27 **P2 keyboard safety:** One shared target guard prevents dashboard/Suggested Picks shortcuts from firing inside form, button, link, editable, role-based, or dialog interactions while preserving body shortcuts; 6/6 focused tests pass (2026-07-12).
