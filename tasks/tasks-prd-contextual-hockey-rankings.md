## Relevant Files

- `tasks/TASKS/prd-contextual-hockey-rankings.md` - Source PRD for contextual rankings requirements, phasing, and acceptance criteria.
- `migrations/20260605_create_contextual_rankings.sql` - Supabase migration for confirmed schema gaps: central metric definitions, optional precomputed entity rankings, composite skater rating scaffold, and contextual indexes over reused source tables.
- `migrations/20260606_add_rolling_shot_attempts_per_60.sql` - Additive Supabase migration that stages `shot_attempts_per_60_*` rolling columns for NST ICF-based shot-attempt rankings.
- `web/lib/rankings/metricDefinitions.ts` - Shared TypeScript metric registry types, constants, and display metadata used by API and UI.
- `web/lib/rankings/skaterWindowAggregation.ts` - Ranking-facing extraction layer that turns existing `rolling_player_game_metrics` rows into Phase 1 skater production-window aggregates without duplicating the rolling rebuild pipeline.
- `web/lib/rankings/skaterWindowAggregation.test.ts` - Unit tests for skater production-window semantics, denominator safety, points/60 derivation, split-strength samples, and refresh-surface reuse.
- `web/lib/rankings/skaterDeploymentAggregation.ts` - Ranking-facing deployment aggregation helpers for EV line/pair buckets, projection-role boundary normalization, PP/PK buckets, role consistency, confidence, and team-scoped traded-player contexts.
- `web/lib/rankings/skaterDeploymentAggregation.test.ts` - Unit tests for deployment bucket normalization, projection defense-role normalization, source preference, EV TOI fallback, special-teams separation, trade scoping, and confidence labels.
- `web/lib/rankings/rankingCalculator.ts` - Contextual peer-group ranking helper for raw metric derivation, lower-is-better normalization, dense ranks, better-than percentiles, minimum samples, peer warnings, and sample confidence.
- `web/lib/rankings/rankingCalculator.test.ts` - Unit tests for contextual ranking math, tie behavior, peer groups, minimum samples, unavailable metrics, and sample confidence labels.
- `web/lib/rankings/rankingTypes.ts` - Shared request, response, row, filter, peer-group, sample-confidence, and URL-state types.
- `web/lib/rankings/rankingTypes.test.ts` - Unit tests for contextual rankings query parsing, defaults, peer-group derivation, and invalid params.
- `web/lib/rankings/rankingQueries.ts` - Server-side Supabase/RPC access for contextual leaderboard data, including exact-snapshot rolling lookup optimized by the contextual index.
- `web/lib/rankings/rollingRankingSelectFields.ts` - Shared rolling-player column allowlist used by rankings reads and rolling upsert storage compaction.
- `tasks/artifacts/archive/rpgm-pass-history/rolling-player-game-metrics-schema-surface-audit.md` - Prior rolling-player schema surface audit reused for the durable storage model review.
- `migrations/20260609_create_rolling_player_metric_support_payloads.sql` - Supabase migration adding a keyed JSONB support table for historical/recompute-only rolling payloads.
- `web/lib/supabase/Upserts/rollingPlayerSupportPayload.ts` - TypeScript constants and payload types for the durable rolling support table contract.
- `web/pages/api/v1/db/update-power-play-combinations/[gameId].ts` - Power-play combination updater now reuses the repo's existing NHL HTML shift-report fallback when JSON shiftcharts are empty.
- `web/__tests__/pages/api/v1/db/update-power-play-combinations-game.test.ts` - Unit tests for PP-combination JSON shiftcharts and HTML shift-report fallback behavior.
- `web/types/nhlRawGamecenter.d.ts` - Type declaration for the existing JS raw-gamecenter helper exports used by TypeScript API routes.
- `web/playwright.config.ts` - Playwright E2E config for repo-standard real-browser tests using `dev:stable`.
- `web/e2e/rankings.spec.ts` - Real-browser `/rankings` smoke covering matrix load, pagination, More Filters, and Metric Explorer navigation.
- `.github/workflows/rankings-e2e.yml` - Manual GitHub Actions workflow for running the `/rankings` Playwright smoke on a Linux runner against either a deployed `base_url` or a local Next.js server with Supabase secrets.
- `web/pages/api/v1/contextual-rankings.ts` - Compatibility-preserving contextual rankings API endpoint; leaves the existing Start Chart `/api/v1/rankings` scaffold untouched.
- `web/__tests__/pages/api/v1/contextual-rankings.test.ts` - Route tests for method handling, query validation, normalized request handoff, and unavailable metric metadata.
- `web/pages/rankings.tsx` - URL-driven first-release contextual rankings page with skater leaderboard, filter state, coming-soon entity states, and API-backed table preview.
- `web/components/Rankings/RankingsFilters.tsx` - Filter controls for entity, season, window, position, deployment, strength, metric, minimum samples, and team ID.
- `web/styles/Rankings.module.scss` - Styling for the contextual rankings page, filters, states, and first-release table preview.
- `web/next.config.js` - Next.js configuration updated to use polling file watching in dev so local verification does not hit `EMFILE` watcher exhaustion.
- `web/lib/rankings/rankingFormatters.ts` - Formatting helpers for metric values, percentiles, sample labels, and explanation text.
- `web/lib/rankings/rankingFormatters.test.ts` - Unit tests for value formatting, percentile copy, sample labels, and safe explanation generation.
- `web/lib/rankings/rankingUrlState.ts` - Shared URL-state parser/serializer for `/rankings` filters, sort state, request paths, and client request metadata.
- `web/lib/rankings/rankingUrlState.test.ts` - Unit tests for default URL state, invalid-query fallback behavior, API path serialization, and coming-soon entity handling.
- `migrations/20260608072046_add_skater_composite_context_key.sql` - Supabase migration adding peer/deployment context columns to `skater_composite_ratings` and expanding its primary key to avoid deployment-scoped composite collisions.
- `migrations/20260608073908_add_rolling_player_penalties_taken_metrics.sql` - Additive Supabase migration adding selected-window, season, 3YA, and career rolling fields for penalties taken and penalties taken/60.
- `web/lib/rankings/rankingCalculator.lowerIsBetter.test.ts` - Helper test that verifies lower-is-better ranking order without enabling unavailable Phase 3 source data.
- `web/lib/rankings/matrixMetricRegistry.ts` - Matrix column registry updated to expose verified live MCM/BEAST and sparse live Results Luck columns while keeping unsupported metrics planned.
- `web/lib/rankings/matrixMetricRegistry.test.ts` - Unit tests for default live/planned matrix column behavior, including sparse live Results Luck registry metadata.
- `web/lib/rankings/playerMatrix.ts` - Matrix API builder updated with Supabase-backed composite row overlays, live MCM/BEAST/Results Luck cells, and composite sorting.
- `web/lib/rankings/playerMatrix.test.ts` - Unit tests for matrix pagination limits, composite row overlays, and Results Luck sorting through the matrix API path.
- `web/components/Rankings/PlayerSnapshotPanel.tsx` - Snapshot panel updated to render live composite offense/defense/MCM/BEAST values from selected matrix rows.
- `web/components/Rankings/PlayerSnapshotPanel.test.tsx` - Component tests for live composite cards and generated metric bullets.
- `web/components/Rankings/PlayerMatrixTable.test.tsx` - Component tests updated for the matrix row composite payload.
- `web/lib/rankings/skaterCompositeMethodology.ts` - Methodology contracts for offense, defense, MCM/BEAST, archetype tags, and results-luck scaffolding.
- `web/lib/rankings/skaterCompositeMethodology.test.ts` - Unit tests for composite methodology contracts and source-table metadata.
- `web/lib/rankings/skaterCompositeWriter.ts` - Composite writer that builds `skater_composite_ratings` rows from verified contextual ranking surfaces and records provenance/freshness metadata.
- `web/lib/rankings/skaterCompositeWriter.test.ts` - Unit tests for composite score math, BEAST tier gates, row provenance, and missing-source metadata.
- `web/lib/rankings/resultsLuckIndex.ts` - Results Luck helper math, selected-window exclusion gate, and component-aware scorer.
- `web/lib/rankings/resultsLuckIndex.test.ts` - Fixture coverage for Results Luck baseline exclusion, peer fallback gating, component-aware hot/cold/normal scoring, and thin-baseline blends.
- `web/lib/rankings/resultsLuckSources.ts` - Narrow rolling source reader/extractor for Results Luck current and selected-window-excluded baseline components.
- `web/lib/rankings/resultsLuckSources.test.ts` - Unit tests for Results Luck raw rolling source extraction and field selection.
- `web/pages/api/v1/db/update-skater-composite-ratings.ts` - Admin route for dry-run or persisted composite-rating generation.
- `web/__tests__/pages/api/v1/db/update-skater-composite-ratings.test.ts` - Route tests for request normalization, dry-run defaults, upsert behavior, and full-pool generation limits.
- `web/lib/supabase/Upserts/rollingPlayerSourceSelection.ts` - Rolling source-selection helpers updated to use WGO PP goals as the PP fallback when NST PP counts are missing.
- `web/lib/supabase/Upserts/rollingPlayerSourceSelection.test.ts` - Unit tests for rolling source-selection fallback behavior, including PP goal fallback.
- `web/lib/supabase/Upserts/fetchRollingPlayerAverages.ts` - Rolling player average builder updated so PP goals/60 fallback rows use PP TOI, staged shot-attempts/60 outputs use NST `icf`, and historical/recompute-only wide fields persist into the rolling support payload table.
- `web/lib/supabase/Upserts/fetchRollingPlayerAverages.test.ts` - Unit tests for rolling-player builder contracts, including PP goals/60 denominator fallback, NST ICF-based shot-attempts/60 output generation, and durable support-payload splitting.
- `web/pages/api/v1/rankings.ts` - Existing rankings API scaffold to adapt or replace with the contextual rankings request contract.
- `web/pages/rankings.tsx` - New `/rankings` page that owns URL state, data loading, filters, and table composition.
- `web/components/Rankings/RankingsFilters.tsx` - Filter controls for entity, season, window, position, deployment, strength, samples, team, and metric.
- `web/components/Rankings/RankingsTable.tsx` - Sortable contextual leaderboard table with loading, empty, unavailable, and low-sample states.
- `web/components/Rankings/RankingExplanation.tsx` - Expandable row explanation component.
- `web/styles/Rankings.module.scss` - Page and component styling for the rankings dashboard.
- `web/lib/supabase/database-generated.types.ts` - Generated Supabase type surface to refresh after schema changes.

### Notes

- Unit tests should use the existing Vitest setup and live alongside the ranking helpers where practical.
- SQL validation should include peer-group ranking, lower-is-better metrics, tie behavior, and minimum-sample filtering.
- Inspect the existing Supabase schema, `lineCombinations`, `/shiftcharts` PP unit logic, and `/trueGoalieValue` before finalizing table names or field mappings.
- Iteration-only rule: before creating tables, fetching new data, or implementing any task based on PRD-inferred data availability, verify whether the data, table, function, hook, or utility already exists in this repo or Supabase and avoid redundant implementations.

### Discovery Notes

- Live Supabase/type-surface verification found existing populated sources for Phase 1 work: `wgo_skater_stats`, `wgo_skater_stats_totals`, `rolling_player_game_metrics`, `lineCombinations`, `powerPlayCombinations`, NST gamelog tables, `player_stats_unified`, `forge_player_game_strength`, and NHL xG aggregate tables.
- `rolling_player_game_metrics` already includes player-window metrics for last 3/5/10/20, season, all, 3YA, and career, plus support fields such as `line_combo_slot`, `line_combo_group`, `pp_unit`, `games_played_lastN_team_games`, `team_games_available_lastN`, and PP share fields.
- `lineCombinations` exists and is populated, but stores arrays by game/team (`forwards`, `defensemen`, `goalies`) rather than one row per player deployment slot.
- `powerPlayCombinations` exists and is populated with `unit`, `PPTOI`, `pp_share_of_team`, `pp_unit_usage_index`, `pp_unit_relative_toi`, and `pp_vs_unit_avg`.
- PRD placeholder column names need mapping to actual schema names before schema changes: examples include `seasons.id` instead of `season_id`, `games.date`/`seasonId` instead of `gameDate`/`season`, `nhl_xg_player_*_aggregates.ixg`/`shot_attempts` instead of `xg_for`/`shot_count`, and NST tables without `team`/`date` columns.
- Phase 1 source mapping: prefer `rolling_player_game_metrics` for player-window metric values and numerator/denominator components; join `players`, `teams`, `games`, and `seasons` for display/filter context; use `line_combo_group`/`line_combo_slot` for EV deployment labels; use `pp_unit` and `powerPlayCombinations.unit` for PP deployment; use `wgo_skater_stats`/`wgo_skater_stats_totals`, `player_stats_unified`, and NHL xG aggregate tables only as fallback or enrichment sources after field-level verification.
- Phase 1 metric mapping from existing rolling fields: `goals_per_60`, `assists_per_60`, `primary_assists_per_60`, `sog_per_60`, `hits_per_60`, `blocks_per_60`, and `ixg_per_60` have direct rolling field families for last 5/10/20 and season-style windows. `points_per_60` is derived from verified points and TOI rolling fields. `shot_attempts_per_60` uses NST individual Corsi For (`icf`) semantics, not on-ice `cf` or WGO `sat_for`; the rolling builder, schema, registry, and representative API verification are complete.
- Metric registry decision for 3.0: seed available Phase 1 metrics in both SQL and TypeScript. `points_per_60` is available as a derived metric from verified `points_*` and `toi_seconds_*` rolling fields, not from a direct `points_per_60_*` field family. `shot_attempts_per_60` is now available from NST ICF after task 20.0 added the staged selected-window rolling field family and representative live verification passed.
- Skater window aggregation decision for 4.0: do not add a duplicate production-window aggregate table or rebuild path. `rolling_player_game_metrics` already stores player-appearance last-N production windows and season-to-date components. The rankings layer now extracts those into explicit `season`, `last_5`, `last_10`, and `last_20` aggregate records, derives `points_per_60` from verified points and TOI fields, refuses zero-denominator rates, preserves `team_id` from the source row, and documents the existing `/api/v1/db/update-rolling-player-averages` refresh endpoint plus `truncate_rolling_player_game_metrics` RPC.
- Current rolling source strength states are `all`, `ev`, `pp`, and `pk`; no verified `5v5` rolling rows or direct mapping were found during 4.0 or the bounded live check in 14.0, so Phase 1 does not expose `5v5` as a filter or alias.
- Deployment aggregation decision for 5.0: reuse verified `lineCombinations`, `powerPlayCombinations`, and `rolling_player_game_metrics` context labels instead of adding `skater_deployment_windows`. Rankings normalize forwards to `L1`-`L4`, defense pairs to PRD buckets `P1`-`P3`, PP units to `PP1`-`PP3`, and PK usage to `PK1`/`PK2` only when PK TOI exists. Fallback EV deployment uses EV TOI rank within position group, not total TOI. Deployment contexts are scoped by player/team so traded players do not mix team roles.
- Existing projection role tags use defense labels like `D1`/`D2`; rankings-specific deployment helpers normalize defense buckets to `P1`/`P2`/`P3` to match this PRD. Task 15.0 confirmed rankings do not currently consume `buildSkaterRoleTags` directly, and added a tested boundary helper for future projection-role consumption.
- Ranking logic decision for 6.0/23.0: contextual rankings use raw metric values from aggregate rows when present, otherwise derive values from verified numerator/denominator fields. Lower-is-better metrics are normalized by negating raw values before rank calculation. Raw ranks now use dense-rank semantics, and percentiles report the share of qualified peers with strictly lower normalized values, so ties share the same rank and better-than percentile. Minimum GP/TOI filters apply before rank/percentile calculation, with unqualified rows retained but unranked.
- API layer decision for 7.0: add `/api/v1/contextual-rankings` rather than changing the existing `/api/v1/rankings` Start Chart scaffold. The endpoint validates query params, reads from `rolling_player_game_metrics` rather than raw events, enriches rows from verified `players` and `teams` fields, computes contextual ranks server-side, caps responses at 100 rows, and returns explicit unavailable metadata for unavailable or null-only metric contexts.
- Page/control decision for 8.0: `/rankings` is the usable first screen, not a landing page. It restores every filter from URL params, writes filter changes back to the URL with shallow routing, defaults to skaters, and renders goalie/team as deliberate coming-soon states until those data contracts are verified. Invalid position/deployment pairs are reset to `all` before API requests. The first-release table preview is intentionally basic; richer sorting, empty/loading/unavailable treatment, and expandable explanations remain in 9.0.
- Live API verification during 8.0 found that ordering `rolling_player_game_metrics` without the pending contextual index can time out. `rankingQueries.ts` now resolves an exact snapshot date first and then queries exact `game_date` rows with an explicit field list. Keep the contextual index task open for production performance validation.
- Phase 1 end-to-end verification during 11.0 found that some contexts can have verified candidate rows but no calculable metric values, such as `ixg_per_60` in the current rolling snapshot and `goals_per_60` for `strength=pp`. The contextual API now returns unavailable metadata for null-only metric contexts instead of rendering misleading unranked rows; source-data follow-up remains open in task 19.0.
- `npm run test:full` was executed during 11.0 and failed in an existing unrelated underlying-stats test: `__tests__/pages/underlying-stats/playerStats/index.test.tsx` could not find `Player 200` in the staged hydration test. Rankings-focused tests passed; follow-up is tracked in task 18.0.
- `npm run build` completed successfully during 11.0 after the null-only metric-context fix, including `/rankings` and `/api/v1/contextual-rankings`.
- Field-mapping decision for 12.1: live source verification found populated `player_stats_unified.nst_icf`, `player_stats_unified.nst_icf_per_60`, `nst_gamelog_as_counts.icf`, and `wgo_skater_stats.individual_sat_for_per_60` candidates. Use NST `icf` as the exact `shot_attempts_per_60` semantic. Reject `rolling_player_game_metrics.cf_*` and `wgo_skater_stats.sat_for` because they are on-ice/team attempt fields, and reject `nst_gamelog_as_counts.iscfs` because it is individual scoring chances rather than all individual attempts.
- Selected-window GP decision for 12.2: no explicit player-appearance GP field exists for each last-N rolling production window. Continue deriving `last_5`/`last_10`/`last_20` GP from player-window semantics as `min(season_games_played or season_participation_games, N)`, using `season_games_played` for all-strength season windows and `season_participation_games` for split-strength season windows. Do not use `games_played_lastN_team_games` as the production sample because that field reflects team-window availability semantics.
- Migration decision for 13.0: applied `migrations/20260605_create_contextual_rankings.sql` to the linked Supabase project after a transaction-wrapped rollback dry run passed. Live verification found `metric_definitions` seeded with 22 rows, `entity_metric_rankings` and `skater_composite_ratings` created empty, and the contextual lookup indexes present on reused source tables. `web/lib/supabase/database-generated.types.ts` was regenerated from the remote schema.
- Strength-state decision for 14.0: keep the Phase 1 API/UI contract to verified rolling states only: `all`, `ev`, `pp`, and `pk`. Do not alias `5v5` to `ev`, because that would mislabel the source semantics. The schema and planned metric definitions may continue to reserve `5v5` for later phases, but `/api/v1/contextual-rankings`, URL normalization, and `/rankings` controls should reject or hide it until true `5v5` rows exist.
- Performance decision for 16.0: after applying `idx_rolling_player_game_metrics_contextual_lookup`, ordered latest-row reads were fast but unsafe because they returned mixed `game_date` snapshots and stale player rows. The API keeps exact-snapshot semantics, but now resolves the latest snapshot date directly from indexed `rolling_player_game_metrics` instead of walking recent `games` dates with count probes. Live checks dropped representative rolling reads from about 4.7-5.5 seconds to about 80-160 ms for the source query and kept response rows on a single snapshot date.
- Local dev decision for 17.0: reproduced repeated `Watchpack Error (watcher): Error: EMFILE: too many open files, watch` with `npm run dev:3001`. Added Next's supported `watchOptions.pollIntervalMs` setting so dev file watching uses polling instead of exhausting native watch descriptors. After restart, `/rankings` compiled and returned 200 without new Watchpack `EMFILE` output.
- Deep research alignment decision: the PRD now requires dense-rank raw ranks, cume-dist-style better-than percentiles, denominator metadata, methodology versions, event-denominator sample gates, and source-quality caveats. Existing completed implementation tasks intentionally remain documented as historical decisions; the required implementation deltas are appended as new follow-up tasks.
- Deep research denominator decision: xG-derived skater and team shot-quality metrics should use Fenwick/unblocked-shot denominators when the source xG model is Fenwick-based, not shots-on-goal or Corsi denominators unless the source model explicitly matches that universe.
- xG denominator verification for 25.0: existing in-house `shot_goal` prediction generation filters `nhl_xg_shot_features` to `is_unblocked_shot_attempt = true`; live Supabase counts on 2026-06-06 showed `118610` approved `shot_goal` predictions and `118610` unblocked shot-feature rows, while total shot features include blocked attempts. Treat `nhl_xg_*_aggregates.shot_attempts*` and goalie `shots_against` in this xG aggregate family as Fenwick/unblocked denominators despite their generic column names.
- Deep research source-quality decision: hits, blocks, giveaways, and takeaways are rink/scorekeeper-sensitive. They can remain useful fantasy signals, but must be caveated or excluded from precision quality composites unless rink-adjusted sources are verified.
- Null-only metric investigation for 19.0: latest `rolling_player_game_metrics` snapshot for season `20252026`/strength `all` is `2026-04-16` with 216 rows, but every `ixg_per_60_*` value and ixG numerator is null while TOI denominators are populated. Upstream NST all-situations counts/rates are populated only through `2026-04-11`, so the current ixG issue is a source freshness/snapshot-selection mismatch, not an absent source column.
- PP goals investigation for 19.0: latest `rolling_player_game_metrics` PP snapshot has 216 rows on `2026-04-16` with TOI denominators but all `goals_per_60_*` goal values null. `wgo_skater_stats` has populated `pp_goals`, `pp_goals_per_60`, and `pp_toi` on `2026-04-16`; the rolling builder was not selecting `pp_goals` and generic PP goals/60 could fall back to all-situations TOI. The code now uses WGO PP goals and PP TOI for PP fallback rows, but the live rolling table still needs a refresh.
- ICF support investigation for 20.0: generated types and live Supabase checks confirmed `rolling_player_game_metrics` did not already have `shot_attempts_per_60_*` columns before this iteration. The rolling builder now selects NST `icf`, emits `shot_attempts_per_60` from ICF/TOI, and the linked Supabase schema now includes the staged additive columns. A bounded refresh for `player_id=8471685` on `2026-04-11` verified calculable all, EV, PP, and PK values, and pinned contextual-ranking API requests return the metric. The default/latest `2026-04-16` snapshot remains null-only because NST ICF sources are still populated only through `2026-04-11`.
- Existing `/api/v1/rankings` is a Start Chart scaffold for `date`, `mode`, and optional fantasy position. To avoid breaking that contract, contextual rankings should use a distinct API surface such as `/api/v1/contextual-rankings` or a route segment under `/api/v1/rankings/contextual`.
- Phase 1 request contract: `entity=skaters`, `season`, optional `as_of_date`, `window=season|last5|last10|last20`, `position=all|F|D`, `deployment=all|L1|L2|L3|L4|P1|P2|P3`, `strength=all|ev|pp|pk`, `metric`, `min_gp`, `min_toi`, `team=all|team_id`, `sort=percentile|raw_rank|metric_value|gp|toi_per_game`, `direction=asc|desc`, and `limit` capped at 100 for Phase 1.
- Phase 1 response contract: return rows with entity identity, team, position group, EV deployment, PP deployment, deployment confidence where derivable, selected-window GP/TOI sample fields, metric metadata, raw value, formatted value, raw rank, percentile, qualified peer count, sample confidence, minimum-sample flag, tags, and explanation items. The response must include enough metadata to render empty, unavailable, and small-peer-group states without row-level refetches.
- Data model decision for 2.0: do not create `skater_window_aggregates` or `skater_deployment_windows` in this iteration because verified sources already provide the underlying data. Add only `metric_definitions`, `entity_metric_rankings`, and `skater_composite_ratings`, plus lookup indexes on reused source tables.

## Tasks

- [x] 1.0 Confirm source data and finalize Phase 1 contracts
  - [x] 1.1 Inspect current Supabase tables for player identity, teams, seasons, games, skater game stats, on-ice stats, `lineCombinations`, PPTOI, hits, blocks, primary assists, and individual xG.
  - [x] 1.2 Map PRD-recommended fields to actual source table and column names, documenting unavailable Phase 1 metrics.
  - [x] 1.3 Decide whether the existing `web/pages/api/v1/rankings.ts` scaffold can be repurposed or needs a compatibility-preserving alternate endpoint.
  - [x] 1.4 Define the Phase 1 request contract for season, window, position, deployment, strength, metric, minimum GP, minimum TOI, team, sort, and limit.
  - [x] 1.5 Define the Phase 1 response contract so the UI can render all table rows without per-row follow-up queries.

- [x] 2.0 Create or adapt the rankings data model
  - [x] 2.1 Add or adapt storage for `metric_definitions` with directionality, descriptions, formulas, default strength state, minimum samples, phase, and availability fields.
  - [x] 2.2 Reuse `rolling_player_game_metrics` where it satisfies skater window aggregate requirements; add `skater_window_aggregates` only for confirmed gaps.
  - [x] 2.3 Reuse `rolling_player_game_metrics`, `lineCombinations`, and `powerPlayCombinations` where they satisfy deployment-window requirements; add `skater_deployment_windows` only for confirmed gaps.
  - [x] 2.4 Add `entity_metric_rankings` or an equivalent efficient RPC result surface for raw rank, percentile, peer count, sample confidence, and sample eligibility only after checking whether an existing ranking/percentile surface can be extended.
  - [x] 2.5 Add minimal `skater_composite_ratings` scaffolding only if no existing composite/rating surface can safely support Phase 2 MCM/BEAST.
  - [x] 2.6 Add indexes for common filters: season, window, semantics, entity type, metric key, peer group, position, deployment, team, and strength state.

- [x] 3.0 Seed and expose metric definitions
  - [x] 3.1 Seed Phase 1 skater metrics: goals/60, assists/60, primary assists/60, points/60, SOG/60, shot attempts/60, hits/60, blocks/60, and individual xG/60 where data exists.
  - [x] 3.2 Mark metrics unavailable when required source numerators or denominators are missing.
  - [x] 3.3 Include lower-is-better support in the registry even if Phase 1 enabled metrics are mostly higher-is-better.
  - [x] 3.4 Add placeholder definitions for planned MCM/BEAST and advanced skater metrics with correct phase and category metadata.
  - [x] 3.5 Generate or update TypeScript metric definition types used by API and UI.

- [x] 4.0 Build skater window aggregation
  - [x] 4.1 Calculate player-production windows using each player's last N games played, not the team's last N games.
  - [x] 4.2 Aggregate source numerators and denominators by player, team, season, window, window semantics, and strength state.
  - [x] 4.3 Preserve TOI totals for all, EV, 5v5, PP, and PK where source data supports them.
  - [x] 4.4 Handle traded players without mixing team contexts in deployment-sensitive aggregates.
  - [x] 4.5 Avoid calculating rates when denominator samples are missing or zero.
  - [x] 4.6 Add refresh logic or SQL functions that can rebuild the Phase 1 aggregate windows.

- [x] 5.0 Build skater deployment aggregation
  - [x] 5.1 Calculate deployment using team last N games for season, last 5, last 10, and last 20 team windows.
  - [x] 5.2 Use `lineCombinations` line/pair assignments where reliable.
  - [x] 5.3 Use EV/5v5 TOI per game as the fallback deployment signal, not total TOI.
  - [x] 5.4 Assign forwards only to `L1`, `L2`, `L3`, or `L4`.
  - [x] 5.5 Assign defensemen only to `P1`, `P2`, or `P3`.
  - [x] 5.6 Store PP and PK deployment buckets separately from EV deployment.
  - [x] 5.7 Derive `high`, `mixed`, and `low` deployment confidence from games played, TOI, and role consistency.
  - [x] 5.8 Reuse or extract existing `/shiftcharts` PP unit logic where practical.

- [x] 6.0 Implement server-side ranking and percentile logic
  - [x] 6.1 Calculate metric raw values from stored numerators and denominators.
  - [x] 6.2 Normalize lower-is-better metrics before ranking.
  - [x] 6.3 Calculate dense raw rank inside each active peer group.
  - [x] 6.4 Calculate percentiles so the displayed value means better than that share of qualified peers.
  - [x] 6.5 Support Phase 1 skater peer groups: all skaters, forwards, defensemen, deployment buckets, and team skaters.
  - [x] 6.6 Apply minimum GP and minimum TOI filtering consistently before rank and percentile calculation.
  - [x] 6.7 Return peer-count warnings or unavailable percentiles when peer groups are too small.
  - [x] 6.8 Include sample confidence labels based on metric type, GP, TOI, and selected window.

- [x] 7.0 Build the API layer
  - [x] 7.1 Validate ranking query params with safe defaults and clear 400 responses for invalid values.
  - [x] 7.2 Fetch leaderboard rows from the derived table, materialized view, or RPC without querying raw events from the page request.
  - [x] 7.3 Include metric display metadata, formatted values, deployment, confidence, tags, and explanation items in the API response.
  - [x] 7.4 Support top-100 row limits and server-side sort options for metric value, raw rank, percentile, GP, and TOI/G.
  - [x] 7.5 Return clear unavailable metadata when a requested metric cannot be calculated from current data.
  - [x] 7.6 Add focused API tests for query parsing, defaults, invalid params, empty data, and unavailable metrics.

- [x] 8.0 Build the `/rankings` page and controls
  - [x] 8.1 Create `web/pages/rankings.tsx` with a skater leaderboard as the default first-release view.
  - [x] 8.2 Serialize every filter into URL query params and restore state from the URL on load.
  - [x] 8.3 Add filters for season, entity, timeframe, position, deployment, strength, metric, minimum GP, minimum TOI, and team.
  - [x] 8.4 Hide goalie and team entity types or render a deliberate coming-soon state until later phases are implemented.
  - [x] 8.5 Prevent invalid deployment/position combinations such as `L2` defensemen or `P2` forwards.
  - [x] 8.6 Show a context summary sentence that names the metric, peer group, strength state, and window semantics.
  - [x] 8.7 Add tooltip/helper copy explaining player-production windows versus team-deployment windows.

- [x] 9.0 Build the leaderboard table and explanations
  - [x] 9.1 Render Player, Team, Pos, Deployment, GP, TOI/G, Metric Value, Raw Rank, and Percentile columns, with Trend and Tags supported as optional or placeholder columns for later phases.
  - [x] 9.2 Make the table sortable by metric value, raw rank, percentile, GP, and TOI/G.
  - [x] 9.3 Show loading, empty, unavailable, and small-peer-group states clearly.
  - [x] 9.4 Display sample confidence badges and low-sample warnings without hiding interesting outliers by default.
  - [x] 9.5 Display EV deployment and special-teams deployment together where available, such as `L3 / PP2`.
  - [x] 9.6 Add an expandable explanation section per row using only fields present in the ranking response.
  - [x] 9.7 Ensure explanation text includes raw rank, percentile, peer group, selected window, and sample caveats when relevant.

- [x] 10.0 Add verification coverage
  - [x] 10.1 Add SQL or helper tests for higher-is-better ranking behavior.
  - [x] 10.2 Add SQL or helper tests for lower-is-better ranking behavior.
  - [x] 10.3 Add tests for percentile calculations inside peer groups.
  - [x] 10.4 Add tests for raw-rank tie behavior using dense-rank semantics.
  - [x] 10.5 Add tests for minimum GP and TOI filtering before rank calculation.
  - [x] 10.6 Add tests for low, medium, and high sample confidence labels.
  - [x] 10.7 Add tests for URL query parsing, default state, and invalid-query fallback behavior.
  - [x] 10.8 Add component tests for empty, loading, unavailable, and low-sample table states.

- [x] 11.0 Verify Phase 1 end to end
  - [x] 11.1 Run the new ranking helper, API, and component tests.
  - [x] 11.2 Run the relevant existing test suite with `npm run test:full` from `web` unless a narrower project-standard command is more appropriate.
  - [x] 11.3 Run `npm run build` from `web` or the closest existing type/build verification command.
  - [x] 11.4 Start the local Next.js dev server and verify `/rankings` loads.
  - [x] 11.5 Verify representative URLs restore exact ranking states after reload.
  - [x] 11.6 Verify rank and percentile change correctly when position, deployment, strength, sample, team, and metric filters change.
  - [x] 11.7 Verify unavailable source data disables affected metrics instead of producing misleading values.

- [x] 12.0 NEW: Resolve field-mapping ambiguities discovered during source verification
  - [x] 12.1 Decide and document the exact semantic source for `shot_attempts_per_60` before enabling it, because WGO/NST expose multiple related fields (`sat_for`, `cf`, `icf`, and individual shot-attempt variants).
  - [x] 12.2 Confirm the best selected-window GP/sample field for production rankings in `rolling_player_game_metrics`; if no explicit player-window GP exists, derive it server-side from selected window semantics and available component fields.

- [x] 13.0 NEW: Validate and apply contextual rankings migration safely
  - [x] 13.1 Run `migrations/20260605_create_contextual_rankings.sql` in a safe migration environment before production apply; static review was completed, but live execution was intentionally skipped to avoid building indexes on populated tables during this task.
  - [x] 13.2 Refresh `web/lib/supabase/database-generated.types.ts` after the migration is applied.

- [x] 14.0 NEW: Resolve `5v5` versus `ev` strength-state exposure
  - [x] 14.1 Decide whether Phase 1 should expose `5v5` as a UI/API alias for existing `ev` rolling rows or keep only verified source states (`all`, `ev`, `pp`, `pk`) until true 5v5 rows exist.
  - [x] 14.2 If `5v5` remains a required filter, add or verify a true 5v5 source before enabling it in rankings.

- [x] 15.0 NEW: Keep defense deployment labels consistent across ranking and projection surfaces
  - [x] 15.1 If contextual rankings later consume `buildSkaterRoleTags` from projection code directly, normalize projection `D1`/`D2`/`D3` role labels to PRD ranking buckets `P1`/`P2`/`P3` at the boundary.

- [x] 16.0 NEW: Validate contextual rankings API production performance after migration apply
  - [x] 16.1 After `idx_rolling_player_game_metrics_contextual_lookup` is applied, compare exact-snapshot API reads against ordered latest-row reads and keep the faster production-safe path.

- [x] 17.0 NEW: Resolve local dev server watcher warnings
  - [x] 17.1 Local Next dev verification emitted repeated `EMFILE: too many open files, watch` warnings. Increase the file-watch limit or adjust local dev watcher settings before relying on long-running dev-server sessions.

- [x] 18.0 NEW: Fix unrelated full-suite underlying-stats hydration test failure
  - [x] 18.1 `npm run test:full` failed during 11.0 in `__tests__/pages/underlying-stats/playerStats/index.test.tsx` because the staged hydration test could not find `Player 200`; investigate whether the fixture, cache revalidation expectation, or async hydration behavior changed. Re-ran the targeted file and full Vitest suite on 2026-06-06; the failure is no longer reproducible and `npm run test:full` passed with 312 files / 1399 tests.

- [x] 19.0 NEW: Investigate null-only calculable values for currently exposed ranking metric contexts
  - [x] 19.1 Live verification found `ixg_per_60` has no calculable values in the current `rolling_player_game_metrics` snapshot despite being registry-available; verify whether the source fields are unpopulated, mis-mapped, or should be marked unavailable until refreshed. Verified source freshness mismatch: latest rolling snapshot is `2026-04-16`, while NST ixG source tables are populated through `2026-04-11`; keep null-only API unavailable handling and track freshness/snapshot follow-up.
  - [x] 19.2 Live verification found `goals_per_60` at `strength=pp` has candidate rows but no calculable values; verify whether PP rolling rows should include metric numerators/denominators or whether the UI should restrict metric/strength combinations by availability. Verified WGO has populated `pp_goals`/`pp_toi` on the latest date and updated the rolling builder to use those fields for PP fallback rows; live rows require a refresh before the API shows values.

- [x] 20.0 NEW: Add selected-window ICF support before enabling `shot_attempts_per_60`
  - [x] 20.1 Add verified NST `icf` selection to the rolling player averages source query and type surface.
  - [x] 20.2 Add an ICF/TOI rolling field family for last 5/10/20 and season-style windows without mapping to on-ice `cf` or WGO `sat_for`.
  - [x] 20.3 Refresh and verify `rolling_player_game_metrics` contains calculable ICF values for representative all, EV, PP, and PK contexts. After the schema migration was applied, a bounded refresh for `player_id=8471685`, season `20252026`, date `2026-04-11` verified all `21.799308`, EV `13.533835`, PP `121.348315`, and PK `0` for `shot_attempts_per_60_last5`; the latest `2026-04-16` snapshot still needs source-freshness work tracked below.
  - [x] 20.4 Flip `shot_attempts_per_60` from unavailable to available only after the rolling field family, SQL seed metadata, TypeScript registry, and API selected fields are all aligned. Updated the TypeScript metric registry, SQL seed, incremental metadata migration, `ROLLING_RANKING_SELECT_FIELDS`, live `metric_definitions`, and ranking/window tests; pinned API smoke checks for all, EV, PP, and PK now return calculable values.

- [x] 21.0 NEW: Resolve local Supabase tooling blockers found during migration apply
  - [x] 21.1 Repair the local npm cache ownership issue so `npx supabase gen types ...` no longer fails with `EPERM` under `/Users/tim/.npm/_cacache`. User repaired home-directory ownership and verified `npx supabase --version` plus `npx supabase gen types ...` from `web/`; Codex sandbox still uses tmp-local `HOME`/`npm_config_cache` because this session cannot write to `~`.
  - [x] 21.2 Replace or isolate the hyphenated `X-FHFH-IFTTT-Secret` key in `web/.env.local`; Supabase CLI auto-loads that file and fails because hyphens are invalid dotenv variable names. Verified the active env key is now underscore-safe as `X_FHFH_IFTTT_SECRET`; the remaining hyphenated text is only a commented historical label and is ignored by dotenv.
  - [x] 21.3 Document the linked Supabase CLI pooler workflow for local migration operations, because direct Postgres access to the Supabase IPv6 host was refused from this network. Added `web/README.md` guidance to run from `web/`, use Management API type generation, link with `npx supabase link --project-ref fyhftlxokyjtpndbkfse`, and prefer `db push --linked` without `--skip-pooler`.

- [x] 22.0 NEW: Add true `5v5` contextual ranking support before exposing a `5v5` filter
  - [x] 22.1 Add or verify true `5v5` rows in `rolling_player_game_metrics` instead of aliasing `ev`. Live verification on 2026-06-08 found `22,644` true `5v5` rolling rows for `861` players across `81` dates from `2026-01-04` through `2026-04-16`, with populated `goals_per_60_last5` and on-ice xG support rows.
  - [x] 22.2 Update the API parser, URL state, UI strength options, and tests only after true `5v5` source rows exist. The rankings parser/UI already accepts `5v5`; focused API/matrix smoke verification now returns latest true-5v5 rows instead of the prior 5-player bounded sample.
  - [x] 22.3 Revisit planned metrics with `default_strength_state = '5v5'` and confirm they map to true `5v5` data before marking them available. Verified live `metric_definitions` keeps `on_ice_gf_percentage`, `on_ice_xgf_percentage`, `rel_5v5_gf_percentage`, `rel_5v5_xgf_percentage`, and `xga_per_60` as `planned`, not `available`.

- [x] 23.0 NEW: Align implemented ranking math with deep-research rank semantics
  - [x] 23.1 Update `rankingCalculator` raw-rank behavior from sports-style `RANK()` gaps to dense-rank semantics, preserving lower-is-better normalization.
  - [x] 23.2 Update percentile calculation to a cume-dist-style better-than percentile over normalized values so ties do not create misleading jumps.
  - [x] 23.3 Update rank/percentile tests that currently assert sports-style `RANK()` behavior.
  - [x] 23.4 Verify API/UI copy still communicates "better than X% of qualified peers" with the active peer group named. Updated API explanation items and row formatter copy; focused contextual ranking/API/component tests passed.

- [x] 24.0 NEW: Add denominator and methodology metadata to the metric contract
  - [x] 24.1 Verify existing `metric_definitions` columns before altering schema; add or adapt fields for `applicable_strength_states`, `denominator_key`, `denominator_description`, `sample_requirements`, `methodology_version`, and `source_quality_flags`. Live schema verification found only generic `metadata` JSONB; added staged additive migration `migrations/20260606_add_metric_methodology_metadata.sql` and updated the base contextual migration.
  - [x] 24.2 Update SQL seed metadata and `web/lib/rankings/metricDefinitions.ts` so every published metric has denominator and sample metadata. The TS registry now enriches every metric with denominator, sample, methodology, applicable-strength, and source-quality metadata, including rink-scorekeeper caveats for hits/blocks.
  - [x] 24.3 Include denominator, sample requirements, methodology version, source-quality flags, and last-updated metadata in contextual ranking API responses. API metric metadata now includes the new methodology fields from the local registry.
  - [x] 24.4 Update `/rankings` tooltips or methodology UI to display denominator, strength-state, sample, and caveat metadata. The rankings page now displays denominator/methodology text and source caveat flags for the selected metric.

- [x] 25.0 NEW: Correct denominator-sensitive xG and shot-quality planned metrics
  - [x] 25.1 Verify whether the current xG sources are shot-on-goal, Corsi/all-attempt, or Fenwick/unblocked-shot based before enabling xS%, SAX%, goalie xGA/shot, or team shot quality. Verified `shot_goal` prediction generation filters features to `is_unblocked_shot_attempt=true`; live approved `shot_goal` count matched live unblocked feature count.
  - [x] 25.2 Add individual Fenwick/unblocked-attempt support where needed for `expected_shooting_percentage` and related skater metrics. Added denominator helpers and updated planned xS%/SAX metadata to use `nhl_xg_player_rolling_aggregates.shot_attempts` as individual unblocked attempts.
  - [x] 25.3 Ensure team shot quality uses `xGF / FF` when xG is Fenwick-based, not `xGF / CF`. Added denominator helper/test for `xGF / shot_attempts_for` where the xG aggregate family uses unblocked attempt semantics.
  - [x] 25.4 Ensure goalie xGA rate metrics use unblocked attempts against when the xG model is Fenwick-based. Added denominator helper/test for goalie `xg_against / shots_against`, with `shots_against` documented as unblocked attempts against in this aggregate family.
  - [x] 25.5 Add denominator correctness tests for skater xS%, team shot quality, and goalie xGA rate metrics before marking them available.

- [x] 26.0 NEW: Add source-quality and rink-effect caveats for RTSS-style events
  - [x] 26.1 Verify whether hits, blocks, giveaways, and takeaways are rink-adjusted in current sources. Verified current source tables expose raw RTSS-style fields (`wgo_skater_stats` hits/blocks/giveaways/takeaways, `nst_gamelog_as_counts` hits/giveaways/takeaways/shots_blocked, and rolling hits/blocks fields) but no rink-adjustment, venue-adjustment, or adjusted event columns.
  - [x] 26.2 Add source-quality flags for non-rink-adjusted RTSS events in metric definitions and API responses. `hits_per_60`, `blocks_per_60`, `mcm_score`, and `beast_tier` now publish `rink_scorekeeper_sensitive_unadjusted` through the registry/API metadata and staged SQL metadata migrations.
  - [x] 26.3 Exclude non-rink-adjusted RTSS events from precision quality composites, or label them explicitly as fantasy/peripheral signals. No precision quality composite currently uses giveaways/takeaways; MCM/BEAST are explicitly labeled as fantasy/peripheral composite/tier metadata because they depend on raw hits/blocks.
  - [x] 26.4 Add UI caveats for MCM/BEAST Riff categories that depend on hits or blocks. Rankings UI now renders human-readable source caveats instead of exposing internal source-quality flag keys.

- [x] 27.0 NEW: Implement non-overlapping baseline logic for Results Luck Index
  - [x] 27.1 Verify existing historical rolling/baseline tables before adding any new baseline storage. Live schema checks found populated `player_baselines` rows and an explicit `rolling_player_game_metrics` probe for season `20252026`/all returned `46259` rows with season and selected-window numerator/TOI fields; no new baseline storage was added.
  - [x] 27.2 Ensure the selected current window is excluded from the baseline used for hot/cold labels. Added `calculateNonOverlappingResultsLuckIndex`, which subtracts selected-window numerator/denominator from season-to-date numerator/denominator before calculating the player baseline.
  - [x] 27.3 Add fallback blending with peer-group average for players without enough prior history. Thin non-overlapping baselines blend toward the peer baseline by baseline TOI weight, and missing player baselines fall back to peer average when supplied.
  - [x] 27.4 Add tests that fail when current-window data leaks into the baseline. Added focused tests for current-window exclusion, thin-sample peer blending, missing-season peer fallback, and season-average source conversion for `points_per_60`.

- [x] 28.0 NEW: Add methodology metadata, glossary, and comparison payload support
  - [x] 28.1 Add or adapt endpoints/RPCs for available filters, metric metadata, methodology metadata, and fixed-list comparison payloads after verifying no existing route already covers them. Verified only `/api/v1/contextual-rankings` existed for this surface; added `/api/v1/contextual-rankings/metadata` for filters, metric methodology, glossary, and comparison contract metadata, and extended the rankings endpoint with optional `entity_ids` fixed-list filtering.
  - [x] 28.2 Add page-level methodology/glossary UI with denominator, strength-state, sample, source-quality, and methodology-version content. Rankings page now renders a compact methodology/glossary panel from the shared metric registry and source-quality metadata.
  - [x] 28.3 Prepare comparison drawer/card UI to compare selected players without issuing row-level refetches. Rankings table now supports row selection and the page renders a comparison card grid from the current ranking payload; selected rows can also be requested through `entity_ids` for fixed-list payloads.
  - [x] 28.4 Include last-updated metadata for ranking snapshots and metric methodology. Ranking responses now include `meta.snapshotDate`, the UI displays the selected snapshot date, and metric metadata includes `methodologyUpdatedAt`.

- [x] 29.0 NEW: Formalize goalie deployment and quality-start methodology
  - [x] 29.1 Verify whether `/trueGoalieValue` already calculates GSAx, quality starts, really bad starts, steals, starts share, or relative goalie metrics in reusable form. Verified `/trueGoalieValue` ranks goalies from weekly aggregates and WGO game rows but does not expose reusable GSAx/QS/starts-share logic; WGO goalie upserts already store classic `quality_start`/`quality_starts_pct`, and generated/live types already expose `nhl_xg_goalie_game_aggregates.goals_saved_above_expected`.
  - [x] 29.2 Implement adjusted core netshare so emergency-callup starts during contiguous top-two goalie absence windows can be excluded when confidence is sufficient. Added `calculateAdjustedCoreNetshare`, which excludes non-core emergency starts only when the configured top-two goalies are both unavailable with sufficient absence confidence.
  - [x] 29.3 Support both quality-start compatibility mode and modern xG mode where `GSAx >= 0` defines a quality start. Added `isGoalieQualityStart` with `classic_save_pct` and `modern_gsax` modes, plus `calculateGoalieGsax`, really-bad-start, steal-game, and starts-share bucket helpers.
  - [x] 29.4 Add tests for injury/call-up denominator logic, starts-share buckets, quality-start modes, and steal-game logic. Added focused goalie methodology tests covering GSAx derivation, modern/classic QS modes, really bad starts, steal games, starts-share buckets, emergency-callup denominator exclusion, and low-confidence denominator retention.

- [x] 30.0 NEW: Formalize team-style adjusted metrics and luck formulas
  - [x] 30.1 Verify whether score- and venue-adjusted 5v5 team metrics already exist before creating team-style aggregates. Live schema probe found existing `nhl_xg_team_game_aggregates` and `nhl_xg_team_rolling_aggregates` with raw `xg_for`, `xg_against`, `goals_for`, `goals_against`, and `shot_attempts_*`; no score-adjusted or venue-adjusted team xG columns were verified, so no new aggregate table was created.
  - [x] 30.2 Model run-and-gun as two axes: event rate (`xGF + xGA`) and control (`xGF%`), instead of a single blended label only. Added `calculateRunAndGunProfile`, which returns separate event-rate pace and xGF% control axes.
  - [x] 30.3 Replace any team-luck formula that rewards `GA/xGA` in the wrong direction with net goals above expected or separate finishing/save luck components. Added `calculateTeamLuckComponents`, with finishing luck as `GF - xGF`, save luck as `xGA - GA`, and net goals above expected as their sum.
  - [x] 30.4 Add tests for team shot-quality denominator, run-and-gun classification, and team-luck directionality. Added `teamStyleMethodology` tests covering Fenwick shot-quality denominator, separate run-and-gun axes, and positive/negative save-luck directionality.

- [x] 31.0 NEW: Version published ranking snapshots and derived methodology
  - [x] 31.1 Verify existing `updated_at` and version fields on ranking/aggregate tables before adding schema. Live schema verification found `entity_metric_rankings` and `skater_composite_ratings` already have `created_at`/`updated_at` but no `methodology_version`; `nhl_xg_team_rolling_aggregates` and `nhl_xg_goalie_rolling_aggregates` already have `model_version`, `created_at`, and `updated_at`; `rolling_player_game_metrics` already has `game_date` and `updated_at`.
  - [x] 31.2 Add `methodology_version` and `updated_at` to published ranking snapshots, composite ratings, and future team/goalie windows where missing. Added staged migration `migrations/20260606_add_contextual_snapshot_methodology_version.sql`, updated the base contextual migration, and updated generated types for the new snapshot `methodology_version`; no redundant team/goalie xG version columns were added because those tables already expose `model_version`.
  - [x] 31.3 Ensure incremental refresh logic updates affected snapshot metadata after game ingest. Added `set_contextual_snapshot_updated_at` triggers for `entity_metric_rankings` and `skater_composite_ratings` so future snapshot upserts update `updated_at`; verified no current app writer exists for those snapshot tables.
  - [x] 31.4 Add API and UI display of snapshot freshness. Ranking responses now include `meta.snapshotUpdatedAt` from `rolling_player_game_metrics.updated_at`, and the rankings page displays the selected snapshot date plus update date.

- [x] 32.0 NEW: Re-label and scope defensive composites until adjusted models exist
  - [x] 32.1 Rename planned defense outputs to `Defense Impact` or `Defensive Impact in Context` unless an adjusted RAPM/GAR-like model is implemented. Live schema verification found no skater RAPM/GAR/adjusted-defense table or column to extend, so planned composite labels now publish as `Defensive Impact in Context` and `Deployment Defensive Impact in Context` through `defensiveCompositeMethodology` and the rankings metadata endpoint.
  - [x] 32.2 Add methodology caveats for raw on-ice defensive metrics that are teammate, opponent, usage, and score-state influenced. Added `context_influenced_unadjusted_on_ice` source caveats for planned raw on-ice xGA/GF%/xGF% metrics in TypeScript metadata, SQL metric metadata, and the rankings UI caveat label.
  - [x] 32.3 Add a future adjusted-defense model task only after verifying source data can support it without redundant tables. Verified only raw/context candidate inputs exist in the repo/live schema, not an adjusted defensive model; appended task 40.0 to evaluate and implement an adjusted defense layer after source contracts are verified.

- [x] 33.0 NEW: Refresh and harden rolling metric freshness after null-only metric investigation
  - [x] 33.1 Refresh or backfill NST all-situations and PP gamelog sources past `2026-04-11` before relying on current-season ixG rankings. Live source verification on `2026-06-09` confirmed all-situations and PP NST counts/rates are populated through `2026-04-16`; the same pass also backfilled ES and PK counts/rates from `2026-04-12` through `2026-04-16` so individual source-dependent ranking metrics no longer depend on the old `2026-04-11` source tail.
  - [x] 33.2 Recompute `rolling_player_game_metrics` after the WGO PP-goals fallback change so PP `goals_per_60` can populate from `pp_goals` and `pp_toi`. Live API verification now returns PP `goals_per_60` from snapshot `2026-04-16` with `snapshotSelectionReason=latest_available`, no freshness fallback message, and populated ranked rows.
  - [x] 33.3 Add metric-specific snapshot freshness checks or fallback-to-latest-calculable-snapshot logic so the contextual rankings API does not choose a structurally latest snapshot with null-only source-dependent metrics. The API now anchors candidate dates to the latest actual `rolling_player_game_metrics` snapshot, walks backward by `games.date`, and selects the latest snapshot with calculable values for the requested metric/filter. Live checks for default `ixg_per_60` all, PP `goals_per_60`, and `shot_attempts_per_60` all now select `2026-04-11` while reporting `2026-04-16` as the latest available rolling snapshot.

- [x] 34.0 NEW: Apply staged ICF rolling schema and complete shot-attempt availability flip
  - [x] 34.1 Apply `migrations/20260606_add_rolling_shot_attempts_per_60.sql` in the linked Supabase environment after resolving migration-tooling blockers.
  - [x] 34.2 Refresh `rolling_player_game_metrics` for representative all, EV, PP, and PK contexts and verify non-null `shot_attempts_per_60_*` values where NST `icf` source rows exist.
  - [x] 34.3 Update SQL metric metadata, `web/lib/rankings/metricDefinitions.ts`, `ROLLING_RANKING_SELECT_FIELDS`, and ranking/window tests to mark `shot_attempts_per_60` available only after live schema and data verification pass.

- [x] 35.0 NEW: Fix unrelated lint hook-rule failure blocking clean lint verification
  - [x] 35.1 `npm run lint` currently fails in `web/components/GoaliePage/GoalieLeaderboard.tsx` because `React.useMemo` is called conditionally after an early return. Fixed by normalizing the ranking list before hooks and moving the empty-state return after the memo calls.
  - [x] 35.2 Re-run lint after fixing the unrelated hook-order errors; contextual-ranking touched files produced no lint errors in the failed run, only an existing anonymous-default-export warning in the rolling-player builder. Lint now exits successfully with existing warnings only.

- [x] 36.0 NEW: Complete latest-snapshot freshness for source-dependent ranking metrics
  - [x] 36.1 Refresh or backfill NST ICF gamelog sources past `2026-04-11`, or make the contextual rankings API select the latest calculable snapshot per metric instead of the latest structural rolling snapshot. The API now selects the latest calculable snapshot per metric/filter and reports the latest structural snapshot separately; operational source refresh remains open below.
  - [x] 36.2 Recompute full `rolling_player_game_metrics` snapshots after ICF and PP-goals fallback changes so latest default requests for `shot_attempts_per_60`, `ixg_per_60`, and PP `goals_per_60` no longer return null-only unavailable responses. After refreshing ES/PK NST counts/rates through `2026-04-16`, a bounded rolling recompute for `2026-04-12` through `2026-04-16` and strengths `ev,pk` upserted `2,664` rolling rows. Direct SQL verification found `2026-04-16` EV rows with `216` non-null ICF/ixG values and PK rows with `154` non-null ICF/ixG values; all-situations and PP were already current through the earlier refresh.
  - [x] 36.3 Verify unpinned `/api/v1/contextual-rankings` requests for `shot_attempts_per_60` return calculable all, EV, PP, and PK rows without requiring `as_of_date=2026-04-11`. Live builder verification on `2026-06-09` returned `snapshotDate=2026-04-16`, `latestAvailableSnapshotDate=2026-04-16`, `snapshotSelectionReason=latest_available`, `unavailable=false`, and no fallback message for `shot_attempts_per_60` at all, EV, PP, and PK strengths.

- [x] 37.0 NEW: Resolve Supabase CLI home-directory telemetry permission blocker
  - [x] 37.1 `npm_config_cache=/Users/tim/Code/fhfhockey.com/.npm-cache npx supabase --version` avoids the npm cache `EPERM`, but then fails writing `/Users/tim/.supabase/telemetry.json.tmp.*`; repair `/Users/tim/.supabase` ownership or configure a stable workspace-local CLI home before relying on Supabase CLI commands. User repaired home-directory ownership; Codex verified the CLI with sandbox-local `HOME=/private/tmp/fhfhockey-codex-home` because direct home writes remain blocked by the workspace sandbox.
  - [x] 37.2 Verify `npx supabase --version` and `npx supabase gen types ...` run without temporary `HOME` overrides after the local ownership repair. User verified both commands from VS Code terminal; Codex verified type generation with the repo access token and sandbox-local CLI paths, producing a 42,204-line TypeScript type file in `/private/tmp`.

- [x] 38.0 NEW: Apply methodology metadata migration in Supabase
  - [x] 38.1 Apply `migrations/20260606_add_metric_methodology_metadata.sql` in the linked Supabase environment. Applied live through the Supabase `us-east-1` pooler after the configured direct DB host failed DNS resolution.
  - [x] 38.2 Regenerate `web/lib/supabase/database-generated.types.ts` from the live schema after the migration is applied, replacing the manual staged type additions if needed. Regenerated remote public schema types with `npx supabase gen types typescript --project-id fyhftlxokyjtpndbkfse --schema public`.
  - [x] 38.3 Verify live `metric_definitions` rows contain non-empty `denominator_key`, `sample_requirements`, `methodology_version`, and `source_quality_flags`. Live verification found all `22` metric rows populated for each methodology field, including `3` defensive caveat rows with `context_influenced_unadjusted_on_ice`.

- [x] 39.0 NEW: Apply contextual snapshot methodology migration in Supabase
  - [x] 39.1 Apply `migrations/20260606_add_contextual_snapshot_methodology_version.sql` in the linked Supabase environment. Applied live through the Supabase `us-east-1` pooler.
  - [x] 39.2 Regenerate `web/lib/supabase/database-generated.types.ts` from the live schema after the migration is applied, replacing the manual staged type additions if needed. Regenerated types now expose `methodology_version` on both contextual snapshot tables.
  - [x] 39.3 Verify live `entity_metric_rankings` and `skater_composite_ratings` expose `methodology_version`, retain `updated_at`, and have the contextual snapshot update triggers installed. Live verification found `methodology_version,updated_at` on both tables and both snapshot `updated_at` triggers installed.

- [x] 40.0 NEW: Evaluate and implement an adjusted skater defensive-impact model
  - [x] 40.1 Verify source contracts for teammate/opponent on-ice context, zone starts, score state, strength state, and rolling sample windows before adding new adjusted-defense tables. The adjusted-defense roadmap now records that on-ice attribution utilities, NST 5v5 zone-start context, score-state helpers, and strength-state rolling rows exist, but no persisted adjusted defensive rankings design matrix/source is wired yet.
  - [x] 40.2 Decide whether the adjusted layer should be RAPM-like, GAR-like, or a narrower contextual residual model, and document the model target and validation plan. The documented target is a regularized RAPM-like contextual residual model before any GAR-like replacement score, with held-out validation and provenance gates before replacing contextual labels.
  - [x] 40.3 Implement adjusted defensive impact only after avoiding redundancy with existing projection/game-prediction models and proving the source data supports held-out validation. Verified the repo already had `web/lib/xg/adjustedImpact.ts`, `/api/v1/db/update-nhl-xg-adjusted-impact`, and `migrations/20260527_create_nhl_xg_adjusted_impact_tables.sql`, so no redundant rankings-only table was added. Added chronological held-out validation to the existing adjusted-impact model path, included the validation report in model-run/player provenance, and required it before non-dry-run persistence. Live Supabase verification found approved shot-goal xG predictions (`118,610` rows through `2026-05-26`), shot features (`162,000` rows), and shift rows (`1,052,578` rows through `2026-06-06`). A limited live dry run against `logistic_l2-s20252026-p1-st1-f1-cfg9bac2706` built `2,996` design rows, passed leakage validation, passed chronological holdout validation, and improved validation MSE over the training-mean baseline by `4.076114%`. The live output tables are not installed yet, so publication is tracked in task `83.0`.

- [x] 41.0 NEW: Operationally refresh stale source-dependent rolling ranking metrics
  - [x] 41.1 Backfill or refresh NST all-situations and PP gamelog source rows past `2026-04-11` before treating latest ixG/ICF-dependent ranking snapshots as current. Live verification found AS and PP already populated through `2026-04-16`; this pass additionally backfilled ES counts/rates (`1,332` rows each) and PK counts/rates (`745` rows each) for `2026-04-12` through `2026-04-16`.
  - [x] 41.2 Run an approved live recompute of `rolling_player_game_metrics` after source refresh and the WGO PP-goals fallback change, respecting the endpoint freshness gates instead of bypassing them by default. Ran a bounded live recompute for season `20252026`, dates `2026-04-12` through `2026-04-16`, and strengths `ev,pk`; it processed `668` players, upserted `2,664` rows, and left the ranking-facing ICF/ixG fields populated at the latest snapshot. The run still reported `countsOi` freshness blockers because ES/PK on-ice tables were not part of this individual-metric backfill; those blockers do not affect `shot_attempts_per_60`, `ixg_per_60`, or PP goals freshness.
  - [x] 41.3 Verify unpinned contextual ranking requests no longer need latest-calculable fallback for `ixg_per_60`, `shot_attempts_per_60`, and PP `goals_per_60`. Live verification returned `latest_available` for all-situations `ixg_per_60`, all/EV/PP/PK `shot_attempts_per_60`, and PP `goals_per_60`, all at snapshot `2026-04-16` with no fallback message.

- [x] 42.0 NEW: Document Supabase direct DB DNS fallback
  - [x] 42.1 Investigate why `db.fyhftlxokyjtpndbkfse.supabase.co` did not resolve locally while the `us-east-1` Supabase pooler did. DNS verification on `2026-06-09` showed the direct host returns no IPv4 `A` record and only an IPv6 `AAAA` record, while `aws-0-us-east-1.pooler.supabase.com` resolves to IPv4 `A` records.
  - [x] 42.2 Document the safe pooler-based `psql` migration path for future live migrations without exposing credentials in logs. `web/README.md` now documents the IPv6-only direct-host caveat, the IPv4 pooler fallback, session-pooler `psql` environment variables, and the rule to avoid logging full connection strings or database passwords.

- [x] 43.0 NEW: Enable every verified skater xG finishing metric on the live rankings site
  - [x] 43.1 Verify existing rolling/database sources before enabling additional PRD metrics. Live schema checks confirmed `rolling_player_game_metrics` already exposes selected-window and season components for goals, individual xG, individual unblocked shot attempts, and TOI samples; no new source table was created.
  - [x] 43.2 Add selected-window formula support for `expected_shooting_percentage`, `sax_percentage`, `goals_above_expected`, and `unrealized_xg` in the skater rolling aggregation layer without treating shot-attempt denominators as TOI samples.
  - [x] 43.3 Mark the four verified xG finishing metrics available in the TypeScript registry and live SQL `metric_definitions`, with `rolling_player_game_metrics` source metadata and Fenwick/unblocked-attempt denominator caveats.
  - [x] 43.4 Add focused tests for derived xG finishing formulas and denominator metadata. Targeted Vitest run passed for `lib/rankings/skaterWindowAggregation.test.ts` and `lib/rankings/metricDefinitions.test.ts`.
  - [x] 43.5 Smoke test every skater metric through the live contextual ranking surface. Verified 13 skater metrics returned live rows before EV on-ice follow-up: Goals/60, Assists/60, Primary Assists/60, Points/60, SOG/60, Shot Attempts/60, Hits/60, Blocks/60, iXG/60, xS%, SAX%, Goals Above Expected, and Unrealized xG.

- [x] 44.0 NEW: Bring remaining planned PRD metrics live only after source/data blockers are resolved
  - [x] 44.1 Add selected-window penalties-taken support before enabling `penalties_taken_per_60`. Verified NST `total_penalties` source fields exist, added rolling `penalties_taken` and `penalties_taken_per_60` field families, applied `migrations/20260608073908_add_rolling_player_penalties_taken_metrics.sql`, recomputed true-5v5 rolling rows, and smoke-tested live `/api/v1/contextual-rankings` plus matrix sorting with `216` ranked matrix rows and `12,919` non-null 5v5 rolling penalty-rate rows through `2026-04-16`.
  - [x] 44.2 Add true `5v5` rolling rows before enabling or labeling true-5v5 metrics such as `rel_5v5_gf_percentage` or `rel_5v5_xgf_percentage`. Completed for direct true-5v5 rolling rows and direct 5v5 metrics using `nst_gamelog_5v5_*`; relative team-without-player metrics remain planned until their baseline contract exists.
  - [x] 44.3 Add on-ice xGF/xGA selected-window rolling fields, or an equivalent verified selected-window source, before enabling `on_ice_xgf_percentage` or `xga_per_60`. Added live rolling support columns, builder outputs, ranking extraction, metadata, and smoke tests for 5v5 `xga_per_60` and `on_ice_xgf_percentage`.
  - [x] 44.4 Wire `results_luck_index` into the ranking API only after choosing the exact base result metric(s), peer baseline policy, and UI explanation contract. Results Luck now uses the component contract from tasks `76.0`, `79.0`, and `80.0` and is exposed as a sparse live composite metric in the matrix registry/API. Live publication on `2026-06-09` upserted `last20` 5v5 rows for all/L1-L4/P1-P3 contexts; SQL verification found `61` all-skaters rows and `15` L2 rows with non-null Results Luck values for snapshot `2026-04-16`, and a matrix smoke sorted L2 `last20` by `results_luck_index` with top sort values matching the live luck values.
  - [x] 44.5 Populate or compute `skater_composite_ratings` before enabling `mcm_score` or `beast_tier`; live schema exists, the composite writer/data contract is verified, and a local publish run upserted `216` all-deployment 5v5 season rows for snapshot `2026-04-16`. The matrix reader now consumes those rows for live `mcm_score`, `beast_tier`, and snapshot composite cards while keeping `results_luck_index` planned.

- [x] 45.0 NEW: Correct NST even-strength source audit and enable EV-backed On-ice GF%
  - [x] 45.1 Re-check local Supabase schema docs and live table names for NST even-strength and 5v5 sources. Verified `nst_gamelog_es_counts`, `nst_gamelog_es_counts_oi`, `nst_gamelog_es_rates`, and `nst_gamelog_es_rates_oi` exist in docs and live DB; goalie-specific `nst_gamelog_goalie_5v5_counts/rates` also exist. No live skater `nst_gamelog_5v5_*` table was found.
  - [x] 45.2 Verify the rolling-player builder already maps EV rows to NST ES sources before adding redundant fetch paths. `fetchRollingPlayerAverages.ts` maps `strength_state='ev'` to the `nst_gamelog_es_*` table family.
  - [x] 45.3 Verify live EV coverage before enabling the metric. Live `rolling_player_game_metrics` had `46,259` EV rows for `20252026`, with `44,807` rows carrying non-null `oi_gf_total_last5` and `oi_ga_total_last5`; NST ES source tables were populated through `2026-04-11`.
  - [x] 45.4 Enable `on_ice_gf_percentage` as an EV-backed metric, not a true-5v5 metric. Added rolling formula extraction from `oi_gf`/`oi_ga`, API select fields, TypeScript registry metadata, SQL metric metadata, and focused tests.
  - [x] 45.5 Smoke test EV `on_ice_gf_percentage` through the live contextual ranking surface. Verified it returns rows with `strength=ev`, selected snapshot `2026-04-11`, latest rolling snapshot `2026-04-16`, and `latest_calculable_metric` fallback reason.

- [x] 46.0 NEW: Add true skater 5v5 NST gamelog source tables and ingestion path
  - [x] 46.1 Verify the Natural Stat Trick skater `playerteams.php?sit=5v5` URL and column contracts for standard counts, standard rates, on-ice counts, and on-ice rates. User verified `sit=5v5`, with preseason represented by `stype=1`.
  - [x] 46.2 Create Supabase tables for `nst_gamelog_5v5_counts`, `nst_gamelog_5v5_rates`, `nst_gamelog_5v5_counts_oi`, and `nst_gamelog_5v5_rates_oi`. Added and applied `migrations/20260607_create_nst_gamelog_5v5_skater_tables.sql`; live verification found all four tables, expected column counts, and `UNIQUE (player_id, date_scraped)` constraints.
  - [x] 46.3 Wire `update-nst-gamelog` to generate and route `fiveOnFiveCounts`, `fiveOnFiveRates`, `fiveOnFiveCountsOi`, and `fiveOnFiveRatesOi` datasets to the new tables using `sit=5v5`.
  - [x] 46.4 Regenerate Supabase TypeScript schema after the live migration. `web/lib/supabase/database-generated.types.ts` now includes all four `nst_gamelog_5v5_*` tables.
  - [x] 46.5 Enable RLS on the four new 5v5 skater NST tables without blocking service-role ingestion. Added and applied `migrations/20260607_enable_rls_nst_gamelog_5v5_skater_tables.sql`; live verification found RLS enabled and a `public_read` SELECT policy for `anon` and `authenticated` on all four tables.
  - [x] 46.6 Add grouped 5v5 NST backfill route support so all four true-5v5 skater datasets can be populated together. Added `datasetGroup=fiveOnFive`, which expands to `fiveOnFiveCounts`, `fiveOnFiveRates`, `fiveOnFiveCountsOi`, and `fiveOnFiveRatesOi`; `datasetType` remains available for a single-table run.
  - [x] 46.7 Populate a bounded sample date for all four 5v5 skater datasets and verify row counts, header mapping, player ID matching, and upsert conflict behavior. Live verification after user backfill found `12,232` rows in each of the four `nst_gamelog_5v5_*` tables across `41` dates from `2026-03-01` through `2026-04-16`; sample joins matched across all four tables for `2026-04-14` through `2026-04-16`.
  - [x] 46.8 Extend rolling-player source selection and rolling field families to consume true 5v5 rows after source population is verified, without aliasing EV rows. Added `strength_state='5v5'` to the rolling builder source map, added rolling on-ice xGF/xGA support columns and metric extraction, regenerated Supabase types, and fixed the live `rolling_player_game_metrics_strength_ck` constraint to allow `5v5`.
  - [x] 46.9 Enable true `5v5` contextual ranking filters and metrics only after true 5v5 rolling rows are populated and API/UI tests pass. Bounded recompute for `2026-03-01` upserted `25` rolling rows for `5` players and live ranking smoke returned rows for `goals_per_60`, `points_per_60`, `ixg_per_60`, `xga_per_60`, `on_ice_xgf_percentage`, and `on_ice_gf_percentage` with `strength=5v5`.

- [x] 47.0 NEW: Realign `/rankings` from a single-metric leaderboard to the primary multi-metric player rankings workstation
  - [x] 47.1 Audit `web/pages/rankings.tsx`, `web/pages/api/v1/contextual-rankings.ts`, `web/pages/api/v1/rankings.ts`, and current `web/components/Rankings/*` usage before replacing or expanding any route.
  - [x] 47.2 Document the intended primary page modes: `Rankings` matrix, `Metric Explorer`, `Deployment Tiers`, `Trending`, `Splits`, and `Wins Above Replacement`, with unsupported modes clearly marked as planned rather than silently empty.
  - [x] 47.3 Make the screenshot-aligned multi-metric rankings matrix the default `/rankings` experience, while preserving the current single-metric contextual leaderboard as the `Metric Explorer` tab.
  - [x] 47.4 Define the separation between the legacy `/api/v1/rankings.ts` scaffold, the current `/api/v1/contextual-rankings` endpoint, and the new multi-metric matrix endpoint before moving code. The matrix route is `/api/v1/contextual-rankings/matrix` because `web/pages/api/v1/rankings.ts` prevents adding a sibling `rankings/` API directory without moving the legacy file.
  - [x] 47.5 Update PRD/task notes if needed so future work treats `/rankings` as a player evaluation workstation, not only a metric dropdown leaderboard.

- [x] 48.0 NEW: Build the multi-metric player matrix API contract
  - [x] 48.1 Verify existing live schema, functions, hooks, and helper modules before adding any new tables or writers, especially `metric_definitions`, `rolling_player_game_metrics`, `entity_metric_rankings`, `skater_composite_ratings`, deployment helpers, and 5v5 NST source tables.
  - [x] 48.2 Add a matrix endpoint or route handler using the repo's existing API conventions without breaking existing `/api/v1/contextual-rankings` consumers. Implemented as `/api/v1/contextual-rankings/matrix` to avoid the legacy `/api/v1/rankings.ts` file/directory route conflict.
  - [x] 48.3 Support request filters for player type, deployment bucket, timeframe/window, season, minimum GP, strength, team, sort metric, sort direction, page, page size, and selected player.
  - [x] 48.4 Return rows with player identity, team, position, GP, TOI/GP, deployment labels, source snapshot dates, sample confidence, and grouped metric cells.
  - [x] 48.5 Return each metric cell with raw value, formatted value, rank, percentile, directionality, availability state, source quality flags, and methodology metadata where available.
  - [x] 48.6 Include response-level metadata for metric groups, default columns, color scale bands, freshness, unavailable metrics, and active peer-group context.
  - [x] 48.7 Add focused API tests for filter parsing, pagination, sort behavior, unavailable metrics, lower-is-better metrics, stale snapshot fallback, and 5v5 sample data after `2026-02-28`.

- [x] 49.0 NEW: Create the grouped metric registry needed for the rankings matrix
  - [x] 49.1 Verify every requested matrix metric against current live sources before marking it available; do not infer source availability from the PRD alone.
  - [x] 49.2 Define default skater metric groups: `Offense`, `Playmaking`, `Shot Volume`, `Defense`, `Physical / Fantasy`, `Regression / Luck`, and `Overall`.
  - [x] 49.3 Map available metrics into the default groups, including goals/60, points/60, assists/60, primary assists/60, shots/60, shot attempts/60, ixG/60, xS%, SAX%, goals above expected, unrealized xG, hits/60, blocks/60, xGA/60, on-ice GF%, on-ice xGF%, and 5v5 variants where verified.
  - [x] 49.4 Keep blocked/planned metrics explicit for penalties taken, Rel 5v5 GF%, Rel 5v5 xGF%, luck score, MCM score, and BEAST tier until their data contracts are implemented.
  - [x] 49.5 Add display metadata for each matrix metric: short label, full label, tooltip, formatter, percentile color behavior, lower-is-better flag, default visibility, and player-type applicability.
  - [x] 49.6 Add tests that prevent unavailable metrics from appearing as live default columns and verify directionality/color-scale metadata.

- [x] 50.0 NEW: Build the dense grouped-column rankings matrix UI
  - [x] 50.1 Refactor or replace the current rankings table with a matrix table that supports frozen identity columns and grouped metric headers.
  - [x] 50.2 Render identity columns for rank, player, team, GP, TOI/GP, and deployment state before the metric heatmap columns.
  - [x] 50.3 Render metric cells with value plus rank/percentile context, using the registry color scale and lower-is-better visual treatment.
  - [x] 50.4 Add sorting by any visible matrix metric and keep sort state synchronized with the URL.
  - [x] 50.5 Add pagination, rows-per-page controls, selected-row state, and keyboard/mouse row selection behavior.
  - [x] 50.6 Add a compact percentile legend and source caveat indicator below the table.
  - [x] 50.7 Provide loading, empty, stale-source, and unavailable-column states that explain what is missing without implying the player has zero production.
  - [x] 50.8 Add component tests for grouped headers, row rendering, selected row, sorting controls, unavailable cells, and legend rendering.

- [x] 51.0 NEW: Build the right-side player snapshot panel
  - [x] 51.1 Source the snapshot panel from the selected matrix row when possible, avoiding redundant per-row fetches unless deeper player-card data is requested.
  - [x] 51.2 Render player identity, headshot/team branding where existing asset conventions support it, team, position, deployment, GP, TOI/GP, and age if available.
  - [x] 51.3 Render compact score cards for offense rating, defense rating, MCM/BEAST status, luck/regression status, and overall/context score as data becomes available.
  - [x] 51.4 Render key advanced metric bars using percentiles for goals, assists, points, shots, hits, blocks, ixG, xGA, takeaways, giveaways, penalties drawn, and penalties taken only where verified.
  - [x] 51.5 Generate `Why he stands out` bullets from percentile thresholds, deployment changes, and source caveats rather than hard-coded player copy.
  - [x] 51.6 Add trend and role-change sections using existing rolling, line-combination, PP, and PK sources where verified; otherwise show planned/unavailable states.
  - [x] 51.7 Add tests for selected-player updates, missing image fallback, unavailable score cards, and generated standout bullets.

- [x] 52.0 NEW: Build the rankings workstation filter and tab shell
  - [x] 52.1 Replace the current narrow filter layout with a top filter bar matching the target workflow: player type, deployment, timeframe, season, minimum GP, strength, team, and more filters.
  - [x] 52.2 Implement player type as a segmented control for forwards, defensemen, and goalies; keep goalie rankings disabled or clearly planned until the goalie-specific contract is ready.
  - [x] 52.3 Implement deployment filter options from verified line-combination and TOI data, including L1/L2/L3/L4, PP unit, PK unit, and defense-pair buckets where source coverage supports them.
  - [x] 52.4 Preserve current metric/timeframe/strength URL state behavior and extend it to the new matrix filters.
  - [x] 52.5 Add a `More Filters` drawer or popover for sample confidence, metric groups, minimum TOI, source quality flags, and column visibility.
  - [x] 52.6 Wire tabs so `Rankings` uses the matrix endpoint, `Metric Explorer` uses the existing contextual endpoint, and planned tabs cannot be confused with finished features.
  - [x] 52.7 Add responsive behavior for desktop and mobile without overlapping controls, truncated labels, or unstable table dimensions. Browser QA on `/rankings` at `1440x900`, `1024x768`, and `390x844` found `10` loaded rows, no filter overlaps, no filter text overflow after widening the player-type control, no matrix footer overlap, no body-level horizontal overflow after moving the global desktop header breakpoint to `1120px`, and expected contained horizontal scrolling for the matrix/tabs.

- [x] 53.0 NEW: Implement composite, archetype, and regression metrics needed by the target rankings page
  - [x] 53.1 Verify whether `skater_composite_ratings` already has a populated writer, scheduled job, or API consumer before adding any new composite generation path. Repo search found only schema/metadata/planned UI references and no app writer; live database verification found `0` rows in `skater_composite_ratings`, `0` non-null `mcm_score`/`beast_tier`/`results_luck_index` rows, and no matching database functions or cron jobs.
  - [x] 53.2 Define and document `Offense Rating` and `Defense Rating` as percentile-based composites with source caveats for unadjusted on-ice defensive metrics. Added `skaterCompositeMethodology` contracts; defense remains labeled as contextual impact with explicit unadjusted on-ice caveats.
  - [x] 53.3 Define `MCM Score` and `BEAST Tier` category rules for fantasy multi-category value, including minimum category thresholds and position-specific weighting. Added MCM component groups, weighted percentile formula, required explainability fields, and BEAST tier gates.
  - [x] 53.4 Define `Shoot First`, `Pass First`, and `Play Driver` tags from verified shot-volume, assist-share, on-ice, and usage signals.
  - [x] 53.5 Choose the exact live contract for `Luck Score`, including base result metric, expected metric, peer baseline, directionality, and UI explanation copy. Contract uses a `results_luck_index` centered at `100`, requires a frozen/non-overlapping baseline, and treats higher values as hotter/luckier.
  - [x] 53.6 Implement a composite writer only after the source audit confirms no redundant function/table already exists. Verified the existing writer and admin route against the live `skater_composite_ratings` primary key, raised the route generation limit to a full player-pool cap of `1000`, and confirmed a local dry-run for `20252026` 5v5 season generates `216` composite rows from verified source metrics.
  - [x] 53.7 Add methodology metadata, freshness tracking, and tests for composite calculations, tier thresholds, and missing-source behavior. Composite rows now carry methodology version, caveats, unavailable metrics, and per-source metric freshness in provenance; focused tests cover composite formulas, BEAST tier gates, missing luck status, source freshness metadata, route dry-run/upsert behavior, and full-pool default limits.

- [x] 54.0 NEW: Add secondary ranking tabs after the matrix foundation is stable
  - [x] 54.1 Verify existing rolling, schedule, line-combination, PP, PK, and team-style data before creating tab-specific tables or endpoints. Repo verification found the current rankings layer already reads rolling player windows (`season`, `last5`, `last10`, `last20`), line-combination context (`line_combo_group`/`line_combo_slot`), PP unit context (`pp_unit`), strength states (`all`, true `5v5`, `ev`, `pp`, `pk`), and sample confidence from `rolling_player_game_metrics`; no new tab-specific tables or endpoints were added for this increment.
  - [x] 54.2 Build `Deployment Tiers` around verified L1/L2/L3/L4, PP, PK, and pair buckets with percentile summaries per deployment group. The live tab now returns 5v5 EV line buckets (`L1-L4`), 5v5 defense pair buckets (`P1-P3`), true PP unit buckets (`PP1-PP3`), and true PK usage buckets (`PK1-PK2`) with available source states and percentile summaries from `rolling_player_game_metrics`.
  - [x] 54.3 Build `Trending` from rolling windows and recent deltas, including last 5/10/20 game changes for TOI, usage, and key metrics. Added `/api/v1/contextual-rankings/trending`, a live Trending tab panel, URL-state fetch wiring, and tests. The tab uses existing `rolling_player_game_metrics` season/last20/last10/last5 windows, ranks last-5 vs last-20 percentile movement across verified key metrics, and shows TOI/G movement plus deployment context without adding redundant tables.
  - [x] 54.4 Build `Splits` only for verified split dimensions such as strength, season window, team, home/away, or deployment context. Added `/api/v1/contextual-rankings/splits`, a live Splits tab panel, URL-state fetch wiring, and tests. The tab reuses existing `rolling_player_game_metrics` ranking surfaces for verified strength, rolling-window, team-filter, and deployment comparisons; home/away remains explicitly unsupported in metadata because it is not exposed by the verified rankings surface. Local API smoke returned `10` rows, available strength/window/deployment sections, snapshot `2026-04-16`, and HTTP `200`.
  - [x] 54.5 Keep `Wins Above Replacement` planned until a defensible model, source contract, and validation strategy are documented. The WAR tab now renders a source-aware unavailable state and explicitly avoids publishing any WAR values or matrix substitutes.
  - [x] 54.6 Add tab-level loading, empty, and source-unavailable states so each tab can launch incrementally without implying unsupported analytics are complete. Rankings, Metric Explorer, Deployment Tiers, and Trending now have table/panel-contained loading, error, empty, or unavailable states, while Splits and WAR remain explicit planned/source-unavailable panels instead of silently empty tabs.

- [x] 55.0 NEW: Resolve legacy `/api/v1/rankings.ts` endpoint ambiguity
  - [x] 55.1 Search the repo for all consumers of `/api/v1/rankings` and identify whether it is still tied to Start Chart, old rankings, or unused scaffolding.
  - [x] 55.2 If the route is unused, replace it with the new rankings contract or a compatibility wrapper that points to the correct endpoint. Not taken for this milestone because the existing file route blocks a nested `/api/v1/rankings/*` directory; the explicit contextual matrix path avoids a risky route move.
  - [x] 55.3 If the route is still needed elsewhere, leave it intact and add the matrix API under a more explicit path.
  - [x] 55.4 Update API documentation, tests, and comments so future work does not confuse `/api/v1/rankings`, `/api/v1/contextual-rankings`, and the matrix endpoint.

- [x] 56.0 NEW: Run screenshot-aligned acceptance testing for `/rankings`
  - [x] 56.1 Start the local dev server and verify the rankings page in-browser against desktop and mobile viewports. Verified `http://localhost:3000/rankings` with in-app browser automation at `1440x900`, `1024x768`, and `390x844`.
  - [x] 56.2 Confirm the default page loads as a dense rankings matrix, not a landing page or single-metric-only leaderboard.
  - [x] 56.3 Confirm filters, tabs, sorting, pagination, row selection, and snapshot-panel updates work without full-page reloads.
  - [x] 56.4 Confirm all live metrics requested in the PRD appear where source data exists, and blocked metrics are visibly unavailable or planned rather than omitted silently.
  - [x] 56.5 Confirm true 5v5 requests use the populated `nst_gamelog_5v5_*` backed rolling rows for dates after `2026-02-28`.
  - [x] 56.6 Confirm no table cells, filter labels, buttons, legends, or snapshot fields overlap or resize unexpectedly across supported viewports. Browser measurements found no filter-control overlaps, no matrix-footer overlaps, no snapshot-panel text overflow, no body-level horizontal overflow, and contained matrix horizontal scrolling at desktop/tablet/mobile widths.
  - [x] 56.7 Run focused unit/API tests plus lint, then record any remaining blockers as new appended tasks instead of burying them in notes.

- [x] 57.0 NEW: Follow-up tasks, blockers, bugs, and ideas from the rankings matrix milestone
  - [x] 57.1 Build the full `More Filters` drawer/popover with sample confidence, metric group toggles, minimum TOI, source quality flags, and column visibility controls.
  - [x] 57.2 Persist matrix column visibility and metric group preferences in URL state without breaking sharable rankings URLs.
  - [x] 57.3 Complete mobile viewport visual QA for `/rankings`, including filter wrapping, sticky matrix columns, horizontal scrolling, the percentile legend, and snapshot panel stacking. Verified at `390x844`: filters wrap to one column, matrix horizontal scrolling is contained inside the matrix wrapper, the percentile legend/footer stacks without overlap, and the snapshot panel stacks below the matrix with static positioning.
  - [x] 57.4 Add browser interaction coverage for sorting, pagination, row selection, tab switching, and Metric Explorer fallback once a browser automation dependency is available in the repo. The repo does not currently include Playwright/full-browser E2E tooling, so added page-level interaction coverage with the existing React Testing Library stack: `/rankings` now has tests proving matrix sort, next-page pagination, page-size changes, row selection, tab switching, Metric Explorer SWR fallback loading, and Metric Explorer sort all update normalized URL state.
  - [x] 57.5 Decide whether to deprecate, wrap, or move legacy `/api/v1/rankings.ts` after confirming external consumers, so a future nested `/api/v1/rankings/*` API namespace remains possible. Repo audit found no active code consumers of `/api/v1/rankings`; remaining references are PRD/task docs identifying it as the Start Chart scaffold. Because external consumers cannot be disproven from repo search alone, kept the legacy route in place, added `Deprecation`/`Link`/`X-FHFH-Legacy-Route` headers plus replacement endpoint metadata, and added focused route tests so future work cannot confuse it with `/api/v1/contextual-rankings` or `/api/v1/contextual-rankings/matrix`.
  - [x] 57.6 Expand the matrix API smoke suite against a larger true-5v5 rolling sample after more post-`2026-02-28` backfill is available. Added a matrix route smoke test for the max supported page-size true-5v5 request shape, and live localhost verification for `page_size=50` returned `50` rows out of `216` ranked players, `5` pages, snapshot `2026-04-16`, `19` metric columns, and no unavailable metrics.
  - [x] 57.7 Add real headshot/team-logo rendering to the snapshot panel using the repo's existing asset conventions instead of the current initial/avatar fallback. Extended contextual ranking rows with `entity.imageUrl` from `players.image_url`, carried that through the matrix response, and updated the snapshot panel to render the player headshot plus `/teamLogos/{ABBR}.png` badge with known local fallbacks (`/pictures/player-placeholder.jpg` and `/teamLogos/FHFH.png`). Focused tests and TypeScript pass, and live matrix smoke returns `imageUrl` for the first 5v5 row plus team abbreviation for logo rendering.
  - [x] 57.8 Define and implement the composite/archetype/regression contracts from task `53.0` only after confirming no redundant Supabase writer, function, hook, or scheduled job already exists. Completed contract/writer verification, persisted all-deployment 5v5 composites, and exposed live MCM/BEAST plus snapshot composite values; regression Luck remains planned because the frozen non-overlapping baseline is intentionally not implemented.
  - [x] 57.9 Build the secondary tabs from task `54.0` incrementally, keeping unsupported analytics visibly planned until their source contracts and tests are complete. Replaced the generic planned tab copy with source-aware secondary tab panels for Deployment Tiers, Trending, Splits, and WAR; verified deployment/window/strength source contracts are labeled separately from still-planned tier rollups, trend deltas, unsupported home/away splits, and WAR. Added page tests for the secondary tab states.
  - [x] 57.10 Reauthenticate the Supabase connector for future live database checks; this iteration fell back to direct `psql` after the connector reported that reauthentication was required. Verified the Supabase connector is authenticated again by listing the `fhfhockey.com` project (`fyhftlxokyjtpndbkfse`) on `2026-06-08`.
  - [x] 57.11 Fix local npm cache ownership so direct `npx supabase ...` works again. The user repaired home-directory ownership and verified direct `npx supabase --version` plus type generation from `web/`; the repo also keeps the Codex-safe fallback wrapper `npm run supabase:safe -- ...`, which disables telemetry and uses a writable temp npm cache.
  - [x] 57.12 Stop or reclaim the lingering local Next dev server on port `3001`; follow-up verification found `localhost:3001` not responding and `lsof -nP -iTCP:3001 -sTCP:LISTEN` returned no listener.
  - [x] 57.13 Fix Supabase pagination in the contextual rankings rolling snapshot fetch so snapshots larger than 1,000 rows are fetched with `.range(...)` pages before ranking.
  - [x] 57.14 Backfill or recompute true `5v5` rows in `rolling_player_game_metrics`; live verification on `2026-06-08` initially found only `5` total `5v5` rows, then a scoped `strength=5v5` recompute upserted `22,644` rows for `861` players from populated NST 5v5 sources. Post-run matrix smoke returned `25` rows on page 1 and `216` total ranked rows for latest `5v5` `goals_per_60`.
  - [x] 57.15 Raise the matrix-internal source row cap above `100` so `/rankings` pagination can represent the full ranked player pool after the true-5v5 backfill; focused matrix smoke now reports `216` ranked rows and `9` pages at `page_size=25`.
  - [x] 57.16 Investigate the `111` freshness blockers reported by the scoped true-5v5 rolling recompute; live source checks showed the ranked 5v5 page row issue is no longer a frontend/Supabase pagination cap, and the freshness warnings are primarily missing or partial NST 5v5 source dates (whole missing dates include `2026-01-12`, `2026-01-21`, `2026-01-30`, `2026-03-07`, `2026-03-16`, `2026-03-25`, `2026-04-03`, and `2026-04-12`).
  - [x] 57.17 Add targeted NST game-log backfill controls so `update-nst-gamelog` can fetch exact dates or a bounded `startDate`/`endDate` range with `datasetGroup=fiveOnFive`, avoiding another broad reverse/forward backfill just to repair source gaps.
  - [x] 57.18 Backfill the identified NST 5v5 source gaps with `overwrite=yes` for all four 5v5 tables, then re-check date/player coverage against WGO source rows; after the targeted `datasetGroup=fiveOnFive` run, the eight repaired dates have matching counts across all four `nst_gamelog_5v5_*` tables and are within `0-3` rows of `wgo_skater_stats` per date.
  - [x] 57.19 Re-run the scoped true-5v5 rolling recompute after the source-gap backfill and verify freshness blockers are either zero or documented as true NHL/NST source mismatches; the recompute upserted `22,644` rows for `861` players with `0` coverage warnings, `0` suspicious output warnings, `0` unknown game IDs, and `9` residual freshness blockers.
  - [x] 57.20 Investigate the remaining true-5v5 freshness residual for `8486166` / Viking Gustafsson Nyberg: live NST contained `Viking Gustafsson-Nyberg` on `2026-04-13` and `2026-04-14`, but the scraper cache only matched exact `players.fullName` values. Added normalized cache lookup so hyphen/spacing variants resolve, backfilled both dates with `datasetGroup=fiveOnFive&overwrite=yes`, verified all four `nst_gamelog_5v5_*` tables now have rows for `8486166`, and recomputed that player's 5v5 rolling rows with `freshnessBlockers: 0`.
  - [x] 57.21 Fix stale `/rankings` URL state that could preserve unsupported `page_size=5` and make the first matrix page appear to show only five players. Live API verification showed the default matrix request returns `10` rows, `216` ranked rows, and `22` pages; URL normalization now snaps unsupported page sizes back to the supported default `10`.
  - [x] 57.22 Fix the matrix API parser so direct or stale requests with unsupported `page_size=5` normalize to the supported default `10` instead of returning five rows. Focused verification against `localhost:3000` now returns `rowCount: 10`, `pageSize: 10`, `216` total ranked rows, and `22` pages for both `page_size=5` and `page_size=10`.
  - [x] 57.23 Add the matrix/API reader path for `skater_composite_ratings` before flipping `mcm_score`, `beast_tier`, offense rating, defense rating, archetype tags, or luck/regression slots from planned to live. Added a safe all-deployment composite overlay for matrix rows, made `mcm_score` and `beast_tier` live default columns, kept `results_luck_index` planned, and verified `/api/v1/contextual-rankings/matrix` returns live composite payloads plus MCM sorting from persisted `skater_composite_ratings`.
  - [x] 57.24 Add deployment context to `skater_composite_ratings` before persisting deployment-scoped offense/defense composites. Added and applied a context-key migration with `peer_group_type`, `peer_group_key`, `position_group`, and `deployment_bucket`, expanded the primary key/upsert conflict target, published 5v5 season composites for `L1-L4` and `P1-P3`, and verified `deployment=L2&sort_metric=mcm_score` returns live deployment offense/defense/MCM/BEAST values without overwriting all-skater rows.
  - [x] 57.25 Clarify matrix pagination copy after a report that the page appeared to show only five players. Live API verification confirmed Supabase-backed rolling snapshot pagination is already using `.range(...)` pages and the default matrix request returns `10` rows, `216` total ranked rows, and `22` pages; updated `/rankings` and the matrix footer to display current-page rows separately from total ranked players.
  - [x] 57.26 Investigate the remaining true-5v5 rolling freshness gate after the penalties-taken recompute. Deterministic live-source paging showed the residual gate was down to `8477919` / Frederick Gaudreau with three lag types (`counts`, `rates`, `counts_oi`) because NST exposes him as `Frederic Gaudreau` while `players.fullName` is `Frederick Gaudreau`. Added the exact NST alias plus `Freddy Gaudreau`, backfilled `2026-04-15` and `2026-04-16` for `datasetGroup=fiveOnFive`, verified all four `nst_gamelog_5v5_*` tables now have Gaudreau tail rows, verified deterministic source blockers are `0`, and confirmed the targeted rolling route returns `operationStatus: "success"` with `freshnessBlockers: 0` for `playerId=8477919`.
  - [x] 57.27 Remove the matrix-internal `1000`-row source cap and page Supabase metadata/composite lookups. `buildPlayerMatrixSurface` now requests the full internally ranked set before applying matrix page slicing, and player/team/composite lookups use `.range(...)` paging over bounded `IN` chunks. Focused tests pass, TypeScript passes, and live API smoke returns `10` rows on page 1, `6` rows on page 22, `216` total ranked rows, and valid MCM composite sorting.
  - [x] 57.28 Investigate why the broad bounded 5v5 recompute request for `2026-03-01` through `2026-04-16` did not return promptly from local `curl` after the Gaudreau source fix. The request eventually completed successfully after `19m 50s`, processed `806` players, upserted `12,960` rows, and returned `freshnessBlockers: 0`, so the apparent hang was an unbounded multi-week `daily_incremental` run exceeding its `4m 30s` runtime budget. Added a preflight blocker so explicit multi-week date-window recomputes require `playerId`, `resumeFrom`, `maxPlayers`, `executionProfile=overnight`, or `confirmBroadRun=true`; the problematic URL now returns HTTP `422` immediately with bounding guidance, while explicit overnight full recomputes remain allowed.
  - [x] 57.29 Consider adding async job/progress reporting for intentional broad rolling recomputes so browser-triggered full-window runs can stream or poll status instead of buffering a single response for 15-20+ minutes. Added explicit buffered `progressGuidance` to `update-rolling-player-averages` blocked, freshness-blocked, and success responses so operators see that no pollable progress endpoint exists yet, how to chunk with `maxPlayers`/`resumeFrom`, when to use `playerId`, and when `executionProfile=overnight` is the deliberate broad-run path. Focused route tests and TypeScript pass; a true async job table remains future work rather than implied behavior.
  - [x] 57.30 Add true browser E2E coverage for `/rankings` after the repo standardizes a full browser automation dependency or CI browser binary. Current coverage exercises the page interaction wiring in jsdom, but not a real Chromium/WebKit layout/runtime. Added Playwright E2E scaffolding plus a CI workflow, and completed a user-approved host Chrome verification on `2026-06-09` against `http://127.0.0.1:3100/rankings?entity=skaters&tab=rankings&strength=5v5&page_size=10`: the matrix loaded live 5v5 rows, pagination advanced to page 2 with `10` rows and `Page 2 of 78`, More Filters opened with metric groups and column controls, Metric Explorer navigated via URL state, and Chrome reported no console errors.
  - [x] 57.31 Document the Codex-safe Supabase CLI invocation (`SUPABASE_TELEMETRY_DISABLED=1 npm_config_cache=/private/tmp/fhfhockey-npm-cache npx supabase ...`) or add a package script so future sandboxed CLI checks do not depend on home-directory cache writes. Added `npm run supabase:safe -- ...`, documented it in `web/README.md`, and verified `npm run supabase:safe -- --version` returns Supabase CLI `2.105.0`.

- [x] 58.0 NEW: Complete entity coverage for goalie and team ranking views
  - [x] 58.1 Build goalie ranking API/matrix rows using verified goalie deployment, GSAx, GSAA, quality-start, steal, and workload methodology. Added `/api/v1/contextual-rankings/goalies` backed by existing `goalie_stats_unified`, WGO/NST goalie fields, and `goalie_start_projections`; it aggregates season/last-N goalie windows, ranks qualified goalie rows for SV%, GSAx, GSAA/60, QS%, Steal Rate, and Start Share, includes role/workload context and source warnings, and pages results without creating redundant tables. Focused tests, TypeScript, and live localhost smokes pass for `save_percentage` and `steal_rate`.
  - [x] 58.2 Build goalie UI tab/card states with goalie-specific filters, sample confidence, and injury/call-up denominator explanations. Enabled the Goalies player-type segment on `/rankings`, added goalie-specific Min Starts and Min Shots filters, wired a goalie matrix table to `/api/v1/contextual-rankings/goalies`, exposed goalie sample confidence, SV%/GSAx/GSAA/60/QS%/Steal Rate/Start Share chips, pagination, source caveats, and a role caveat noting emergency call-up denominator adjustment remains planned. Browser verification on `localhost:3002/rankings?entity=goalies...` rendered rows with no console errors.
  - [x] 58.3 Build team ranking API/matrix rows using verified team-style methodology, luck components, shot quality, event rate, and control metrics. Added `/api/v1/contextual-rankings/teams` backed by existing `team_power_ratings_daily`, `team_underlying_stats_summary`, and `nst_team_stats` sources; it ranks offense/defense ratings, xGF/60, xGA/60, xGF%, shot quality, event rate, finishing/save/net luck, pace, and special-teams rating, and it surfaces raw/contextual team-style caveats plus snapshot mismatch warnings instead of creating redundant tables.
  - [x] 58.4 Build team UI tab/card states with team-style badges and explicit raw-vs-adjusted caveats. Enabled the Teams segment on `/rankings`, hid player-only filters for team mode, added a team matrix table with style badges, PP/PK/trend/context chips, pagination, metric sorting, source warnings, and an explicit raw-vs-adjusted team-style notice; browser verification on `localhost:3002/rankings?entity=teams...` rendered live team rows with no console errors.
  - [x] 58.5 Add acceptance tests proving skater, goalie, and team views all render live/planned/unavailable states correctly. Expanded `/rankings` page tests to exercise skater, goalie, team, and planned WAR URL states, including the live goalie/team endpoints and visible caveat copy; focused page tests and TypeScript pass.

- [x] 59.0 NEW: Close latest-source freshness for production rankings
  - [x] 59.1 Backfill NST all-situations, PP, and any xG/ICF-dependent sources past 2026-04-11 where source data exists. Added verified NST dataset groups for all-situations and PP skater sources, backfilled `2026-04-12` through `2026-04-16` with `datasetGroup=rankingFreshnessSkaterSources&overwrite=yes`, and verified targeted AS/PP NST tables now reach `2026-04-16`; AS source row counts match WGO on each repaired date and PP source rows are populated.
  - [x] 59.2 Run approved full rolling recompute after source refresh and PP-goals fallback changes. After adding the PP-combination HTML shift-report fallback and repairing `2026-04-12` through `2026-04-16`, the bounded latest-source rolling recompute upserted `2664` rows for `668` players in `1m 23s` with `coverageWarnings=0`, `freshnessBlockers=0`, freshness gate `PASS`, and HTTP `200`; the prior Postgres row-size error remains mitigated by rolling storage compaction and tracked for durable cleanup in task `73.0`.
  - [x] 59.3 Verify unpinned latest default requests for ixG/60, shot attempts/60, PP goals/60, and xG finishing metrics no longer require latest-calculable fallback. Local API verification on `2026-06-09` returned `snapshotDate=2026-04-16`, `latestAvailableSnapshotDate=2026-04-16`, and `snapshotSelectionReason=latest_available` for all-situations `ixg_per_60`, all-situations `shot_attempts_per_60`, PP `goals_per_60`, PP `ixg_per_60`, plus xG finishing metrics `expected_shooting_percentage`, `goals_above_expected`, and `unrealized_xg`.
  - [x] 59.4 Keep fallback-to-latest-calculable behavior as a safety net, but mark freshness fallback clearly in API/UI metadata. The contextual API continues to return `snapshotSelectionReason=latest_calculable_metric` plus a freshness message when fallback is used, and `/rankings` surfaces the API `meta.message` in matrix and Metric Explorer unavailable/fallback states.

- [x] 60.0 NEW: Make Results Luck Index live only after frozen-baseline persistence is production-safe
  - [x] 60.1 Define the production storage or snapshot source for frozen non-overlapping baselines. The production output remains `skater_composite_ratings.results_luck_index`, but code now locks a persistence contract requiring selected-window exclusion provenance before any non-null value can be written. Live verification on `2026-06-09` found `85,620` `player_baselines` rows with latest snapshot `2026-06-09`, but `0` non-null `skater_composite_ratings.results_luck_index` rows; `player_baselines` is explicitly blocked as a drop-in source because its sustainability payloads do not by themselves prove the selected rankings window was excluded.
  - [x] 60.2 Wire results_luck_index into the ranking API/matrix only after baseline leakage tests pass against live-like fixtures. Baseline leakage and component-aware scorer fixtures pass, the matrix registry now marks Results Luck as sparse live, and `playerMatrix.ts` now reads/sorts `skater_composite_ratings.results_luck_index` directly instead of falling back to MCM. Live publication on `2026-06-09` upserted `season` and `last20` 5v5 composite rows for all/L1-L4/P1-P3 contexts; SQL verification showed `last20` non-null Results Luck counts of `61` all, `13` L1, `15` L2, `9` L3, `5` L4, `12` P1, `3` P2, and `4` P3. Season rows intentionally remain null because the selected-window-excluded baseline gate is not valid for a full-season current window.
  - [x] 60.3 Add UI copy explaining “hot/lucky” versus “sustainable” and selected-window exclusion. The planned Results Luck matrix column now explains that values above `100` mean current results are running hotter than a selected-window-excluded baseline, and its planned reason explicitly blocks publication until frozen baseline provenance proves the selected window is excluded.
  - [x] 60.4 Add tests proving current-window values do not leak into the baseline. Existing focused coverage in `resultsLuckIndex.test.ts` verifies the selected current window is subtracted from the season numerator/denominator before calculating the baseline, covers thin-baseline peer blending, and covers peer fallback when a matching season baseline is unavailable; the test was rerun with the updated persistence contract.

- [x] 61.0 NEW: Decide and document adjusted team-style and adjusted defensive-model roadmap
  - [x] 61.1 For team style, either implement score/venue-adjusted 5v5 aggregates or label current team style as raw/contextual. Added `TEAM_STYLE_SOURCE_CONTRACT` and exposed it through rankings metadata; current team-style helpers are explicitly labeled `raw_contextual_5v5` until verified score- and venue-adjusted 5v5 xG/Fenwick/event-rate aggregates are wired.
  - [x] 61.2 For skater defense, complete task 40.0’s adjusted-defense source/model audit. Added `ADJUSTED_DEFENSE_MODEL_ROADMAP` documenting current source status: NHL on-ice attribution utilities exist, NST 5v5 zone-start context exists, score-state helpers exist, and `rolling_player_game_metrics` supports strength states, but no persisted adjusted defensive rankings design matrix/source is wired yet.
  - [x] 61.3 Add validation criteria before any adjusted model is allowed to replace contextual labels. The roadmap now requires chronological held-out validation, improvement over raw on-ice xGA/xGF baselines, regularization/shrinkage, stability checks across TOI/deployment buckets, and provenance proving teammate/opponent/score/zone/rest controls before contextual labels can be replaced.
  - [x] 61.4 Document methodology limitations in the rankings glossary. The metadata glossary now includes `raw_contextual_team_style` and `contextual_defensive_impact`, and `/api/v1/contextual-rankings/metadata` exposes both the team-style contract and adjusted-defense roadmap.

- [x] 62.0 NEW: Operationalize the rankings recompute pipeline
  - [x] 62.1 Document the ingestion → normalization → rolling recompute → snapshot publish flow. Added a `Contextual rankings pipeline` section to `web/README.md` covering source ingestion (`nst_gamelog_*`, `nst_gamelog_5v5_*`, WGO, line/special-teams context), `update-rolling-player-averages` writes to `rolling_player_game_metrics`, runtime ranking APIs read latest per-player rolling snapshots, and composite publishing writes `skater_composite_ratings`.
  - [x] 62.2 Add or verify scheduled jobs/queues/edge functions for post-game recompute. Verified `update-rolling-player-averages` is present in `rules/context/cron-schedule.md`, runs through `withCronJobAudit`, and is mapped in `/api/v1/db/cron-report`; also documented that `update-skater-composite-ratings` is audited when invoked but is not yet a scheduled post-game cron in the current inventory.
  - [x] 62.3 Ensure methodology_version and updated_at are updated on every published snapshot. Migration/schema verification shows published contextual snapshot tables (`entity_metric_rankings`, `skater_composite_ratings`) have `methodology_version`, `updated_at`, and update triggers; live verification on `2026-06-09` confirmed both tables expose those columns, `skater_composite_ratings` has `432` rows with populated values, and `entity_metric_rankings` is currently empty but column-compatible.
  - [x] 62.4 Add monitoring/logging checks for stale snapshots, null-only metric contexts, and failed recomputes. Added `npm run check:rankings-source-health`, which verifies source-dependent ranking endpoints return populated finite metric rows, reports stale/latest-calculable fallback snapshots, and fails on unavailable/null-only contexts; live verification against `localhost:3002` returned 25 finite rows for 5v5 goals/60, 5v5 shot attempts/60, 5v5 ixG/60, and PP goals/60 on snapshot `2026-04-16` with no fallback warnings. Failed recomputes remain covered by `withCronJobAudit` plus `/api/v1/db/cron-report`.

- [x] 63.0 NEW: Fix rankings snapshot row selection so the player pool is not limited to skaters who played on the latest NHL game date
  - [x] 63.1 Verify the live Supabase `rolling_player_game_metrics` table was not being capped by the 1,000-row Data API default for the default matrix request. Live SQL showed the previous exact-date snapshot had `216` distinct qualified 5v5 skaters, and localhost API/page verification showed page-size normalization returned `10` rendered rows, not `5`.
  - [x] 63.2 Identify the actual bug: `buildContextualRankingsSurface` selected rows with `game_date = latest_global_game_date`, which excluded players whose latest team/player row was on an earlier game date.
  - [x] 63.3 Change the rolling snapshot reader to page exact-date Supabase queries across the recent game-date window, dedupe to each player's newest row at or before the selected snapshot, and then rank the full peer pool.
  - [x] 63.4 Keep metric overlay ranks peer-group-relative by reusing a short-lived raw snapshot cache instead of filtering page `entityIds` before rank calculation.
  - [x] 63.5 Add regression coverage for Supabase pagination beyond 1,000 rows and latest-row-per-player snapshot selection.
  - [x] 63.6 Verify localhost `/api/v1/contextual-rankings/matrix` now returns `10` rows on page 1, `776` total ranked 5v5 skaters, `78` pages, and `776` qualified peers for metric cells.

- [x] 64.0 NEW: Optimize rankings matrix response time after full latest-row snapshot selection
  - [x] 64.1 Replace repeated per-metric ranking surface builds with a shared in-request ranked snapshot or multi-metric ranking builder. Added `buildContextualRankingsSurfaces` so the matrix fetches the latest-player snapshot once, ranks all requested live metric columns from shared candidates, and keeps each cell's rank/percentile peer-group-relative. Updated matrix tests to guard that the builder is called once for the shared metric set.
  - [x] 64.2 Consider a persisted latest-player-snapshot table or RPC/view that returns one current rolling row per player per season/strength/as-of date without scanning multiple game dates through PostgREST. Live Supabase verification found no existing latest-player rolling RPC/view. `distinct on (player_id)` and array-date RPC candidates were slower than the app fallback (`~48s` timeout-prone direct plan and `~8-15s` array/lateral plans), so this increment keeps the exact-date fallback and adds a targeted latest-snapshot index migration instead of adopting a slower RPC.
  - [x] 64.3 Add performance acceptance coverage for the default matrix endpoint so the full-player-pool fix remains under an agreed response-time budget. Added `web/scripts/check-rankings-matrix-performance.ts` and `npm run check:rankings-matrix-performance`; the smoke enforces `<=8s` cold and `<=2s` warm local budgets plus semantic guards for `10` visible rows, `>=700` ranked rows, `>=70` pages, and `>=700` qualified peers.

- [x] 65.0 NEW: Investigate cold matrix snapshot fetch performance after the shared multi-metric builder
  - [x] 65.1 Evaluate whether a database RPC/view can return the latest row per player using `distinct on (player_id)` or an indexed window query faster than multiple PostgREST date pages. Live `EXPLAIN ANALYZE` showed the direct DB distinct plan scanning much more than expected and timing out around `120s`; a lateral exact-date array plan completed but still took about `8.5s` in-database before returning full rows.
  - [x] 65.2 Investigate why a batched PostgREST `.in("game_date", dates)` snapshot query returned API failures around `8-9s` locally, while bounded parallel exact-date queries succeeded. The batched form caused API failures locally and live SQL chose broad hash/index scans; exact-date probes continue to use safer indexed reads, so the app keeps bounded parallel exact-date fetching.
  - [x] 65.3 Set an explicit cold and warm response-time budget for `/api/v1/contextual-rankings/matrix` before closing task `64.3`. Provisional local budget is cold `<=8s` after code reload/cache miss and warm `<=2s` with the short-lived snapshot cache, while preserving `776` ranked 5v5 skaters and peer-group-relative metric counts. `64.3` remains open until this is covered by an automated or repeatable perf smoke.
  - [x] 65.4 Apply and verify `migrations/20260608_add_rolling_player_latest_snapshot_index.sql`, then re-measure cold `/api/v1/contextual-rankings/matrix` performance and confirm the plan uses `idx_rpgm_latest_snapshot_lookup`. Applied live through Supabase on `2026-06-08`; narrow `EXPLAIN ANALYZE` uses `idx_rpgm_latest_snapshot_lookup`, and `npm run check:rankings-matrix-performance` returned `10` rows, `776` total ranked rows, `78` pages, `776` metric peers, with timings of `7006ms` cold and `1009-1016ms` warm.

- [x] 66.0 NEW: Complete performant Deployment Tiers special-teams rollups
  - [x] 66.1 Ship the first live Deployment Tiers tab without creating redundant tables: added `/api/v1/contextual-rankings/deployment-tiers`, a page panel, URL-state fetch wiring, and tests. The live tab currently uses one shared 5v5 ranking snapshot and returns EV line buckets, defense pair buckets, and PP unit-label buckets in about `5.5s` locally.
  - [x] 66.2 Build a lightweight direct snapshot rollup or persisted helper for PP and PK strength summaries so `PP1-PP3` and `PK1-PK2` can use true special-teams metric contexts without calling the full ranking-surface builder once per strength/bucket. Added direct latest-player PP/PK snapshot rollups with paged exact-date reads, metadata paging, a short-lived direct snapshot cache, and a special-teams TOI gate independent from the 5v5 page minimum; localhost verification with default `min_toi=300` returned EV, PP, and PK sections with `sourceState: "available"`.
  - [x] 66.3 Add a Deployment Tiers performance smoke with explicit cold/warm budgets comparable to the matrix smoke, including guards that EV, PP, and PK sections report honest `available`/`partial`/`unavailable` source states. Added `npm run check:rankings-deployment-tiers-performance`; the smoke enforces populated EV/PP/PK buckets and available source states with `45s` cold and `5s` warm local budgets. Verification against `localhost:3002` returned `17624ms` cold, then `4815ms` and `3671ms` warm.
  - [x] 66.4 Once `66.2` and `66.3` pass, mark task `54.2` complete and remove the temporary PK source-unavailable limitation from the tab copy.

- [x] 67.0 NEW: Restyle rankings matrix metric cells as bordered percentile chips
  - [x] 67.1 Review `fhfh-styles.md`, `brand-style-cheat-sheet.md`, the provided mockups, and `PlayerPickupTable` percentile styling before changing `.matrixMetricCell`.
  - [x] 67.2 Replace full-cell gradient fills with compact rounded-square metric chips that use a solid semantic border, semi-transparent background, and readable data typography while preserving dense table scanning. Updated `.matrixMetricCell` and the matrix legend to use semantic CSS variables for border/text color plus translucent tone backgrounds.
  - [x] 67.3 Keep existing percentile tier semantics, lower-is-better handling, muted/unavailable states, and matrix sort/selection behavior intact. Reused the existing `scoreTileGreen`/`scoreTileAmber`/`scoreTileOrange`/`scoreTileMuted` class mapping and left the metric-cell data contract unchanged.
  - [x] 67.4 Verify desktop and mobile layouts so chip text does not overlap, resize rows unexpectedly, or reduce matrix readability. Desktop Chrome verification at `localhost:3002/rankings?tab=rankings&page_size=10` showed compact bordered chips inside the contained horizontal matrix scroller; no table/page runtime error after clearing generated `.next` output.
  - [x] 67.5 Update or add focused tests only if the style change alters rendered class/state behavior; otherwise record visual QA notes in this task. No class/state behavior changed, so existing ranking page tests were kept and visual QA was recorded above.

- [x] 68.0 NEW: Fix recurring local Next dev watcher `EMFILE` and generated `.next` corruption
  - [x] 68.1 Reproduce the repeated `Watchpack Error (watcher): Error: EMFILE: too many open files, watch` failure during `/rankings` browser verification and identify whether it is caused by file-descriptor limits, Chrome refresh loops, Next watcher scope, cache churn, or generated output writes. The direct rerun did not reproduce `EMFILE`, but config review found the existing top-level `watchOptions` was not being applied to Webpack dev watching and the failure mode matched native watcher exhaustion plus generated `.next` cache churn.
  - [x] 68.2 Add a durable local-dev mitigation, such as watcher excludes, polling configuration, script-level environment defaults, or documented file-descriptor limits, so `.next/server/pages-manifest.json`, `_document.js`, `_error.js`, and webpack cache rename failures do not recur during QA. Added Webpack dev watcher ignores for `.next`, `node_modules`, coverage, Storybook output, archived task artifacts, and the duplicate package-lock artifact; added `npm run dev:stable` with Watchpack polling as a lower-file-descriptor fallback.
  - [x] 68.3 Document `.next` cleanup and dev-server restart as a fallback recovery step only after the primary watcher mitigation is in place. `web/README.md` now documents `npm run dev:stable` first and `rm -rf .next` only as recovery after generated output is already corrupted; `dev:stable` verified on `localhost:3002` with `/rankings?tab=splits` returning HTTP `200`.

- [x] 69.0 NEW: Add Trending tab performance smoke and optimize cold/warm budgets
  - [x] 69.1 Add a repeatable smoke script for `/api/v1/contextual-rankings/trending` that verifies status `200`, populated rows, expected windows (`season`, `last20`, `last10`, `last5`), latest snapshot metadata, and semantic first-row shape. Added `web/scripts/check-rankings-trending-performance.ts` and `npm run check:rankings-trending-performance`.
  - [x] 69.2 Set explicit local cold and warm response-time budgets for Trending comparable to the matrix and Deployment Tiers smokes; initial localhost verification returned about `18.3s` cold and `6.5s` warm for `limit=25`. The repeatable smoke now enforces `25s` cold and `8s` warm budgets; verification against `localhost:3002` returned `9439ms` cold, then `266ms` and `365ms` warm.
  - [x] 69.3 Optimize the Trending builder if measured warm responses remain above budget, prioritizing shared ranked snapshots, fewer redundant metric-window passes, or a persisted latest-player snapshot helper only after verifying no existing repo/Supabase function already covers it. No additional optimization was needed in this pass because warm runs were below the explicit budget.

- [x] 70.0 NEW: Add Splits tab performance smoke and reduce repeated ranking-surface work
  - [x] 70.1 Add a repeatable smoke script for `/api/v1/contextual-rankings/splits` that verifies status `200`, populated rows, strength/window/deployment sections, source states, snapshot metadata, and explicit unsupported home/away metadata. Added `web/scripts/check-rankings-splits-performance.ts` and `npm run check:rankings-splits-performance`.
  - [x] 70.2 Set explicit local cold and warm response-time budgets for Splits; initial localhost verification for `limit=10` returned HTTP `200` but took about `35.1s`. The repeatable smoke now enforces `45s` cold and `20s` warm budgets; verification against `localhost:3002` returned `17371ms` cold and `549ms` warm.
  - [x] 70.3 Optimize the Splits builder if measured responses remain above budget, prioritizing shared ranked snapshots across comparison contexts or a verified existing helper before creating any new persistence path. No additional optimization was needed in this pass because cold and warm runs were below the explicit budgets.

- [x] 71.0 NEW: Standardize browser E2E infrastructure before closing `/rankings` real-browser coverage
  - [x] 71.1 Choose the repo-standard E2E runner and browser source for local and CI runs. Chose Playwright-managed Chromium as the repo-standard E2E runner, separate from existing `puppeteer-core` browserless/webhook usage.
  - [x] 71.2 Add the minimal config, script, and CI/local dependency contract for true browser tests without coupling rankings QA to the webhook browserless runtime. Added `@playwright/test`, `playwright.config.ts`, `e2e:install`, `test:e2e`, `test:e2e:rankings`, and README instructions for default, project-local, and prestarted-server browser runs.
  - [x] 71.3 Implement `/rankings` real-browser coverage from task `57.30` only after `71.1` and `71.2` are complete. Added `web/e2e/rankings.spec.ts` and verified Playwright discovers it with `npm run test:e2e:rankings -- --list`; actual Playwright browser execution is still blocked in this Codex macOS sandbox by `MachPortRendezvousServer ... Permission denied`, so user-approved Chrome extension verification covered the real browser runtime on `2026-06-09`. The run verified live 5v5 matrix load, page-2 pagination, More Filters, Metric Explorer URL navigation, and zero Chrome console errors.

- [x] 72.0 NEW: Verify goalie/team ranking sources before extending entity coverage
  - [x] 72.1 Check existing docs/code for goalie and team sources before creating any new table or fetch path. Verified existing sources include `wgo_goalie_stats`, `goalie_stats_unified`, `goalie_start_projections`, `team_power_ratings_daily`, `team_underlying_stats_summary`, and `nst_team_stats`.
  - [x] 72.2 Live-check row counts and latest dates using existing Supabase credentials without exposing secrets. Verified `wgo_goalie_stats` has `139,893` rows through `2026-06-06`, `goalie_stats_unified` has `10,050` rows through `2026-06-06`, `goalie_start_projections` has `7,565` rows through `2026-06-09`, `team_power_ratings_daily` has `7,763` rows through `2026-06-09`, `team_underlying_stats_summary` has `174,020` rows through `2026-04-07`, and `nst_team_stats` has `64` rows.
  - [x] 72.3 Expose a typed rankings entity coverage contract through metadata so goalie/team work can reuse existing sources and clear gates. Added `entityCoverageContracts.ts`, exposed `entityCoverage` from rankings metadata, documented goalie/team source gates and caveats, and added focused metadata tests. TypeScript passes.

- [x] 73.0 NEW: Replace temporary rolling wide-row storage compaction with a durable storage model
  - [x] 73.1 Audit `rolling_player_game_metrics` persisted columns and classify fields as ranking-read fields, recompute-only support fields, historical compatibility fields, and deprecated fields before deleting or moving any data. Reused the prior schema-surface audit and reconciled it against current generated types: `rolling_player_game_metrics` now exposes `1114` row fields, while the rankings/deployment read contract uses the `118` fields in `ROLLING_RANKING_SELECT_FIELDS`. Current downstream readers also include FORGE projection trend fetches, projection fallback skater selection, sustainability metadata, validation payloads, and legacy audit scripts, so this table cannot be narrowed until those compatibility surfaces are accounted for.
  - [x] 73.2 Decide whether to split recompute-only/historical rolling components into a support table, JSONB payload, or narrower compatibility view so populated xG/ICF/PP fields cannot exceed Postgres row-size limits. Decision: keep the primary `rolling_player_game_metrics` row as the narrow latest/ranking-read contract, move recompute-only historical/all/last3/3YA/career/avg compatibility payloads into a keyed support table or JSONB support column/table, and provide a compatibility reader/view only for legacy validation/audit/projection paths that still need the old wide surface. This matches the current temporary compaction behavior, which preserves ranking fields plus PP support fields while nulling storage-heavy historical families.
  - [x] 73.3 Add a migration and typed contract for the durable storage shape only after verifying no existing table, view, or function already provides the needed support payload. Verification against generated Supabase types and schema docs found only `rolling_player_game_metrics`, `rolling_games`, NHL xG rolling aggregate tables, `player_baselines`, and `truncate_rolling_player_game_metrics`; no existing rolling support table/view/function provides the needed keyed historical payload. Added `rolling_player_metric_support_payloads` with RLS enabled, public read/service-role write grants, snapshot/payload indexes, and the matching TypeScript payload contract.
  - [x] 73.4 Replace `compactRollingUpsertRowForRankingStorage` with the durable storage/read contract once migration coverage and rollback safety are in place. The writer now splits each rolling row into a narrow ranking row plus a `rolling_player_metric_support_payloads` JSONB row, upserts support payloads before the primary row, preserves pruned historical/deprecated fields instead of discarding them, and extends the truncate RPC migration so full refreshes clear both primary and support tables. Focused rolling tests and TypeScript pass; live recompute requires the new migration to be applied first.

- [x] 74.0 NEW: Resolve residual `powerPlayCombinations` freshness blockers on the latest production rolling recompute
  - [x] 74.1 Investigate why `update-power-play-combinations` returns `No power play combinations found` for game IDs `2025021301` through `2025021312`, while rolling/WGO still expect PP TOI context for some players. Live NHL verification found JSON shiftcharts return `total=0` for those finished games despite penalty plays, while the repo's existing NHL HTML shift-report fallback returns populated shift rows.
  - [x] 74.2 Verify whether those game IDs lack NHL shift-chart PP data, have source-format differences, or need a WGO-backed PP-unit fallback path. Verified this is an NHL JSON shiftcharts source-format/availability gap, not missing gamecenter play-by-play; HTML reports for examples `2025021301` and `2025021312` returned `708` and `736` shift rows respectively.
  - [x] 74.3 If a fallback is needed, use existing `powerPlayCombinations` columns and verified WGO PP TOI/team context; do not create a redundant table. Reused the existing raw-gamecenter HTML shift-report fallback and wrote the same `powerPlayCombinations` row contract; no new table or redundant WGO-only PP-unit source was added.
  - [x] 74.4 Re-run the bounded rolling recompute for `2026-04-12` through `2026-04-16` and require `freshnessBlockers=0` before closing task `59.2`. PP-combination repair processed all `37` games with `0` failures, then rolling recompute returned HTTP `200`, `coverageWarnings=0`, and `freshnessBlockers=0`.

- [x] 75.0 NEW: Reduce noisy PP combination repair logs before routine browser/API runs
  - [x] 75.1 Remove or gate the raw `console.log(rows)` output in `update-power-play-combinations/[gameId].ts`, which currently dumps every generated PP combination row during bulk repair.
  - [x] 75.2 Replace it with a compact audited summary containing game ID, row count, teams processed, and failure reason samples. The updater now logs a compact summary with `gameId`, `rowCount`, `teamCount`, and `shiftSource`, and returns the same summary to callers.
  - [x] 75.3 Add or update focused route tests if logging behavior becomes part of the response or audit contract. Added focused updater tests covering JSON shiftchart behavior, HTML shift-report fallback behavior, and the compact summary return; existing bulk route tests still pass.

- [x] 76.0 NEW: Implement production-safe Results Luck persistence before live matrix/API wiring
  - [x] 76.1 Verify no existing table, view, function, or scheduled writer already persists selected-window-excluded Results Luck provenance; current `player_baselines` payloads remain blocked as a drop-in source because they do not prove selected-window exclusion. Repo/generated-type verification found only `player_baselines`, sustainability score/prior tables, `skater_composite_ratings`, rating daily tables, `sustainability_trends_v`, and `refresh_team_power_ratings`; no table/view/function/job persists `baselineWindowExcluded=true` Results Luck provenance. Current code has helper math in `resultsLuckIndex.ts`, methodology requirements in `skaterCompositeMethodology.ts`, planned matrix copy, and a composite writer that still writes `results_luck_index: null`.
  - [x] 76.2 Define the exact current results signal inputs for `results_luck_index` using verified rolling fields only, including how goals-above-expected, SAX%, IPP, and on-ice shooting context are weighted. Added a typed methodology contract with weights summing to `1.0`: goals above expected `0.35`, SAX% `0.25`, IPP `0.20`, and on-ice shooting context `0.20`. The contract names only `rolling_player_game_metrics` field families already present in generated types, including goals/ixG/shot-attempt components, `ipp_points_*`, `ipp_on_ice_goals_for_*`, `on_ice_sh_pct_*`, `oi_gf_*`, and `oi_xgf_*`; methodology tests and TypeScript pass.
  - [x] 76.3 Persist baseline provenance in `skater_composite_ratings.components_json` or a verified support table with `baselineSource`, `baselineSnapshotDate`, `baselineWindowExcluded`, `baselineWeight`, `peerBaselineValue`, and warning metadata. The composite writer now persists a `resultsLuck.baselineProvenance` block in `components_json` with the required fields, warning metadata, and the exact signal component contract while keeping `results_luck_index: null` until the selected-window exclusion fixture tests pass. Composite writer tests and TypeScript pass.
  - [x] 76.4 Add live-like fixture tests proving the selected current window is excluded from the baseline before any non-null `results_luck_index` is written. Added an explicit `canPersistResultsLuckIndex` gate and fixture tests proving a non-season peer fallback index is not publishable when `currentWindowExcluded=false`, while a selected-window-excluded result is publishable. Results Luck tests, composite methodology/writer tests, and TypeScript pass.
  - [x] 76.5 Only after `76.1` through `76.4` pass, flip the matrix registry/API from planned to live and close task `60.2`. Completed on `2026-06-09`: registry/source metadata was flipped to sparse live, matrix composite cells/sorting now use `results_luck_index`, focused Results Luck/matrix tests and TypeScript pass, `season` and `last20` 5v5 composite rows were published for all/L1-L4/P1-P3, and direct matrix smoke for `window=last20&deployment=L2&sort_metric=results_luck_index` returned `121` rows with top sort values matching live Results Luck values.

- [x] 77.0 NEW: Verify Playwright rankings E2E on a host or CI runner that can launch Chromium
  - [x] 77.1 Run `npm run e2e:install` and `npm run test:e2e:rankings` on the host machine or CI environment where Chromium can launch normally, or use an explicit user-approved host-browser fallback when the Codex sandbox cannot launch Playwright Chromium. Rechecked on `2026-06-09`: the default Playwright install failed in this sandbox because `~/Library/Caches/ms-playwright/__dirlock` cannot be created; a workspace-local install with `PLAYWRIGHT_BROWSERS_PATH=/Users/tim/Code/fhfhockey.com/web/.ms-playwright` succeeded, but the actual test still failed before page load with `MachPortRendezvousServer ... Permission denied (1100)`. Added `npm run e2e:install:workspace` and `npm run test:e2e:rankings:workspace`, documented them in `web/README.md`, and git-ignored `.ms-playwright`; `npm run test:e2e:rankings:workspace -- --list` discovers the rankings spec. The downloaded workspace-local browser cache was removed after verification. User then approved Chrome/Computer fallback, and host Chrome verified the live `/rankings` runtime on `2026-06-09` with matrix load, pagination, filters, Metric Explorer navigation, and no console errors.
  - [x] 77.2 If CI is available, add the browser install and `test:e2e:rankings` command to the CI workflow with Playwright artifacts retained on failure. Added `.github/workflows/rankings-e2e.yml` as a manual workflow that installs dependencies, installs Playwright Chromium, runs `npm run test:e2e:rankings`, and uploads Playwright reports/test-results on failure. The workflow can test a deployed URL via `workflow_dispatch` `base_url`, or start the local Next.js server when Supabase secrets are available.
  - [x] 77.3 After the real browser run passes, close task `71.3` and task `57.30`. Closed after user-approved host Chrome verification passed on `2026-06-09`; Playwright remains available through the manual CI workflow for a native runner.

- [x] 78.0 NEW: Apply rolling support-payload migration before the next live rolling recompute
  - [x] 78.1 Apply `migrations/20260609_create_rolling_player_metric_support_payloads.sql` to Supabase so `rolling_player_metric_support_payloads` exists and `truncate_rolling_player_game_metrics()` clears both primary/support tables. Applied the migration to the linked Supabase database on `2026-06-09`; live SQL verification confirmed the table exists, RLS is enabled, `public_read` SELECT policy exists for `anon`/`authenticated`, expected indexes exist, and `truncate_rolling_player_game_metrics()` now includes `rolling_player_metric_support_payloads`.
  - [x] 78.2 Regenerate `web/lib/supabase/database-generated.types.ts` after the migration so the live schema/type docs include the new support table. Regenerated types with Supabase CLI `2.105.0`; generated type checks now include `rolling_player_metric_support_payloads` and the existing truncate RPC.
  - [x] 78.3 Run a small bounded rolling recompute after migration application and verify both `rolling_player_game_metrics` and `rolling_player_metric_support_payloads` receive rows for the same `(player_id, game_date, strength_state)` keys. Ran a one-player, one-day, one-strength live recompute for `player_id=8478402`, `game_date=2026-04-16`, `strength=5v5`; it upserted `1` primary row with `coverageWarnings=0`, `suspiciousOutputWarnings=0`, `unknownGameIds=0`, and `freshnessBlockers=0`. Live verification confirmed a matching support payload row with `payload_schema_version=1`, `prunedFieldCount=737`, `727` historical compatibility fields, and `10` deprecated compatibility fields. Supabase CLI advisors could not run in this sandbox because the available DB URL resolves through a local `postgres` host for `psql` but not for the CLI; direct SQL verification covered the migration contract.

- [x] 79.0 NEW: Implement component-aware Results Luck scoring before flipping the matrix/API live
  - [x] 79.1 Build a Results Luck scorer that handles the signed/difference semantics of goals above expected and SAX%, the ratio semantics of IPP, and the contextual team/on-ice shooting component without forcing every component through the generic `current / baseline` rate helper. Added `calculateComponentAwareResultsLuckIndex`, which scores signed-difference components around `100` with an explicit scale, ratio components with current/baseline ratio semantics, and contextual on-ice components with signed context semantics.
  - [x] 79.2 Feed the scorer from verified rolling fields and persisted `baselineProvenance`, then call `canPersistResultsLuckIndex` before writing any non-null `skater_composite_ratings.results_luck_index`. The composite writer now consumes verified raw rolling Results Luck sources from task `80.0`, runs `calculateComponentAwareResultsLuckIndex`, and calls an explicit component-aware persist gate before writing any non-null `results_luck_index`; blocked source rows still write null with provenance and score diagnostics.
  - [x] 79.3 Add fixture tests covering hot, cold, normal, thin-baseline blend, non-season fallback blocked, and season peer-fallback cases before closing `76.5` and `60.2`. Added component-aware fixtures for hot, cold, normal, already-blended thin baselines, blocked non-season peer fallback, and allowed season peer fallback; `resultsLuckIndex.test.ts` passes.

- [x] 80.0 NEW: Add raw rolling Results Luck source extraction for the composite writer
  - [x] 80.1 Verify whether an existing helper already fetches latest per-player rolling rows for composite snapshots with arbitrary field selection before adding a new query path. Repo search found only private contextual-ranking/deployment readers with fixed `ROLLING_RANKING_SELECT_FIELDS`, a projection-only EV/PP trend reader with a fixed select clause, and validation payload code that selects `*` for one player; no reusable arbitrary-field composite snapshot reader exists.
  - [x] 80.2 Add a narrow Results Luck rolling source reader for the selected snapshot/window that returns current and selected-window-excluded baseline component values for goals above expected, SAX%, IPP, and on-ice shooting context. Added `resultsLuckSources.ts` with a fixed field allowlist, pure row extractor, and Supabase fetch wrapper; tests verify selected-window-excluded values for goals above expected, SAX%, IPP, and on-ice GF-above-xGF context.
  - [x] 80.3 Wire the reader into `skaterCompositeWriter` so `calculateComponentAwareResultsLuckIndex` runs with persisted `baselineProvenance`, but still writes `results_luck_index: null` unless the publish gate passes. `buildSkaterCompositeRatingRows` now fetches Results Luck rolling sources for the selected snapshot/window/player pool, passes each source to the row builder, and stores component score/provenance diagnostics in `components_json`.
  - [x] 80.4 Add writer-level fixtures proving non-null `results_luck_index` is written only when all required component baselines are verified and `baselineWindowExcluded=true`. Added writer fixtures for a publishable selected-window-excluded source and a blocked non-season peer fallback; Results Luck source/scorer/writer tests and TypeScript pass.

- [x] 81.0 NEW: Fix Results Luck matrix composite read/sort bug found during live flip
  - [x] 81.1 Verify whether the matrix API already maps `skater_composite_ratings.results_luck_index` independently from MCM before publishing the live registry flip. Inspection found `playerMatrix.ts` only treated `mcm_score` and `beast_tier` as composite keys, and composite sorting reused `mcm_score` for every composite metric.
  - [x] 81.2 Add a metric-specific composite value selector so Results Luck cells and sort order use `results_luck_index`, while MCM and BEAST retain their existing MCM percentile behavior. Patched `playerMatrix.ts` to include `results_luck_index` in the composite key set, render its raw/formatted values from `results_luck_index`, clamp its display percentile, and sort by the requested composite field.
  - [x] 81.3 Add regression coverage and live verification. Added a player-matrix unit test proving `sort_metric=results_luck_index` orders by Results Luck rather than MCM; focused tests and TypeScript pass, and the live `last20` L2 matrix smoke returned top rows sorted by matching live Results Luck values.

- [x] 82.0 NEW: Fix ranking freshness NST dataset-group gap for ES/PK sources
  - [x] 82.1 Verify whether the existing bounded NST freshness group already covered all source tables needed for ranking-facing skater metrics before adding new fetch logic. Live verification found AS and PP tables were current through `2026-04-16`, but ES and PK counts/rates were still stuck at `2026-04-11`; code inspection showed `rankingFreshnessSkaterSources` omitted `evenStrength*` and `penaltyKill*` dataset types.
  - [x] 82.2 Extend `rankingFreshnessSkaterSources` to include ES and PK counts/rates/OI dataset types so future bounded freshness backfills can target all ranking-relevant skater NST sources without creating a redundant fetch path. Updated `update-nst-gamelog.ts` and its request-scoping test.
  - [x] 82.3 Backfill the immediate stale ES/PK source gap and verify API freshness. Backfilled ES counts/rates and PK counts/rates for `2026-04-12` through `2026-04-16`, recomputed bounded EV/PK rolling rows, and verified unpinned contextual ranking requests for all/EV/PP/PK `shot_attempts_per_60`, all-situations `ixg_per_60`, and PP `goals_per_60` now use `snapshotSelectionReason=latest_available` at `2026-04-16`.

- [x] 83.0 NEW: Publish adjusted skater defensive impact only after the existing adjusted-impact output contract is live
  - [x] 83.1 Apply or verify `migrations/20260527_create_nhl_xg_adjusted_impact_tables.sql` in Supabase so `nhl_xg_adjusted_impact_model_runs` and `nhl_xg_adjusted_player_impacts` exist with RLS enabled, expected indexes, and no duplicate rankings-specific output table. Live verification on `2026-06-09` found the migration absent from Supabase history and both tables missing, so the existing table migration was applied along with `migrations/20260609214122_enable_rls_nhl_xg_adjusted_impact_tables.sql`. Direct SQL verification confirmed both tables exist, RLS is enabled, `public_read` SELECT policies are present for `anon`/`authenticated`, expected primary/ranking indexes exist, and generated Supabase types now include the live tables.
  - [x] 83.2 Run a full-season adjusted-impact dry run with the approved shot-goal xG source model, require leakage validation and chronological held-out validation to pass, and record model-run validation metrics before writing rows. Dry run against approved source model `logistic_l2-s20252026-p1-st1-f1-cfg9bac2706`, feature version `1`, season `20252026`, `minPlayerRows=100`, `minValidationRows=5000`, and `iterations=100` returned `118,610` prediction rows, `118,610` feature rows, `1,048,367` shift rows, `117,846` design rows, and `764` skipped design rows. Leakage validation passed with no warnings. Chronological held-out validation passed with `108,188` training rows through `2026-04-11`, `9,658` validation rows from `2026-04-12` through `2026-05-26`, validation MSE `0.023591`, baseline MSE `0.025369`, and MSE improvement `7.008554%`.
  - [x] 83.3 Populate `nhl_xg_adjusted_impact_model_runs` and `nhl_xg_adjusted_player_impacts` only after task `83.2` passes, then verify row counts, latest provenance, feature version, target family, and player coefficient coverage. The non-dry-run endpoint passed the same leakage/held-out gates and upserted `1` model run plus `802` player-impact rows. Direct SQL verification found target family `on_ice_xg_differential_v1`, source model `logistic_l2-s20252026-p1-st1-f1-cfg9bac2706`, feature version `1`, season `20252026`, `117,846` training rows, `940` training players, model MSE `0.024134`, and `802` player rows with held-out validation provenance set to passed.
  - [x] 83.4 Decide whether adjusted-impact coefficients should remain a methodology-only diagnostic, become a rankings metadata source, or replace contextual defensive labels for a gated defensive metric; do not surface it on `/rankings` until provenance and validation metadata are available for every published row. Decision for this iteration: keep the adjusted-impact coefficients as a live methodology/diagnostic source only. They are validated and persisted, but they should not replace contextual defensive labels yet because the current model is an on-ice xG differential baseline, not a defense-specific promoted rankings metric with zone-start/rest/stability display contracts.
  - [x] 83.5 If the adjusted-impact layer is promoted to rankings, add metric registry/API/UI tests proving it is labeled as adjusted, distinct from raw contextual defense, and unavailable when live model provenance is missing or fails validation. Not promoted in this iteration; no `/rankings` registry/API/UI surface was added. Future promotion testing is tracked in task `84.0`.

- [x] 84.0 NEW: Promote adjusted-impact coefficients into rankings only after a defense-specific product contract is defined
  - [x] 84.1 Review the live `nhl_xg_adjusted_player_impacts` coefficient scale, stability by TOI/deployment bucket, player samples, and sign/direction semantics before deciding any user-facing score transformation. Live review of `802` player-impact rows found coefficient range `-0.001158` to `0.000851`, median `0.000004`, p05 `-0.000526`, p95 `0.00044985`, median sample `1,479` rows, and average approximate standard error `0.00508858`. Joined samples show positive and negative coefficients are not a direct replacement for current contextual Defense Rating: top positive examples include Beck Malenstyn, Travis Sanheim, and Mike Matheson, while top negative examples include Nick Suzuki, Kirill Kaprizov, Cole Caufield, Quinn Hughes, and Lane Hutson. Position bucket review found forwards average `-0.00003202`, defensemen average `0.00003985`, and `77` adjusted-impact rows missing current composite deployment joins.
  - [x] 84.2 Decide whether the promoted metric is overall adjusted xG impact, defense-only adjusted impact, or a separate methodology annotation for the existing Defense Rating. Decision: do not promote to a user-facing rankings metric in this iteration. The live coefficient is an overall on-ice xG differential baseline with teammate/opponent/context controls, not a defense-only decomposition or a validated replacement for contextual Defense Rating. Treat it as methodology/diagnostic evidence until task `84.3` defines the missing promotion controls.
  - [x] 84.3 Add missing context controls if required for promotion, especially explicit zone-start/rest controls and a defense-specific target or decomposition if the metric is meant to replace contextual defense. Added `adjustedImpactPromotionContract` and exposed it through rankings metadata so adjusted-impact remains `diagnostic_live` and `blocked_missing_controls` for rankings promotion. Verification found `nhl_xg_shot_features` live payloads include score, strength, shot, shift-age, deployment, and zone-code context, but `0` of `162,000` feature rows contain `ownerRestDays` or `opponentRestDays`; the repo has a travel/fatigue table migration and builder, but the table is not installed live or joined into adjusted-impact rows. NST 5v5 on-ice zone-start tables exist as source candidates but are not transformed into adjusted-impact controls. A defense-specific target/decomposition is still missing, so adjusted-impact must not replace contextual Defense Rating.
  - [x] 84.4 Add rankings registry/API/UI tests proving promoted adjusted-impact metrics are labeled as adjusted, distinct from raw contextual defense, and unavailable when live model provenance is missing or fails validation. Because adjusted-impact was not promoted in this iteration, added registry/metadata/API/UI guards instead: metadata exposes `adjustedImpactPromotion.readiness.status=blocked_missing_controls`, the published metric list excludes `adjusted_xg_impact` and `adjusted_defensive_impact`, and the Rankings Metric Explorer test asserts adjusted-impact labels are not rendered from the current metric payload.
