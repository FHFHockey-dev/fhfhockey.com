## Relevant Files

- `tasks/TASKS/wigo-charts-audit/README.md` — Source audit and per-component plan index.
- `web/hooks/useWigoPlayerDashboard.ts` and its existing test — Player/season context and aggregate queries.
- `web/utils/fetchWigoPlayerStats.ts` and its existing test — Aggregate precedence, totals, game-log queries and units.
- `web/pages/api/v1/db/calculate-wigo-stats.ts` and `web/__tests__/pages/api/v1/db/calculate-wigo-stats.test.ts` — Aggregate writer and reconstruction coverage.
- `web/components/WiGO/StatsTable.tsx`, `StatsTable.test.tsx`, `StatsTableRowChart.tsx`, `StatsTableRowChart.test.tsx` — Expanded-chart identity, errors and references.
- `web/components/WiGO/WigoComparisonMatrix.tsx` — Existing uncommitted wide-screen matrix; preserve and verify alongside the standard table.
- `web/components/WiGO/PerGameStatsTable.tsx` and its existing test — Snapshot, season gating and pace.
- `web/utils/calculateWigoRatings.ts`, `fetchWigoRatingStats.ts`, `fetchWigoPercentiles.ts` and their existing tests — Rating/cohort calculations and caching.
- `web/hooks/usePercentileRank.ts` and its existing test — Radar source and missing-data behavior.
- `web/components/WiGO/teamPerformanceDriverModel.ts` and `teamPerformanceDrivers.test.ts` — Four team-context metrics and cohort validation.
- `web/components/WiGO/WigoDashboardSections.tsx`, `web/pages/wigoCharts.tsx` — Component wiring and responsive behavior.

### Notes

- Generated from all 20 component plans using `tasks/TASKS/rules/generate-tasks.mdc` on 2026-09-19. The source filename is README.md, hence `tasks/tasks-README.md`.
- Keep one parent per original plan. Execution order: 1 → 13/14/16/19 → 5/6/15/17/18 → 8–12/20 → 2–4/7. Independent correctness work may proceed while source-dependent tasks remain unresolved.
- The working tree already contains layout changes, an 84-game pace change, radar work and game-log error propagation. These are pre-existing work, not completion claims for this task. Reconcile and verify before checking affected items off.
- Use existing Vitest files where possible: `npm test -- --run <paths>` from `web/`. Run targeted lint/type checks as warranted; no routine production build.
- Every checked item needs evidence. Unchecked live-data tasks remain open even when unit tests pass. Production writes, recomputes, migrations and deployments require separate authorization.
- Preserve all 36 metrics, seven timeframes, DIFF and supporting components. Preserve existing user edits and the current layout direction.

## Tasks

- [ ] 1.0 Shared pipeline and responsive shell ([source plan](TASKS/wigo-charts-audit/00-shared-pipeline.md))
  - [x] 1.1 Reconcile the audit with existing uncommitted WiGO work before editing; preserve prior layout and calculation changes.
  - [ ] 1.2 Record an explicit context contract: player/team identity, requested/applied season, regular-season versus playoffs, strength, as-of date, source completeness and unit for every response. Resolve season before labeling data “This season”; preserve intentionally historical comparisons.
  - [ ] 1.3 Identify refresh jobs and last_updated/source dates for wgo_skater_stats, totals, wigo_career/recent/rates, percentile tables and both RPCs. Trace only the writers actually serving this page. Missing writer/deployed SQL evidence is a blocked validation item, not permission to infer correctness.
  - [ ] 1.4 Measure cold load, warm revisit, player switch, row expansion, min-GP change and mobile tab change: request count, payload bytes, table-ready and all-panels-ready time, chart mounts and calculation time. Set a performance budget from this baseline.
  - [ ] 1.5 Share narrowly scoped React Query inputs: player/season totals; compatible season game logs; season/strength league cohorts with player selection and min-GP filtering downstream. Preserve explicit errors and provenance. Choose stale times by refresh cadence; do not add a universal data framework.
  - [ ] 1.6 Remove hidden duplicate chart/effect work with a single responsive tree or hydration-safe conditional mounts. Keep all table content available on mobile. Track tab URL changes and player/season changes consistently.
  - [ ] 1.7 Verify acceptance: Extend useWigoPlayerDashboard.test.tsx and relevant panel tests. Verify rapid A→B→A selection, URL back/forward, season rollover, failed season lookup, cold/warm loads and mobile/desktop breakpoints. Acceptance: no wrong-context data, no redundant fetch for identical shared inputs during one fresh-cache interaction, and measured improvement with full feature parity.

- [ ] 2.0 Page header, search and selection ([source plan](TASKS/wigo-charts-audit/01-page-header.md))
  - [ ] 2.1 Trace search→player record→headshot lookup→cache→URL. Check duplicate names, diacritics, traded/inactive players, unsupported goalie selection and invalid URL IDs. Numeric IDs should be finite valid player IDs, not merely parseable numbers.
  - [ ] 2.2 Ensure keyboard navigation, Enter/Escape, focus and Clear produce consistent selection in all sections. Surface player-detail errors currently returned by the hook but not consumed by the page.
  - [ ] 2.3 Measure search request volume before changing debounce or fields; share results between responsive instances and avoid fetching the selected player name again. Keep search separate from expensive statistics loading.
  - [ ] 2.4 Verify acceptance: Extend NameSearchBar.test.tsx and useWigoPlayerDashboard.test.tsx for selection, clear, deep links, errors and keyboard behavior. Acceptance: the visible name and all data always belong to the same selected ID; all header controls remain reachable.

- [ ] 3.0 Headshot and .playerIdentityCard ([source plan](TASKS/wigo-charts-audit/02-player-identity.md))
  - [ ] 3.1 Check identity provenance, current versus historical team expectations, trade updates, sweater number and team abbreviation aliases. Label current-team context if statistics span multiple teams.
  - [ ] 3.2 Test absent, invalid and failing headshots/logos. Preserve a usable identity card for no-team and no-image players and meaningful alt text.
  - [ ] 3.3 Measure image dimensions, priority and duplicate responsive image loads. Request only the displayed image size and reuse successful fallback lookups.
  - [ ] 3.4 Verify acceptance: Use existing dashboard tests where possible plus browser image-error and layout checks. Acceptance: correct player and team, reliable fallback, no clipped names or layout shift; no new database computation is required.

- [x] 4.0 Biographical .playerMetaGrid ([source plan](TASKS/wigo-charts-audit/03-biographical-data.md))
  - [x] 4.1 Verify raw player field units and nullable shapes before conversion. Add explicit unavailable display for invalid dates, missing height/weight and unsupported positions; never render NaN or plausible zero measurements.
  - [x] 4.2 Keep age as of today unless a historical-age feature is explicitly requested. Specify UTC/local birthday convention and handle leap-day births consistently.
  - [x] 4.3 Reuse the selected-player data; no independent fetch or derived-data pipeline is warranted.
  - [x] 4.4 Verify acceptance: Check birthday before/on/after, leap day, malformed date, null values, and rounding across feet/inches boundaries. Acceptance: correct readable age/height/weight/position with no additional network request.

- [ ] 5.0 Season production .productionSnapshot ([source plan](TASKS/wigo-charts-audit/04-season-snapshot.md))
  - [ ] 5.1 Reconcile totals to unique completed regular-season player games at the same cutoff. Verify PTS=G+A, traded-player season totals, DNP exclusion and refresh lag. Trace the totals writer before declaring this source authoritative.
  - [ ] 5.2 Gate season-dependent requests or explicitly label any latest-season fallback. Display requested/applied season and missing/stale state consistently with the comparison table.
  - [ ] 5.3 Compute each /GP figure from unrounded counts and valid GP; do not turn absent totals into zero. Reuse the shared totals query for the pace, PPG and TOI panels.
  - [ ] 5.4 Verify acceptance: Extend PerGameStatsTable.test.tsx for zero GP, missing values and unresolved season. Acceptance: all four totals and per-game values match independently reconstructed fixtures and agree with STD when both have the same source cutoff.

- [x] 6.0 Per-game averages and 84-game pace ([source plan](TASKS/wigo-charts-audit/05-per-game-pace.md))
  - [x] 6.1 Replace the intended modern forward pace with 84 and make its basis explicit in label and calculation. Preserve actual historical totals. If this becomes a historical-season projection, obtain that season’s scheduled length rather than assuming all earlier seasons had 82 games.
  - [x] 6.2 Use total/GP × schedule length, round only display and never multiply S%, rates or GP into a count projection. Distinguish pace from a forecast of remaining games or injury-adjusted production.
  - [x] 6.3 Remove ambiguous percentage inference after confirming source units; verify S%=100×G/SOG where available and define zero-shots behavior. Reuse snapshot inputs.
  - [x] 6.4 Verify acceptance: Extend PerGameStatsTable.test.tsx with 42 points/21 GP→2.00 per game→168 per 84, 0 GP, null totals and S% unchanged. Check all labels and help text. The [NHL schedule announcement](https://www.nhl.com/news/nhl-announces-2026-27-regular-season-schedule) confirms 84 games beginning in 2026–27. This plan does not itself modify the pace.

- [ ] 7.0 Opponent schedule and recent results ([source plan](TASKS/wigo-charts-audit/06-opponent-log.md))
  - [ ] 7.1 Verify schedule ordering, unique game IDs, gameType policy, postponed/cancelled games and live states before identifying the next game. Handle preseason, offseason and season completion explicitly.
  - [x] 7.2 Validate vs/@, W/L, scores, OT/SO and timezone/day boundaries. Next-game highlighting should identify the actual game and remain correct after clock/status changes.
  - [x] 7.3 Cache by team and season, share across teammates and responsive views, and use a freshness interval appropriate to future versus live games. Do not fetch per player when only team context changes.
  - [ ] 7.4 Verify acceptance: Check a normal week, last game, no future games, postponement, a trade and late UTC starts. Acceptance: accurate recent/upcoming schedule and local times, with loading/error/empty states; no implication that team games guarantee player participation.

- [ ] 8.0 Offense, defense and overall percentile composites ([source plan](TASKS/wigo-charts-audit/07-player-ratings.md))
  - [ ] 8.1 Audit every configured weight, field mapping, favorable direction, missing-metric renormalization, GP qualification and regression threshold. Reconstruct one rating by hand. Determine whether position-specific populations are intended; do not introduce them silently.
  - [ ] 8.2 Check whether percentile-named tables contain raw rates or pre-ranked values, unique player/season/strength rows, season completeness and ingestion freshness. Explain 50.0 values from evidence rather than assuming they are placeholders.
  - [ ] 8.3 Resolve the request’s defense PP versus current PK mismatch explicitly before changing semantics. Preserve the existing nine cells in the redesign; any added PP-defense rating must be a deliberate product change.
  - [ ] 8.4 Cache raw cohorts by season independently of player/min-GP; compute target ratings from the cached cohort. Avoid fetching strengths with no used weights if confirmed unnecessary. Optimize repeated cohort ranking in low-GP regression only after profiling.
  - [ ] 8.5 Verify acceptance: Extend calculateWigoRatings.test.ts, fetchWigoRatingStats.test.ts and PlayerRatingsDisplay.test.tsx: ties, inverted defense directions, absent metric, no PP/PK TOI, partial overall, regression and filter changes. Acceptance: explainable numeric reconstruction and no whole-league refetch solely for min-GP changes.

- [ ] 9.0 Team Chance Generation ([source plan](TASKS/wigo-charts-audit/08-chance-generation.md))
  - [ ] 9.1 Confirm snapshot season and that xgf/gp are matching cumulative 5v5 totals. Bound the lookup to the intended season using a real season field or authoritative season dates; do not compare stale previous-season 5v5 with current special teams.
  - [ ] 9.2 Validate one latest row per team, snapshot dates and metric-valid cohort sizes. The 704-row cap assumption depends on unique team/date rows; verify uniqueness and pagination before relying on it.
  - [x] 9.3 Own a cached league snapshot fetch shared by all four cards and responsive instances. Preserve per-source as-of ranges and insufficient-data states. Do not re-fetch all team snapshots separately per card.
  - [ ] 9.4 Verify acceptance: Extend teamPerformanceDrivers.test.ts for 24 teams but fewer valid xGF/GP values, duplicates, stale dates and zero GP. Acceptance: independently calculated xGF/GP and correctly oriented percentile, clearly labeled as team context. Check source coverage separately from total team identities.

- [ ] 10.0 Team Chance Suppression ([source plan](TASKS/wigo-charts-audit/09-chance-suppression.md))
  - [ ] 10.1 Verify xGA and GP cover the same season, game type and snapshot window, using the validated shared team dataset from plan 08.
  - [ ] 10.2 Check lower-is-better inversion, ties, zero GP, missing/negative source values and minimum valid cohort size. Keep displayed xGA/GP separate from its percentile and status.
  - [ ] 10.3 Reuse shared snapshot and derived cohorts. Preserve the explanation and team identification in any new layout.
  - [ ] 10.4 Verify acceptance: Extend teamPerformanceDrivers.test.ts with ordered and tied xGA/GP fixtures and insufficient valid data. Acceptance: fewer expected goals allowed always improves percentile within an unchanged cohort; no separate network request for this card.

- [ ] 11.0 Team Finishing ([source plan](TASKS/wigo-charts-audit/10-finishing.md))
  - [ ] 11.1 Confirm actual GF and expected GF use identical strength, date cutoff and season. Treat zero/missing xGF as unavailable, never an infinite or fabricated percentile.
  - [ ] 11.2 Check league midrank, tied ratios, valid cohort size and display: GF/xGF=1.10 should read 110.0%, while its percentile is a separate number.
  - [ ] 11.3 Reuse snapshots from plan 08. Preserve the distinction between team finishing context and a player shooting/finishing rating in labels and tooltips.
  - [ ] 11.4 Verify acceptance: Extend teamPerformanceDrivers.test.ts with GF=xGF, above/below expectation and zero denominator. Acceptance: ratio, percentile, status and explanation agree and remain visible in the redesigned layout.

- [ ] 12.0 Team Special Teams ([source plan](TASKS/wigo-charts-audit/11-special-teams.md))
  - [ ] 12.1 Verify PP goals/opportunities and successful kills/opportunities against canonical team records. Fix the source unit contract before replacing magnitude-based percentage conversion.
  - [ ] 12.2 Validate cohort alignment, latest dates, missing one side and minimum valid sample. Keep this equal-weight team composite separate from the player ratings’ TOI-weighted special-teams result.
  - [ ] 12.3 Reuse shared snapshots; compute both ranks once per snapshot. Show PP%, PK%, combined percentile and the source window without extra fetches.
  - [ ] 12.4 Verify acceptance: Extend teamPerformanceDrivers.test.ts for ties, decimal versus percentage-point contract fixtures, missing PP/PK and known combined percentiles. Acceptance: displayed PP/PK and composite are independently reproducible and team-scoped.

- [ ] 13.0 Complete timeframe comparison table ([source plan](TASKS/wigo-charts-audit/12-comparison-table.md))
  - [ ] 13.1 Build a metric/timeframe contract matrix with source column, unit, numerator, denominator, included games/seasons and fallback precedence. Define STD, LY, 3YA and CA inclusion of current season and partial seasons; decide deliberately whether recent windows cross season boundaries.
  - [ ] 13.2 Resolve conflicting NST TOI handling: recent ATOI divides summed TOI by 60 while per-60 assumes the sum is minutes. Verify source units with a known game. Resolve seasonal PPTOI total-versus-average and minutes-versus-seconds mismatch. Do not patch display scales to conceal writer defects.
  - [ ] 13.3 Locate the live wigo_rates writer and scheduling/refresh metadata; it can override otherwise-correct career/recent rates. This audit did not locate its producer in inspected source paths. Independently reconstruct totals and weighted rates: count×3600/TOI_seconds; PP rates use PP seconds, not all-strength seconds.
  - [ ] 13.4 Reconcile cross-provider games by stable game identity and coverage, rather than assuming date_scraped is a game date. Never combine complete counts with incomplete TOI silently. Preserve nulls when denominators or source coverage are missing.
  - [ ] 13.5 Validate DIFF=(left−right)/abs(right)×100 after count/GP normalization; define zero baseline and same-period behavior. Explain relative percentage change versus percentage-point difference and distinguish increasing values from universally better performance.
  - [ ] 13.6 Optimize the writer only after correctness: bound reads, fetch needed columns, reuse source rows and consider bounded concurrency for the current sequential player loop. Stage affected-player recomputes/diffs for review; production publishing requires explicit authorization. Preserve all 36 metrics and every timeframe in the UI.
  - [ ] 13.7 Verify acceptance: Extend fetchWigoPlayerStats.test.ts, tableUtils.test.ts, statMetadata.test.ts and existing calculate-wigo-stats.test.ts with statistical fixtures (current writer coverage is only an error path). Fixture: 5 goals/100 minutes→3.0 G/60; 5 games→20:00 ATOI; 2 PPP/10 PP minutes→12.0 PPP/60. Acceptance: all 36×7 values have a documented contract and fixture/sample reconciliation; no removed rows, columns or precision to achieve speed.

- [ ] 14.0 Expanded stat game-log charts ([source plan](TASKS/wigo-charts-audit/13-expanded-row-charts.md))
  - [x] 14.1 Key log data by player/season/stat using existing query infrastructure or a scoped request guard; reset expansion on context changes and prevent slow old responses replacing newer data. Cache completed log requests and propagate errors.
  - [ ] 14.2 Normalize each mapped column to the same unit as table references. Explicitly verify PTS1%, S%, PP%, IPP and on-ice percentages, PP per-60 mappings and whether date_scraped denotes game date.
  - [ ] 14.3 Label the chart’s actual game season and reference periods. Keep current behavior explicit unless multi-season log navigation is intentionally added. For count references divide by matching GP; for rates and percentages avoid unweighted mean-of-games masquerading as pooled season rates.
  - [ ] 14.4 Preserve all row expansion controls and seven reference toggles; load logs on demand and reuse compatible game-log data. Keep null observations distinct from zero.
  - [ ] 14.5 Verify acceptance: Extend StatsTable.test.tsx, StatsTableRowChart.test.tsx and fetchWigoPlayerStats.test.ts with out-of-order promises, player switches, rejected requests, percent scaling and unequal-TOI games. Acceptance: no stale plot, distinct empty/error states, and per-game points/reference lines have compatible units.

- [ ] 15.0 Point Consistency doughnut ([source plan](TASKS/wigo-charts-audit/14-point-consistency.md))
  - [ ] 15.1 Validate one completed game per row, regular-season filters, missing points and DNP exclusion. Exclude or label unknown values rather than counting them as zero-point or Cardio games.
  - [x] 15.2 Keep buckets from 0 through observed maximum (including 6+ when needed), with count and percentage. Check their count sum against GP and their percentage sum against 100 before display rounding.
  - [x] 15.3 Reuse canonical game logs with PPG; memoize distribution by game dataset. Preserve the Cardio supplementary row and explain its overlap to avoid double-count interpretation.
  - [ ] 15.4 Verify acceptance: Check all-zero games, an outlier high-point game, missing one of the Cardio fields, duplicate games and no games. Acceptance: mutually exclusive point buckets sum to valid GP; Cardio is separately labeled and does not inflate the doughnut.

- [ ] 16.0 Category Percentile Radar ([source plan](TASKS/wigo-charts-audit/15-percentile-radar.md))
  - [ ] 16.1 Reproduce the failure with response/error inspection: date boundaries (including preseason start>end), selected ID, game coverage, RPC permissions, row limits and source freshness. Verify the deployed function; do not claim the screenshot identifies the root cause.
  - [ ] 16.2 Choose an explicit category-average cohort and minimum games; validate per-game basis, ties, finite values and position policy. Preserve fantasy-category interpretation of PIM and +/−. Adopt the shared ranking helper only after agreeing on compatible semantics.
  - [ ] 16.3 Replace missing-player zeros with explicit no-data state; clear stale player results and surface errors. Render actual zero labels (current truthiness check hides them). Pass data declaratively where practical and remove reliance on private Chart.js label internals if implicated.
  - [ ] 16.4 Cache the league averages by date/season window, derive target locally and avoid duplicate legacy RPC effects across responsive mounts. A migrated source must preserve all eight categories and undergo parity verification.
  - [ ] 16.5 Verify acceptance: Test missing target, valid zero, all tied, top/bottom, rejected RPC, offseason dates, rapid selection and cleared selection. Browser acceptance: visible polygon for a known valid response, readable eight axes and explicit empty/error/loading states. The exact production failure remains unverified.

- [ ] 17.0 Brush/zoom TOI and PPTOI% ([source plan](TASKS/wigo-charts-audit/16-time-on-ice.md))
  - [ ] 17.1 Validate seconds end-to-end, mm:ss formatting and equality of chart season average with summed game TOI/GP. Verify PP time≤total game time. Investigate the screenshot’s single plotted date using raw row count/date range before blaming chart styling.
  - [ ] 17.2 Confirm PPTOI% denominator is player share of team PP time, consistent with the aggregate schema comment; it is not automatically player PP time/player total time. Verify ratio-of-sums versus mean-of-game-percentages for the season line and trade/zero-PP games.
  - [ ] 17.3 Share totals and logs with other panels; avoid rebuilding unchanged chart arrays/options where profiling warrants. Ensure player/season and toggle changes reset incompatible zoom state.
  - [x] 17.4 Preserve both modes, all series, hover values, horizontal drag-to-zoom/pan/pinch and an obvious reset. Current configuration is drag zoom, not a separate persistent overview brush; specify any new brush intentionally.
  - [ ] 17.5 Verify acceptance: Validate seconds/minutes fixtures, zero PP opportunity, null data, one/many games and traded-team denominators. Browser-check each mode and zoom/reset. Acceptance: correct axes/averages, no fabricated points and no totals refetch on toggling mode.

- [ ] 18.0 Brush/zoom Points Per Game ([source plan](TASKS/wigo-charts-audit/17-points-per-game.md))
  - [x] 18.1 Define windows as the last N actual games played, not calendar days or team games. Decide and document whether missing points invalidate a window or use valid observations; apply consistently to bars and averages.
  - [ ] 18.2 Reconcile season PPG with summed points/valid GP and snapshot/table, including source-lag state when totals and logs update at different times.
  - [ ] 18.3 Reuse canonical logs/totals. Memoize chart data/options before considering rolling-sum optimization; 84 games alone is not evidence that rolling math is a bottleneck.
  - [x] 18.4 Preserve bars, 5/10-game lines, season line, tooltips and drag/zoom/pan/reset. Make series distinguishable without relying only on color.
  - [ ] 18.5 Verify acceptance: Check games 4/5/9/10, missing values, zero-point streak, outlier game and player switch while zoomed. Acceptance: independent rolling calculations agree, zoom changes the viewport without changing the full-season reference definition, and no redundant totals request.

- [ ] 19.0 Brush/zoom Game Score ([source plan](TASKS/wigo-charts-audit/18-game-score.md))
  - [ ] 19.1 Confirm the deployed SQL and intended score model/version; document included strengths and the definitions of every raw field. Reconstruct representative games, including negatives. Do not silently substitute another “Game Score” formula.
  - [ ] 19.2 Compare returned game identities/count to canonical season logs. For incomplete inputs, decide field-by-field whether zero is valid or the score is unavailable; blanket COALESCE can fabricate scores. Preserve missing-game visibility and explicit rolling-window semantics.
  - [ ] 19.3 Check endpoint latency and relevant player/season index/query plan before considering persisted scores. Reuse stable query keys and memoized chart datasets.
  - [x] 19.4 Ensure zoom plugin registration works when this chart loads independently; do not depend on another chart registering it first. Provide visible reset and preserve negative values and rolling lines.
  - [ ] 19.5 Verify acceptance: Validate every weighted term with hand-calculated fixtures and compare expected versus returned games. Browser-check standalone chart initialization, negative scores, 5/10 windows and zoom reset. Acceptance: documented model, complete or explicitly incomplete game coverage, no silent shortening of “last 5 games”.

- [ ] 20.0 Bottom rate/category percentile bars ([source plan](TASKS/wigo-charts-audit/19-rate-percentiles.md))
  - [ ] 20.1 Verify unique player-season-strength keys, positive exposure, raw-rate versus pre-ranked meaning, join completeness, traded players and numeric types. Evaluate completeness using valid metric coverage, not GP alone; a high GP cohort can still have missing/stale metric columns.
  - [x] 20.2 Keep requested/applied season visible: current implementation substitutes both target and cohort from the prior season, not just a benchmark population. Align player GP messaging with that source and distinguish fetch failure from genuine incomplete data.
  - [ ] 20.3 Validate midrank percentile, ordinal rank, finite inputs and the policy retaining the selected player below min GP. Treat TOI/GP and percentage metrics with their own labels; never describe all bars as per-60.
  - [x] 20.4 Cache season/strength cohorts apart from selected-player canonical GP; calculate rank and percentile together instead of scanning twice per metric if worthwhile. Keep AS/ES/PP/PK and min-GP interactions local when inputs are cached.
  - [ ] 20.5 Verify acceptance: Extend fetchWigoPercentiles.test.ts, calculatePercentiles.test.ts and RateStatPercentiles.test.tsx for ties, singleton, defense-only row, fallback target/GP mismatch, source errors and low-GP target. Acceptance: all 16 bars have valid source/units/cohort, accurate percentile and rank, and conspicuous fallback status.

### Execution evidence

- 2026-09-19: 1.1 complete. Inspected working-tree status and relevant existing diffs. Current wide-screen matrix already keys logs by player/season/metric; standard StatsTable still has unguarded promises. The fetch helper already rethrows log errors. Starting 14.1 against the remaining standard-table race.
- 2026-09-19: 14.1 complete for the remaining standard-table path. Replaced unguarded promises with a player/season/stat query using the existing matrix query-key convention, disabled requests without valid context, reset expansion on context changes and cached fresh logs for 60 seconds. Preserved existing fetch-error propagation and matrix behavior.
- Verification: `StatsTable.test.tsx` passes 8 tests, including six new cases covering old responses/errors, player/season changes, cache reuse and missing context. `StatsTableRowChart.test.tsx` passes 2 tests; `fetchWigoPlayerStats.test.ts` passes 7 tests. Targeted ESLint passes. The first standard-table test attempt lacked a query provider; the test harness was corrected and its rerun passed. Live source/units and browser/performance acceptance remain open.
- Type-check: default-heap run exhausted memory; retry with `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit --incremental false` passed. Targeted diff whitespace check passed. Task-list structure checked: 20 parent tasks, 93 subtasks, all source-plan links valid.

- 2026-09-19: Live source inspection and local pipeline corrections are documented in [LIVE-EVIDENCE.md](TASKS/wigo-charts-audit/LIVE-EVIDENCE.md). Confirmed NST game TOI is seconds; corrected recent per-60 denominators and seasonal PPTOI averages in the local writer. Production aggregates have not been republished. Shared player/season totals and season ratings cohorts now have cache reuse fixtures. Radar midrank ties and inverted preseason ranges have regression coverage. These are partial steps, not completion of their broader acceptance gates.
- 2026-09-19: 18.1 complete. Rolling windows require N consecutive actual observations with finite values; incomplete windows remain null, while real zeros and negative scores count. PPG and Game Score bars preserve missing values. Game Score now aligns RPC scores to the shared PPG game calendar, reports missing coverage, and rejects ambiguous date matches because the deployed RPC does not return game IDs. Changing player/season remounts the score canvas to reset zoom. Plan 19 remains open for model reconstruction, live parity, endpoint profiling and browser verification.
- Verification: `utils/formattingUtils.test.ts` passes 12 cases; `components/WiGO/GameScoreLineChart/index.test.tsx` passes 3 cases covering omitted games, duplicate score dates and calendar errors. No remote writes were performed.

- 2026-09-19: Biography checks 4.2–4.4 complete in local code. Extracted the existing pure formatting logic into `playerBiography.ts`, rejected normalized impossible dates/future births and unsupported positions, and retained shared selected-player inputs without new requests. Age uses UTC with March 1 for Feb 29 births in non-leap years. Nineteen tests pass for before/on/after birthdays, leap years, malformed/missing values, metric conversions and inch carry. Raw source-field provenance in 4.1 remains open.
- Chart-change targeted ESLint passed. The full TypeScript check exposed an optional rating-weight array from the earlier cohort optimization; fixed its guard, and its fetch regression test passes.

- 2026-09-19: Parent 4 (biographical grid) complete. Source trace confirms NHL roster and landing `heightInCentimeters`/`weightInKilograms` flow unchanged through `nhlRosterPreview.ts` and both player writers into `players`; `fetchWigoPlayerDetails` supplies that same record to the grid. The schema declares birth date and measurements non-null, but the individual-player writer can supply zero for absent measurements; display guards handle zero as unavailable and also tolerate null/malformed inputs. The grid now calls the tested pure formatters, adding no data request. All 19 biography tests and targeted ESLint pass. Full TypeScript check passes after guarding both optional rating strength and optional weight array. No browser checks or production publishing are claimed by this completion.

- 2026-09-19: 15.2–15.3 complete locally. Point Consistency retains buckets from zero through the observed maximum, with Cardio excluded from the doughnut and an overlap explanation on its row. Invalid/missing points produce explicit unavailability; missing activity fields affect Cardio only. `useWigoGameLog` shares one cached player/season request across Consistency, PPG and Game Score coverage. Eight Consistency tests pass (including a seven-point game, 100% total, missing fields and request reuse), as do the three existing Game Score coverage tests after migration to the shared query. Targeted ESLint passes. Source inspection confirms the NHL game-log writer requests gameTypeId=2 and gamesPlayed>=1; unique-game and completed-status live validation remain open under 15.1/15.4.

- 2026-09-19: Parent 6 (84-game pace) complete locally. Labels/help identify observed count/GP × 84, with rounding only at display and no GP/S% projection; historical totals are unchanged. Shooting percentage now derives from matching goals/shots, with unavailable output for zero/missing shots. Eleven PerGameStatsTable tests pass, including 42/21→2.00→168, null counts, zero GP, unchanged percentage and stale source-percent rejection. Fraction source units were previously verified against live data.
- Shared game-calendar follow-up: all 47,230 current-season rows have one GP, valid unique player/game IDs, nonmissing points and game-type-2 joins. Full detail and the remaining final-status limitation are in LIVE-EVIDENCE.md. Added duplicate/identity reader safeguards; 14 fetch tests and targeted ESLint pass.

- 2026-09-19: Header/selection progress (parent 2 remains open). Search now selects immediately using the returned player record, leaving missing-image lookup to the player-ID-keyed detail query. Removed the search component's redundant external image lookup and browser-side player update. Old results disappear while a changed term is debouncing, and ARIA no longer references hidden options. Search/player/aggregate error messaging accepts plain Supabase error objects. Eighteen focused tests pass across NameSearchBar and useWigoPlayerDashboard, including immediate image-less selection, no redundant lookup, stale-option suppression, plain-object errors and ten query-ID cases. Targeted ESLint passes. Broader keyboard, goalie, diacritic and browser navigation acceptance remains open.

- 2026-09-19: Identity-image progress (parent 3 remains open). Failed team logos now show an explicit fallback; failed headshots use accurate placeholder alt text and stop image priority. A different player's/team's image remains eligible after a prior failure. Added the same URL-scoped fallback to the selected-player thumbnail. Four PlayerHeader regression tests pass. Targeted lint has no errors (two warnings in the mocked image test). Browser/layout-shift and source provenance checks remain open.

- 2026-09-19: Browser verification started with the existing Playwright setup because `agent-browser` is not installed. The radar timeout/retry test passed in Chromium against the local server (46 seconds including startup). It verifies explicit timeout messaging, exactly two initial attempts, a Retry action, and a visible canvas on the third fixture response. This proves browser error/recovery behavior, not live RPC calculation parity. The complete desktop matrix/interaction test is being run separately.

- 2026-09-19: Chromium desktop matrix/interaction acceptance passed on local code after fixing a real overflow: the missing-Game-Score notice previously followed a full-height canvas, creating a 1082px document in a 1080px viewport. The chart now reserves notice space inside its panel. The rerun verifies all 36 metrics × seven timeframes plus DIFF, all C01–C19 areas, seven reference toggles, stat selection, TOI modes, zoom/reset on all three trend charts, rate filters, readable canvas labels, no clipped cells/overflow, Clear focus/URL behavior and keyboard selection of a second player. The radar uses an explicit cohort fixture; other visible values are live data and are not certified by this UI test. Game Score unit tests (3) and targeted lint also pass. Tasks 17.4/18.4/19.4 now have browser interaction evidence; independent Game Score module registration is present in RollingAverageChart. Full statistical/source acceptance remains separate. Screenshot and measurements are under `tasks/TASKS/wigo-single-viewport/evidence/`.

- 2026-09-19: Remaining Chromium cases passed: long identity/missing image/radar/seven-point bucket/empty log; source failures and absent historical values; 1440/1024/390px access and URL tabs. The mobile test initially matched two outgoing Overview tables during navigation; it now awaits the comparison URL and targets the STD-containing table, asserting all seven timeframe headers plus DIFF. Updated game-log fixtures to include IDs required by the shared reader.
- Team-model correction: minimum coverage now counts valid observations per metric, and negative GF/xGF/xGA values are unavailable. Fifteen team model tests pass, including insufficient metric coverage despite 24 team identities, tied midrank, zero denominators, and 110% finishing distinct from 50th percentile. Targeted lint passes. Plans9–12 remain open: live evidence reveals Special Teams is using an individual playoff game's percentages, requiring a cumulative regular-season source correction before acceptance.

- 2026-09-19: Corrected all four team-card reader pipelines after confirming BOTH NST5v5 and WGO team rows are daily. New `fetchTeamDriverSnapshots.ts` paginates season-bounded sources, uses canonical type2 games, accumulates unique daily5v5 observations and PP/PK numerator/denominator totals, and rejects incomplete expected-date/game coverage. The component caches league inputs by season and derives player-team results locally. Percentages have an explicit percentage-point boundary. Card labels persist in an explicit incomplete state. Twenty-four model/reader tests and targeted lint pass. TBL's two missing NST dates are documented in LIVE-EVIDENCE.md; no backfill or publish performed. Plans9–12 remain open for source completeness, cohort reconciliation and final browser evidence.

- Final verification of this team-source change: full TypeScript check passes. Chromium desktop acceptance passes again (1.1 minutes), including preserved four-card identity in the incomplete state and zero additional team-game fetches when switching players within the fresh season cache. The earlier browser run was intentionally interrupted after discovering daily NST semantics; it was not counted as a pass.

- 2026-09-19: Opponent schedule corrected locally: sort/deduplicate by game identity; restrict to selected team/season and preseason/regular/playoff game types; skip postponed/cancelled games for next-game selection; never highlight the final completed game as upcoming. Live scores and explicit postponement/cancellation labels replace stale start-time displays. Invalid timestamps now render TBD instead of throwing again in fallback formatting. Team/season queries share a60-second fresh cache and refresh every30seconds when live or5minutes otherwise. The accessible table label states player participation is not guaranteed. Nine focused tests, lint and TypeScript pass, including away OT/SO results, local UTC-date boundary, missing data, team change and cache reuse. Live-source validation remains open: direct NHL fetch failed DNS resolution; web-tool fallback also could not access the endpoint. No source success is claimed.

- 2026-09-19: 20.2 complete locally. The percentile panel now labels requested/applied season in both responsive views and uses the applied cohort GP for player context and min-GP warnings, including historical fallback. Fallback text explicitly states that both player values and league cohort come from the prior season. Cohort and canonical-coverage fetch errors propagate to the existing error state instead of becoming empty data or zero coverage. Rank and percentile now share one calculation per metric, filtering nonfinite inputs from both populations. Twelve focused tests pass across the reader, panel and ranking helper, including fallback GP mismatch, historical low-GP warnings, source/coverage failures and infinite metric inputs. Targeted lint and diff whitespace checks pass. Parent 20 remains open for live metric coverage, join policy and cohort caching.
- Percentile-change verification: full TypeScript check passed with the documented 8GB heap. No production writes or browser verification were performed for this change.

- 2026-09-19: 20.4 complete locally. Percentile reads now use the existing QueryClient for independent season/strength cohorts, season maximum GP and player/season canonical GP, with 60-second freshness. Requested and fallback cohorts share the same cache; fallback decisions remain player-specific. One combined rank/percentile calculation per metric was completed in the preceding change. Twelve focused reader/panel tests pass, including player switches, strength switches, fallback-versus-current player isolation, cache revisits and a minimum-GP change that updates percentile without fetching. Targeted lint, diff checks and full TypeScript pass. Live inspection found additional source defects recorded in LIVE-EVIDENCE.md; parent20 remains incomplete.
